"""Work Domain — 티켓의 이름·번호·순서·흐름을 한 곳에 모은다 (S6).

`app/tickets/` 는 여전히 **미러와 화면**을 담당한다. 여기 있는 것은 그 위의 도메인
규칙이다 — 어떤 이름으로 불리는가(`codes`·`resolve`), 몇 번인가(`numbering`),
어떤 상태인가(`workflow`), 어떤 순서인가(`rank`), 무엇과 이어져 있는가(`relations`),
무슨 일이 있었는가(`activity`).

왜 `app/tickets/` 안에 두지 않았는가: 이 규칙들의 소비자가 티켓만이 아니다.
프로젝트가 코드를 갖고, 스프린트가 순서를 쓰고, Migration 이 채번과 예외 분류를
그대로 부른다. 티켓 모듈 안에 두면 그 셋이 전부 티켓 화면 코드에 의존하게 된다.
"""
