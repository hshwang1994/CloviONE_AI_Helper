- [Critical|missing-feature|notion-mapping] The backend list endpoint is paginated (PageParams, DEFAULT_PAGE_SIZE=20) and left-joins ALL users, but the registry config for 'notion-mapping' sets neither `paginated: true` nor `searchable: true`. So DataScreen.buildUrl() sends no page/page_size and no server-side `q`; the backend returns only the first 20 users and NO pager is rendered. The always-visible search box only client-side-filters those 20 loaded rows, so typing user #21's email returns '검색 결과가 없습니다'. In any org with >20 employees, users beyond the first page are completely unreachable — they cannot be verified, manually mapped, or unmapped from this screen at all, which defeats the screen's entire per-user-mapping purpose.
    FIX: registry.js:294 — add `paginated: true` (and ideally `searchable: true`) to the 'notion-mapping' config so DataScreen sends page/page_size (and q) to GET /api/admin/notion-mapping and renders the pager. Peer paginated screens (approvals/jobs/audit/notifications) already do this.
- [Critical|missing-feature|audit] The backend GET /api/admin/audit (app/audit/router.py:31-54) supports SIX server-side filter params — action, object_type, object_id, user_id, since, until — and NONE are surfaced. The audit config exposes only columns, detailFields and paginated:true; DataScreen.jsx has no filters mechanism at all (buildUrl at lines 24-29 only knows searchable+paginated). The entire forensic purpose of the screen ('누가 무엇을 언제 바꿨는지') is unreachable: an auditor cannot ask 'what did user X do', 'who deleted this object', 'all role_change actions', or 'everything during the incident window'. On a production log of thousands of rows the only interaction is paging through ALL records in fixed reverse-chron order.
    FIX: Add a config.filters array to the audit config (e.g. select for action/object_type from OBJECT_KO/VERB_KO keys, text for user_id/object_id, date inputs for since/until) and teach DataScreen.buildUrl (frontend/src/screens/DataScreen.jsx:24-29) + queryKey (line 31) to append/track them. This is the flagship gap — the screen does not serve its domain end-to-end without it.
- [Critical|missing-feature|backup] The backend endpoint GET /api/admin/backups/restore-instructions (system_admin) returns note + 4-step restore guidance, but the screen never calls it. There is no button, header action, or link, and grep confirms 'restore'/'복원' appear nowhere in frontend/src. The screen's help text ('데이터베이스를 백업합니다. 오래된 백업은 자동 정리됩니다.') never even mentions restore. A backup screen exists to enable restore/DR, yet a system_admin has zero in-app guidance on how to restore despite the backend serving it — half the domain (restore) is entirely unserved.
    FIX: registry.js backup config (line 344): add a headerAction (or an action with a result/detail popup) that GETs /api/admin/backups/restore-instructions and renders res.note + res.steps in a modal/callout. At minimum update help text to point system_admin to the restore path. Since runHeaderAction (DataScreen.jsx:55) has no result/render support, either extend it to display returned guidance or add a dedicated info modal.
- [High|empty-state|change-password] The password-policy guidance is shown ONLY to non-must_change users (the {% else %} branch, change_password.html:12-13). First-login users (must_change=True) — the exact audience forced onto this screen — get "첫 로그인입니다. 임시 비밀번호를 새 비밀번호로 변경하세요" with NO mention of the 12-char / 3-character-class rule the backend enforces (auth/router.py:263-267). They type a password blind, submit, and only then discover the rules via a server error. A failed first attempt is essentially guaranteed for the page's primary audience.
    FIX: Show the requirement hint ("대문자, 소문자, 숫자, 특수문자 중 {{ min_classes }}종류 이상, {{ min_length }}자 이상") to BOTH branches in change_password.html:10-14 — append it to the must_change subtitle, or move it outside the {% if %} so every user sees the policy before typing.
- [High|missing-feature|change-password] There is no password-visibility toggle on any of the three password fields (change_password.html:16-27), yet the simpler login page HAS one (login.html:73-74, "표시" button). Users must type a 12+ char, 3-class password blind and re-type it identically in the confirm field. A single unseen typo fails the confirm check or the policy, forcing a full re-type — a feature that already exists on the easier screen was dropped on the harder one.
    FIX: Add a "표시"/"숨김" toggle button (reuse .btn-ghost + .input-wrap pattern from login.html) to at least the new-password field, toggling type between password/text. Prefer a single toggle that reveals new+confirm together.
- [High|missing-feature|chat (user)] On mobile (≤760px) the entire conversation sidebar is hidden via `.chat-side { display: none; }` (screens.css:126) and there is NO hamburger/drawer/toggle anywhere (confirmed in Chat.jsx and App.jsx). The '+ 새 대화' button, the conversation list, and rename/delete all live only in that aside. A phone user therefore cannot start a second conversation, switch conversations, or rename/delete — they are locked into whichever conversation auto-opened. It's a true dead-end for the core multi-conversation flow.
    FIX: frontend/src/screens/Chat.jsx:237 + screens.css:126 — add a mobile drawer: a topbar/chat-header toggle button that slides `.chat-side` in as an overlay on small screens instead of `display:none`.
- [High|bug|chat (user)] The sidebar goes stale immediately after sending. The `send` mutation onSuccess (Chat.jsx:169) and `retry` onSuccess (Chat.jsx:195) invalidate only ["messages", id], never ["conversations"]. The backend auto-titles a new conversation from the first message (service.py:190-192) and bumps updated_at for reordering, but the sidebar keeps showing '새 대화' and never re-sorts. The updated title is even returned in the thread response (get_messages -> conversation_view) but the code reads only thread.data.items and discards it. With staleTime 30s and refetchOnWindowFocus off, it stays wrong until an unrelated create/rename/delete fires.
    FIX: In Chat.jsx:169 and :195 also call qc.invalidateQueries({ queryKey: ["conversations"] }) on success (or seed the conversations cache from thread.data.conversation).
- [High|missing-feature|settings] The entire setting version-history + rollback workflow is dropped from the UI. The backend fully supports it — GET /api/admin/settings/{key}/versions and POST /api/admin/settings/{key}/rollback (router.py:66-95), and apply_setting() writes a config_versions snapshot on EVERY change (service.py:114-118). The React Settings page (Settings.jsx) only lists and single-field-edits; there is no way to view a setting's history or revert a bad change. This is a deliberate-looking drop: the same team wired versions/rollback UI for workflows (frontend/src/screens/registry.js:122-126), and format.js:27 even maps the audit action 'rollback'→'롤백', so audit logs will surface '설정 롤백' events that can never be produced from this screen.
    FIX: Add a version-history affordance to the SettingEditor modal (Settings.jsx:49-105): fetch GET /api/admin/settings/{key}/versions, render the snapshot list with created_at, and a '이 버전으로 되돌리기' button that POSTs to /{key}/rollback then invalidates the ['settings'] query. Mirror the workflows registry.js rollback pattern.
- [High|missing-feature|settings] Settings version history and rollback are completely missing from the UI even though the backend fully supports them. router.py exposes GET /api/admin/settings/{key}/versions and POST /api/admin/settings/{key}/rollback, and service.apply_setting calls snapshot_config on EVERY write (service.py:114-118) — so every change silently accumulates restorable versions the admin can never see or restore. The service docstring even states 'any change is reversible via the config_versions rollback endpoint' (service.py:11-12). The sibling Workflows screen implements exactly this pattern (registry.js:120-129: '버전 기록' subList + '이 버전으로 롤백' calling /versions and /rollback), proving the codebase has the components — Settings.jsx just dropped it.
    FIX: In Settings.jsx add a '버전 기록' action/section in SettingEditor that GETs /api/admin/settings/{key}/versions (renders version, created_at, snapshot) and a per-version '롤백' button that POSTs /api/admin/settings/{key}/rollback with {version}, then invalidates ['settings']. Reuse the subList/rowAction pattern already built for workflows in registry.js:120-129.
- [High|missing-feature|users] Pagination was dropped in the React port. The backend caps the user list at DEFAULT_PAGE_SIZE=20 (app/core/pagination.py:13) and returns total/page/page_size, but Users.jsx builds the query with no page param (Users.jsx:48-56) and renders only query.data.items with no pager and no '총 N건' count. In an org with more than 20 accounts, users 21+ are silently invisible and unreachable except by lucky search — the primary admin user directory cannot show or browse everyone.
    FIX: Add page state + a c-pager exactly as DataScreen.jsx:103-109 does (send page=/page_size=, read total, show '이전/다음' + '총 N건'). The .c-pager CSS already exists (screens.css:37).
- [High|missing-feature|users] Session visibility is fully built on the backend but has zero UI consumer. GET /api/admin/users/{id} returns active_session_count and GET /api/admin/users/{id}/sessions returns client_ip / user_agent / last_seen_at / expires_at (router.py:176-192, 376-402), yet no file in frontend/src ever calls either (grep confirms only 'revoke-sessions' is used). UserDetail renders the list-row object, never fetching the detail endpoint. Consequently the '세션 해제' button (Users.jsx:172) is a blind destructive action: the admin sees no session count and no IP/device list before cutting sessions, and run() discards the response so the returned revoked_count is never surfaced (Users.jsx:150-156) — the admin gets a generic '처리했습니다' with no idea whether 0 or 5 sessions were killed.
    FIX: Fetch GET /api/admin/users/{id} when the drawer opens; show active_session_count as a Row and add a session-list sub-drawer (IP/기기/최근 접속) like DataScreen's SubListDrawer. Surface revoked_count in the toast ('세션 N개를 해제했습니다').
- [High|bug|dashboard] The "확인이 필요한 항목" (needs-attention) alert list is built only from jobs/cert/backup/disk metrics (Dashboard.jsx:43-52) and never checks component/heartbeat status. The backend already computes worker/scheduler = "down" when a heartbeat is stale >90s (service.py:42-48, 155-159), but a down worker or scheduler produces NO alert. If the background worker dies while the queue happens to be empty (or the scheduler dies), the dashboard renders "지금 조치가 필요한 문제가 없습니다" (all clear) at the top while jobs silently stop processing and scheduled runs never fire. The 'down' status is only visible in the lower 서비스 상태 section, which operators scanning the alert banner will miss.
    FIX: In Dashboard.jsx DashboardBody, after building `services`, push alerts for any component whose status is 'down'/'unknown' (worker, scheduler, and 'down' integrations), e.g. iterate d.components and add {label:'워커 중단', kind:'danger', to:'/diagnostics'} when status==='down'. Absolute path: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\Dashboard.jsx:43-52
- [High|dead-affordance|dashboard] The dashboard emits the same alert links, actions and 'view all' links to all four read roles (operator, admin, system_admin, auditor) without consulting auth.data.role, but the target endpoints enforce stricter RBAC server-side, so several links land the user on a 403 error screen. Concretely: an auditor clicking a 실패/대기/성공률 alert -> /jobs (jobs GET requires operator/admin/system_admin, auditor blocked); an operator clicking '전체 보기 →' on 최근 주요 변경 -> /audit (requires admin/system_admin/auditor, operator blocked); operator or auditor clicking 인증서/디스크 alert -> /diagnostics then '진단 수집' -> 403 (bundle requires admin/system_admin); any non-system_admin clicking the backup CTA -> /backup then '+ 백업 실행' -> 403 (backup POST requires system_admin). Since the dashboard is the default landing page for all these roles, these are live broken journeys.
    FIX: Gate the alert `to`/nav links and the backup CTA by the current role (read auth.data.role via useAuth) — omit or disable links the viewer cannot follow, or route them to a role-appropriate destination. Cross-checked routers: jobs/router.py:20, audit/router.py:20, health/router.py:48, backups/router.py:41. Frontend: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\Dashboard.jsx:44-52,107,116
- [High|missing-feature|users] Pagination was dropped in the React port. The backend paginates users (app/users/router.py list_users uses PageParams, DEFAULT_PAGE_SIZE=20, and returns total/page/page_size). Users.jsx never sends page/page_size query params and reads only `query.data.items` (line 71), ignoring total/page/page_size entirely. There are no pager controls and no 'N total' indicator. For any org with more than 20 users, everyone past the first 20 (by created_at desc) is silently invisible and unmanageable. Search is not an escape hatch — a search that matches >20 users is also capped at 20 with no indication.
    FIX: Frontend Users.jsx: add page state, send `&page=`/`&page_size=` in the queryFn (line 50-54), read total/page/page_size from the response, and render pagination controls (or a 'total N, showing first 20 — refine search' notice). At minimum surface `query.data.total` so admins know rows are being hidden.
- [High|dead-affordance|users] The entire session-inspection feature is dropped, and '세션 해제' (revoke all sessions) fires blind. Backend exposes GET /api/admin/users/{id} (returns active_session_count) and GET /api/admin/users/{id}/sessions (per-session id, client_ip, user_agent, last_seen_at, expires_at) — a grep of frontend/src shows NEITHER endpoint is ever called. UserDetail (Users.jsx line 143) renders from the stale list row (`sel`), never fetching the detail endpoint, so active_session_count and the session list never appear. The revoke button (line 172) then destroys ALL sessions with no way to see how many/which exist, and run() (line 150-156) discards the response so the backend's returned revoked_count is never shown either.
    FIX: Fetch GET /api/admin/users/{id} when the drawer opens (useQuery keyed on user.id) to show active_session_count and fresh data; add a sessions list (GET .../sessions) so the admin sees IP/UA/last-seen before revoking. Surface revoked_count from the revoke-sessions response instead of the generic '처리했습니다'.
- [High|design|diagnostics+maintenance] The 진단 (Diagnostics) page dumps the entire bundle as a raw JSON blob (Ops.jsx:10,21 — `JSON.stringify(q.data, null, 2)` in `<pre class="ops-pre">`). The backend bundle (app/health/service.py build_diagnostic_bundle) is rich structured data — dashboard, disk, memory, cert_days_remaining, last_backup_at/status, recent_job_errors, integration_errors, recent_critical_audit, settings — but the UI shows it as an untranslated debug dump. Every key is raw English (generated_at, last_backup_status, recent_job_errors), values include raw enum strings ('down','failed') and ISO-8601 timestamps. This directly violates the 'no raw English/enum/ISO timestamps leaking' bar and looks like a bare template, not the '서비스·버전·설정 요약' the empty-state promises (Ops.jsx:22).
    FIX: Render the bundle as structured Korean UI: StatCards for disk/memory/cert_days_remaining/last_backup (reuse ui/kit StatCard + Badge for status enums via existing STATUS_TEXT), DataTables for recent_job_errors and integration_errors with format.js relative-time for timestamps. Keep the raw JSON behind a collapsible '원본(JSON) 보기' for support hand-off, not as the primary view (Ops.jsx:18-23).
- [High|missing-feature|diagnostics+maintenance] The 유지보수 page toggles maintenance_mode but gives no way to view or edit maintenance_message — the exact text end users are shown when the mode is on (confirmed: app/settings/gate.py:31 raises MaintenanceModeError(maintenance_message(...))). The operator flips the switch that displays a notice to every user yet cannot see or set that notice here, so the page does not serve its stated purpose end-to-end. The copy even says '점검 공지' conceptually but no message field exists (Ops.jsx:52-60).
    FIX: On the Maintenance card surface the current maintenance_message and allow editing it inline (PUT /api/admin/settings/maintenance_message), ideally prompting for/showing the message when enabling the mode. Ops.jsx:52-60.
- [High|dead-affordance|diagnostics+maintenance] Both controls on this page are dead for 2 of the 4 admin-console roles. The sidebar (App.jsx NAV lines 34-66, rendered unconditionally by Sidebar) shows 진단 and 유지보수 to every role that reaches /admin (operator, auditor, admin, system_admin — page auth only blocks ROLE_USER). But GET /api/admin/diagnostics/bundle requires admin/system_admin only (app/health/router.py:48), and PUT /api/admin/settings/maintenance_mode requires WRITE_ROLES=admin/system_admin (app/settings/router.py:30). So an operator or auditor opens 진단, clicks '진단 수집' → 403 ErrorState; opens 유지보수 (they CAN read settings, so the page renders normally) and clicks the enabled '유지보수 모드 켜기' button → confirm → PUT → 403 toast. Enabled controls that always fail — never disabled, never hidden, no 'insufficient permission' explanation. Ops.jsx never reads auth/role at all.
    FIX: Gate by role. Read useAuth() role in Ops.jsx; for non-write roles render the maintenance toggle disabled with a '권한 없음' note, and hide/disable '진단 수집' for non-admin roles. Better: role-gate NAV items in App.jsx (add allowedRoles per item, filter in Sidebar) so operator/auditor never see controls they cannot use. frontend/src/screens/Ops.jsx:15,57 and frontend/src/app/App.jsx:34-113.
- [High|dead-affordance|notification-bell] The '전체 알림 보기' (View all notifications) footer button always navigates to /notifications (NotificationBell.jsx:101). For normal users (isUser), App.jsx:235-237 bounces every non-/chat route back to /chat, and the full notifications page only exists as an admin-only REGISTRY route rendered inside AdminBody (never rendered for users). So a normal user who clicks '전체 알림 보기' is silently dumped into the chat screen — a dead end. Because the popover only loads the 8 most recent items (page_size=8), a normal user literally has NO way to see older notifications, even though the backend fully supports per-user paginated listing (/api/notifications is get_current_user, not admin-gated).
    FIX: For isUser, either hide the footer '전체 알림 보기' button, or add a real user-reachable notifications route/screen (the backend endpoint already works for users). Do not route users to /notifications, which the layout redirect kills. NotificationBell.jsx:100-102.
- [High|dead-affordance|notification-bell] The bell is rendered for ALL users including role=user (Topbar mounts <NotificationBell isUser={isUser}/> unconditionally), and regular users genuinely receive notifications (account_locked, job_failed via chat, approval_decided). But the footer '전체 알림 보기' button always does nav('/notifications'), and the full /notifications page lives inside AdminBody which is never rendered for a user — the Layout redirect effect (App.jsx:236: if(role && isUser && !pathname.startsWith('/chat')) nav('/chat')) immediately bounces them back to chat. So for a regular user the button visibly flashes and returns to /chat, doing nothing. There is no working 'see all my notifications' surface for users at all; the 8-item popover is their entire notification history.
    FIX: NotificationBell.jsx:101 — for isUser, either hide the '전체 알림 보기' footer, or route it somewhere that actually renders for users. Longer-term, give regular users a real notifications view (a route rendered outside AdminBody) instead of bouncing them to /chat.
- [High|missing-feature|templates] The templates config has NO detailFields, so clicking a template row opens a detail drawer that only re-renders the 4 list columns (name, target_type, enabled, created_at). The backend _view (router.py:59-73) returns description, target_ref, prompt_id, policy_id, input_schema, and approval_policy — none are shown anywhere in the read UI. Most damaging: target_ref (WHICH workflow/runner the template is bound to) and prompt_id/policy_id (the whole point of a template) are invisible. The only way to see them is to open the edit form, which read-only roles (operator, auditor are in READ_ROLES) can open but not save (403). Every sibling screen (prompts, policies, workflows, documents, approvals, audit, jobs) surfaces its payload via detailFields; templates surfaces nothing, so the screen does not serve its stated purpose ('save automations as templates ... linked to a target workflow/runner') for viewing or auditing.
    FIX: Add detailFields to the templates config (registry.js ~line 187, before/after `actions:`), e.g. `detailFields: [field("description","설명"), field("target_ref","대상 ID"), field("prompt_id","프롬프트 ID"), field("policy_id","정책 ID"), jsonField("input_schema","입력 스키마"), jsonField("approval_policy","승인 정책"), dateCol("updated_at","수정")]`. Optionally add target_ref as a list column too.
- [High|missing-feature|policies] The backend fully supports rolling back / re-publishing an old policy version (POST /api/admin/policies/rollback, router.py lines 225-238, wired into policies_router at line 244), and the app already ships the exact UI pattern for it — the workflows screen has a '버전 기록' subList with an '이 버전으로 롤백' rowAction (registry.js lines 120-129). The policies screen exposes NONE of this. Once a newer version is published the previous published version is auto-archived (service.py transition, line 87) and becomes completely inert in the UI (no action's when() matches 'archived'), so an admin has no way to revert to a known-good prior policy. A core version-management capability is dead.
    FIX: Add a '버전 기록/롤백' action to the policies config (registry.js ~line 169) mirroring workflows lines 120-129: a subList over the policy versions with a rowAction posting to /api/admin/policies/rollback with body {name, version}. The list_all endpoint already returns every version (router.py lines 114-125), so versions can be listed by filtering on name.
- [High|missing-feature|policies] The lifecycle is forward-only in the UI but bidirectional in the backend. VALID_TRANSITIONS (models.py lines 20-26) allows test→draft and review→draft (send a version back for edits) and →archived from draft/test/review/published, yet the policies actions (registry.js lines 169-174) only offer draft→test→review→published→new-version. Because editing is restricted to draft (editWhen: r.status === 'draft', line 166), a policy moved to 'test' or 'review' with a mistake can neither be edited (not draft) nor sent back to draft (no button) — the reviewer's only forward options push the flawed content toward 'published'. The backend-supported reject-back path is completely unreachable.
    FIX: Add reject/send-back actions to registry.js (~line 169): '초안으로 되돌리기' when status is test or review, posting transition with body {status: 'draft'}, so a reviewer can return a version for edits (backend already permits test→draft and review→draft).
- [High|dead-affordance|workflows] The '테스트' (test reachability) action always reports success. The backend returns {status: 'reachable'|'unreachable', detail: 'HTTP 404'|'timeout'|'connection_error'}, but the registry test action has no `result` handler, so runAction falls to announce(res, '테스트 완료') and toasts a GREEN success toast even when the webhook is unreachable/down. The whole point of the button — telling the operator if the webhook is alive — is defeated; the `detail` string is never shown at all. Integrations/runners health actions do this correctly with a `result` handler.
    FIX: frontend/src/screens/registry.js:119 — add a result handler mirroring integrations/runners health (line 62-63): `{ label: "테스트", path: (r) => "/api/admin/workflows/" + r.id + "/test", result: (res) => ({ ok: res.status === "reachable", msg: res.status === "reachable" ? "연결됨 (" + (res.detail||"") + ")" : "연결 실패: " + (res.detail||"확인 실패") }) }`
- [High|missing-feature|workflows] payload_schema and response_schema (the workflow's request/response data contract) are fully supported end-to-end by the backend — accepted in create (WorkflowConfig), accepted in PATCH (WorkflowUpdateRequest), versioned in snapshots, and returned in workflow_view — but the screen has ZERO surface for them: not in the create form, not in the edit form, and not even read-only in detailFields. An admin literally cannot set, view, or edit a workflow's payload/response schema anywhere in the app. Grep confirms the strings appear nowhere in frontend/src.
    FIX: frontend/src/screens/registry.js — add `{ name: "payload_schema", label: "요청 스키마(JSON)", type: "json" }` and `{ name: "response_schema", label: "응답 스키마(JSON)", type: "json" }` to create.fields (~line 98-107) and edit.fields (~line 108-116), and add `jsonField("payload_schema","요청 스키마"), jsonField("response_schema","응답 스키마")` to detailFields (line 97). jsonField is already imported (line 1) and used by prompts/policies/templates.
- [High|bug|workflows] The last_test_status column ('테스트') leaks raw untranslated English and has no semantic color. Provider emits status values 'reachable'/'unreachable', but neither is in kit.jsx STATUS_TEXT or STATUS_KIND, so Badge renders the literal English 'reachable'/'unreachable' in a Korean UI and falls back to the neutral (gray) tone — 'reachable' is not green and 'unreachable' is not red. This is the ONLY surviving signal of a test (given finding #1), and it is both unreadable and colorless.
    FIX: frontend/src/ui/kit.jsx — add to STATUS_TEXT (line 8-17): `reachable: "연결됨", unreachable: "연결 안 됨"`; add to STATUS_KIND (line 18-25): `reachable: "ok", unreachable: "danger"`. Used by registry.js:96 badgeCol("last_test_status").
- [High|missing-feature|schedules] Backend POST /api/admin/schedules/runs/{run_id}/retry (router.py:516-545) lets ops retry a FAILED schedule run, but the '실행 이력' sublist (registry.js:219-224) is columns-only with no rowAction. Failed runs display an error_message cell with no way to act on it. The jobs screen retries failed jobs, but schedule runs are a dead-end — a whole backend action is unreachable from the UI.
    FIX: Add a rowAction to the schedules '실행 이력' subList in registry.js: { label:'재시도', variant:'primary', path:(sub)=>'/api/admin/schedules/runs/'+sub.id+'/retry', confirm:()=>'이 실행을 다시 시도할까요?' }. Note SubListDrawer.cols (DataScreen.jsx:163-164) renders the rowAction on EVERY row unconditionally — add per-row `when` support (or gate visibly to status==='failed') so 성공/대기 rows don't show a retry button that 409s ('실패한 실행만 재시도할 수 있습니다').
- [High|missing-feature|schedules] Dry-run/preview is completely unexposed. Backend POST /{id}/dry-run (router.py:406-425) returns payload_preview + next_fire_times_utc (spec §18.6), and run-now accepts a dry_run body flag (router.py:448-449) — neither is wired in the registry. There is no '미리보기'/'드라이런' action. Because next_run_at is null for cron schedules until enable (create_schedule sets next_run_at only for once, router.py:264), a user has NO way to preview when a cron will fire or what payload it will send before approving/enabling it.
    FIX: Add an OPS-role action to the schedules block, e.g. { label:'미리보기', subList/result:... } that POSTs /api/admin/schedules/{id}/dry-run and renders next_fire_times_utc + payload_preview (a result-handler toast or a small drawer). Simplest: an action with a `result` that lists the next 3 fire times.
- [High|missing-feature|runners] Detail drawer surfaces almost nothing useful. The runners config has NO `detailFields` (registry.js:66-89), so clicking a runner shows only the 6 column values. Critically hidden are ops-essential fields that `runner_view` returns (service.py:52-69): `circuit_open_until` (when the circuit breaker cooldown ends / dispatch resumes), `secret_status` (whether the bound secret file exists on the server), `last_health_at` (whether the 'up' badge is fresh or days-stale), plus `timeout_seconds`, `concurrency_limit`, `auth_type`, `health_url`, `owner`, `tags`, `version`, `provider_type`, `retry_policy`, `config_version`, `created_at/updated_at`. An operator seeing '연속 실패: 5 / 상태: 성능 저하' has no way to learn when the runner becomes usable again.
    FIX: Add a `detailFields` array to the runners config (registry.js, after line 72), mirroring the pattern used by workflows (line 97) and prompts (line 138). Include at minimum: dateCol('circuit_open_until','회로 차단 해제'), field('secret_status','인증 정보 상태'), dateCol('last_health_at','마지막 상태 확인'), field('timeout_seconds','타임아웃(초)'), field('concurrency_limit','동시 실행 수'), field('auth_type','인증'), field('health_url','상태 확인 주소'), field('owner','담당자'), col('config_version','버전'), field('description','설명').
- [High|dead-affordance|runners] The 'maintenance_state' badge column ('상태', registry.js:71) displays 점검/성능 저하/정상, but there is NO control anywhere to set it. It is absent from create.fields (73-82) and the edit form reuses create.fields (DataScreen.jsx:22), so `maintenance` mode is unreachable. RunnerUpdateRequest.maintenance_state (schemas.py:75) fully supports it, and can_dispatch (service.py:188-189) blocks dispatch when state=='maintenance'. Operators cannot take a runner offline for planned maintenance, nor manually clear it, from the console.
    FIX: Add a dedicated `edit` config with editMethod 'PATCH' that includes a maintenance_state select (options: normal/degraded/maintenance), OR add explicit '점검 모드' / '점검 해제' actions in the actions array (registry.js:83-88) that PATCH {maintenance_state:'maintenance'|'normal'}, gated with `when` on current state.
- [High|missing-feature|runners] Version history and rollback are completely absent from the UI. Backend exposes GET /{id}/versions and POST /{id}/rollback (router.py:249-323), config is snapshotted on every create/update (service.py:129-153), and rollback even has an approval gate. The sibling workflows screen exposes a '버전 기록' subList action with a per-version rollback rowAction (registry.js:120-129). runners has neither, so a misconfigured runner cannot be inspected historically or reverted from the console.
    FIX: Add a '버전 기록' action to registry.js runners actions (83-88) copying the workflows subList block (120-129): endpoint (r)=>'/api/admin/runners/'+r.id+'/versions', columns [version, created_by, created_at], and rowAction that POSTs to '/api/admin/runners/'+r.id+'/rollback' with body {version: sub.version}.
- [High|ux|runners] The '테스트' action gives false-positive success feedback. The backend test endpoint returns {ok, status_code} where ok is False when the runner responds 4xx/5xx (still HTTP 200 from our API — router.py:216-220). The action (registry.js:87) has no `result` handler, so runAction falls to the else branch and always shows a green '테스트 완료' toast (DataScreen.jsx:52) even when ok===false / status_code===503. The returned status_code is never shown. Contrast the '헬스' action (86) which has a proper result handler.
    FIX: Give the 테스트 action a `result` handler like 헬스: result: (res) => ({ ok: !!res.ok, msg: res.ok ? '테스트 성공 (HTTP '+res.status_code+')' : '테스트 실패: HTTP '+res.status_code }) — registry.js:87.
- [High|missing-feature|integrations] Version history and rollback are fully built in the backend but completely absent from the screen. router.py exposes GET /{id}/versions (192-209) and POST /{id}/rollback (216-273, with approval gating), a dedicated config_versions table exists (migration 0004), and every create/edit writes a snapshot via snapshot_config (service.py:99,140). The screen even shows a config_version column (registry.js:50) — but integrations.actions (registry.js:60-64) has NO '버전 기록' subList and NO rollback action. The sibling workflows screen surfaces both. So the config_version column is a dead number: an admin can see version 5 but has zero way to view what changed or revert. All accumulated snapshots and the entire rollback endpoint are unreachable from the UI.
    FIX: In registry.js integrations.actions (line 60-64), add a '버전 기록' subList + rollback rowAction mirroring workflows (registry.js:120-129): endpoint (r)=>'/api/admin/integrations/'+r.id+'/versions', columns [version, created_by, dateCol(created_at)], rowAction posting to '/api/admin/integrations/'+r.id+'/rollback' with body {version}.
- [High|missing-feature|documents] The '기간(period)' field is required on the '+ 문서 생성' form (registry.js:254) but is write-only: request_generation stores period ONLY inside the idempotency key (service.py:154-159), never in config_json, and generation_view (service.py:191-206) never returns it. So the list and detail can never show WHICH PERIOD a generation was for — the core identity of a period-based document report is invisible after creation. Two different-target docs for the same period also silently collide on the idempotency key (target_ref='?' fallback), and the user has no visibility into why.
    FIX: Add `period` to generation_view() in app/documents/service.py (persist it as its own column or inside config_json), then surface it as a column and detailField in the documents config (registry.js:259/261), e.g. col('period','기간').
- [High|missing-feature|documents] The documents config (registry.js:247-262) sets neither `paginated:true` nor `searchable`, but the backend list_generations is server-paginated (default page_size=20, pagination.py:13). DataScreen.buildUrl only sends page params when config.paginated is set, so the screen永 only ever loads the 20 most-recent generations with NO pager, NO total count, and the search box filters client-side over just those 20. On a document-generation LOG that accumulates over time, all older records become permanently unreachable — unlike the analogous jobs/audit/approvals screens which all set paginated:true.
    FIX: Add `paginated: true` (and optionally pageSize) to the documents config in registry.js so DataScreen renders the pager and requests successive pages; backend already returns total/page/page_size.
- [High|dead-affordance|prompts] The prompt lifecycle is a one-way trap in the React screen. registry.js prompts.actions (lines 149-154) only exposes forward edges: draft→test (테스트로), test→review (검토로), review→published (발행). Edit is draft-only (editWhen line 145). But models.py VALID_TRANSITIONS (lines 20-26) also allows test→draft, review→draft, and →archived from EVERY state. So a prompt pushed to 'test' or 'review' with a typo cannot be edited (frozen outside draft), cannot be sent back to draft to fix it, cannot be archived/retired, and cannot spawn a new version (새 버전 is published-only, line 153). The admin is forced to either publish a flawed prompt live or leave a permanently-stuck orphan row.
    FIX: Add transition actions for the missing edges the backend already supports: a '초안으로' action when status is 'test' or 'review' (POST .../transition body {status:'draft'}), and a '보관'(archive) action available on draft/test/review/published (POST .../transition body {status:'archived'}, danger+confirm). This mirrors the vanilla 상태변경 form (dist/stage sections.js lines 125-128) which let admins pick any valid target status. registry.js:149-154
- [High|missing-feature|prompts] The rollback capability is completely unexposed. Backend has POST /api/admin/prompts/rollback (router.py lines 225-238) which republishes an old version's content as a new published version — the intended recovery path (spec §17). The React prompts config has no rollback action at all (grep confirms rollback UI exists only for workflows, registry.js:120-128). After publishing a bad v2, an admin has no console path to restore v1's content; the backend endpoint is dead-wired. Vanilla exposed it as the '롤백' row action (dist/stage sections.js lines 135-138).
    FIX: Add a rollback affordance for prompts, e.g. a '버전 기록' subList (like workflows) or a rollback action collecting the target version, calling POST /api/admin/prompts/rollback with body {name, version}. registry.js:132-155
- [High|missing-feature|prompts] Version comparison (diff) is dropped. Backend has GET /api/admin/prompts/diff/view?name=&from=&to= (router.py lines 216-223, covered by test_diff_between_versions) that returns a unified diff between two versions. The React prompts screen has no diff/compare action (grep confirms no diff UI in frontend/src except code comments). The screen's stated purpose is version management with a '새 버전' button, yet users cannot compare what changed between versions. Vanilla shipped this as the '비교' action and diffModal (dist/stage sections.js lines 131-134, common.js diffModal).
    FIX: Add a '비교' action that collects from/to version numbers and renders the diff. Requires a diff-capable affordance in DataScreen (the current generic action runner only toasts/subLists — a diff viewer modal would need to be reintroduced, as in vanilla common.js diffModal). registry.js:132-155
- [High|dead-affordance|approvals] The backend approve/reject endpoints accept a decision comment (DecisionRequest.comment, max 2000 chars) which decide() stores as decision_comment AND sends verbatim as the notify_user body to the requester (service.py:147-155). The registry '승인'/'거절' actions (registry.js:272-273) only carry a `confirm` string and no `fields`, so runAction posts `body: a.body || {}` = {} (DataScreen.jsx:45) — the comment is NEVER collected or sent. Worse, the detail drawer renders a `field("decision_comment", "결정 메모")` row (registry.js:270) that no control can ever populate, so it is permanently '—'. Rejections therefore carry no reason and the requester's 'approval_decided' notification arrives with an empty body — the user can never learn why their request was rejected.
    FIX: Add `fields: [{ name: "comment", label: "결정 메모 / 거절 사유", type: "textarea" }]` to the 승인 and (especially) 거절 actions in registry.js:272-273. runAction already routes actions with `fields` through the FormDrawer (DataScreen.jsx:41,136-140), which submits the form body — so the comment would flow to the endpoint with no framework change.
- [High|missing-feature|notion-mapping] mapping_view returns `error_message` (e.g. '일치하는 Notion 사용자가 없습니다', 'N명이 일치하여 충돌합니다', '매핑 조회 실패', 'Notion 매핑 Workflow가 구성/활성화되지 않았습니다') and it is present in the list response, but the config never renders it in columns or detailFields. Combined with runAction (DataScreen.jsx:52), clicking '검증' always fires a green '검증 완료' toast and then closes the drawer (setSel(null)) even when verification found no match or a conflict. The admin sees a success toast, the drawer disappears, and the reason for failure is never shown anywhere — a dead-end feedback loop on the screen's primary action.
    FIX: registry.js:299 — add `field("error_message", "오류")` to detailFields (and consider a column). Optionally give the '검증' action (registry.js:304) a `result:` handler that surfaces the returned mapping.status/error_message instead of an unconditional '검증 완료'.
- [High|bug|notion-mapping] '자동 동기화' (registry.js:300-302) POSTs to /sync which returns 202 with {job_id, status:'queued'|'running'} — it only ENQUEUES a job. runHeaderAction (DataScreen.jsx:55-62) calls announce(res,...) which, because res.status is 'queued' (not 'approval_pending'), toasts '자동 동기화 완료' as success and immediately refresh()es the list before the job has run. There is no job polling, no job_id tracking, and no 'in-progress' indicator. The sync docstring explicitly says the screen should re-fetch mappings 'once this job finishes' — but nothing detects completion. The admin sees '완료', the refreshed list is unchanged (job still queued/running), and the result is never observed.
    FIX: Give the sync headerAction a `result:` handler that reports '동기화 작업이 시작되었습니다 (진행 중)' rather than '완료', and/or poll the returned job_id / re-query on job completion. At minimum stop reporting an enqueue (202) as a finished operation.
- [High|missing-feature|notifications] The full 'notifications' screen is entirely read-only: it defines no `actions`, so there is no per-row 'mark read' control even though the backend fully supports it (POST /api/notifications/{id}/read, router.py:78). Opening a row just shows a read-only detail drawer (DataScreen.jsx:112-124) and does NOT mark the notification read either. So on the dedicated 'view all notifications' page a user can never mark anything read — an unread notification stays unread forever. The bell popover (NotificationBell.jsx:42, openItem line 67) DOES mark-read on click, so the full screen is a strict downgrade of the small popover.
    FIX: registry.js:336-343 — add `actions: [{ label: "읽음", variant: "primary", when: (r) => !r.read_at, path: (r) => "/api/notifications/" + r.id + "/read" }]`. DataScreen already wires row actions to POST + refresh + toast (DataScreen.jsx:40-53).
- [High|missing-feature|notifications] No '모두 읽음' (mark all read) affordance on the full screen, despite the backend POST /api/notifications/read-all (router.py:68) that the bell already uses (NotificationBell.jsx:48). A user who opens '전체 알림 보기' with dozens of unread notifications has no way to clear them in bulk — the one operation the dedicated inbox page most needs is absent.
    FIX: registry.js:336-343 — add `headerActions: [{ label: "모두 읽음", path: () => "/api/notifications/read-all", confirm: "안 읽은 알림을 모두 읽음 처리할까요?" }]`. DataScreen renders headerActions in the page header and calls runHeaderAction (DataScreen.jsx:74-81,55-62).
- [High|bug|notifications] The '읽음' column uses badgeCol("read_at") (registry.js:341) on a nullable ISO-timestamp field, which renders garbage. read_at is either null (unread) or an ISO string like "2026-07-18T16:21:23.704973" (read). Badge→statusText (kit.jsx:26-35): null maps to STATUS_TEXT["unknown"] = '알 수 없음', and a read timestamp is not in STATUS_TEXT so the raw microsecond UTC string is shown verbatim as the badge text. Result: unread rows show '알 수 없음' (looks like an error) and read rows show a raw ISO timestamp. The status column is meaningless and actively misinforms; it also never passes through fmtDateTime.
    FIX: registry.js:341 — replace badgeCol("read_at","읽음") with a boolean render, e.g. `{ key: "read_at", label: "읽음", render: (r) => r.read_at ? "읽음" : "안 읽음" }` (optionally a Badge keyed on a computed boolean). Same broken cell also appears in the detail drawer since it reuses config.columns (DataScreen.jsx:118).
- [High|dead-affordance|audit] The search box (placeholder '검색', rendered unconditionally at lines 88-91) is a misleading affordance on audit. Audit is paginated and NOT searchable, so buildUrl never sends q to the server (line 26 gates on config.searchable) and the client filter at line 67 only matches the 20 rows of the CURRENT page. Searching a user_id/action that exists but is on another page yields the empty-state '검색 결과가 없습니다. 검색어를 지워보세요.' (line 98) — a false negative on a forensic tool, worse than no search because it implies the record does not exist.
    FIX: Either gate the toolbar search input on config.searchable (hide it for audit), or replace it with the real server-side filter controls from the Critical finding so typing actually queries the whole audit trail rather than one page.
- [High|missing-feature|jobs] The backend list endpoint supports a server-side `status` filter (validated against ALL_STATUSES) but the jobs screen exposes NO status filter. The operator's #1 task is find-failed-then-retry, yet the only way to locate failed jobs is manual paging. Worse: the Dashboard deep-links its '실패 작업 N' alert straight to /jobs (Dashboard.jsx:44) — the operator clicks it expecting to see the failed jobs and instead lands on an unfiltered, paginated dump with no way to isolate them. The retry action (registry.js:323) only renders on failed rows, so if the failed job is on page 3 it's effectively unreachable.
    FIX: Add a status filter control. registry.js jobs config (line 315-326) has no filter mechanism and DataScreen.buildUrl (DataScreen.jsx:24-29) only serializes q+page. Add a `filters` concept to DataScreen that appends `status=<value>` to the query, and expose a status dropdown (queued/running/succeeded/failed/cancelled) in the jobs config so the backend `status` param is used.
- [High|missing-feature|jobs] The dedicated `GET /api/admin/jobs/stats` endpoint (queue_stats: queued, running, succeeded, failed, cancelled, ready, oldest_queued_at) is NEVER called anywhere in the frontend — grep for jobs/stats returns nothing. The screen's stated purpose is '처리 현황과 실패를 봅니다' (see processing status), but there is no at-a-glance health summary: no count tiles, no backlog-age (oldest_queued_at) indicator, no 'ready to run' count. An operator cannot see queue health without paging the raw list. DataScreen has no mechanism to render a stats/summary row at all.
    FIX: Wire GET /api/admin/jobs/stats into the jobs screen as a StatCard row (kit.jsx StatCard already exists, as used in Dashboard.jsx). Either give DataScreen an optional `statsEndpoint`+`statCards` config, or build a thin Jobs wrapper screen that renders the stat row above the DataScreen. registry.js:315-326.
- [High|bug|backup] A FAILED backup shows a false success toast. create_backup (router.py:41) is decorated status_code=201 and run_backup (service.py:64) swallows all exceptions setting status='failed', so a failed backup still returns HTTP 201. The '+ 백업 실행' headerAction (registry.js:349) has no result handler, so runHeaderAction (DataScreen.jsx:55-62) calls announce(res, '백업 실행 완료') and shows a green success toast even when the backup failed — while the new row simultaneously renders a red '실패' badge. The row-level '검증' action correctly inspects res.verify.ok, but create does not inspect res.backup.status.
    FIX: Give the '+ 백업 실행' headerAction a result handler that reads res.backup.status ('verified'/'succeeded' = ok; 'failed' = error showing res.backup.error_message) — AND extend runHeaderAction in DataScreen.jsx:55 to honor a.result like runAction does (currently header actions ignore result entirely). Also consider a busy/progress indicator: run_backup is synchronous and can be slow on a large DB, but the header button gives no in-flight feedback.
- [High|missing-feature|backup] The backup config defines NO detailFields at all, so error_message is never visible anywhere. backup_view returns error_message populated on failure ('restore verify 실패: ...', '재검증 실패: ...', or '{ExceptionType}: {msg}'), but it is neither a column nor a detail field. Clicking a failed backup row opens a drawer showing only the 4 columns (created_at, status, size, path) — the reason for failure is completely hidden, making a failed backup undiagnosable from this screen. (Contrast: documents surfaces error_message in detailFields; schedules run-history shows it as a column.)
    FIX: registry.js backup config (after line 351): add detailFields: [field('id','ID'), field('backup_type','유형'), field('created_by','실행자'), field('checksum','체크섬(SHA-256)'), dateCol('verified_at','검증 시각'), field('error_message','오류')]. error_message and verified_at are the highest priority.
- [High|dead-affordance|shared-infra] On screens ≤860px the sidebar (the ONLY admin navigation) is hidden with `display:none` and there is no hamburger, drawer, or any replacement control in the topbar. An admin on a phone/narrow window can reach only the Dashboard (via brand logo or the 사용자/관리자 segmented toggle) and is completely cut off from Users, Integrations, Runners, Workflows, Approvals, Audit, Backup and every other section. This directly contradicts the app's heavy responsive investment elsewhere (card-mode tables at ≤760px in kit.css:64, full-screen modals at ≤640px in kit.css:93) — all of which becomes unreachable because navigation vanishes.
    FIX: global.css:94 hides `.c-sidebar` with no substitute. Add a topbar hamburger button (rendered only ≤860px) that opens the existing <Sidebar/> as an overlay/off-canvas drawer, closing on nav or backdrop click. Wire it in App.jsx Topbar (line ~191) and AdminBody (line ~201).
- [High|bug|shared-infra] On the 4 paginated screens (audit/jobs/approvals/notifications) the toolbar search only filters the rows of the CURRENTLY-LOADED page. buildUrl (DataScreen.jsx:26) only appends q= when config.searchable is truthy, but NO registry entry ever sets searchable, so the server never receives the query; queryKey (line 31) also omits q for these screens so typing does not refetch; filtered (line 66-68) then does an in-memory filter of just the current page. A user searching an audit log for an event on page 3 gets 'no results' on page 1 and the empty state literally says '검색 결과가 없습니다' (line 98) — falsely implying the record does not exist.
    FIX: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\DataScreen.jsx:24-34,66-68 — for paginated lists, drive search server-side: always send q= (add a `serverSearch`/reuse searchable flag on paginated configs), include q in the queryKey, and reset page to 1 when q changes. Do not silently in-memory-filter a single page while a pager implies the full dataset.
- [High|bug|shared-infra] FormModal cannot CLEAR an optional text/number/textarea/json field on edit. submit() (kit.jsx:224-229) only writes a key into the body when `val !== "" && val != null`, so blanking such a field just omits it. For PATCH edits (workflows owner/purpose, prompts purpose, etc.) the old value silently persists; for PUT full-replace edits (templates, schedules) omitted fields get reset to server defaults — the exact round-trip the registry comments (registry.js:188,226-228) warn against. It is also inconsistent: the select branch (line 227) deliberately sends null to clear, but text/number/json get no such path.
    FIX: C:\Users\hshwa\clovirone-web-assistant\frontend\src\ui\kit.jsx:224-229 — when a field was initialized with a value and is now blank, send null (or explicitly include all declared keys for PUT screens) instead of dropping the key, matching the select-clear behavior.
