# 라운드30 남은 발견사항(test-gap 제외) — 영역별


## AREA=A (46)

- [High|bug|shared-infra] 
  ISSUE: The 'source(출처)' filter on the Notion-mapping screen is a broken/misleading control. That screen is paginated (registry.js:1326 `paginated:true, searchable:true`) but the source filter is `clientFilter:true` (registry.js:1337), and DataScreen only filters the current page: `searched` = current-page items, then `filtered = searched.filter(clientFilterDefs...)` (DataScreen.jsx:317-323). So selecting source=manual only surfaces manual rows that happen to be on the current page; matching rows on oth
  FIX: clientFilter is only sound on fully-loaded (non-paginated) lists. Either (a) add real server-side `source` support to GET /api/admin/notion-mapping and drop clientFilter, or (b) if it must stay client-side, keep the pager visible in the filtered-empty state and add a Callout that the filter only covers the current page. In DataScreen.jsx guard against pairing `paginated` with any `clientFilter` filter (warn/ignore), and in the filtered-empty branch (DataScreen.jsx:464) still render the pager whe
- [Medium|empty-state|documents] 
  ISSUE: The documents screen gives almost no onboarding/prerequisite guidance: only emptyTitle + a one-line emptyHelp (registry.js:1070-1074), whereas peer screens (integrations/runners) provide emptySituation/emptyPrerequisite/emptySteps/emptyExpected/emptyRelatedLink. Critically, document_automation_enabled is a Settings toggle (app/settings/registry.py:120, default True but disableable); when off, every '+ 문서 생성' submit returns 409 '문서 자동화 기능이 비활성화되어 있습니다' (service.py:230-231), yet nothing in the emp
  FIX: When document_automation_enabled is off, surface a banner/disabled '+ 문서 생성' with a link to #/settings; add emptySteps/emptyPrerequisite/emptyRelatedLink (to #/workflows) like integrations/runners. registry.js:1070-1091.
- [Medium|ux|documents] 
  ISSUE: The '+ 문서 생성' form takes workflow_id as a free-text field (DOC_GENERATE_FIELDS, registry.js:272) requiring the admin to manually copy a UUID from the workflows screen, and template_id is buried as a raw key inside the config JSON textarea (registry.js:275). A backend workflow list endpoint exists that could populate a dropdown, but the form does not use it. Wrong/disabled IDs only fail at submit time ('대상 Workflow가 없습니다' / '비활성화된 Workflow로는...', service.py:238-241).
  FIX: Replace the workflow_id text input with a select populated from GET /api/admin/workflows (enabled only), and offer a template picker instead of a raw JSON key. registry.js:271-276.
- [Medium|bug|departments] 
  ISSUE: The departments screen renders an always-on client search box (showSearch = !paginated = true, DataScreen.jsx:336) but omits `searchFields`/`searchPlaceholder`, so searchText(r) falls back to JSON.stringify(row) (DataScreen.jsx:314-316). Search therefore matches the raw UUID `id`, the raw boolean `active` ('true'/'false'), and the raw UTC-ISO `created_at` — none of which match what the user sees. Typing the KST date shown in the '생성' column fails (stored value is UTC; near a midnight boundary ev
  FIX: Add `searchFields: ["name"]` and `searchPlaceholder: "부서 이름으로 검색"` to the departments config at frontend/src/screens/registry.js:1251 (mirror job-titles registry.js:1289-1290).
- [Medium|missing-feature|integrations] frontend/src/screens/registry.js
  ISSUE: The disable action has no blast-radius visibility. The screen's own help text (registry.js:282) promises "활성/비활성화는 이 연동을 참조하는 러너의 실제 호출을 막습니다", and app/runners/provider_http.py:40-43 confirms disabling makes every runner bound via integration_id fail dispatch with RunnerUnavailableError. Yet the disable action (registry.js:339, onoff helper) uses only the generic confirm "이 항목을 비활성화할까요?" — the admin cannot see which/how many runners will break, and the runners screen has no integration_id filter
  FIX: Surface the affected runners before disabling: either add a referencing-runners count/list to integration_view (backend) and show it in a per-row disable confirm the way activeToggle does for departments/job-titles (registry.js:176-179 uses r.user_count), or add an integration_id filter to the runners screen and link to it. At minimum give the disable confirm a stronger warning that referencing runners will stop dispatching.
- [Medium|missing-feature|integrations] frontend/src/screens/registry.js
  ISSUE: The '상태 확인' (last_health_status) filter offers only up/down (registry.js:303) but the model defaults last_health_status to HEALTH_UNKNOWN='unknown' (app/integrations/models.py:14,30). Every freshly created row and all 4 discovery-seeded integrations start in 'unknown' and stay there until a health check runs, so on a real/fresh install the most common state is unfilterable. Worse, selecting '정상' or '중단' silently excludes all never-checked rows, hiding most of the list with no hint that an 'unkno
  FIX: Add the missing option to the filter, e.g. options: opt([["up","정상"],["down","중단"],["unknown","미확인"]]) at registry.js:303 so operators can find integrations that have never been health-checked.
- [Medium|empty-state|policies] 
  ISSUE: Because filters are initialized from f.value (DataScreen.jsx:50-54) and status defaults to 'published', hasFilter (DataScreen.jsx:334) is TRUE on first load. On a brand-new install with zero policies, the empty branch (DataScreen.jsx:464-468) therefore renders '검색 결과가 없습니다 / 조건에 맞는 항목이 없습니다. 검색어나 필터를 지워보세요.' with only a '검색·필터 지우기' button. The real onboarding empty state (emptyTitle '등록된 정책이 없습니다' + role-aware emptyHelp CTA at registry.js:715-719) is effectively dead code — it can essentially ne
  FIX: Distinguish user-applied filters from default (f.value) filters when computing hasFilter, or when filtered is empty AND only default filters are set, fall through to the onboarding EmptyState (config.emptyTitle/emptyHelp) instead of the 'search results empty' branch (DataScreen.jsx:334, 464-468).
- [Medium|missing-feature|schedules] frontend/src/screens/registry.js:933
  ISSUE: The schedule list surfaces no last-run OUTCOME and never auto-refreshes. Columns show `last_run_at` as a bare timestamp (line 943) with no success/failure indicator, and there is no `pollWhile`/status column. To learn whether a nightly cron is silently failing every night, an admin must open each row and drill into '실행 이력'. The config comment at lines 986-987 explicitly admits '목록엔 자동 갱신도 상태 열도 없다'. For a screen whose stated purpose is monitoring scheduled automation, this defeats the primary at
  FIX: Add a last-run status field to the list _view in app/schedules/router.py (join the most recent ScheduleRun → status) and render it as a badge/mapCol next to last_run_at in registry.js columns; optionally add a manual refresh or pollWhile for schedules with an active/queued run.
- [Medium|design|schedules] frontend/src/screens/registry.js:980
  ISSUE: The '소유자' (owner) detail field renders `owner_user_id` as a raw opaque UUID. Backend _view (app/schedules/router.py:141) returns only the raw id, unlike the audit/prompt/policy screens which resolve created_by → name/email (see nameVersionsAction, actor_name pattern). The owner field is therefore unreadable — an admin cannot tell who created a schedule without a separate lookup.
  FIX: Have _view (router.py:133) join the owner User and return owner_name/owner_email (masking as elsewhere); render owner_name || owner_email in detailFields instead of the raw UUID.
- [Medium|dead-affordance|workflows] 
  ISSUE: The two reserved workflows (ClovirONE AI 업무 도우미, notion-user-mapping) have ALL editing removed via editWhen (registry.js:560), and RESERVED_EDIT_HINT (registry.js:129) tells the admin '이 화면에는 수정 경로가 없으니 시스템 관리자에게 문의하세요 (직접 DB 조치 필요)'. But the backend PATCH /api/admin/workflows/{id} (router.py:64) fully supports editing these rows, and notion-user-mapping's webhook_url is live-invoked by Notion mapping (notion_mapping/service.py:131). So a legitimate need — e.g. n8n host/port changes — has no wor
  FIX: Replace the blanket editWhen block with a surgical edit form for reserved rows: lock only the contract field (name) via a read-only/disabled input while allowing webhook_url/purpose/owner/tags/mode edits (the backend PATCH already accepts them). At minimum, drop the false 'DB surgery required' hint since the PATCH endpoint works, and reconcile edit vs rollback so the same fields aren't blocked in one path and mutable in the other. registry.js:129-137,558-560,597-623
- [Medium|dead-affordance|runners] frontend/src/screens/registry.js:428
  ISSUE: The runner create AND edit forms expose an editable '재시도 정책(JSON)' field (retry_policy, create line 428 / edit line 445) and a '기능(JSON)' field (capabilities, create 426 / edit 443), but neither value is ever consumed at runtime. app/runners/service.py only json.dumps/loads them and provider_http.invoke() has NO retry logic at all. By contrast schedules.retry_policy IS read (app/schedules/scheduler.py:114), and the integrations form explicitly labels capabilities as '설명용 메타데이터이며, 어떤 런타임 동작도 이 값으
  FIX: Either wire retry_policy into provider_http.invoke()/the job dispatch path, or add the same 'metadata only, does not affect runtime' caveat to the retry_policy and capabilities field help in the runners create/edit config (mirror integrations.create capabilities help at registry.js:323).
- [Medium|missing-feature|runners] frontend/src/screens/registry.js:393
  ISSUE: A runner can display fully green on this screen (enabled=활성, maintenance=정상, last_health_status=정상, and '테스트 통과') while being functionally unable to dispatch any work, because provider_http.invoke() blocks dispatch when the linked integration is disabled ('연결된 Integration이 비활성화되어 있습니다', app/runners/provider_http.py:40-43). The screen surfaces NO signal for this: runner_view (service.py:52) never joins the integration's enabled state, and both the '헬스' and '테스트' actions bypass can_dispatch (raw G
  FIX: Include the linked integration's enabled status in runner_view and add a column/detail field (or a Callout in the detail drawer) that flags 'dispatch blocked: linked integration disabled' when integration_id points to a disabled integration.
- [Medium|ux|runners] frontend/src/screens/registry.js:365
  ISSUE: The runners screen renders a search box (it is non-paginated, so DataScreen showSearch = !paginated = true) but sets no config.searchFields, so DataScreen falls back to searchText = JSON.stringify(row) (DataScreen.jsx:314-316). Search therefore matches raw hidden values the user cannot see: id (UUID), secret_ref, raw enums ('up'/'down'/'normal'/'local_http'), ISO timestamps (created_at/updated_at/circuit_open_until), and the capabilities/retry_policy JSON blobs. Typing 'up' or 'normal' silently 
  FIX: Add searchFields: ['name','owner','base_url','version'] (visible, meaningful columns) to the runners config, matching the remediation pattern already used on other screens.
- [Medium|ux|prompts] 
  ISSUE: The prompts status filter sets no default value (registry.js:653-654), so a first visit shows a wall of interleaved draft/test/review/published/archived rows across every prompt name — the admin cannot tell 'what is actually in use now' without manually filtering. The policies screen defaults its status filter to 'published' (registry.js:740) for exactly this documented reason (policies:734-736). Prompts has the identical problem but did not adopt the fix.
  FIX: Add value: 'published' to the status filter option object at registry.js:654 (matching policies:740). The empty '전체' option remains so users can still widen the scope.
- [Medium|missing-feature|templates] 
  ISSUE: Building/editing a template requires the admin to manually copy-paste raw UUIDs for target_ref (workflow), prompt_id, and policy_id (registry.js:855-857, 893-895 are all free-text inputs whose help says 'go to the other screen and find the ID'). There is no dropdown/autocomplete of the available workflows/prompts/policies, even though the backend list endpoints exist to populate one and the create path is the primary way to serve this screen's domain. A typo only surfaces as a server-side 400 (_
  FIX: Turn target_ref/prompt_id/policy_id into select fields (or datalistFrom-style autocomplete like policies name filter uses) sourced from /api/admin/workflows, /api/admin/prompts, /api/admin/policies. DataScreen already supports f.optionsFrom / select fields; wire them so the user picks a name and the ID is submitted.
- [Medium|empty-state|templates] 
  ISSUE: The backend forces enabled=False on every create (router.py:141), and the ONLY action that actually uses a template — '이 템플릿으로 문서 생성' — is hidden unless r.enabled && target_type==='workflow' (registry.js:871). So immediately after creating a template the admin lands on a row whose detail drawer offers only 수정 / 활성화 / 감사 로그, with no inline explanation of why the 'use this template' CTA is absent. Only the page-top help Callout mentions activation; the drawer itself gives no hint that you must ena
  FIX: In detailFields add a conditional note (like the existing _runner_target_note at registry.js:843) shown when !r.enabled: e.g. '이 템플릿은 비활성 상태입니다 — 활성화해야 문서 생성에 사용되고 바인딩(프롬프트·정책·입력 스키마·승인 정책)이 적용됩니다.' so the missing CTA is explained at the point of confusion.
- [Medium|empty-state|approvals] 
  ISSUE: The status filter defaults to 'pending' (registry.js:1176 sets filter value:"pending"). DataScreen initializes filters from that default, so hasFilter is ALWAYS true on first load. When there are no pending approvals — the normal, healthy state of an approval queue — the empty branch takes the '(q || hasFilter)' path and shows the generic "검색 결과가 없습니다 / 조건에 맞는 항목이 없습니다. 검색어나 필터를 지워보세요" with a '검색·필터 지우기' button, NOT the carefully written domain empty state (emptyTitle "승인 요청이 없습니다" + emptyHelp e
  FIX: In DataScreen.jsx empty-state branch (~line 464), distinguish a user-applied filter from a config-default filter: compute hasFilter by comparing against each filter def's f.value (treat filters equal to their config default as 'not actively filtered'), so the domain emptyTitle/emptyHelp shows when the only active filter is the default 'pending'. Alternatively give approvals a config flag to render the domain empty state when the active filter equals the default. Files: frontend/src/screens/DataS
- [Medium|design|backup] frontend/src/screens/registry.js:1691
  ISSUE: The "실행자 ID" (executor) field renders created_by as a raw user UUID with no name/email resolution. On an accountability-oriented ops screen, an admin/auditor cannot tell WHO ran a given backup without manually cross-referencing the users table by UUID. The app has an established pattern for resolving actor UUIDs to human names (audit log actor_name; prompts/policies show created_by_name/created_by_email) that was not applied here — the backend backup_view (service.py:62) returns only the raw cre
  FIX: In app/backups/service.py backup_view(), resolve created_by to created_by_name/created_by_email (same join the audit actor_name uses), and change the registry field to render the name with the UUID as fallback (like nameVersionsAction's created_by render at registry.js:227).
- [Medium|missing-feature|jobs] 
  ISSUE: No bulk retry / bulk cancel, despite the screen's stated purpose being incident triage of a jammed queue (help text line 1434; comment line 1444-1446 '큐가 정체된 실제 장애 상황에선 실패/대기 행을 빠르게 훑어야 한다'). The stats cards surface the failed/queued counts and clicking drills to a filtered list (registry.js:1457-1465), but the only remediation is per-row: open each of, say, 40 failed jobs after a runner outage and click '재시도' with a confirm each time. Backend also only offers per-job POST /{id}/retry and /{id}/
  FIX: Add batch endpoints (e.g. POST /api/admin/jobs/retry-failed and /cancel-queued accepting the same status/job_type filters, gated to OPS_ROLES + CSRF + audit) and surface them as a summary/header action on the jobs screen (e.g. 'failed N건 모두 재시도') so the count tiles become actionable end-to-end.
- [Medium|dead-affordance|audit] 
  ISSUE: The '작업(정확히, 예: user.update)' filter (registry.js:1546) is exact-match free text — the user must type the raw action string verbatim (e.g. 'prompts.update_content', 'cli.user.enable', 'user.role_change_requested'). But the raw string is NEVER plainly shown: the '작업' column renders only the Korean actionKo() translation and exposes the raw value only via a hover `title` tooltip (registry.js:46), which doesn't work on touch and can't be copied. The detail drawer reuses that same column render, so 
  FIX: Add `datalistFrom: (items) => items.map(r => r.action)` to the action filter (registry.js:1546) so DataScreen (already supports datalistFrom, DataScreen.jsx:440-450) suggests real action strings from the loaded page; and add a plain-text raw-action row to detailFields (e.g. `{ key: "action_raw", label: "작업(원문)", render: (r) => r.action || "—" }`) so the exact filter value is visible and copyable.
- [Low|bug|login] 
  ISSUE: _verify_login_origin (router.py lines 92-123) is the login-CSRF defense but explicitly does NOT block when Sec-Fetch-Site is absent AND neither Origin nor Referer is present ('this check can't decide and does not block'). It is a hardening layer only. A non-browser or header-stripping client can POST /login (form-urlencoded fallback path) with no origin signal and bypass the guard entirely. Modern browsers always send Sec-Fetch-Site, so real victims are largely protected, but the fallback leaves
  FIX: Acceptable as documented defense-in-depth. If stricter posture is desired, treat total absence of all three signals on the form-urlencoded (no-JS) POST path as failable, or require a same-origin token on the no-JS form.
- [Low|missing-feature|documents] 
  ISSUE: The only list filter is status (registry.js:1087). There is no way to narrow by mode, requester, period, or date — peer screens (e.g. integrations) add clientFilter selects for such fields over the already-fetched page. The backend list only accepts a status server filter, but client-side filters could still be offered.
  FIX: Add clientFilter selects for mode and (once resolved) requester, matching the integrations.filters clientFilter pattern. registry.js:1087.
- [Low|ux|integrations] frontend/src/screens/registry.js
  ISSUE: When an admin (needs_approval) edits a sensitive field (base_url/health_url/secret_ref/auth_type) the PATCH returns 202 approval_pending (app/integrations/router.py:115-135) and the UI correctly toasts '승인 요청이 접수되었습니다' via announce(). But the list refreshes to the OLD (unchanged) row with no '변경 승인 대기' indicator, so the admin can re-open edit and submit the same change again, stacking duplicate approval requests without knowing one is already pending.
  FIX: Expose pending-approval state per integration (integration_view could include a has_pending_change flag by cross-referencing approvals of request_type 'integration.change_config') and show a badge/column plus a warning in the edit form when a change is already awaiting approval.
- [Low|bug|policies] 
  ISSUE: The '이 정책을 쓰는 템플릿' impact-preview sublist (registry.js:777-784) fetches the entire /api/admin/templates list and client-filters by policy_id (filterRows), and is not paginated. The backend caps the list at 500 rows (page_size le=500). SubListDrawer shows a generic '결과가 500건으로 제한되어 일부 항목이 보이지 않을 수 있습니다' warning (DataScreen.jsx:688), but this preview is meant to be consulted before a publish/archive/rollback that the screen's own help warns is high-impact — if >500 templates exist, referencing tem
  FIX: Add a backend policy_id query filter to list_templates and use it (paginated) for this sublist, or make the truncation warning specific ('영향받는 템플릿 목록이 잘렸을 수 있습니다') so users don't trust an incomplete impact preview (registry.js:777-784).
- [Low|dead-affordance|schedules] frontend/src/screens/registry.js:985
  ISSUE: The '활성화' button's extraEnableWhen only hides it for once-schedules with a falsy next_run_at (already-run). A once-schedule that was created but left disabled until AFTER its run_at passes still has next_run_at set (now in the past), so the button renders but the backend always returns 409 '실행 시각이 이미 지났습니다' (router.py:413-414). The click surfaces an error toast, so it is not silent, but it is a guaranteed-failure affordance.
  FIX: Extend extraEnableWhen at registry.js:985 to also hide enable for once-schedules whose next_run_at is in the past (e.g. compare against a client clock, or have _view expose an `enable_blocked` flag), pointing the user to '수정' to set a new run_at.
- [Low|missing-feature|schedules] frontend/src/screens/registry.js:922
  ISSUE: There is no delete/archive capability — the backend exposes no DELETE endpoint (only the 10 confirmed routes), so completed once-schedules and long-disabled crons accumulate in the list permanently with no cleanup path. The config comment at line 922 acknowledges this ('스케줄은 삭제/보관 엔드포인트가 없어 비활성·완료된 once형도 계속 목록에 남으므로'). Over time the list becomes cluttered with dead entries, and the only mitigation is a client-side 'enabled=false' filter.
  FIX: Add a backend soft-delete/archive endpoint (DELETE or POST /{id}/archive) in app/schedules/router.py and surface it as a WRITE_ROLES action + an 'archived' filter; at minimum add a default hide-completed toggle.
- [Low|missing-feature|schedules] frontend/src/screens/registry.js:1029
  ISSUE: A schedule run stuck in 'queued' or 'running' (e.g. after a worker crash) has no recourse from this screen. The run-history '재시도' action only appears for status==='failed' (line 1029) and the backend retry_run rejects any non-failed status (router.py:571-572, '실패한 실행만 재시도할 수 있습니다'); there is no cancel/requeue endpoint. Non-terminal stuck runs are therefore un-actionable here.
  FIX: Add a backend cancel/requeue endpoint for non-terminal schedule runs and surface it as an OPS_ROLES rowAction in the '실행 이력' subList; if these are meant to be handled on the jobs screen, add a 'related job' deep-link from the run row.
- [Low|ux|schedules] frontend/src/screens/registry.js:942
  ISSUE: Two coupled polish gaps: (1) the create/edit form always shows both `cron_expression` and `run_at` regardless of schedule_type with no conditional display (comment at 961-962), relying solely on help text so users can fill the field the chosen type ignores; (2) a disabled schedule's scheduled run time is invisible — the next_run_at column renders '—' when disabled (line 942) and detailFields omit next_run_at/run_at (line 980), so the only way to see when a disabled once-schedule will fire is to 
  FIX: Drive conditional visibility of cron_expression vs run_at from schedule_type in the FormModal; and show next_run_at in detailFields with a label like '다음 실행(비활성 — 예정)' so a disabled schedule's timing is visible without entering edit mode.
- [Low|dead-affordance|workflows] 
  ISSUE: payload_schema / response_schema are full JSON data-entry fields in both create and edit forms (registry.js:578-579,591-592) and are shown in detail (jsonField, registry.js:569), but they are non-functional — the help text itself admits '설명용 메타데이터입니다 — 이 값으로 실제 요청/응답을 검증하는 런타임 동작은 없습니다.' Admins can spend effort authoring schemas that never gate or validate anything.
  FIX: Either wire payload/response schema validation into invoke/test (making the fields load-bearing), or de-emphasize them (collapse into an 'advanced/optional metadata' section) so they don't read as functional contract enforcement. registry.js:578-579,591-592
- [Low|contract|runners] frontend/src/screens/registry.js:424
  ISSUE: integration_id is create-only: it appears in the create form (line 424) but not the edit form, and backend RunnerUpdateRequest (schemas.py:75-89) has no integration_id, so it can never be changed via PATCH. There is ALSO no DELETE endpoint (app/runners/router.py has none) and no delete action in the registry. A mistyped integration_id is therefore permanently unrecoverable through this screen (only disable is possible). Yet the create-form help at line 424 tells the user the only recourse is '이 
  FIX: Either add integration_id to RunnerUpdateRequest + the edit form (so typos are fixable), or correct the create-form help text to stop referencing deletion (which is unsupported) and state the real recovery path.
- [Low|design|runners] frontend/src/screens/registry.js:485
  ISSUE: provider_type is rendered in the detail drawer with plain field('provider_type','제공자 유형') at line 485, so it shows the raw value 'local_http' with no mapping — inconsistent with every other enum on this screen (auth_type, maintenance_state, last_health_status, enabled) which is Korean-mapped/badged. It is also not covered by the PROVIDER map (which only has n8n/http_service/notion_via_n8n).
  FIX: Wrap provider_type in a mapCol with a label map (e.g. { local_http: '로컬 HTTP' }) so the detail shows a friendly label instead of the raw enum.
- [Low|missing-feature|runners] frontend/src/screens/registry.js:481
  ISSUE: The runner detail drawer shows no reverse references — i.e. which prompts/templates currently target this runner. Prompts reference runners via runner_id (app/prompts/models.py:38; registry comment at runners.onQuery line 384-385 confirms '프롬프트의 runner_id'). Disabling, editing, or maintenance-flagging a runner that is referenced by live prompts gives the admin no impact warning, unlike workflows which warn via RESERVED_WORKFLOW_NOTES when a row is depended upon. The screen's own help even claims
  FIX: Surface referencing prompts/templates in the runner detail (e.g. a count or list from a backend lookup), and/or a confirm warning on disable when the runner is referenced by active prompts.
- [Low|empty-state|prompts] 
  ISSUE: The prompts empty state provides only emptyTitle + emptyHelp (registry.js:634-637), whereas DataScreen fully supports and other screens use a richer step-by-step guidance block — emptySituation/emptyPrerequisite/emptySteps/emptyExpected (see integrations registry.js:287-291 and DataScreen.jsx:476-478). A first-time admin lands on a one-line CTA instead of the lifecycle guidance (draft→test→review→publish) the app uses elsewhere.
  FIX: Optionally add emptySituation/emptySteps/emptyExpected to the prompts config (registry.js:634) mirroring integrations. Low priority because policies/templates are also thin, suggesting this may be a deliberate choice for content screens.
- [Low|contract|templates] 
  ISSUE: The input_schema field (registry.js:861 create, :896 edit) is type:'json' but does NOT set jsonObject:true, even though the backend types it as dict (router.py:33) and rejects non-objects with a 422. The identical documents 'config' dict field DOES set jsonObject:true for exactly this reason. Result: a user who types an array/string/number into '문서 생성 기본값(JSON)' passes the client-side check and only fails after a server round-trip with a raw Pydantic 'Input should be a valid dictionary' message 
  FIX: Add jsonObject: true to both the create and edit input_schema field definitions (registry.js:861 and :896) to match the documents config field and the backend dict contract.
- [Low|design|templates] 
  ISSUE: The create/edit form abstracts approval_policy into a friendly checkbox '발행 전 승인 필요' (registry.js:865/897 + toApiBody/fromRow), but the detail drawer renders the same value with jsonField as raw JSON — approval_policy shows literally {"required": true} (registry.js:850). This leaks the contract shape and is inconsistent with the checkbox vocabulary the admin actually set.
  FIX: Replace jsonField("approval_policy", …) at registry.js:850 with a custom render that shows '발행 전 승인 필요: 예/아니오' (reading r.approval_policy.required), matching the form's checkLabel.
- [Low|design|templates] 
  ISSUE: created_by is surfaced as '작성자 ID' via plain field() (registry.js:850) and shows a raw user UUID with no name/email resolution and no deep-link to the user. The admin cannot tell who authored a template without manually cross-referencing the users screen. The backend _view returns only the id string (router.py:88), so there is no created_by_name to fall back on.
  FIX: Either have the backend _view include created_by_name/email, or (cheaper) render created_by as a link to the users screen (#/users?... — a users deep-link pattern already exists) so the raw ID is at least navigable. At minimum keep it honest as it is, but a name would materially help provenance.
- [Low|ux|templates] 
  ISSUE: templates is not searchable and not paginated, so DataScreen shows the client search box (showSearch=true) but the config sets no searchFields, meaning searchText falls back to JSON.stringify(row) — matching raw UUIDs (id, target_ref, prompt_id, policy_id, created_by) and ISO timestamps that are not visible in the list. This is the exact false-match/confusion pattern the DataScreen comment (DataScreen.jsx:312-316) says searchFields was introduced to prevent.
  FIX: Add searchFields: ["name", "description"] to the templates config (near registry.js:794) so the search box only matches the human-visible text.
- [Low|design|approvals] 
  ISSUE: The '요청 내용' detail renderer (registry.js:1196-1210) shows request_payload as top-level key/value rows but leaves the KEYS as raw English identifiers in an otherwise fully-Korean console: 'generation_id', 'previous_role', 'target_name', 'target_email', 'source_row_count', 'target_parent_page' (document.publish/user.role_change), and 'config'/'definition' (runner/integration/schedule) as opaque nested JSON. Values for role/previous_role are translated via ROLE_KO, but the surrounding labels a revi
  FIX: Add a small key-label map (e.g. previous_role→'이전 역할', target_name→'대상 이름', target_email→'대상 이메일', source_row_count→'원본 행 수', target_parent_page→'대상 페이지', generation_id→'문서 생성 ID') applied in the fmtVal/entries loop at registry.js:1196-1210, falling back to the raw key when unknown.
- [Low|missing-feature|backup] frontend/src/screens/registry.js:1663
  ISSUE: The screen lets a system_admin create and verify backups but provides no way to retrieve/download a backup artifact or restore from one via the console — the only "restore" affordance is a read-only text modal (복원 안내). A backup you can never download off the box or act on from the UI is a hollow end-to-end flow. This is by explicit spec design (§14.6, script-only restore) and is honestly communicated in the help text and modal, so it is an inherent domain limitation rather than a defect — but th
  FIX: No code change required if the script-only restore policy stands; if off-site retrieval is desired, add a backend download endpoint (streaming the .sqlite3 with a checksum header) plus a row action. Otherwise leave as-is — the honest messaging is correct.
- [Low|empty-state|backup] frontend/src/screens/registry.js:1653
  ISSUE: The empty state provides only a one-line role-aware emptyHelp, while comparable first-run operational screens (integrations, runners) provide richer onboarding via emptySituation/emptyPrerequisite/emptySteps/emptyExpected (DataScreen renders these gated on canCreate). A first-run system_admin gets minimal guidance on what a backup is, when to run it, and what to expect afterward.
  FIX: Add emptySituation/emptyPrerequisite/emptySteps/emptyExpected to the backup config (mirroring integrations at registry.js:287-290), e.g. steps for '+ 백업 실행 → 상태가 확인됨(verified)이 되는지 확인 → 정기 백업 습관화', and note retention keeps the most recent 14 good backups.
- [Low|dead-affordance|jobs] 
  ISSUE: The '가장 오래된 대기' stat card (registry.js:1467-1476) has no onClick, while every sibling card (대기/실행 중/실행 가능/실패/완료/취소됨) drills down via ctx.setFilter. The inline comment at lines 1451-1454 explicitly condemns non-actionable 'dead tiles' as the reason onClick was added to the others — yet this tile turns danger-red during a backlog and clicking it does nothing.
  FIX: registry.js:1474 — give the pushed age card an onClick that drills to the backlog it represents, e.g. `onClick: () => ctx.setFilter('status','queued')` (same target as the adjacent '실행 가능(ready)' tile).
- [Low|design|jobs] 
  ISSUE: '요청자 ID' in the detail drawer (registry.js:1505, field('user_id','요청자 ID')) renders a raw user UUID with no name/email resolution — unlike the audit screen (actor_name/actor_email, registry.js:1560/1569) and the prompt/policy version lists (created_by_name/created_by_email, registry.js:227). For an ops screen, an unresolved UUID gives no answer to 'who requested this?', and user is not in OBJ_ID_PARAM so there is no deep-link to #/users either.
  FIX: Have _job_view resolve and return requester_name/requester_email for user_id (same pattern audit uses), and render name→email→UUID in registry.js:1505; optionally add user to OBJ_ID_PARAM so the requester field can deep-link to #/users.
- [Low|empty-state|job-titles] 
  ISSUE: The fresh-install empty state has only emptyTitle + emptyHelp + a working '+ 직책 추가' CTA (registry.js:1284) — adequate, but it lacks the §9 step-by-step onboarding fields (emptySituation/emptyPrerequisite/emptySteps/emptyExpected) and an emptyRelatedLink. departments→job-titles→users is the user-onboarding chain (Users.jsx:131,151 blocks user creation until job titles exist), yet unlike the integrations→runners→workflows chain it never chains forward to the users screen, so a first-time admin isn
  FIX: Add emptyRelatedLink: { href: '#/users', label: '다음: 사용자 등록으로 이동' } and optional emptySteps/emptyExpected to the job-titles config (registry.js:1284); DataScreen already role-guards these with canCreate.
- [Low|ux|audit] 
  ISSUE: The '행위자 ID' filter (registry.js:1547) expects a user UUID, but the screen never displays that UUID for named actors: the actor column shows actor_name || actor_email (registry.js:1560), and user_id was deliberately dropped from detailFields as a 'duplicate' (registry.js:1562,1569). So for the common case (actor has a name), the value the filter needs exists nowhere on this screen — you must leave to the Users screen to copy a UUID. There is also no per-row 'filter by this actor / this target' a
  FIX: Either show the raw user_id in the detail drawer for actors (add a detailField rendering r.user_id), or add row actions like '이 행위자로 필터'/'이 대상으로 필터' that call setFilter(user_id / object_id) using the values already on the row.
- [Low|ux|audit] 
  ISSUE: The stated purpose is '누가 무엇을 언제 바꿨는지' (who changed WHAT), but the detail drawer shows '변경 전'(before) and '변경 후'(after) as two independent objectField blocks (registry.js:1571). The reviewer must mentally diff the two blocks to see which keys actually changed — the app already has a real computed unified diff for prompts/policies ('비교' action, registry.js:236-239), but that capability is not applied to audit before/after where 'what changed' is the whole point.
  FIX: Add a merged/diff renderer for before→after (highlight only changed keys) in the audit detailFields, reusing the diff-presentation pattern already used for prompt/policy version compare.
- [Low|bug|shared-infra] 
  ISSUE: `pollJobUntilDone` masks session expiry during polling. Every other API path funnels 401 through `handleApiError` which toasts and redirects to /login (DataScreen.jsx:16-26), but the poll loop swallows all GET errors with `catch (e) { continue; }` (DataScreen.jsx:152). If the session expires mid-poll (e.g. Notion auto-sync job), the loop silently retries until maxTries, then shows the misleading '작업이 아직 진행 중입니다. 잠시 후 목록을 새로고침하세요.' info toast (DataScreen.jsx:161) — the user thinks the job is runn
  FIX: In the poll catch (DataScreen.jsx:152), detect `e.status === 401` and call `handleApiError(e, toast)` then return, instead of unconditionally continuing. DataScreen.jsx:145-162.

## AREA=B (29)

- [Medium|design|chat (user)] 
  ISSUE: Conversations are persistent, renamable and archivable — so multi-day threads are the expected case — yet every message's timestamp label renders only HH:MM (fmtTime, Chat.jsx:64-71 used at Chat.jsx:514). There are no date dividers in the thread, so a message from three days ago at 15:23 and one from today at 15:23 are visually indistinguishable when scrolling a reopened conversation. The sidebar's fmtShort already distinguishes same-day vs other-day, but the thread itself never shows a date.
  FIX: Insert date-separator rows between messages whose created_at day differs (e.g. a centered '2026년 7월 26일' chip), or fall back to fmtShort-style month/day prefix on the per-message label when the message is not from today. Chat.jsx:514 / add a grouping pass over `items` before map at Chat.jsx:1060.
- [Medium|dead-affordance|dashboard] frontend/src/screens/Dashboard.jsx
  ISSUE: The '작업 지표 (최근 24시간)' tiles (처리 요청/성공/성공률/평균 처리, lines 335-344) call goto('/jobs'), which returns undefined for the auditor role (jobs is operator/admin/system_admin only). For auditor these four tiles render as plain non-interactive cards with no chevron and no explanation. The sibling '현재 큐 상태' tiles (362-363) and '시스템 리소스' tiles (392-405) explicitly append jobsNote/diagNote ('· 관리자 문의') for this exact case — the file's own comments (lines 360-361) state this pattern is required so the tiles d
  FIX: Append jobsNote to the four job-metric labels the same way the queue section does, e.g. label={'처리 요청' + jobsNote}, label={'성공' + jobsNote}, etc. at Dashboard.jsx:335-344 (jobsNote is already computed at line 179).
- [Medium|bug|diagnostics+maintenance] 
  ISSUE: The Diagnostics top-of-page health verdict (its stated whole purpose: a single authoritative '정상/주의' line) omits backup staleness and backup absence. healthVerdict() only flags dash.last_backup_status === 'failed' (Ops.jsx:104); it never checks last_backup_at == null or a stale age. Dashboard.jsx by contrast raises danger alerts for '마지막 백업 없음' (Dashboard.jsx:220) and '마지막 백업이 오래됨' when age > 7 days (Dashboard.jsx:224-225), and shows an age badge in its backup card (Dashboard.jsx:421-423). Resul
  FIX: Reuse Dashboard.jsx's daysSince() + BACKUP_STALE_DAYS in healthVerdict() and add null-check + stale-age branches (Ops.jsx:103-104); also render the age badge in the 백업 section (Ops.jsx:419-420) exactly like Dashboard.jsx:421-423 so the two screens' backup verdicts match.
- [Medium|contract|settings] frontend/src/screens/Settings.jsx:439
  ISSUE: The structured editor's client-side pre-validation is incomplete: clearing a required NUMERIC object field sends `null`, which coerce() does not catch. In StructuredObjectFields the number inputs do `onChange={e => patch({min_length: e.target.value === "" ? null : Number(...)})}` (lines 224/231 for password_policy, 252/259 for session_policy). coerce() only range-checks when the value is `!= null` (lines 440-441 password_policy, 446-447 session_policy), so a cleared field yields e.g. `{min_lengt
  FIX: In coerce() (Settings.jsx ~439-448) mirror the ui_branding.product_name check: for password_policy require min_length and min_classes to be present integers (reject null/missing) before the range checks; for session_policy require idle_timeout_seconds and absolute_timeout_seconds to be present integers >= 60. Throw the same Korean messages so the error is caught client-side instead of via a server 422.
- [Medium|bug|users] 
  ISSUE: useNameOptions (Users.jsx:61-70) injects the currently-assigned department/title into the form select with a '(비활성)' suffix whenever the id is not found among activeItems — but activeItems is ALSO empty while the org query is loading or has errored (q.data undefined → items=[] → activeItems=[]). So on a network error (or the brief initial load) an actually-ACTIVE department is shown to the admin as '영업팀 (비활성)' in the edit form, directly contradicting the deptHelp/titleHelp error/loading message 
  FIX: Guard the injected-current-option branch the same way inactiveSuffix defers: only append '(비활성)' when the list actually loaded (e.g. add `!q.isLoading && !q.isError && items.length > 0` to the condition at Users.jsx:66). When the list is unavailable, inject the current id with the plain currentLabel and no suffix.
- [Medium|ux|diagnostics+maintenance] frontend/src/screens/Ops.jsx
  ISSUE: In Diagnostics, the primary '진단 수집' button is wired to `disabled={q.isFetching}` with label `{q.isFetching ? '수집 중…' : '진단 수집'}` (line 242). But the screen auto-polls every 60s via `collect(false)` (lines 178-181), which sets `q.isFetching = true` each cycle. So every minute the primary action flickers to '수집 중…' and goes disabled for the duration of a background fetch the user did not initiate — meaning the primary control is intermittently unavailable and its label mutates under the user. This
  FIX: Mirror Dashboard.jsx: track a local `manualCollecting` state set true only in `collect(true)` and cleared in an effect when `!q.isFetching`; drive the button's disabled/label off `manualCollecting` (or `toggle`-style manual flag) instead of raw `q.isFetching`, so 60s background polls don't disable/relabel the primary button. Ops.jsx:242.
- [Low|bug|chat (user)] 
  ISSUE: Ticket cards can double-render alongside the body list. The body de-duplication (stripDuplicatedTicketLines) is gated on `typeof st.start_index === 'number'` (Chat.jsx:454), but card numbering uses ticketPageStart (Chat.jsx:458), which ALSO derives a start number from st.context.last_result_start when start_index is absent (Chat.jsx:292-302). So for a runner payload that omits start_index but carries context.last_result_start, cards get numbered '상세' buttons (Chat.jsx:484) while the body keeps i
  FIX: Gate the body strip on the same computed startNo (ticketPageStart result) rather than raw st.start_index, so numbering and de-dup use one source of truth. Chat.jsx:454 vs 458/484.
- [Low|empty-state|chat (user)] 
  ISSUE: A failed first send leaves an orphaned empty '새 대화' in the sidebar. doSend creates and commits the conversation via POST /api/conversations (Chat.jsx:960) BEFORE sending the message; if send.mutateAsync then throws (network/503/429/422), the conversation row is already persisted with zero messages and shows up in the list as '새 대화'. It's recoverable (retrying with the same draft key reuses it), but abandoning leaves clutter with no cleanup path.
  FIX: Either defer conversation creation until the message POST succeeds (create-with-first-message server-side), or on first-send failure delete the just-created empty conversation. Chat.jsx:958-987.
- [Low|design|dashboard] 
  ISSUE: The '서비스 상태' grid renders cards in raw insertion order (web, worker, scheduler, then DB order of integrations) at Dashboard.jsx:303-326, with no severity sorting. The alerts grid right above it explicitly sorts danger-first (Dashboard.jsx:286), so the two adjacent grids follow different ordering rules. In a deployment with many integrations, a '중단'(down) service card can sit below several '정상' cards, forcing the operator to scan/scroll to find the broken one.
  FIX: Sort `Object.keys(services)` so down/unknown entries come first (mirror the danger-first sort used for alerts at line 286), keeping web/worker/scheduler grouping if desired.
- [Low|design|dashboard] 
  ISSUE: Three different affordances express 'go to detail' across sibling dashboard sections: clickable StatCards/Cards with a chevron (KPI, queue, inventory, resource, service tiles), a Button in the backup card (Dashboard.jsx:425-428), and a text link '전체 보기 →' in the audit section (Dashboard.jsx:435-437). The mix reads as slightly inconsistent for what is fundamentally the same navigation intent, and the 'web' service card silently lacks the chevron that all its grid siblings have (svcNav returns und
  FIX: Standardize the detail-navigation pattern (e.g., keep chevron cards for tiles and a consistent link style for section 'see all'), and consider a muted non-interactive treatment for the web card so its lack of chevron reads as intentional rather than broken.
- [Low|missing-feature|chat (user)] 
  ISSUE: USER_GUIDE.md:42 advertises a quick-prompt button '내 담당 프로젝트' (my projects), but QUICK_PROMPTS (Chat.jsx:93-100) contains no project-oriented prompt at all — only ticket prompts plus '무엇을 할 수 있어?'. Project queries are a documented capability that has lost its discoverability affordance on the welcome screen, and the guide now describes chips that don't exist.
  FIX: Add a project prompt (e.g. '내가 담당한 프로젝트 보여줘') to QUICK_PROMPTS at Chat.jsx:93-100, or reconcile USER_GUIDE.md:42 with the actual 6 prompts.
- [Low|design|chat (user)] 
  ISSUE: On ultra-wide (4K) screens the composer does not anchor to the viewport. .chat-composer (screens.css:199 + 303) is constrained to max-width min(1000px) and centered with its own card background + top border, while .chat-topbar (screens.css:182) spans full width with a full-width bottom border. The result is a full-width top divider but a 1000px-wide floating composer bar with page-background gutters on both sides at the very bottom — the framing looks unfinished / asymmetric versus the top of th
  FIX: Give the composer a full-width backdrop (a full-bleed wrapper with card background + border-top) and keep only the inner flex row centered at 1000px, mirroring how the full-width topbar frames its content. screens.css:199/303/187.
- [Low|ux|chat (user)] 
  ISSUE: While awaiting a reply (busy/awaitingReply) the Send button is disabled but the textarea stays fully enabled and focusable with placeholder 'Enter 전송' (Chat.jsx:1126-1127 disables the textarea only on maintenance/rateLimit). A user who types and presses Enter gets feedback only after the attempt, via a transient toast '답변을 기다리는 중입니다.' (Chat.jsx:943) — there is no passive on-screen cue that input is currently blocked, unlike the persistent maintenance/rate-limit banners used for the other locked 
  FIX: Show a passive inline hint near the composer while awaitingReply/busy (e.g. swap the placeholder to '답변을 기다리는 중…' or render a small muted note above the composer), consistent with the persistent banner pattern already used for maintenance/rate-limit. Chat.jsx:1126-1130.
- [Low|design|chat (user)] 
  ISSUE: The per-message meta stack order is bubble → timestamp → choices → copy (Chat.jsx:514, 517-528, 529-537). For the last assistant message the timestamp is sandwiched between the bubble and the quick-choice chips, and the '복사' button lands far below the choices — so the copy control is visually detached from the message it copies and the timestamp interrupts the bubble→actions grouping.
  FIX: Group actions together: keep the copy button adjacent to the bubble (or right-align it on the bubble) and move the timestamp to the end of the message row, so meta and actions read as one cluster. Chat.jsx:514 / 529-537.
- [Low|a11y|dashboard] frontend/src/screens/Dashboard.jsx
  ISSUE: The manual '새로고침' button gives no screen-reader feedback. On click the button text flips to '새로고침 중…' and back, but the '…기준' timestamp is deliberately kept OUT of an aria-live region (comment lines 117-119, to avoid the 30s auto-poll spamming SR users). Net effect: an SR user who triggers a manual refresh gets no confirmation it ran or completed — no announced result at all.
  FIX: Announce only the manual (not the 30s auto) refresh completion via a polite live region gated on manualRefreshing, e.g. render a visually-hidden aria-live="polite" node that reads '갱신됨: <updated>' when a manual refresh resolves (Dashboard.jsx:106-121).
- [Low|missing-feature|dashboard] frontend/src/screens/Dashboard.jsx
  ISSUE: build_dashboard returns disk.total_gb/used_gb and memory.total_kb/available_kb (app/health/service.py:93-115), computed on every 30s poll, but the dashboard renders only disk.free_gb + disk.used_pct and memory.used_pct (Dashboard.jsx:390-402). Total capacity context (e.g. '12 / 50 GB free', absolute RAM) is dropped, so used_gb/total_gb/total_kb/available_kb are dead payload. An operator sees '80% used' with no sense of scale.
  FIX: Either surface capacity (e.g. disk value 'fmtNum(disk.free_gb) + " / " + fmtNum(disk.total_gb) + "GB"') or drop the unused fields from the backend dict to stop computing/serializing them each poll.
- [Low|ux|dashboard] frontend/src/screens/Dashboard.jsx
  ISSUE: The '디스크 여유' tile (line 390) is rendered non-clickable with NO note for roles that cannot reach /diagnostics (operator/auditor: diagTo undefined), while its immediate neighbor '디스크 사용' (line 392) carries diagNote ('· 관리자 문의'). Minor sibling inconsistency within the same '시스템 리소스' grid — one resource tile explains why it isn't clickable, the adjacent one silently doesn't.
  FIX: Either append diagNote to the '디스크 여유' label as well, or accept it as purely informational (no drilldown) consistently — but match the neighbor's treatment (Dashboard.jsx:390).
- [Low|ux|users] 
  ISSUE: When '보관된 계정 보기' is toggled on and results exist, there is no persistent indicator that the populated list is showing archived accounts — the only cue is the small checked checkbox in the toolbar (line 278). The rows look identical to the live list (same columns, and archived users still show '사용 중'/'비활성화' active badges with no '보관됨' marker and no '보관 시각'), so an admin who scrolled down or context-switched can mistake an archived account for a live one. The empty archive state names it (line 293
  FIX: When showArchived is true, render a persistent Callout/banner above the table (e.g. '보관된 계정을 보고 있습니다') and/or add a '보관됨' badge or '보관 시각' column to archived rows so the archived context is visible while browsing, not only when empty.
- [Low|design|users] 
  ISSUE: The role Badge color-coding (line 179) only distinguishes system_admin (danger) and admin (warn); operator, auditor, and plain user all render as neutral gray. The inline comment states the intent is to 'color-code role grade' for the most sensitive column, but operator/auditor (elevated read/ops roles) are visually indistinguishable from ordinary users, so the color cue only half-delivers on its stated goal.
  FIX: Either give operator/auditor a distinct non-alarming tone (e.g. 'info') to separate privileged-but-non-admin roles from plain users, or drop the ambition and rely on the Korean label — but make the intent consistent. Current state is a partial signal that can read as 'everyone below admin is the same'.
- [Low|ux|settings] 
  ISSUE: Version history / rollback is not surfaced for read-only roles. The '버전 기록' footer button is always rendered (Settings.jsx:525, not gated by canWrite) and operator/auditor CAN view history, but the intro callout only mentions '버전 기록에서 이전 값으로 되돌릴 수 있습니다' when canWrite is true (Settings.jsx:344). Read-only viewers are never told the history exists, and version history is only discoverable by first opening a row's editor — there is no affordance on the list itself.
  FIX: Mention version history in the intro callout for read-only roles too (viewing, not rollback), and/or add a per-row '버전 기록' quick action so it is discoverable without opening the editor. Settings.jsx:344-345.
- [Low|ux|settings] 
  ISSUE: The session_policy '최대 세션 길이' field is expressed only in minutes, so the default 8-hour value shows as the bare number '480' (Settings.jsx:255-259, absMin = seconds/60). Hour-scale durations are hard to read/verify in minutes; an admin must mentally divide 480/60 to confirm it is 8 hours. The table summary already shows the friendly '최대 8시간' (fmtDuration), so the edit control is the weakest link.
  FIX: Add a live helper under the input echoing the human-readable duration (reuse fmtDuration → '= 8시간'), or offer an hours+minutes pair / unit selector while still storing seconds. Settings.jsx:254-260.
- [Low|empty-state|settings] 
  ISSUE: The list empty state is bare: <EmptyState title='표시할 설정이 없습니다' help='시스템 설정 항목이 아직 없습니다.'/> with the default 📭 icon and no guidance or action (Settings.jsx:363). It is a defensive fallback that the code notes is normally unreachable (effective_settings always returns all keys), but if the API returns a malformed/empty payload the user gets a dead end with no recovery hint.
  FIX: Since this only fires on an abnormal response, treat it more like an error: add a situation line ('설정을 불러왔지만 항목이 비어 있습니다') and a retry action (q.refetch), or route this case through ErrorState instead of EmptyState. Settings.jsx:363.
- [Low|ux|diagnostics+maintenance] 
  ISSUE: Within the Diagnostics page the '서비스 상태' cards are static non-interactive <Card> (Ops.jsx:331) while the immediately-following '외부 연동' cards are role=button clickable cards navigating to /integrations (Ops.jsx:352-356) — and both carry the identical .dash-svc styling. Two visually identical card grids stacked adjacently, one clickable and one not, is an inconsistent affordance that invites users to click the dead service cards.
  FIX: Either give the service cards a destination when actionable (e.g. worker/scheduler → /jobs, mirroring Dashboard.jsx:252-256 svcNav) or visually distinguish the non-interactive service grid from the interactive integrations grid so the clickability difference is legible (Ops.jsx:323-333).
- [Low|empty-state|diagnostics+maintenance] 
  ISSUE: Empty-state treatment is inconsistent across the Diagnostics sections. '외부 연동' empty uses the rich <EmptyState> with icon/help/relatedLink (Ops.jsx:365-370), but '최근 작업 오류' (Ops.jsx:440) and '최근 주요 변경' (Ops.jsx:470) fall back to a bare '<p className="pending-note">' one-liner inside a Card. Three sibling sections, three different empty presentations.
  FIX: Pick one convention for reassuring 'nothing here' states on this page — either promote the two audit/error empties to a lightweight EmptyState, or keep the calm one-liner but apply it consistently (Ops.jsx:440, 470 vs 365-370).
- [Low|missing-feature|diagnostics+maintenance] 
  ISSUE: The Maintenance screen exposes '버전 기록' (version history + rollback) only for maintenance_message (Ops.jsx:639), but offers no history/audit surface for the maintenance_mode toggle itself — an admin viewing this screen cannot see who last turned maintenance on/off or when, even though the backend records config_versions for every setting and the generic /{key}/versions endpoint (settings/router.py:78) supports maintenance_mode too. The toggle is the higher-stakes action (it blocks all user writes
  FIX: Add a small 'who last changed this / 변경 기록' affordance for maintenance_mode near the toggle card (Ops.jsx:596-605), reusing the same SettingVersions modal already imported, so the app-blocking action has at least the visibility the notice text already has.
- [Low|a11y|settings] frontend/src/screens/Settings.jsx:521
  ISSUE: For the 4 structured object settings in non-advanced mode, each input's aria-describedby includes the id 'setting-help', but that node is only rendered in advanced/unstructured mode. describedBy is built once (Settings.jsx:521) as `[...,'setting-help' when isObjectSetting,...]` and passed to StructuredObjectFields (551-552) and to the domain input (194), yet the <p id="setting-help"> is rendered only when `!STRUCTURED_OBJECT_KEYS.includes(key) || advanced` (line 573). So structured fields refere
  FIX: Compute describedBy to include 'setting-help' only when the help node is actually rendered (i.e. only for unstructured/advanced object editing), or pass a mode-specific describedBy into StructuredObjectFields that omits 'setting-help'.
- [Low|design|settings] frontend/src/screens/Settings.jsx:304
  ISSUE: Two console.warn drift-detection statements (Settings.jsx:304 and :311) ship to the production browser console — they fire in any user's console whenever a registry key lacks a SETTING_LABELS entry or a summarizeSetting branch. This violates the project's 'no console.log in production code' rule (typescript/coding-style.md) and leaks internal maintenance guidance to end users' devtools.
  FIX: Guard these behind a dev flag (e.g. `if (import.meta.env.DEV)`) or route them through a proper client logger so they don't run in production builds. Keep them active only in development where the drift is actionable.
- [Low|dead-affordance|diagnostics+maintenance] frontend/src/screens/Ops.jsx
  ISSUE: In the '외부 연동' section, `const val = it.enabled === false ? 'disabled' : (it.last_health || '미점검')` (line 348) intends (per the comment at line 341) to show '미점검' for an enabled integration that has never been health-checked. But `Integration.last_health_status` is a NOT NULL column defaulting to 'unknown' (app/integrations/models.py:29-30) and the bundle maps it verbatim to `last_health` (app/health/service.py:255). So `it.last_health` is always a non-empty string ('up'/'down'/'unknown') and th
  FIX: Either treat 'unknown' as the never-checked signal explicitly — `const raw = it.last_health; const val = it.enabled === false ? 'disabled' : (!raw || raw === 'unknown') ? '미점검' : raw;` — or drop the misleading comment/fallback and accept '알 수 없음'. Ops.jsx:348.
- [Low|bug|diagnostics+maintenance] frontend/src/screens/Ops.jsx
  ISSUE: healthVerdict()'s integration check (lines 97-102) flags only `last_health === 'down'` (danger) and `last_health === 'degraded'` (warn). The backend never emits 'degraded' — health checks set only HEALTH_UP/HEALTH_DOWN and the column defaults to 'unknown' (app/integrations/service.py:185,189; models.py:12-14). So the 'degraded' branch is unreachable, and an enabled integration stuck at 'unknown' (endpoint never successfully probed) is silently excluded from the top '주의 N건' summary. This is incon
  FIX: Remove the dead 'degraded' branch and decide 'unknown' handling deliberately: if an enabled, never-verified integration should surface, add `else if (v.last_health === 'unknown') warn(serviceLabel(name) + ' 연동 미점검')` to match the component treatment; otherwise document why integrations intentionally differ. Ops.jsx:97-102.

## AREA=C (0)


## AREA=D (31)

- [High|contract|notification-bell] 
  ISSUE: The list response's 'unread' field is the GLOBAL unread count (router.py:55 calls unread_count(db,user.id) unconditionally), independent of unread_only and page_size. The bell trusts list.data.unread as the authoritative header/badge number (NotificationBell.jsx:256-258) precisely because it must not be confused with total or the visible row count. No test pins this: existing tests only check unread==1 in a trivial single-item case where unread, total, and page all coincide.
  FIX: Add test_list_unread_field_is_global_not_filtered: create 12 unread notifications (> page_size 8), GET /api/notifications?page_size=8&unread_only=true, assert len(items)==8, total==12, but unread==12 (full global count, not the page count). Also assert unread stays correct when unread_only=false and read items exist (unread < total).
- [Medium|a11y|dashboard] frontend/src/screens/Dashboard.jsx
  ISSUE: In the '서비스 상태' section, clickable service Cards set aria-label={svcLabel(k) + ' 상세 열기'} on a role="button" div. Per the accessible-name algorithm, aria-label becomes the entire accessible name and the inner status <Badge> (정상/중단/응답 없음) is NOT announced. So screen-reader users hear 'ClovirONE ... 상세 열기' with no health status for exactly the cards that matter. Ironically the NON-clickable cards (web, or auditor's worker/scheduler when procTo is undefined) have no aria-label, so those DO read thei
  FIX: Include the status in the label, e.g. Dashboard.jsx:314 aria-label={svcLabel(k) + ': ' + (unknownComp ? '응답 없음' : statusText(services[k])) + ', 상세 열기'} (import statusText from ../ui/kit.jsx). This is partially mitigated because down/unknown components and down integrations are also surfaced in the top alerts, but an up/degraded integration status is still lost for SR users.
- [Medium|design|users] 
  ISSUE: The 'Notion 연결' column renders <Badge value={r.notion_mapping_status}/> for every row, and the backend defaults every unmapped user to status 'unmapped' (router.py _user_row notion_status default + _notion_status_map). kit.jsx maps unmapped -> 'warn' (orange) — a tone chosen deliberately for the Notion-mapping screen where unmapped is the actionable reason-to-exist. On the user LIST, unmapped is the normal/expected state for most users, so a fresh or Notion-optional deployment shows an entire co
  FIX: In Users.jsx column render (line 194) pass an explicit neutral/info kind for 'unmapped' on the list (e.g. <Badge value={r.notion_mapping_status} kind={r.notion_mapping_status==='unmapped' ? 'neutral' : undefined}/>), reserving warn for the Notion mapping screen. Or render unmapped as a muted '—'/'미연결' text rather than a warn pill.
- [Medium|design|settings] 
  ISSUE: The entire screen is a single flat 5-column table (설정/키/값/상태/설명) with no grouping or sectioning. It mixes fundamentally different setting categories — security (password_policy, session_policy, allowed_email_domains), data retention (conversation/notification_retention_days), and branding/feature-flags (ui_branding, document_automation_enabled) — as undifferentiated rows. It reads like a raw DB dump rather than a polished settings screen, with no visual hierarchy to help an admin find the securi
  FIX: Group rows into k-section blocks with Korean section headers (e.g. '보안', '데이터 보존', '브랜딩·기능') using the existing .k-section / .k-section-title styles in kit.css, or add a category badge column. Settings.jsx:321-328 (columns), :364 (single DataTable render).
- [Medium|design|settings] 
  ISSUE: Raw snake_case English keys are exposed prominently to non-developer admins: a permanent '키' column shows values like conversation_retention_days / document_automation_enabled for every row (Settings.jsx:323), and the editor modal subtitle repeats it ('{setting.key} · 기본값', Settings.jsx:533). This is exactly the 'raw English leaking' smell — the Korean '설정' label column already identifies each row, and the accessible open-label already derives from the Korean label (kit.jsx rowOpenLabel uses row
  FIX: Drop the '키' column from the default table (move it into the editor's detail area only, or remove entirely); in the editor subtitle at Settings.jsx:533 show only the status ('기본값'/'변경됨') without the raw key, or render the key in muted small text as clearly-secondary metadata.
- [Medium|dead-affordance|notification-bell] 
  ISSUE: Static (non-navigable) notification rows are click-to-expand to reveal 2-line-clamped title/body, but sighted mouse/touch users get NO visible affordance that the row is expandable. Navigable rows render a chevron (global.css:216-220 '.noti-item-main:not(.noti-item-main--static)::after'); static rows get only cursor:pointer on hover plus an SR-only aria-label hint. Every notification a plain role:"user" receives (job_failed, account_locked, password_change_required, maintenance_announcement) is 
  FIX: In NotificationBell.jsx:408-415 render a visible expand indicator on static rows (a chevron/caret or '더보기' label mirroring the navigable-row ::after in global.css:216-220), ideally shown only when the content is actually clamped. Rotate/hide it on is-expanded.
- [Medium|bug|notification-bell] 
  ISSUE: The bell badge/count goes stale after the user reads notifications on the full /notifications screen. When on that screen the bell (Topbar) and the DataScreen list are BOTH mounted, but DataScreen.refresh() only invalidates queryKey [config.key] = ['notifications'] (DataScreen.jsx:138). The bell's queries are ['noti-unread'] and ['noti-list',...], which do NOT start with ['notifications'], so they are never invalidated. After clicking '모두 읽음' or marking items read on the full screen, the topbar 
  FIX: In DataScreen.jsx:138 refresh(), also invalidate the bell keys when the config is the notifications screen, e.g. qc.invalidateQueries({queryKey:['noti-unread']}) and qc.invalidateQueries({queryKey:['noti-list']}) (or add a shared invalidate helper). This makes screen->bell match the existing bell->screen invalidation.
- [Medium|missing-feature|notifications] frontend/src/screens/registry.js:1633
  ISSUE: Acting on a notification from the full list via '관련 항목 보기'/'관련 목록 열기' does NOT mark it read, unlike the bell popover which marks-read-on-open. DataScreen.runAction short-circuits on a.navigate (frontend/src/screens/DataScreen.jsx:196) and returns before any read call, while NotificationBell.openItem() (frontend/src/app/NotificationBell.jsx:287-294) fires POST /{id}/read AND navigates. So a user who drills into an unread notification's related item from this screen leaves it unread, keeping the u
  FIX: Mirror NotificationBell.openItem: on the notifications 'navigate' actions (registry.js:1633-1643) also mark unread rows read before navigating — e.g. add a localPatch+POST /{id}/read step, or teach DataScreen.runAction (DataScreen.jsx:196) to honor a per-action markReadOnNavigate flag that POSTs /api/notifications/{id}/read for !read_at rows.
- [Medium|bug|shared-infra] 
  ISSUE: Saved theme choice is not honored at boot, so the FOUC-prevention the boot code was written for (App.jsx:252-254 comment) fails: any user whose chosen theme differs from their OS theme gets a wrong-theme flash on every reload until /api/me resolves. Cause: `getStoredTheme()` reads the base key `THEME_KEY` (App.jsx:245) and `initialTheme()` uses it at module load (App.jsx:254), but the base key is NEVER written — `toggleTheme` and the UserMenu effect only write the account-scoped `themeKey(userId
  FIX: In `toggleTheme` (App.jsx:364-368) and the UserMenu load effect (App.jsx:322-332), also mirror the value to the base key: `localStorage.setItem(THEME_KEY, next)` (and on load), so the module-scope `initialTheme()` (App.jsx:254) reflects the last explicit choice. Alternatively make `getStoredTheme()` return the most-recent themeKey:* value. App.jsx:245.
- [Medium|a11y|shared-infra] 
  ISSUE: Error and empty state transitions are not announced to screen readers on any screen. `Skeleton` has an `aria-live="polite"` node ('불러오는 중…', kit.jsx:125), but when it is replaced by `ErrorState` (kit.jsx:170-208) or `EmptyState` (kit.jsx:143-168), those render as plain `<div className="k-empty">` with no `role="alert"`/`role="status"`, no aria-live, and the title is a `<div>` not a heading. A screen-reader user who triggers a refetch that fails, or lands on an empty list, gets silence — they nev
  FIX: Wrap ErrorState in `role="alert"` (kit.jsx:193) and EmptyState's title/help region in `role="status" aria-live="polite"` (kit.jsx:145), and render the `k-empty-title` as an `<h2>`/heading so the state change is announced and navigable. kit.jsx:143-208.
- [Medium|dead-affordance|shared-infra] 
  ISSUE: The session-expired 'minimal' treatment is applied to the topbar but NOT the sidebar. On a 401, App.jsx:600 passes minimal={auth.isError} to Topbar (hiding segment/bell/user-menu because they are dead controls), yet Layout still renders AdminBody (App.jsx:585,615) and Sidebar (App.jsx:511). With role undefined, Sidebar (App.jsx:213) shows every role-less nav item (~13 links) fully clickable — and each one routes to a DataScreen that immediately shows the 401 'login required' ErrorState. So the s
  FIX: When auth.isError, render the sidebar in the same minimal spirit as the topbar: hide it, disable its items, or replace it with a single '세션이 만료되었습니다 — 다시 로그인' panel. App.jsx AdminBody/Sidebar (around 505-511) gated on the same auth.isError already computed at 600.
- [Medium|missing-feature|shared-infra] 
  ISSUE: ProfileModal (App.jsx:289-298) surfaces '현재 활성 세션 N개' as a security signal and even warns when >1 ('숫자가 예상보다 많다면 …'), but offers no control to act on it — the only affordance is a '비밀번호 변경' link. There is no self-service 'log out other sessions / end this session'. Admins can revoke OTHER users' sessions (Users.jsx '세션 해제'), but a user cannot revoke their own. For a product built around opaque session tokens and shared/kiosk PCs, telling the user their account may be compromised while giving the
  FIX: Add a '다른 기기에서 모두 로그아웃' action in ProfileModal calling a self session-revoke endpoint (add the backend endpoint if it does not exist). App.jsx:289-298.
- [Low|design|change-password] 
  ISSUE: The secondary escape action has inconsistent visual weight between the two modes: must_change renders a full-width ghost BUTTON "다른 계정으로 로그인" (html:103, styled by base.css .auth-card .btn-ghost), while voluntary mode renders only a small underlined text link "← 돌아가기" inside a subtitle paragraph (html:109). Same role (leave this screen), two very different affordance sizes.
  FIX: Give both modes a consistent secondary-action treatment — either both as full-width ghost buttons or both as text links — so the escape affordance reads the same regardless of how the user arrived.
- [Low|design|users] 
  ISSUE: Active-state wording is inconsistent within one screen. The column header is '활성' (line 185) and the toolbar filter options are '활성'/'비활성' (lines 275-276), but the Badge renders 'active'->'사용 중' and 'disabled'->'비활성화' (kit.jsx STATUS_TEXT lines 12-13), used both in the list cell (line 188) and the detail Row (line 579). An admin who filters by '활성' then sees badges labeled '사용 중', and filters by '비활성' but sees '비활성화' — three different words for the same two states.
  FIX: Align the vocabulary: either relabel the STATUS_TEXT entries for active/disabled to '활성'/'비활성' for this context, or pass explicit badge text, so column header, filter options, and badge all read the same. Note the enable/disable action buttons already use '활성화'/'비활성화'.
- [Low|design|settings] 
  ISSUE: Mobile card view can cramp/overflow the '키' column. In kit.css the ≤760px card view renders each td as flex justify-content:space-between (kit.css:122), and only the '설명' cell gets special vertical stacking (screens.css:345-349). A long unbreakable snake_case key like document_automation_enabled sits next to its '키' label with no word-break/min-width:0, risking horizontal overflow or awkward wrapping on ~320px phones.
  FIX: If the '키' column is kept, give it the same mobile stacking treatment as '설명' (screens.css:348) and add word-break/overflow-wrap:anywhere to the key span. Best resolved together with removing/demoting the key column.
- [Low|design|settings] 
  ISSUE: Boolean copy is inconsistent between the two places a user sees it. The value column renders booleans via displayValue as '켜짐/꺼짐' (kit.jsx:135, used at Settings.jsx:324), but the editor's bool <select> uses '켜기/끄기' (Settings.jsx:546-547). Same concept, two vocabularies, seen back-to-back when opening a row.
  FIX: Align the labels — use '켜짐/꺼짐' (state) consistently in both the table and the select, or '켜기/끄기' (action) in both. Settings.jsx:546-547.
- [Low|dead-affordance|diagnostics+maintenance] 
  ISSUE: On the Maintenance screen, read-only roles (operator/auditor, which App.jsx:528 explicitly admits) see a permanently-disabled primary '공지 저장' button (Ops.jsx:634, disabled via !canWrite) while the sibling '미리 검증' button is hidden for the same roles (Ops.jsx:633, canWrite ? ... : null). Inconsistent treatment: one gated affordance is hidden, the adjacent one is shown grayed-out and can never be enabled — a visual dead affordance in the action bar.
  FIX: Gate '공지 저장' visibility on canWrite the same way '미리 검증' is (Ops.jsx:634), so read-only users see a coherent action bar (only '버전 기록'), instead of a disabled save button they can never use.
- [Low|dead-affordance|notification-bell] 
  ISSUE: Static rows are ALWAYS wrapped in an expand toggle with aria-expanded and the aria-label suffix '— 눌러서 전체 내용 펼치기' (NotificationBell.jsx:408-415), regardless of whether title/body actually overflow 2 lines. When a notification's text fits (most 'title'-only alerts like account_locked), clicking the row toggles is-expanded but nothing changes visually, and screen-reader users are told they can 'expand full content' when there is none — a promise-with-no-payoff / no-op interaction.
  FIX: Gate the toggle behavior and the aria-expanded/'펼치기' hint on an actual overflow check (e.g. measure scrollHeight > clientHeight on the clamped element, or a hasOverflow flag). Non-overflowing static rows should render as plain non-interactive content.
- [Low|contract|notification-bell] 
  ISSUE: The bell keeps its own hand-curated copies of OBJ_ROUTE, OBJ_ID_PARAM and ROUTE_ROLES (NotificationBell.jsx:20-39) that duplicate registry.js (OBJ_ID_PARAM/objRouteHref) and App.jsx (SCREEN_ROLES). The comment admits this is to avoid a circular import, and the sets have already drifted (bell knows 4 id-params vs registry's 12; ROUTE_ROLES is a separate hardcoded subset of SCREEN_ROLES). If a future notification type, route, or route-role changes in registry/App but not here, the bell will silent
  FIX: Extract the route/id-param/role tables into a shared, import-safe module (e.g. lib/notiRoutes.js) consumed by NotificationBell.jsx, registry.js and App.jsx, so the maps cannot drift; or add a test asserting the bell's subset stays consistent with registry.js/SCREEN_ROLES.
- [Low|ux|notification-bell] 
  ISSUE: When the unread-count query fails but a previously cached number remains, the badge shows a warning ring + tooltip '표시된 숫자가 최신이 아닐 수 있습니다' (NotificationBell.jsx:312-315, .noti-count--stale) but offers no way to act. Recovery depends entirely on refetchOnWindowFocus/refetchOnMount. A user who reads the warning has no manual refresh/retry affordance on the bell itself.
  FIX: Let clicking the bell (or opening the popover) trigger unread.refetch() when unread.isError, and/or surface a small retry control in the popover header when countError is true, so the stale state has an explicit recovery path.
- [Low|ux|notification-bell] 
  ISSUE: State inconsistency: when the list query errors but the independent unread-count query still holds count>0, the popover header renders '알림 · 안 읽음 N' AND a live '모두 읽음' button (NotificationBell.jsx:332-335) directly above an ErrorState body ('불러오지 못했습니다' / '다시 시도'). The header speaks confidently about a count and offers a bulk action while the body says it couldn't load — two contradictory messages in one small popover.
  FIX: In the list-error branch, suppress or de-emphasize the header count and the '모두 읽음' button (or move mark-all into the error affordance), so the popover tells one consistent story when the body is in an error state.
- [Low|bug|notification-bell] 
  ISSUE: openItem's double-fire guard is ineffective for the case its own comment claims to cover. NotificationBell.jsx:287 checks `!pendingIds.has(n.id)` but openItem never adds the id to pendingIds (unlike markOne at :279). So a rapid double-click on the SAME unread navigable item body fires readOne.mutate twice: on the second click within the same tick, n.read_at is still stale-false and pendingIds is still empty, so both POSTs go out. The comment at :285-286 explicitly promises this prevents '빠른 더블클릭
  FIX: Have openItem add the id to pendingIds before mutating (mirroring markOne), or accept the idempotency and correct the misleading comment. NotificationBell.jsx:284-295.
- [Low|bug|notification-bell] 
  ISSUE: read-all optimistic rollback under-restores the count when there are more unread than the 8 loaded rows. readAll.onMutate sets noti-unread to 0 unconditionally (NotificationBell.jsx:183) and list.unread to 0 (:176), but on failure onError only adds back ctx.changedIds.size / restored (:199, :202), which is at most the ~8 loaded items. If true unread was e.g. 15, a failed read-all leaves the badge showing 8 instead of 15 momentarily. It self-corrects because onSettled: invalidateNoti (:208) refet
  FIX: Snapshot the pre-mutation unread total and restore that exact value in onError instead of re-deriving from changedIds, or rely on invalidateNoti and drop the manual restore. NotificationBell.jsx:187-203.
- [Low|design|notification-bell] 
  ISSUE: Three security/routing-relevant lookup tables are hand-duplicated in the bell (OBJ_ROUTE, OBJ_ID_PARAM at NotificationBell.jsx:20-27, and ROUTE_ROLES at :36-39) as local copies of App.jsx SCREEN_ROLES and registry.js OBJ_ROUTE/OBJ_ID_PARAM (to avoid a circular import). If a future notification type gains a related object, or App.jsx SCREEN_ROLES changes, these local copies can silently drift, producing either dead links (bell shows a clickable item that lands on a '권한이 없습니다' screen) or over-hidd
  FIX: Extract the shared route/param/role maps into a dependency-free module (e.g. lib/deeplinks.js) imported by both registry.js and NotificationBell.jsx so there is a single source of truth. NotificationBell.jsx:20-39.
- [Low|design|integrations] frontend/src/screens/registry.js
  ISSUE: The 'enabled' column renders with generic badgeCol (registry.js:308) which shows '예/아니오' (statusText(true/false) in kit.jsx), while the 'enabled' filter on the same screen labels the states '활성/비활성' (registry.js:302). This is the exact filter/column vocabulary mismatch that activeCol was created to fix (DataScreen.jsx:748-752), but activeCol is hardcoded to the 'active' key so it wasn't applied to integrations' 'enabled' column.
  FIX: Render 'enabled' with domain vocabulary matching the filter, e.g. a small helper like enabledCol that maps true/false to '활성'/'비활성' badges (mirroring activeCol at DataScreen.jsx:752) instead of badgeCol('enabled','활성').
- [Low|contract|job-titles] 
  ISSUE: The '감사 로그에서 보기' action (registry.js:1309) omits the roles gate that every other screen's identical action carries (roles: ['admin','system_admin','auditor'] — see integrations:355, runners:477, workflows:626, etc.). It is harmless because the whole job-titles route is already admin/system_admin-gated (App.jsx SCREEN_ROLES:52), so no dead-end can occur — but it is an inconsistency a consistency reviewer should note.
  FIX: For consistency add roles: ['admin','system_admin'] to the job-titles audit action (registry.js:1309); functionally optional since the screen is already role-gated.
- [Low|dead-affordance|notifications] frontend/src/screens/registry.js:1603
  ISSUE: The '읽음' column renders a status badge (안읽음/읽음) via readCol (registry.js:1603) that looks actionable but is not clickable; the only way to mark a single notification read is row-click → open drawer → footer button '읽음 처리' (registry.js:1622). The bell popover offers a one-click inline check per row (NotificationBell.jsx:416-422). For triaging a long paginated list this is a drawer round-trip per item with no inline affordance.
  FIX: Add an inline row action (or make the read column interactive) that POSTs /api/notifications/{id}/read for unread rows without opening the drawer, matching the bell's per-row check button.
- [Low|ux|shared-infra] 
  ISSUE: FormModal's unsaved-changes guard raises false 'discard changes?' prompts on number fields. Defaults are stored as numbers (e.g. runner timeout `value: 60` → values[name] === 60 number, kit.jsx:427), but any edit goes through `onChange(e.target.value)` yielding a string. The dirty check is `JSON.stringify(values) !== JSON.stringify(initialRef.current)` (kit.jsx:440); typing then restoring the same numeric value makes values hold '60' (string) vs initial 60 (number), so JSON.stringify differs and
  FIX: Normalize number-typed fields before the dirty comparison (coerce both sides via the field type), or compare a normalized snapshot rather than raw values. kit.jsx:435-445.
- [Low|bug|shared-infra] 
  ISSUE: Theme choice flashes (FOUC) on every reload for anyone whose chosen theme differs from their OS setting. Boot-time initialTheme() reads getStoredTheme() which reads the ACCOUNT-LESS key localStorage['clovirone_theme'] (App.jsx:245), but toggleTheme() only ever writes the ACCOUNT-SPECIFIC key themeKey(userId) = 'clovirone_theme:<id>' (App.jsx:366). The account-less key is never written, so getStoredTheme() always returns '' and boot falls back to OS preference; the user's real choice is only appl
  FIX: On toggle also persist the account-less key AND clear it inside logout() (App.jsx:369-373) to preserve shared-PC isolation; or, if the flash is accepted intentionally, correct the overstated FOUC comment at App.jsx:252.
- [Low|missing-feature|shared-infra] 
  ISSUE: The shell never shows the signed-in user their own role, despite a 5-role RBAC system that visibly changes which nav/actions appear. The topbar shows only name + avatar (App.jsx:381-382), and ProfileModal lists 부서/직책/마지막 로그인/활성 세션/Notion 상태 but omits role (App.jsx:285-300). A user (or an operator/auditor confused about why buttons are missing) has no way to see whether they are operator, auditor, admin, etc.
  FIX: Add role to ProfileModal (and/or the user-menu header) using the existing ROLE_KO label map from Users.jsx. App.jsx:285-300.
- [Low|design|shared-infra] 
  ISSUE: The topbar-fg token cleanup was done for the container but not its children. global.css:32 sets .c-topbar color: var(--topbar-fg, #fff) with a comment celebrating replacing hardcoded #fff so the topbar fg becomes per-theme tunable — but the actual text-bearing children still hardcode white: .c-brand #fff (35), .c-seg-item #fff (53), .c-hamburger #fff (72), .c-user-btn #fff (77). Since these are descendants of .c-topbar, they would inherit the token if the explicit #fff were removed; as written, 
  FIX: Let the topbar's text children inherit color from .c-topbar (drop the explicit color:#fff on .c-brand/.c-seg-item/.c-hamburger/.c-user-btn) or point them at var(--topbar-fg). global.css:35,53,72,77.

## AREA=E (52)

- [High|missing-feature|dashboard] 
  ISSUE: The current maintenance-mode state is never shown on the dashboard, even though it is the single highest-blast-radius operational state (app/settings/gate.py blocks ALL user writes when on). The '확인이 필요한 항목' alerts assembly (Dashboard.jsx:166-237) has no maintenance alert, and build_dashboard (app/health/service.py:248-294) returns no maintenance flag at all. Ironically the dashboard goes out of its way to surface maintenance-mode *changes* in '최근 주요 변경' (backend specifically matches setting.upd
  FIX: Add a maintenance flag to the build_dashboard payload (app/health/service.py:248) and, in DashboardBody, push a danger/warn item into `alerts` (and/or a top Callout banner like the stale banner at Dashboard.jsx:273) whenever maintenance mode is active, linking to /maintenance.
- [High|dead-affordance|documents] 
  ISSUE: The '재시도' (retry) action on failed/quality_failed rows (registry.js:1138-1144) reopens the generate form but the backend has no way to actually retry the same document. request_generation (app/documents/service.py:251-262) computes an idempotency key of doc:{schedule_id}:{period}:{target_ref}:v{version}; the failed generation's row still holds that key, so re-submitting the SAME period+target raises ConflictError '이미 생성된 문서입니다 (기간/대상 중복): failed'. There is no DELETE/cancel/force-regenerate endpo
  FIX: Add a backend endpoint to delete/reset a failed|quality_failed generation (freeing its idempotency_key) or a force-regenerate path, then wire the '재시도' action to it. Backend: app/documents/router.py (add DELETE or /regenerate), app/documents/service.py:257-262. Until then, '재시도' should at minimum warn that the same period will be rejected.
- [High|dead-affordance|policies] 
  ISSUE: The status filter defaults to value:"published" (registry.js:740). New policies are always created as status=draft (app/prompts/router.py:199, STATUS_DRAFT). After an admin clicks '+ 정책 추가' and creates a policy, refresh() re-runs the list query with status=published, so the just-created draft is NOT returned and the list shows '검색 결과가 없습니다' even though the toast said '추가했습니다'. The create→see-your-result loop, the primary purpose of the screen, is broken. Retrying with the same name then hits 409
  FIX: Do not pre-apply the published scope in a way that hides freshly-created content: either remove value:"published" at registry.js:740, or after a successful create switch the status filter to the new item's status / clear it (DataScreen.jsx onSubmit at ~line 517-523) so the new draft is visible.
- [Medium|design|login] 
  ISSUE: login.css uses min-height:100vh on .login-split (line 15) and .login-panel (line 234) while .panel-foot is position:absolute; bottom:28px (lines 226-229). On mobile browsers 100vh is the LARGE viewport height (excludes address-bar retraction), so the panel is taller than the visible area and the pinned footer sits under the browser chrome. Worse, the panel centers its content with justify-content:center while the footer is out-of-flow; on a short viewport (landscape phone ~375px tall) with the s
  FIX: login.css: switch min-height:100vh to 100dvh on .login-split (line 15) and .login-panel (line 234), and take .panel-foot out of absolute positioning (make it a flex child with margin-top:auto, or add padding-bottom to .login-panel equal to footer height + 28px so centered content can never reach it).
- [Medium|design|login] 
  ISSUE: The hero brand mark is a raster PNG served at a small size: login.html line 13 <img src=clovirone-logo-white.png> styled height:30px (login.css line 46). Every other brand mark in the product is vector inline SVG (the right panel uses the _wordmark.html SVG via line 51), and a vector white wordmark already exists in the same folder (app/static/img/clovirone-wordmark.svg). On the design-forward hero at high-DPI / 4K the raster mark renders visibly softer than the crisp SVG UI around it, and it's 
  FIX: Replace the <img> in login.html line 13 with the inline _wordmark.html include used by the panel (white currentColor in the hero), e.g. {% with class='hero-wordmark' %}{% include "_wordmark.html" %}{% endwith %}, or point the img at clovirone-wordmark.svg; the '업무 도우미' text already exists separately as .ver / the wordmark's sub param.
- [Medium|ux|change-password] 
  ISSUE: The forced-change greeting hardcodes "첫 로그인입니다" (this is your first login). But must_change_password is set True in TWO cases: on user creation AND on admin_reset_password (app/users/service.py:351). A user whose password an admin reset — who has logged in many times — is told "첫 로그인입니다", which is factually false and confusing on a mandatory screen they cannot skip.
  FIX: change_password.html:18 — drop the false "첫 로그인입니다" claim or branch on the real reason. Either use neutral copy for both paths (e.g. "{display_name}님, 계속하려면 임시 비밀번호를 새 비밀번호로 변경하세요."), or have the server pass a flag distinguishing first-login from admin-reset and render the matching sentence.
- [Medium|dead-affordance|change-password] 
  ISSUE: The form is JSON-POST-only: the submit button and (in must_change mode) the logout button both render server-side as disabled and are only enabled by change_password.js. The <noscript> fallback (html:86-98) fires ONLY when JS is disabled, not when the script fails to load with JS enabled (404 / network / parse error). In that case a must_change user sees a form with a greyed-out submit AND a greyed-out logout, no noscript message, and no error text — every control is dead with zero explanation a
  FIX: change_password.html:82,99-111 — do not gate the ONLY escape entirely behind JS. Provide a JS-independent exit for must_change (e.g. a plain visible link to log out / contact admin that isn't inside <noscript>), and/or surface a static "이 페이지를 불러오지 못했습니다" hint that shows when the enhancer never runs, so a script-load failure isn't a silent dead-end.
- [Medium|a11y|change-password] 
  ISSUE: change_password.css:36-41 overrides the shared base focus style (base.css:99-103, a full 2px `outline: solid var(--color-primary-soft)` chosen specifically to meet the 3:1 focus-visibility ratio) with `outline: none` plus a box-shadow glow at only 12% opacity (`color-mix(... 12%, transparent)`). The remaining focus cue is a 1px border-color change plus a barely-visible glow — materially weaker than the base indicator it replaces, risking insufficient keyboard-focus visibility on a form every use
  FIX: change_password.css:36-41 — keep a real outline (e.g. `outline: 2px solid var(--color-primary-soft); outline-offset: 1px;`) instead of `outline: none`, or raise the box-shadow ring opacity well above 12% so the focused field clears the 3:1 non-text contrast requirement in both light and dark themes.
- [Medium|dead-affordance|login] 
  ISSUE: The only account-recovery affordance on the login screen is a mailto link that renders ONLY when branding.support_email is configured (login.html lines 116-144; login.js setLoginHint lines 170-191). When support_email is empty, the 'forgot password / account locked / disabled / archived' hint degrades to passive text ('시스템 관리자가 임시 비밀번호를 재발급해 드립니다') with NO clickable control. The app has no self-service password reset, so a genuinely locked-out user with support_email unset has zero on-screen pat
  FIX: Ensure support_email is always populated (validate it as required in ui_branding settings), or render a non-mailto fallback contact affordance (e.g. an admin console URL or a documented recovery instruction) so the hint is never a purely-informational dead end. At minimum surface a warning in admin settings when support_email is blank.
- [Medium|dead-affordance|chat (user)] 
  ISSUE: The backend GET /api/conversations/{id}/messages endpoint exposes an `after` cursor param (app/chat/router.py:121; service.py:116-132) with carefully-built stale-cursor semantics for incremental fetch, but the frontend thread query NEVER sends it (Chat.jsx:644 calls the URL with no `after`). Consequence: while polling (every ~1.5s during processing) the client refetches the ENTIRE conversation — all messages plus their full structured ticket-card JSON — and react-query replaces the whole array, 
  FIX: Use the cursor: track the last message id and poll `.../messages?after=<lastId>`, merging returned items into the cache instead of replacing the whole thread; or drop the `after` param from the backend if incremental polling isn't wanted. Chat.jsx:642-651.
- [Medium|missing-feature|dashboard] 
  ISSUE: Backup failures are effectively invisible on the dashboard. The backup card renders `last_backup_status` as a Badge (Dashboard.jsx:418), but last_successful_backup (app/backups/service.py:149-155) only ever returns STATUS_SUCCEEDED/VERIFIED rows, so that badge is structurally always '완료'/'확인됨' and can never display a failure — a decorative, non-informative affordance. Ongoing failed backup runs are surfaced only indirectly via the '마지막 백업이 오래됨' stale-age alert (Dashboard.jsx:223-225), which is g
  FIX: Surface the most recent backup ATTEMPT (regardless of outcome) or a recent-failure count on the dashboard, so a failing backup shows a real '실패' state instead of a stale success badge; alternatively show the stale-age warning to admin as well as system_admin, or drop the always-success badge since it carries no signal.
- [Medium|dead-affordance|change-password] 
  ISSUE: A forced-change (must_change) user is completely locked out if change_password.js is enabled but fails to load (CSP block, network drop, extension interference). Both the submit button (change_password.html:82) and the ONLY escape, the logout button (change_password.html:103), ship `disabled` and are enabled solely by JS `enableControls()` (change_password.js:52-61). There is no back link in the must_change branch, and `/logout` requires an X-CSRF-Token header that only the JS fetch supplies, so
  FIX: Detect script-init failure or, at minimum, render a visible message when the controls have not been enabled shortly after load (e.g. a fallback `<p>` shown by CSS when JS hasn't stamped a ready class within N seconds), and/or provide a no-JS logout `<form method=post action=/logout>` path for must_change users so they always have an escape. The current design comment at change_password.html:80-81 only accounts for JS-disabled (noscript), not JS-load-failure.
- [Medium|design|documents] 
  ISSUE: The '요청자' (requester) column (registry.js:1107, col('requested_by','요청자')) renders a raw user UUID, not a person. generation_view returns requested_by as the bare row.requested_by UUID (app/documents/service.py:307) and the documents router does NO name resolution — unlike the approvals router which batch-attaches requester_name/requester_email so its list shows a human name. The registry comment (1105-1107) even acknowledges 'approvals already shows the requester in the list'. An operator canno
  FIX: Batch-resolve requester_name/requester_email in the documents list router (mirror app/approvals/router.py) and render name || email || UUID in the '요청자' column. registry.js:1107; app/documents/router.py:43-67 / service.py:292-317.
- [Medium|missing-feature|documents] 
  ISSUE: 'pending' generations are polled indefinitely (pollWhile: r.status==='pending', registry.js:1086) but there is no cancel affordance and no backend endpoint to cancel a generation or its underlying document_generate job. If the n8n/runner job hangs or is lost, the row stays '대기' forever and the screen polls forever with no user recourse or timeout signal.
  FIX: Add a backend cancel endpoint for pending generations (and/or the job) and expose a '취소' row action when status==='pending'; alternatively surface a stale-pending indicator. registry.js:1086,1125-1150; app/documents/router.py.
- [Medium|missing-feature|prompts] 
  ISSUE: The prompts screen has no 'templates using this prompt' blast-radius action, even though its own help text (registry.js:633) warns that publishing makes every template referencing this prompt use the new version on the next document run. The sibling policies screen HAS this affordance ('이 정책을 쓰는 템플릿', registry.js:777-784). Templates carry prompt_id (app/templates/models.py:22) and document generation resolves the binding by NAME to the currently-published version (app/documents/service.py:88), s
  FIX: Add an action mirroring policies (registry.js:777-784) to the prompts actions array (registry.js:689-706): { label: '이 프롬프트를 쓰는 템플릿', subList: { title, endpoint: () => '/api/admin/templates', filterRows: (t, parent) => t.prompt_id === parent.id, columns: [...], emptyTitle, emptyHelp } }. Given resolution is by name, consider matching all versions of the name rather than only parent.id.
- [Medium|bug|notion-mapping] 
  ISSUE: The '출처'(source) filter is declared clientFilter:true (registry.js:1337) but the list is server-paginated (paginated:true, registry.js:1326). It only filters the rows already loaded on the current page, while the pager total and '총 N건' still reflect the UNFILTERED server query (DataScreen.jsx:321-322, 490). Selecting source='수동 지정' silently hides manual mappings that live on other pages — directly defeating the filter's stated purpose (the code comment at registry.js:1334-1336 says it exists 'to
  FIX: Backend list_mappings (app/notion_mapping/router.py) has no `source` query param — add server-side `source` filtering there and drop clientFilter on registry.js:1337 (like the status filter), OR disable pagination while this filter is active. Do not ship a paginated clientFilter as a real audit tool.
- [Medium|missing-feature|notion-mapping] 
  ISSUE: Backend list_mappings supports `archived: bool` (app/notion_mapping/router.py) to show/manage Notion mappings for archived accounts, but the screen exposes no toggle/filter for it (registry.js filters at 1331-1337 are status/user_ids/source only). The list is user-keyed and hides archived users (WHERE User.archived_at IS NULL), and the Users screen also hides them, so an archived user's stale Notion mapping row cannot be viewed or unmapped from anywhere in the UI. This backend capability is enti
  FIX: Add an `archived` boolean filter/toggle to registry.js filters (a '보관된 계정' view) that sends `?archived=true`, mirroring the archived pattern used on the Users screen, so admins can reach and unmap offboarded accounts' mappings.
- [Medium|missing-feature|jobs] 
  ISSUE: schedule_run and document_generate job rows have no way to reach (or even cleanly identify) the schedule/document they drive. `_job_view` (app/jobs/router.py:40-57) deliberately withholds payload_json, so schedule_id / schedule_run_id / generation_id are only recoverable by string-parsing idempotency_key. For schedule_run jobs it is worse: scheduler enqueue (app/schedules/scheduler.py:101-108) passes no user_id/conversation_id/message_id, so the detail drawer's ONLY breadcrumb is the raw `schedr
  FIX: Expose reference IDs (not content) in _job_view — add schedule_id/schedule_run_id/generation_id from payload_json (these are references, exactly like the already-exposed conversation_id/message_id; the least-privilege docstring is about chat text/attachments/emails, not linkage IDs). Then add navigate actions in registry.js jobs.actions (~line 1519) mirroring '감사 로그에서 보기': when job_type==='schedule_run' → '#/schedules?id='+schedule_id, when 'document_generate' → '#/documents?id='+generation_id.
- [Medium|dead-affordance|job-titles] 
  ISSUE: The list shows a '보유 인원(보관 포함)' count per title (registry.js:1294) — e.g. '보유 인원 3명' — but there is no way to find out WHICH users hold the title. The users backend (app/users/router.py GET "") only supports q/role/active/archived filters, NOT title_id, and job-titles offers no '이 직책을 가진 사용자 보기' drill-through. An admin sees a count that invites 'who?' with no answer path. (The count does still correctly serve the delete/deactivate decision, so this is a partial-domain gap, not a broken control.)
  FIX: Add a title_id (and department_id) query filter to app/users/router.py list_users, then add an action on job-titles: { label: '이 직책을 가진 사용자 보기', when: r => r.user_count, navigate: r => '#/users?title_id=' + r.id } and have Users.jsx consume ?title_id= (like other onQuery deep-links). registry.js:1302-1310.
- [Medium|missing-feature|audit] 
  ISSUE: The screen renders a '결과' (result) column with success/failure badges, but there is NO way to filter or search by result. Isolating failures — failed logins (action=user.login_failed, result=failure, app/auth/router.py:349) and failed self password-changes (password_change_failed, result=failure, app/auth/router.py:546) — is the single most important query for a security audit log, yet the only way to find them is to eyeball 100 rows per page. The backend GET /api/admin/audit (app/audit/router.p
  FIX: Add `result: str | None` Query param + `AuditLog.result == result` filter in app/audit/router.py, and add `{ key: "result", type: "select", label: "결과", options: opt([["success","성공"],["failure","실패"]]) }` to the filters array at frontend/src/screens/registry.js:1538.
- [Low|design|login] 
  ISSUE: The hero decorative tiles are absolutely positioned by percentage (login.css lines 76-84: .t1 left:2%, .t2 left:42%, .t3 left:12%) inside .hero-stack, which has no max-width (line 67). The layered/overlapping card composition is tuned for a ~700-1000px hero; on an ultra-wide/4K hero (panel is max-width:620px so the hero eats all remaining width, ~3200px at 3840px) the tiles spread far apart and lose their stacked look, leaving large empty gaps.
  FIX: login.css line 67: give .hero-stack (or the hero content column) a max-width (e.g. max-width:640px to match .hero-copy) so the tile composition stays coherent, or use a fixed-width stage centered within the hero.
- [Low|design|login] 
  ISSUE: The same full copyright line 'Copyright © 2026 Goodmorning Information Technology Co., Ltd. All Rights Reserved.' is rendered twice and shown simultaneously at >=1024px: hero-foot bottom-left (login.html line 43) and panel-foot centered (line 151). The comment justifies matching the text across breakpoints, but showing the identical legal line twice on one screen reads as redundant clutter.
  FIX: Show only one copyright per breakpoint: keep panel-foot but hide it (display:none) at >=1024px where hero-foot is already visible, or vice-versa; the wording can still be identical so the <1024px case is unaffected.
- [Low|ux|login] 
  ISSUE: The email input has autofocus (login.html line 78). On mobile this immediately opens the soft keyboard on page load, collapsing the viewport and causing layout jump before the user has oriented; combined with the 100vh footer issue it worsens the initial reflow.
  FIX: Consider gating autofocus to non-touch/wide viewports (e.g. set focus in login.js only when matchMedia('(min-width:1024px)') matches) rather than the static HTML autofocus attribute.
- [Low|ux|change-password] 
  ISSUE: The live "✓ 새 비밀번호 확인 일치" (confirm-match) indicator is pushed into #cp-rules (change_password.js:187-189), which sits in the NEW-password field group — ABOVE the confirm-password input. While the user is typing in the confirm field (the last field on screen) the match/mismatch feedback updates one field up, out of their line of sight.
  FIX: Render a match/mismatch indicator directly beneath the confirm-password input (its own status line under html:66-70), or move #cp-rules below the confirm field, so the feedback appears where the user is actually looking.
- [Low|design|change-password] 
  ISSUE: #cp-rules reserves `min-height: 3em` (base.css:247) but renderRules() returns early and renders nothing until the new-password field has input (change_password.js:174-175). On initial load this leaves a ~39px empty, unlabeled gap between the new-password field and the confirm field, reading like a rendering glitch.
  FIX: Either render the two always-applicable base rules (length + character-classes) immediately at 0 chars so the reserved space is populated, or reduce/remove the min-height and accept the small shift, or collapse the reserve until the field is focused.
- [Low|ux|login] 
  ISSUE: login.js probeHealth() (lines 99-114) fires fetch('/readyz') on EVERY anonymous GET /login render, and /readyz (health/router.py lines 27-36) opens a fresh DB session + SELECT 1. Under SQLite WAL write contention (the app's own docs warn about DB locks during backup/migration), /readyz can transiently return 503; probeHealth then shows '서버 응답이 느립니다. 잠시 후 다시 시도하세요.' on a login page that actually works fine, potentially discouraging users from logging in. It also adds an unauthenticated, unthrottl
  FIX: Consider probing lazily (only after a submit failure) rather than on load, lengthen/soften the transient-blip window (currently one 1.5s retry), or downgrade the message so it reads as advisory rather than blocking. Optionally gate the client probe behind a config flag.
- [Low|bug|login] 
  ISSUE: The 15s client abort (SUBMIT_TIMEOUT_MS, login.js lines 196, 241-242) aborts the fetch on timeout, but the backend login() may have already created the session and issued Set-Cookie. If POST /login exceeds 15s (Argon2 verify under threadpool saturation / slow sync SQLAlchemy), the aborted fetch discards the response (Set-Cookie not applied) and shows '서버 응답이 없습니다. 다시 시도하세요.' The user's retry creates a second session; the first orphaned session persists until TTL. Rare, but produces a confusing f
  FIX: This is largely inherent to client timeouts; acceptable, but consider re-checking auth state (e.g. GET /api/me) on AbortError before declaring failure, so a silently-successful login redirects instead of showing an error.
- [Low|ux|login] 
  ISSUE: On successful login, the submit handler navigates via window.location.href then returns (login.js line 259), but the try/finally still runs the finally block (lines 301-308), which resets submitLabel back to '로그인' and re-enables the button while the page is still navigating. Users can briefly see the default '로그인' label flash over '로그인 중…' during the redirect.
  FIX: Set a navigating flag before window.location.href and skip the label/disabled reset in finally when navigating (or don't reset label on the success path).
- [Low|contract|chat (user)] 
  ISSUE: The client hardcodes the 5000-char message limit in two places — textarea maxLength={5000} (Chat.jsx:1126) and the counter '{text.length}/5000' (Chat.jsx:1110) — but the real limit is the server-configurable settings.max_message_length (app/core/config.py:36 default 5000; enforced service.py:157). The two only coincide at the default; if a deploy sets max_message_length lower, the counter still says /5000 and the box still accepts 5000 chars, then the send fails with a 422 the user only sees as 
  FIX: Surface max_message_length via /api/me (or a config endpoint) and drive maxLength + the counter denominator from it, instead of the literal 5000. Chat.jsx:1110,1126.
- [Low|a11y|change-password] 
  ISSUE: The Caps Lock hints (cp-caps-hint / -current / -confirm) use role="status" (a polite live region) but are toggled only by flipping the `hidden` attribute (change_password.js:141-144), while their text content never changes and CSS keeps them display:block+visibility:hidden (base.css:201-202). Live regions announce on content change or insertion, not on a visibility/`hidden` flip of unchanged text, so screen-reader users typing a mixed-case temp password with Caps Lock on likely get no spoken war
  FIX: Announce on state change: set the hint's textContent when Caps Lock turns on and clear it when off (content change triggers the live region), instead of only toggling the `hidden` attribute on static text. change_password.js:141-144.
- [Low|ux|change-password] 
  ISSUE: For a voluntary change, the back link is overwritten with the same-origin document.referrer (change_password.js:23-32) and the post-success redirect goes to backLink.href (change_password.js:392-394). If the user reached the page by reloading /change-password (referrer == /change-password), then after a successful change they are redirected straight back to /change-password (must_change is now false, so GET just re-renders the form with '로그인됨: name'), instead of returning to the app. Confusing l
  FIX: When the computed referrer path equals /change-password (or /login), ignore it and fall back to '/' so a successful change never redirects back onto the change-password screen. change_password.js:26-27.
- [Low|contract|change-password] 
  ISSUE: The generic-failure branch trusts server error.message only for codes in KOREAN_MESSAGE_CODES = { forbidden: true } (change_password.js:475-478), but POST /change-password has no code path that produces a `forbidden` code: it raises only rate_limited(429), wrong_current_password(401), unauthorized(401), csrf_failed(403), validation_error(422), or falls to internal_error(500) — all handled by earlier branches. So this whitelist entry is unreachable dead code; every real fallback always shows the 
  FIX: Either drop the unreachable `forbidden` entry (and rely on the static message), or if a forbidden path is intended (e.g. account archived/disabled mid-session, which currently surfaces as unauthorized), wire it explicitly. change_password.js:475.
- [Low|bug|change-password] 
  ISSUE: The support/reset mailto links percent-encode the whole address via Jinja's `urlencode` filter: `mailto:{{ support_email | urlencode }}?subject=...` (change_password.html:90 and :114). Jinja urlencode encodes '@' to %40, producing mailto:admin%40company.com. Per RFC 6068 the '@' delimiter should not need encoding; while most clients decode %40, some desktop mail clients may fail to open the composer, breaking the only actionable escape for a stuck (esp. must_change) user.
  FIX: Do not urlencode the address itself — emit the raw address and only percent-encode query parameters (subject), e.g. `mailto:{{ support_email }}?subject={{ mailto_subject_reset }}`. change_password.html:90,114.
- [Low|missing-feature|settings] app/settings/router.py:81
  ISSUE: The version-history table cannot show WHO changed a setting. ConfigVersion stores created_by (versioning.py:30), but the setting_versions endpoint deliberately returns only {version, snapshot, created_at} (router.py:81-87), so the UI columns can only render 버전 / 이전 값 / 변경 시각 (Settings.jsx:633-638). An admin reviewing a setting's history sees when it changed but never by whom — accountability for the change lives only in the separate audit log, not on the screen where the change is being reverted
  FIX: Add created_by (resolved to a user name/email) to the setting_versions response in app/settings/router.py:81-87, then add a '변경자' column to the SettingVersions DataTable in Settings.jsx:633-638. Resolve the id to a display name the same way the audit log screen does.
- [Low|ux|users] 
  ISSUE: Reset-password (UserDetail submitReset, Users.jsx:454-460; button at 503) revokes ALL of the target's active sessions and force-sets must_change_password (app/users/service.py admin_reset_password:337-367), yet unlike disable/archive/revoke-sessions it has no confirmation dialog, and on the admin-typed-password path the success toast is just '비밀번호를 재설정했습니다.' (Users.jsx:457) with no mention that every session was terminated. The admin gets no warning that they just logged the user out everywhere.
  FIX: Add a confirm() before opening the reset form (or before submit), and change the success toast to disclose the side effect, e.g. '비밀번호를 재설정했습니다. 이 사용자의 모든 세션이 해제되고 다음 로그인 시 비밀번호를 변경해야 합니다.' (Users.jsx:457).
- [Low|ux|users] 
  ISSUE: When a non-system_admin admin submits an edit that changes a non-role field (name/department/title) AND promotes the user to admin in the same save, the backend applies the non-role fields immediately and returns 202 approval_pending (app/users/router.py patch_user:243-292). The frontend's announce() (Users.jsx:239-244) then shows only '승인 요청이 접수되었습니다. 관리자 승인 후 반영됩니다.', implying the entire edit is pending — but the name/department change already took effect. This is a misleading feedback message
  FIX: In announce() distinguish partial application, e.g. when res.status==='approval_pending' but the submitted diff also had non-role keys, say '역할 변경은 승인 후 반영됩니다. 나머지 변경 사항은 저장되었습니다.' (Users.jsx:242).
- [Low|design|users] 
  ISSUE: The detail '비밀번호 변경 요구' badge is rendered as `<Badge value={!!d.must_change_password} />` (Users.jsx:591). A boolean true maps to statusText '예' with STATUS_KIND['true']='ok' → green. 'Must change password = yes' is a pending-action state, not a positive/success state, so green reads as 'all good' when the opposite is true; meanwhile the adjacent lock badge correctly uses an explicit danger/neutral tone (Users.jsx:590). The color semantics are inconsistent for a same-category action-needed flag.
  FIX: Pass an explicit kind for this badge (neutral, or warn to signal action pending), e.g. `<Badge value={!!d.must_change_password} kind={d.must_change_password ? 'warn' : 'neutral'} />` (Users.jsx:591).
- [Low|missing-feature|departments] 
  ISSUE: The '소속 인원(보관 포함)' count (registry.js:1259) is a terminal number with no drill-through — an admin sees e.g. '5명' but there is no path anywhere in the app to see WHICH users belong to the department before renaming/deactivating/deleting it. The backend org service exposes only usage_count (a count), and the Users list endpoint (app/users/router.py:70-101) filters by q/role/active/archived but not department_id/title_id, so no member list can be reached. The screen already uses a deep-link pattern
  FIX: Add a `department_id` (and `title_id`) query filter to the users list endpoint (app/users/router.py:70), then add a departments row action `{ label: '소속 사용자 보기', navigate: (r) => '#/users?department_id=' + r.id }` at registry.js:1271 so the count becomes actionable (mirrors the existing audit deep-link).
- [Low|empty-state|policies] 
  ISSUE: Unlike the peer chain screens (integrations/runners provide emptySituation/emptyPrerequisite/emptySteps/emptyExpected/emptyRelatedLink for §9 step-by-step onboarding), policies only has emptyTitle + emptyHelp. A policy has no observable effect until a Template references it by policy_id and a document is generated (app/documents/service.py apply_template_bindings inlines policy.content into the n8n payload), yet the empty screen gives no guidance on that flow or on what the policy JSON should co
  FIX: Add emptySteps/emptyExpected and an emptyRelatedLink to #/templates (mirroring integrations/runners), and expand the create/edit content help to reference docs/PROMPT_POLICY_MANAGEMENT.md and show a fuller example (registry.js:715-719, 757, 760).
- [Low|design|workflows] 
  ISSUE: The '버전 기록' sub-list column shows the raw actor UUID under '변경자 ID' (versionsAction col('created_by', ...) at registry.js:199; backend get_versions returns unresolved r.created_by at router.py:143). Prompts/policies' nameVersionsAction resolves created_by_name/created_by_email (registry.js:227) for the same concept, so an admin reviewing who made or should roll back a change sees a copyable UUID here instead of a human name/email.
  FIX: Have the versions endpoint resolve created_by to name/email (as _prompt_view does) and render it in the sub-list, or at least fall back to a resolved label. app/workflows/router.py:134-148, registry.js:199
- [Low|missing-feature|workflows] 
  ISSUE: '테스트' only performs a GET reachability probe (reachResult, registry.js:598,264-267; provider_n8n.test never executes the workflow). There is no run-now / dry-run / manual-invoke affordance on this screen to confirm a registered workflow actually works end-to-end — the doc (WORKFLOW_REGISTRY.md §16.4) sends the admin to the Scheduler for that. The screen therefore cannot verify the core artifact it manages actually functions, only that the port is open.
  FIX: This is honestly labeled but domain-incomplete; if verification is desired, add a read-only workflow invoke/dry-run endpoint (guarded to operation_mode=read) and a '지금 실행(읽기 전용)' action, or add an inline link to the Scheduler run-now for this workflow. Backend currently exposes no invoke endpoint (provider.invoke is internal), so this needs a new endpoint first. app/workflows/router.py, registry.js:596-598
- [Low|dead-affordance|prompts] 
  ISSUE: The prompts 'name' filter is a bare exact-match text input (registry.js:653) while the backend only supports exact-match on name (app/prompts/router.py:162, model.name == name). Without knowing the exact prompt name in advance, the filter is effectively unusable. The policies screen solved the same exact-match limitation by adding datalistFrom autocomplete suggestions drawn from the loaded page (registry.js:739); prompts did not.
  FIX: Add datalistFrom: (items) => items.map((r) => r.name) to the name filter at registry.js:653 (DataScreen already renders datalist filters, DataScreen.jsx:440-450).
- [Low|missing-feature|prompts] 
  ISSUE: Version comparison (diff) can only compare a historical version against the currently-open parent version — the 'to' side is hardcoded to parent.version with no from/to picker (registry.js:236-239, acknowledged in the comment at 216-217). The backend GET /diff/view accepts arbitrary from/to (app/prompts/router.py:272-279), so comparing two arbitrary historical versions directly is not exposed.
  FIX: Acceptable workaround exists (open version history from any row to make it the parent). If desired, add explicit from/to selection in the version-history sublist to fully surface the diff endpoint's capability.
- [Low|missing-feature|approvals] 
  ISSUE: The queue can only be filtered by status (registry.js:1176; backend list at app/approvals/router.py:57-62 accepts only a `status` query param + pagination). A mixed pending queue spanning all 5 request types (schedule.enable, runner.change_config, integration.change_config, user.role_change, document.publish) across many requesters cannot be narrowed by request_type, object_type, or requester. Because the list is server-paginated, a clientFilter would only filter the visible page, so this needs 
  FIX: Add optional query params (request_type, requested_by) to app/approvals/router.py list_approvals and expose matching server-side filters in registry.js approvals.filters (like runners' server filters), so admins can triage a large mixed queue.
- [Low|ux|notion-mapping] 
  ISSUE: The '사용자 ID' filter (registry.js:1333) is a free-text input that the backend matches as exact comma-separated UUIDs (User.id.in_(...) in app/notion_mapping/router.py). A human typing a name or email gets a silent 0-result, and it duplicates the existing email/name search box (searchable:true). The code comment admits it exists mainly to be pre-filled by the Users-screen deep link ('직접 입력도 가능하게 남겨 둔다'), making it a footgun as a hand-entered human filter.
  FIX: Hide user_ids from the visible filter bar (keep the ?user_ids= query-param plumbing for deep-links only), or add a placeholder/help on registry.js:1333 stating it requires exact user UUIDs, matching the searchPlaceholder honesty already applied at registry.js:1330.
- [Low|missing-feature|notion-mapping] 
  ISSUE: The screen's stated purpose is triaging connection states (미연결/충돌/확인됨), but there is no at-a-glance summary/count row — admins must eyeball or paginate to gauge how many users are unmapped or in conflict. No config.summary is set (registry.js:1312-1329), and unlike the jobs screen (registry.js:1448) there is no backend summary endpoint to feed one.
  FIX: Add a lightweight aggregate endpoint in app/notion_mapping/router.py (counts by status, respecting archived) and wire a config.summary stat row (like jobs), so the triage screen surfaces '미연결 N · 충돌 M · 확인됨 K' without paging.
- [Low|contract|backup] app/backups/router.py:44
  ISSUE: GET /api/admin/backups hard-caps the result at .limit(50) with no pagination, and the frontend config sets neither paginated nor capWarning. If the list ever reached 50 rows, older backups would silently disappear with no on-screen indication (the capWarning Callout pattern used elsewhere in DataScreen is absent). In practice this is nearly unreachable because apply_retention keeps only ~14 good + 1 latest-failed backups, so steady-state row count stays well under 50 — hence Low.
  FIX: Either add capWarning: 50 to the backup config so truncation (if it ever occurs) is surfaced, or document that the 50-limit is dead headroom given retention=14. No functional change strictly needed.
- [Low|missing-feature|job-titles] 
  ISSUE: job-titles has no onQuery deep-link and the shared org router (app/org/router.py) has no GET /{item_id} single-item endpoint (only list/POST/PATCH/DELETE). Consequently, when an audit or notification event references a job_title, the audit screen can only offer '관련 목록 열기' (open the whole list) — never '관련 항목 보기' to that specific row (registry.js:1582 requires OBJ_ID_PARAM[object_type], which omits job_title). Every other CRUD screen (runners/integrations/workflows/prompts/policies/templates/sche
  FIX: Add GET /api/admin/job-titles/{item_id} in _make_org_router (app/org/router.py), add onQuery: (p) => p.id ? { open: 'select', id: p.id } : null and selectKey to the job-titles config (registry.js:1281), and register job_title in OBJ_ID_PARAM (registry.js:111).
- [Low|dead-affordance|audit] 
  ISSUE: Audit rows with object_type='schedule_run' (produced by schedule.retry_run, app/schedules/router.py:597) get ZERO navigation: '관련 항목 보기' is hidden (no OBJ_ID_PARAM entry) and '관련 목록 열기' explicitly excludes schedule_run via `r.object_type !== "schedule_run"` (registry.js:1585). This contradicts the sibling notifications screen, which was deliberately changed to include schedule_run in '관련 목록 열기' with the rationale '목록 전체로라도 보내는 게 아무 동작도 없는 것보다는 낫다' (registry.js:1637-1643). The audit screen still 
  FIX: Drop the `r.object_type !== "schedule_run"` exclusion in the '관련 목록 열기' when-clause at registry.js:1585 so schedule_run rows at least route to '#/schedules', matching notifications.jsx.
- [Low|missing-feature|audit] 
  ISSUE: There is no export/download of audit records (CSV/JSON) — a standard compliance expectation for an audit log. The backend (app/audit/router.py) exposes only a paginated JSON list read; to hand an auditor a dataset you would have to page through the UI 100 rows at a time and hand-collect. Neither backend nor UI offers a bulk/date-range export.
  FIX: If in scope for the domain, add a GET /api/admin/audit/export (streaming CSV honoring the same filters) and a headerAction 'CSV 내보내기' in the audit config; otherwise document the omission in docs/KNOWN_LIMITATIONS.md so it is a decision, not an accident.
- [Low|missing-feature|notifications] app/notifications/router.py:37
  ISSUE: The '읽음 상태' filter (registry.js:1599) exposes only '안 읽음만'; there is no '읽음만' option and no filter by notification type. This is bounded by the backend, which only accepts unread_only:bool (app/notifications/router.py:37) with no type/read filter — so an admin cannot narrow to, e.g., only 'job_failed' or 'runner_unavailable' notifications, nor review already-read ones. Domain coverage is thin for a high-volume admin inbox.
  FIX: Backend: add optional type and read/unread ('all'|'read'|'unread') query params to GET /api/notifications; frontend: surface a type select (values from TYPE_KO) and a full read-state select. Until backend support exists, this is a documented limitation, not a UI regression.
- [Low|missing-feature|notifications] app/notifications/router.py:32
  ISSUE: Notifications accumulate unbounded: there is no delete/dismiss/archive control and the router (app/notifications/router.py) exposes no such endpoint, nor is any auto-cleanup surfaced. Heavy recipients (admins receive every approval_requested/approval_decided/job_failed/schedule_failed/runner_unavailable via notify_admins) will page through an ever-growing list with no way to clear it; '모두 읽음' only marks read, it does not remove rows.
  FIX: Add a backend DELETE /api/notifications/{id} (and optionally a bulk clear/retention policy), then expose a per-row '삭제' action and/or a header 'read 정리' control. The screen faithfully reflects the backend today, so this is a backend capability gap to close first.
