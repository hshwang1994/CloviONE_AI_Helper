# 페이지별 감사 발견사항 (자동 집계)


## Critical (22)

- **[chat (user)/test·test-gap]** The entire React chat UI (frontend/src/screens/Chat.jsx, 258 lines) has ZERO tests. frontend/package.json declares no test framework (no vitest/jest/@testing-library) and no `test` script; there is no *.test.jsx anywhere under frontend/. Every user-facing behavior the port introduced — send/optimistic-clear-restore, polling, retry button, choice buttons, ticket cards, attachments, rename/delete inline editing, copy, jump-to-latest — is completely uncovered. All existing chat tests are backend (Python) API/handler tests that never exercise the component.
  - fix: Add a JS test runner to frontend (vitest + @testing-library/react + jsdom, `"test": "vitest run"` in package.json). Create frontend/src/screens/Chat.test.jsx that mounts <Chat/> with a mocked api() and QueryClient and asserts the render/interaction behaviors enumerated in the findings below. Wire it into CI alongside pytest. 
- **[chat (user)/test·test-gap]** The only chat-UI test harness (tests/js/harness.js line 12-13) boots app/static/js/chat.js, which no longer exists — the vanilla JS was deleted in the React port (CHAT_JS_MISSING confirmed). tests/js/ now contains only harness.js + domstub.js; the chat_ui_tests.js file CLAUDE.md §1 tells every new session to run (`node tests/js/chat_ui_tests.js`) is gone. The 'UI tests' are dead: running them errors or no-ops, giving a false sense that the chat screen is covered.
  - fix: Either delete the orphaned harness.js/domstub.js and correct CLAUDE.md's test-count instructions, or repoint the harness at the React build. Add a smoke test asserting tests/js runnables actually exist and exit 0, so a deleted-target harness fails loudly instead of silently. 
- **[users/code·bug]** Creating a user with a blank password silently discards the generated temp password. The create form field explicitly tells the admin "비우면 임시 비밀번호가 자동 생성됩니다" (Users.jsx:55), and the backend returns it in res.temp_password / res.temp_password_notice (router.py:169-172, shown exactly once and never logged/audited per spec §11.1). But onSubmit (Users.jsx:98) only calls announce(res, ...), which inspects res.status/res.approval_pending and never reads res.temp_password. The password is lost forever, so the new account can never log in. The help text is a dead promise and the whole create-with-generated-password flow is broken end-to-end.
  - fix: In the create onSubmit (Users.jsx:98), after success check res.temp_password and, if present, surface it in a modal/callout (copy-to-clipboard) with res.temp_password_notice before closing — do not rely on a 3.5s toast for a one-time secret. Mirror this wherever reset-password is later wired. 
- **[users/code·dead-affordance]** The 복구 (unarchive) button in the detail drawer (Users.jsx:137) is unreachable. The list query never sends archived (Users.jsx:28-32) and the backend hides archived users by default (router.py:85-87), so once a user is archived (Users.jsx:138) they vanish from the list and cannot be selected again — there is no UI toggle to set archived=true. The archive action is therefore one-way through the UI and the unarchive control can never be opened.
  - fix: Add an "보관된 계정 보기" filter/toggle that passes archived=true to /api/admin/users (include it in the queryKey), so archived users can be listed, selected, and unarchived. Backend already supports the archived query param. 
- **[users/design·bug]** Auto-generated temp password is silently discarded on create, so newly created users can never log in. The create form promises 'password 비우면 임시 비밀번호가 자동 생성됩니다' (Users.jsx:55), the backend returns it once in the 201 body as res.temp_password (router.py:169-172, 'shown exactly once' per spec §11.1), and api() returns that full body — but the onSubmit at Users.jsx:98 passes the response only to announce(), which ignores temp_password and shows a generic '사용자를 추가했습니다.' toast (Users.jsx:66-70). The admin has no way to learn the generated password and the new account is unusable.
  - fix: In the create onSubmit (Users.jsx:98), when res.temp_password is present, show it in a persistent copyable modal (NOT a 3.5s auto-dismiss toast), with the res.temp_password_notice copy, since it is shown exactly once. Do not rely on announce() for this. 
- **[users/design·missing-feature]** The 'reset password / issue temp password' feature is entirely dropped from the UI. Backend POST /api/admin/users/{id}/reset-password exists and returns a one-time temp_password (router.py:339-363); USER_LIFECYCLE.md §5 lists it as a core admin function. There is no button anywhere in frontend/src (only the audit-label translation 'reset_password' survives in format.js:30). When a user forgets their password, an admin has no UI path to reset it.
  - fix: Add a '비밀번호 재설정' action to the UserDetail footer (Users.jsx:130-140) that POSTs to /reset-password and surfaces the returned temp_password in the same one-time copyable modal used for create. 
- **[settings/code·bug]** 4 of 8 settings are type "object" (ui_branding, password_policy, session_policy, allowed_email_domains) but SettingEditor only handles bool/int/text. For object rows the editor renders a plain text <input> pre-filled with String(setting.value) = "[object Object]" (or a comma-joined list for allowed_email_domains), and coerce() returns that raw string (Settings.jsx:45-49 — anything not "bool"/"int" falls through to `return val`). PUT then sends {value:"[object Object]"}. Backend object validators (_password_policy/_session_policy/_email_domains) reject non-dict with 422 "객체여야 합니다", so those 3 are uneditable. WORSE: ui_branding has validate=None (registry.py:92), so its PUT SUCCEEDS and overwrites the branding object {product_name:...} with the literal string "[object Object]", corrupting the login/chat product name for every user.
  - fix: Settings.jsx:36-76 — render object/json settings with a JSON textarea (the kit already has FormField type 'json' and FormModal that JSON.stringify/parse round-trip objects at kit.jsx:184-186,212,226). In coerce() add a branch for type 'object' that JSON.parse(val) with a try/catch surfacing a 'JSON 형식이 올바르지 않습니다' error, instead of returning the raw string. Pre-fill val with JSON.stringify(setting.value, null, 2) at Settings.jsx:39 rather than String(setting.value). 
- **[settings/design·bug]** Half of all settings are object-typed (ui_branding, password_policy, session_policy, allowed_email_domains — 4 of 8 registry keys have value_type "object") and are BOTH unreadable and unsavable. The value column renders String(r.value), so these rows literally show "[object Object]" (allowed_email_domains shows a bare comma list). The editor's coerce() (Settings.jsx:45-49) has no "object" branch, so it opens a plain text input pre-filled with "[object Object]" and returns the raw STRING on save. Even a user who types valid JSON gets rejected because coerce never JSON.parses — the backend validators (_password_policy etc.) require isinstance(value, dict) and return "객체여야 합니다.". Result: the admin can never change the product name, password policy, session timeouts, or allowed email domains through the UI.
  - fix: Adopt the shared FormModal/FormField 'json' field type (kit.jsx:184-226) which already does JSON.stringify(iv,null,2) for display and JSON.parse(val) with error handling on submit. In Settings.jsx add an object branch to coerce() that JSON.parses, render a textarea (type json) for spec.type==="object", and pre-fill the drawer/table with JSON.stringify(setting.value, null, 2) instead of String(). Settings.jsx:18 (table render), :39/:68-71 (editor input), :45-49 (coerce). (frontend/src/screens/Settings.jsx:18)
- **[settings/test·test-gap]** No test exercises SettingEditor's coerce() for object-type settings. coerce() (Settings.jsx:45-49) handles only bool/int and returns the raw string for everything else; the editor renders a plain text input seeded with String(setting.value) (='[object Object]' for a dict). So editing ui_branding/password_policy/session_policy/allowed_email_domains (4 of 8 registry keys) sends {value:'[object Object]'} or an arbitrary string and the backend validator rejects it 422 — object editing is broken through the UI. Backend tests never catch this because they POST proper dicts as JSON, bypassing the component.
  - fix: Add a frontend unit test (e.g. frontend/src/screens/Settings.test.jsx) that renders SettingEditor with an object-type setting (type='object', value={min_length:12}), simulates edit+save, and asserts api() was called with a parsed object body, not a string. It should currently FAIL, documenting that object settings cannot be saved from the UI. Assert the initial input is not literally '[object Object]'. 
- **[workflows/feature·missing-feature]** The entire version-history + rollback subsystem is unreachable from the UI. Backend supports GET /api/admin/workflows/{id}/versions and POST /api/admin/workflows/{id}/rollback, and every create/update/enable/disable/rollback writes a config snapshot and increments config_version. The registry 'workflows' config exposes NO versions viewer and NO rollback action, DataScreen.jsx has no mechanism for either, and grep confirms zero frontend references to versions/rollback for workflows. This is a config-versioned registry (spec §16/§23.5) whose core value — audit/rollback of workflow config — is completely dead. Even config_version (returned by workflow_view) is not shown as a column, unlike the integrations screen which shows col('config_version','버전').
  - fix: In registry.js 'workflows': add config_version to columns (mirror integrations line 46), and add a version-history affordance (e.g. a '버전' action that fetches /{id}/versions and lets the user rollback via POST /{id}/rollback with {version}). DataScreen currently has no generic 'list sub-resource + act' pattern, so this likely needs a small dedicated component/drawer or a DataScreen extension. registry.js:84-114. 
- **[policies/feature·missing-feature]** The policy's actual content (the JSON business rules) is never viewable in the UI. The detail drawer (DataScreen.jsx:95-100) renders only config.columns — for policies that is name/version/status/created_at (registry.js:141) — and there is no detail-fields mechanism. `content` is returned by _policy_view (router.py:99) but is not a column. Editing is the ONLY place content surfaces, and edit is gated to draft (editWhen: r.status==='draft', registry.js:146). So for any test/review/published/archived policy you cannot read its rules at all. The screen's stated purpose ('업무 규칙(정책)을 관리합니다') is unmet: you can create/publish a policy but never inspect what a live policy actually says.
  - fix: Add a read-only content viewer to the detail drawer that pretty-prints the JSON (e.g. <pre>{JSON.stringify(r.content, null, 2)}</pre>) independent of edit permission. Either add a `detailFields`/`detailRender` hook to DataScreen.jsx:95-100 or add a content column with a custom render in registry.js:141. File: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\registry.js:141 and DataScreen.jsx:95-100. 
- **[prompts/feature·missing-feature]** The prompt CONTENT (the actual AI instruction — the entire point of the screen, per help text 'AI에게 주는 지시문을 버전으로 관리합니다') is never viewable in the detail drawer. DataScreen's drawer (DataScreen.jsx:95-100) renders ONLY config.columns, and prompts' columns are name/version/status/created_at (registry.js:119). 'content' is not a column and there is no detail-field config, so for any test/review/published/archived version you can NEVER read the prompt text. It is only visible by opening the edit form, which is gated to draft-only (editWhen: r.status === 'draft', registry.js:126). A prompt-management console where you cannot read the published prompt.
  - fix: Add a detail-only field renderer to DataScreen (e.g. config.detailFields) and expose 'content' (plus purpose/runner_id/published_at) in the prompts drawer; or add a read-only content view path. registry.js:115-136 / DataScreen.jsx:95-100. 
- **[schedules/feature·missing-feature]** Run history and run outcomes are completely unreachable from the UI. The backend has GET /api/admin/schedules/{id}/runs plus an entire ScheduleRun model tracking status (queued/running/succeeded/failed/skipped), scheduled_at, started_at, finished_at, error_message, retry_count and response_summary. The schedules config (registry.js 180-217) surfaces none of it. The list shows only a 'last_run_at' timestamp — never whether any run succeeded or failed or why. The screen's stated purpose ('정해진 시간에 자동 실행을 예약') is only half-served: you can schedule but can never observe whether the scheduled job actually ran or worked.
  - fix: Add a run-history surface for schedules. Since config-driven DataScreen has no nested sub-list, either add a per-row action/drawer that fetches /api/admin/schedules/{id}/runs and renders status+error_message+timestamps, or add a dedicated 'schedule-runs' screen. Add a Korean map for run status enums (queued/running/succeeded/failed/skipped) alongside JOB_TYPE in registry.js. (frontend/src/screens/registry.js:180)
- **[approvals/feature·missing-feature]** The approver approves BLIND. The confirm dialog says '승인하면 실제로 적용됩니다' (approving applies it for real), yet the screen never shows WHAT is being applied. approval_view returns request_payload (the exact change — e.g. the new role for user.role_change, the new runner/integration config, the schedule definition), object_id, requested_by, approver_id, decision_comment, decided_at, and expires_at. The DataScreen detail drawer only iterates config.columns (request_type, object_type, status, created_at), so NONE of these are ever rendered — even though every list row already contains sel.request_payload client-side. This defeats the entire purpose of an approval screen: you cannot make an informed approve/reject decision.
  - fix: registry.js approvals: render the payload/context in the detail drawer. Either add columns for the fields that matter (e.g. a render column that JSON-pretty-prints request_payload, plus object_id / requested_by / expires_at / decision_comment / decided_at), or extend DataScreen's drawer to show a config.detailFields list beyond the table columns. At minimum surface request_payload before the approve/reject buttons (frontend/src/screens/registry.js:232-242, frontend/src/screens/DataScreen.jsx:95-100). 
- **[documents/feature·missing-feature]** The screen's entire reason to exist — generate a Notion doc, REVIEW the preview, then publish — is unreachable. Backend produces preview {title, body, source_row_count, notion_links} (service.py:191-206) and exposes it both in the list and via a dedicated GET /api/admin/documents/{id} whose only purpose is 'preview 본문 포함 상세' (router.py:92-94; DOCUMENT_AUTOMATION.md:132). But registry documents.columns (registry.js:230) is only [id, mode, status, published_ref, created_at], the detail drawer renders only those 5 columns (DataScreen.jsx:95-100), and the GET /{id} endpoint is never called anywhere in the frontend. A user who picks '미리보기만'(preview_only) gets a preview_ready row whose generated document title/body they can NEVER read.
  - fix: Add a custom document detail view (or extend the registry to render extra fields) that calls GET /api/admin/documents/{id} and displays preview.title / preview.body / source_row_count / notion_links. registry.js:230 + a new DocumentDetail component. 
- **[documents/feature·empty-state]** quality_failed rows give the user zero explanation. When quality gates fail the backend stores the exact reasons in quality_problems_json (document_generate.py:96-97) — e.g. '본문이 최소 길이(30자) 미만', '민감정보 감지: 주민등록번호 패턴' — and generation_view returns them as `quality_problems`. DOCUMENT_AUTOMATION.md:131 says the list should show '품질 문제'. The screen shows only a '품질 미달' status badge and never renders quality_problems in any column or detail, so the user cannot tell what failed or how to fix it.
  - fix: Surface `quality_problems` (a list) in the detail view, rendered as a bullet list, when status is quality_failed. Extend the documents detail rendering in registry.js/DataScreen. 
- **[jobs/feature·dead-affordance]** The backend exposes POST /api/admin/jobs/{id}/retry (spec §10.2 operator retry, audit-logged as job.retry) to requeue a FAILED job, but the 'jobs' registry config declares no `actions`. The detail drawer renders zero action buttons (DataScreen.jsx:92 reads config.actions||[]). The screen's own help text says it exists to view failures ('실패를 봅니다'), yet an operator who finds a failed chat_message / document_generate / notion_mapping_sync / schedule_run job has NO way to retry it from the console. The core operator capability is a completely dead backend endpoint.
  - fix: In registry.js jobs config (line ~279-284) add an actions array with a retry action gated on status: { label: '재시도', variant: 'primary', when: (r) => r.status === 'failed', path: (r) => '/api/admin/jobs/' + r.id + '/retry', confirm: '이 작업을 다시 시도할까요?' }. DataScreen.runAction already handles POST + toast + refresh. 
- **[jobs/feature·dead-affordance]** POST /api/admin/jobs/{id}/cancel is unreachable from the UI (no `actions` in jobs config). Besides cancelling a QUEUED job, this endpoint calls _terminalize_linked_record to unwedge the linked ScheduleRun / DocumentGeneration / Message (router.py:129,136-176) — otherwise a bad queued job leaves its schedule/document stuck forever. The operator has no way to stop a queued job or invoke that safety cleanup.
  - fix: Add to jobs.actions: { label: '취소', variant: 'danger', when: (r) => r.status === 'queued', path: (r) => '/api/admin/jobs/' + r.id + '/cancel', confirm: '대기 중인 이 작업을 취소할까요?' } in registry.js. 
- **[notion-mapping/feature·dead-affordance]** The '충돌 해결' (resolve-conflict) action asks the admin to type the correct notion_user_id, but the candidate Notion users are never rendered anywhere. The backend returns them (mapping_view 'candidates' = list of {notion_user_id, notion_email} from candidates_json) in the list response, and the detail drawer only iterates config.columns (DataScreen.jsx L95-100), so 'candidates' is invisible. resolve-conflict rejects any id not in the candidate list ('후보 목록에 없는 Notion user id'), so blind guessing always fails. The documented conflict-resolution feature is unusable from the UI.
  - fix: registry.js 'notion-mapping' (~L273): render the candidates list in the detail drawer (or a column), ideally as clickable rows that prefill the resolve-conflict form's notion_user_id. Requires DataScreen detail to render non-column fields, or add a custom candidates renderer. Data is already in the list payload. 
- **[audit/feature·missing-feature]** The audit log is append-only and grows unbounded (every user/config/integration/schedule change is logged), but the screen has NO pagination. DataScreen.jsx calls api(config.endpoint) with no page param, reads only response.items, and never renders next/prev controls. The backend returns total/page/page_size and supports page (default 20, max 100), all ignored. Result: only the newest ~20 audit entries are EVER visible in the UI; the entire history older than page 1 is unreachable. For an audit/compliance tool this defeats its stated purpose.
  - fix: Add page state to DataScreen (frontend/src/screens/DataScreen.jsx:20-26 queryKey/queryFn) and render pager controls (prev/next + 'N of TOTAL') from query.data.total/page/page_size; send ?page=&page_size=. At minimum add pagination for the unbounded log screens (audit, jobs). Backend already supplies PageParams (app/core/pagination.py) and total (app/audit/router.py:83). 
- **[audit/feature·missing-feature]** The before/after change diff — the actual record of WHAT changed, masked and stored by the backend — is returned per row (app/audit/router.py:74-75) but never displayed anywhere. The audit columns (registry.js:289) are only created_at/action/object_type/result/user_id, and the detail drawer (DataScreen.jsx:95-100) renders ONLY config.columns, not all response fields — despite the code comment claiming '상세 모달(전체 필드)'. Also unshown: object_id (which specific object), client_ip, request_id — the exact forensic fields an auditor needs. The help text promises '누가 무엇을 언제 바꿨는지' but the '무엇을 바꿨는지' (before to after) is invisible; you see only a verb and an object type.
  - fix: Add a per-row detail view that shows before/after (rendered as a JSON/field diff), object_id, client_ip, and request_id. Either add these to the audit config with custom renderers, or make the DataScreen detail drawer render all backend fields for the row (not just config.columns) — registry.js:285-290 / DataScreen.jsx:95-100. 
- **[shared-infra/code·missing-feature]** The detail drawer renders ONLY config.columns (DataScreen.jsx:95-100), yet the header comment (line 8) promises "행 클릭 → 상세 모달(전체 필드)". On the Approvals screen the detail shows just request_type/object_type/status/created_at — the approver clicks 승인/거절 without ever seeing the request payload, target object id, requested_by, or the diff. Since approvals gate sensitive secret-binding/base_url/role changes (per CLAUDE.md), this defeats the security control: you approve blind.
  - fix: Add an optional `detailColumns`/`detailFields` to the config and render the full record (especially the approval payload) in the Drawer instead of reusing table columns. DataScreen.jsx:95; registry approvals entry. 

## High (82)

- **[change-password/test·test-gap]** change_password.js:13-30 contains a carefully-documented fix: a 5xx (or any non-401, non-ok) response from /api/me must show an error in-place and NOT redirect to /login, because redirecting a must_change user on a 500 creates an infinite loop (/login → / → /change-password → 500 → /login). This exact past bug has ZERO test pinning it. Only a 401 should redirect. A future 'simplification' to `if(!response.ok) location='/login'` would silently reintroduce the loop.
  - fix: There is no JS harness for change_password.js at all (tests/js/harness.js boots only chat.js). Add a change_password_ui_test.js that boots change_password.js with an injected fake /api/me returning status 500, and asserts (a) window.location.href is NOT changed to '/login', (b) errorBox.textContent contains the '오류 500' message. Add a sibling case: /api/me returning 401 DOES set window.location.href='/login'. 
- **[change-password/test·test-gap]** The session-rotation CSRF contract of POST /change-password (router.py:272-284) is not asserted at the token level. The endpoint returns {ok, csrf_token} for a freshly rotated session per spec §11.3, but no test verifies the returned csrf_token is present, differs from the pre-change token, works for a subsequent CSRF-protected mutation, and that the OLD csrf_token is now rejected. The happy-path test re-derives auth via /api/me and never touches the returned token.
  - fix: In test_auth_change_password.py add test_change_password_returns_working_rotated_csrf: capture csrf before change; POST change-password; assert response JSON has csrf_token != old; assert POST /logout with the NEW token → 200; and in a separate assertion that the OLD csrf token (from the pre-rotation session) is rejected with 403 csrf_failed. 
- **[change-password/test·test-gap]** The entire submit flow of change_password.js (lines 44-84) is untested: success (ok && data.ok) → window.location.href='/'; error with data.error.details array → message = details.join(' ') so policy violations are shown; error with only data.error.message → shown; fetch throwing → '서버에 연결할 수 없습니다.'; and the submit button being disabled during the request and re-enabled in finally (double-submit guard). No JS harness exercises change_password.js.
  - fix: In the new change_password_ui_test.js, boot with a fake /api/me returning a valid csrf_token, fill the form, dispatch submit, and assert per route: 200 {ok:true} → location='/'; 422 {error:{details:['a','b']}} → errorBox shows 'a b' and location unchanged; a rejecting fetch → errorBox shows the connection message; and that submitButton.disabled is true mid-flight and false after settle. 
- **[dashboard/test·test-gap]** The diagnostics-bundle permission boundary has NO negative test. /api/admin/diagnostics/bundle requires require_roles('admin','system_admin'), while the dashboard shares READ_ROLES that also include 'operator' and 'auditor'. Every existing diagnostics test (test_diagnostic_bundle_masks_and_excludes_secrets, test_no_endpoint_leaks_secret_plaintext) calls it as admin/system_admin and asserts 200. Nothing asserts that operator or auditor — who CAN read the dashboard — are 403 on the bundle. A regression widening the bundle's roles to READ_ROLES would ship undetected.
  - fix: Add tests/security/test_dashboard_backups.py::test_operator_and_auditor_cannot_read_diagnostics_bundle: login_as('operator') then login_as('auditor'), assert client.get('/api/admin/diagnostics/bundle').status_code == 403 for each; and a positive test_admin_can_read_diagnostics_bundle asserting 200. Mirror the existing test_auditor_can_read_dashboard / test_user_cannot_read_dashboard pair. 
- **[dashboard/test·test-gap]** jobs_24h numeric aggregation is entirely unasserted. test_dashboard_aggregates only checks `'jobs_24h' in body`. No test seeds Jobs and verifies total, succeeded, success_rate_pct, avg_processing_seconds, queued, or failed_open. The core value of the dashboard (build_dashboard, service.py:101-128) is uncovered — wrong counts, a broken success-rate formula, or a mis-summed average would all pass.
  - fix: Add tests/integration/test_dashboard_backups.py::test_dashboard_jobs_24h_metrics: seed jobs with STATUS_SUCCEEDED/FAILED/QUEUED and known started_at/finished_at via fake_clock, then assert jobs_24h.total, succeeded, success_rate_pct (e.g. 2 of 3 -> 66.7), avg_processing_seconds (exact mean of durations), queued, and failed_open equal expected integers. 
- **[dashboard/test·test-gap]** The 24h-window boundary asymmetry is untested. total_24h and succeeded_24h filter on Job.created_at >= now-24h, but failed_open and queued count ALL jobs regardless of age (service.py:109-114). A failed job older than 24h is excluded from total yet still counts toward failed_open (and drives the frontend danger alert). This deliberate asymmetry has no test and is easy to accidentally 'fix' into a window.
  - fix: Add test_dashboard_window_excludes_old_from_totals_but_not_failed_open: seed a failed job with created_at older than 24h (fake_clock.advance) plus a recent job; assert jobs_24h.total counts only the recent job while jobs_24h.failed_open counts the old failed job too. 
- **[dashboard/test·test-gap]** recent_critical_audit has zero coverage. build_dashboard filters AuditLog to CRITICAL_ACTIONS (user.disable, user.role_change, runner.rollback, setting.rollback, approval.approve, backup.restore), orders by created_at desc, and caps at 5 (service.py:130-139); the frontend renders it via actionKo/objKo. No test seeds audit rows to confirm non-critical actions are excluded, ordering is newest-first, or the limit-5 cap holds.
  - fix: Add test_dashboard_recent_critical_audit_filters_orders_and_caps: insert a non-critical AuditLog plus 6 critical ones at increasing timestamps; assert recent_critical_audit has length 5, excludes the non-critical action, and is ordered newest-first (created_at descending). 
- **[change-password/design·empty-state]** Password complexity rules are shown ONLY to voluntary users (the {% else %} branch, change_password.html:13), and hidden from first-login users (must_change=True, line 11) — who are the primary FORCED audience for this screen (theme.js:7 and chat/router.py:40 both confirm first-login is the mandatory path here). A brand-new user with a temp password is told 'change it' but given zero guidance on the 12-char / 3-class requirement, so they must type a password, fail server validation, and read the error to discover the rules. The guidance is served to exactly the wrong audience.
  - fix: Show the complexity requirements in BOTH branches. In change_password.html, keep the personalized first-login line but append the rules (or add a persistent requirements list under the new-password field) so must_change users see '대문자/소문자/숫자/특수문자 중 3종류 이상, 12자 이상' before their first attempt. 
- **[change-password/design·contract]** The subtitle hardcodes '3종류 이상을 섞어 12자 이상' (change_password.html:13), but the backend enforces a server-configurable policy: auth/router.py:258-263 reads password_policy from settings_cache (min_length default 12, min_classes default 3). If an admin raises the policy (e.g. min_length 14), the on-screen instructions silently become wrong — the user follows '12자' guidance and gets rejected with no explanation of the mismatch.
  - fix: Render the numbers from the effective policy instead of hardcoding: pass min_length/min_classes into the template context from settings_cache.current_value('password_policy') (as auth/router.py already does for validation) and interpolate them into the subtitle / requirements list. 
- **[change-password/code·empty-state]** The must_change (first-login) branch is the PRIMARY reason this page exists, yet its subtitle (change_password.html:11) gives zero password-policy guidance ('첫 로그인입니다... 새 비밀번호로 변경하세요'). The policy hint (12자/3종) only renders in the else branch (line 13). A first-login user types a password blind, submits, and only learns the requirements from a 422 error after a slow Argon2 verify+hash round-trip — trial-and-error on the app's main onboarding step.
  - fix: Show the password-policy hint in BOTH branches of change_password.html (move the 'new password must be...' line outside the {% if must_change %} so it always appears above the form). 
- **[dashboard/design·bug]** The '성공률' (success rate) StatCard is hardcoded to kind="ok" (green/'정상' tone) regardless of the actual value (Dashboard.jsx:82). A 12% success rate is rendered in the same reassuring green as 100%, actively misleading an operator scanning for problems — which is this dashboard's stated purpose ('운영자가 지금 확인해야 할 것').
  - fix: Make the tone value-driven: kind = success_rate_pct >= 95 ? 'ok' : success_rate_pct >= 80 ? 'warn' : 'danger' (and neutral/'—' when null). Dashboard.jsx:82. 
- **[chat (user)/test·test-gap]** Retry error/lifecycle paths at the API are untested. app/chat/service.py:239 raises ConflictError('실패한 요청만 다시 시도할 수 있습니다') when a message is not role=user or not processing_status=failed, and service.py:236 raises NotFoundError for a missing message. Only the happy path (test_chat_handler.py:154) and IDOR/403 (test_chat_security.py:62) are covered. Retrying a still-pending or already-done message, or a nonexistent id, has no test — a regression could let users re-enqueue non-failed messages or 500 on a bad id.
  - fix: In tests/integration/test_chat_api.py add test_retry_non_failed_message_returns_409 (POST /api/messages/{id}/retry on a pending msg → 409, error.code='conflict', no new Job enqueued) and test_retry_unknown_message_returns_404 (random id → 404). 
- **[chat (user)/test·test-gap]** The stale-guidance cleanup on retry is unpinned. app/chat/service.py:246-253 deletes prior 'a-{message_id}-fail-%' error_notice messages on retry — a fix the code itself flags as a confirmed round16 regression ('성공 답변과 함께 …실패 안내…가 남아 사실과 반대되는 상태'). grep of tests/ finds no test touching the 'fail-' notice id or error_notice on the chat path. If this deletion breaks, a successful retry will show a contradictory '연결에 문제가 있어 완료하지 못했습니다' notice next to the real answer with no test catching it.
  - fix: Add tests/integration/test_chat_handler.py::test_retry_removes_stale_failure_guidance: post→fail (produces the a-{id}-fail-{job} notice), retry→succeed, then assert the conversation contains the success reply and NO message whose structured.error_notice is true / whose id matches a-{id}-fail-%. 
- **[chat (user)/test·test-gap]** Cross-conversation client_message_id conflict is untested. app/chat/service.py:168 raises ConflictError('이미 다른 대화에서 사용된 메시지 ID') when the same client_message_id is reused under a different conversation. Only the same-conversation idempotent duplicate is tested (test_chat_api.py:81 asserts 202 + no dup). The 409 branch — a genuine data-integrity guard — has no test.
  - fix: Add tests/integration/test_chat_api.py::test_reused_client_message_id_in_other_conversation_conflicts: create conv A and B, POST the same client_message_id to A (202) then to B, assert 409 with error.code='conflict' and that no message/job was created under B. 
- **[chat (user)/design·missing-feature]** On mobile (max-width:760px) the entire conversation sidebar is hidden with `.chat-side { display:none }` (screens.css:105) and there is NO replacement — no hamburger, drawer, or in-column control. The '+ 새 대화' button, conversation switcher, rename (✎) and delete (🗑) all live inside that hidden sidebar. On phones a user is locked into the auto-selected most-recent conversation (Chat.jsx:181-183): they cannot start a second conversation, switch, rename, or delete. A brand-new user can create exactly ONE conversation (by typing on the welcome screen) and is then permanently stuck in it. Core conversation management is completely inaccessible on the primary user surface at mobile widths.
  - fix: Add a mobile drawer/hamburger toggle to reveal chat-side (slide-over over chat-main-col), OR surface a '+ 새 대화' and a conversation menu inside chat-main-col at <=760px. screens.css:105 / Chat.jsx:214-225. 
- **[chat (user)/design·dead-affordance]** The 'jump to latest' button `.jump-latest` ("새 메시지 ↓", Chat.jsx:235) has NO CSS anywhere (confirmed: no `.jump-latest` rule in screens.css/global.css/kit.css/tokens.css). It renders as a bare default browser button in normal document flow between the scroll body and the composer, pushing the composer down, instead of floating over the messages as a pill. chat-main-col is `position:relative` (screens.css:66), which shows the intended absolute-positioned overlay was never written. It appears whenever the user scrolls up during an active reply — a visibly broken/unstyled control on an otherwise polished chat.
  - fix: Add `.jump-latest { position:absolute; bottom:88px; right:24px; ... }` styling (pill, shadow, brand color) in screens.css, matching the design of chat-choice/chat-attach chips. screens.css (missing rule) / Chat.jsx:235. 
- **[users/code·missing-feature]** Admin reset-password is entirely unexposed. Backend POST /api/admin/users/{id}/reset-password (router.py:339-363) generates/returns a one-time temp password and sets must_change_password, but Users.jsx has no button anywhere. Combined with the create-temp-password bug, once a user has no working password there is NO way in the UI to give them one — permanent lockout.
  - fix: Add a "비밀번호 초기화" action in UserDetail footer (Users.jsx:130-140) calling POST .../reset-password, and display the returned temp_password once (same one-time-secret modal as the create fix). 
- **[users/code·dead-affordance]** The detail drawer shows a 잠금 (locked) badge (Users.jsx:150) but provides no unlock control. Backend POST /api/admin/users/{id}/unlock (router.py:366-373) exists. An admin can see a user is locked out (too many failed logins) but cannot remedy it from the UI — the status is read-only with no corresponding action.
  - fix: Add an "잠금 해제" button in the UserDetail footer, shown when user.locked is true, calling run("/api/admin/users/"+id+"/unlock", ...) like the other lifecycle actions. 
- **[users/code·dead-affordance]** The drawer shows a "Notion 연결" status badge (Users.jsx:149) but no way to verify/refresh it. Backend POST /api/admin/users/{id}/notion-mapping/verify (router.py:417-431) re-checks and updates the mapping status. Users stuck at 미연결/오류 have no recovery affordance on this screen — the badge implies actionable state with no control.
  - fix: Add a "Notion 연결 확인" action calling POST .../notion-mapping/verify, then refresh the row so the badge updates (invalidate ["users"]). 
- **[users/design·dead-affordance]** The detail drawer shows a '잠금' (locked) badge (Users.jsx:150) but offers no way to unlock. Backend POST /api/admin/users/{id}/unlock exists (router.py:366-373) and USER_LIFECYCLE.md §6 documents unlock as the manual recovery for the 5-failed-login/15-min lockout. A locked-out user is visibly flagged but cannot be unlocked from the UI — the admin must wait out the timer or use the CLI. Showing a status with no corresponding action is a dead affordance.
  - fix: In UserDetail footer (Users.jsx:130-140), when user.locked is true, render an '잠금 해제' button that runs POST /{id}/unlock (reuse the existing run() helper). 
- **[users/design·missing-feature]** Session visibility and revocation are dropped. Backend GET /api/admin/users/{id}/sessions lists active sessions and POST /{id}/revoke-sessions kills them (router.py:376-414), and GET /{id} returns active_session_count (router.py:181-191). The React detail never calls GET /{id} (it just reuses the list row `sel`, Users.jsx:93,111-118), so the session count is never shown and there is no '세션 전체 해제' control. A security-relevant admin action (force-logout a compromised/departing user) is unavailable.
  - fix: Have UserDetail fetch GET /api/admin/users/{id} to show active_session_count, and add a '세션 전체 해제' danger action posting to /{id}/revoke-sessions with a confirm; optionally list sessions from /{id}/sessions. 
- **[users/design·dead-affordance]** Archived users are permanently unreachable, making the unarchive/복구 flow dead code. The list defaults to archived=false which hides archived accounts (router.py:77-87), and the toolbar (Users.jsx:76-79) exposes only free-text search — no archived toggle. So once archived, a user vanishes from the list and its detail can never be opened, meaning the '복구' (unarchive) button (Users.jsx:136-138) and the '보관 시각' row (Users.jsx:152) can never be reached or populated for any visible user. Archive is effectively one-way in the UI.
  - fix: Add an '보관된 계정 보기' toggle/tab to the toolbar that sets archived=true on the query (backend already supports it), so archived users are listable and the existing '복구' action becomes reachable. 
- **[settings/code·missing-feature]** Backend fully supports settings version history and rollback — GET /api/admin/settings/{key}/versions and POST /api/admin/settings/{key}/rollback (router.py:66-95), and apply_setting snapshots config_versions on every write (service.py:114-118). The service docstring even states a bad value 'is reversible via the config_versions rollback endpoint.' The Settings page exposes NONE of this: no history view, no rollback control. Combined with the missing dry-run, an admin who saves a bad value has no in-UI way to see prior values or revert.
  - fix: Settings.jsx SettingEditor — add a '변경 이력' section that queries ["settings",key,"versions"] via GET /api/admin/settings/{key}/versions and lists {version, snapshot.value, created_at}, each with a '되돌리기' button calling POST /api/admin/settings/{key}/rollback {version}, then invalidate ["settings"]. Guard by write role like the rest of the drawer. 
- **[settings/design·missing-feature]** Version history and rollback are completely absent from the UI even though the backend fully implements them: GET /api/admin/settings/{key}/versions and POST /api/admin/settings/{key}/rollback (router.py:66-95). The settings service comment explicitly presents rollback as the safety net ('any change is reversible via the config_versions rollback endpoint'), yet there is no way to view past versions or roll back a bad change anywhere on the page. The only advertised undo mechanism for settings is unreachable — the endpoints are orphaned.
  - fix: Add a '변경 이력' section/tab inside the SettingEditor drawer that fetches /{key}/versions and lists version + created_at + snapshot value, each with a '이 버전으로 되돌리기' button POSTing to /{key}/rollback (with a confirm). Settings.jsx:36-76 (SettingEditor). (frontend/src/screens/Settings.jsx:36)
- **[settings/test·test-gap]** Settings.jsx (the React component) has zero test coverage and there is no frontend test runner (frontend/package.json scripts are only dev/build/preview; no *.test.* files; tests/js/ targets the old vanilla admin). None of the list-render, loading Skeleton, ErrorState+retry, EmptyState, row-click-opens-Drawer, or save-success (invalidateQueries + close) behaviors are verified.
  - fix: Introduce a frontend test harness (vitest + @testing-library/react) and a Settings.test.jsx covering: (a) list renders rows from q.data.settings map with restart_required Badge; (b) q.isLoading shows Skeleton; (c) q.isError shows ErrorState and onRetry calls refetch; (d) rows.length===0 shows EmptyState; (e) clicking a row opens the Drawer with that key; (f) successful save triggers qc.invalidateQueries(['settings']) and closes the drawer. 
- **[settings/test·test-gap]** The audit side-effect of settings lifecycle is untested. update_setting (router.py:59-63) and rollback (router.py:91-95) each write an audit record (action 'setting.update'/'setting.rollback', object_type app_setting, object_id=key, before/after values, rolled_back_to on rollback). No test reads /api/admin/audit or the audit table to confirm the record exists with correct before/after — a §7 checklist requirement and a claimed masking invariant.
  - fix: Add test_update_writes_audit_with_before_after and test_rollback_writes_audit to test_settings_api.py: after a PUT, query /api/admin/audit (as auditor/admin) and assert an entry with action='setting.update', object_id=key, before.value=old, after.value=new; after a rollback assert action='setting.rollback' and after.rolled_back_to=version. 
- **[settings/test·test-gap]** No CSRF negative test on settings write endpoints. The router mounts require_csrf at router level (router.py:26) for PUT /{key}, POST /{key}/dry-run, POST /{key}/rollback, but every test in test_settings_api.py supplies a valid X-CSRF-Token. The generic test_csrf.py only hits /logout and /change-password, so a regression that drops CSRF from these state-changing settings endpoints would go unnoticed.
  - fix: Add test_settings_write_requires_csrf: while logged in as admin, call PUT /api/admin/settings/notification_retention_days (and the dry-run and rollback endpoints) with no/forged X-CSRF-Token and assert 403 with error.code=='csrf_failed'. 
- **[settings/test·test-gap]** Rollback error paths are untested. rollback_setting -> get_version (service.py:149-154) will fail for a nonexistent version or a key that has never been changed (no config_versions rows), and PUT/POST bodies missing 'version' should be 422. test_update_and_rollback only exercises the happy path (rollback to version 1 after two changes).
  - fix: Add tests to test_settings_api.py: (a) rollback to a version that doesn't exist -> assert 404/422 with a clean error envelope (not a 500); (b) rollback a key with no version history -> assert graceful error; (c) POST rollback with json={} (missing version) -> 422. 
- **[diagnostics+maintenance/design·design]** The Diagnostics page's sole output is a raw JSON.stringify dump inside <pre class="ops-pre">. It leaks English keys (generated_at, success_rate_pct, avg_processing_seconds, recent_job_errors, integration_errors, cert_days_remaining, last_backup_at/status, components), raw enum values (up/down/queued/failed), and ISO timestamps (2026-07-19T...) straight to a Korean-facing admin console that everywhere else translates these via the Badge/statusText kit. The empty-state copy oversells it as a '서비스·버전·설정 요약' (a summary), but the result is an unformatted developer/debug dump with zero visual hierarchy — reads as a bare template, not the polished summary promised.
  - fix: Render the bundle as a structured Korean summary: group into cards/sections (구성요소 상태 / 최근 24시간 작업 / 디스크·메모리 / 최근 오류) reusing StatCard + Badge(statusText) so enums and booleans localize, and format generated_at with a Korean date/time formatter. Keep the raw JSON behind an optional '원본 보기' toggle for export only, not as the primary view (Ops.jsx:10,21). (frontend/src/screens/Ops.jsx:21)
- **[users/test·test-gap]** The reset-password endpoint's admin-provided-password branch is completely untested. tests/integration/test_admin_users.py::test_reset_password_flow only exercises the generated-temp-password path (router.py:360 `if provided is None`). The branch where an admin supplies an explicit password (router.py:346-363, service.py:325-334) — which must OMIT temp_password from the response, still force must_change_password=True, kill old sessions, and let the user log in with exactly that chosen password — has no coverage. A regression could silently ignore the provided password or leak a temp_password and no test would catch it.
  - fix: Add tests/integration/test_admin_users.py::test_reset_password_with_admin_chosen_password asserting 200, `'temp_password' not in body`, must_change_password True, the CHOSEN password logs in (200) while any prior password is dead (401); and test_reset_password_weak_rejected asserting a weak password → 422 and password_hash unchanged (service.py:326-333). 
- **[users/test·test-gap]** No test verifies the frontend's 202 approval-pending handling. Users.jsx:66-70 `announce()` branches on `res.status === 'approval_pending'` to show an info toast instead of a success toast for create/edit (Users.jsx:98,102). The backend returns 202 {status:'approval_pending'} when a plain admin promotes a user to role=admin (router.py:250-258). With no frontend test harness at all (frontend/package.json has no vitest/jest/RTL), nothing guards against the operator seeing a false '사용자 정보를 저장했습니다' success while the role change is only pending — the exact silent-success regression class the repo already burned on (round16).
  - fix: Stand up a Vitest + React Testing Library harness under frontend/ and add Users.test.jsx: mock api() to return {status:'approval_pending'} for a role→admin PATCH, assert the info toast '승인 요청이 접수되었습니다...' fires and the success toast does NOT, and that the row is not shown as already-admin. 
- **[users/test·test-gap]** No test covers surfacing the one-time temp password on user creation, and the current UI appears to drop it. POST /api/admin/users returns body.temp_password + temp_password_notice exactly once (router.py:169-173), but Users.jsx:67-70,98 only passes the response to announce(), which ignores temp_password and shows a generic success toast — so the generated password is never shown to the operator and the new user is effectively locked out. There is no frontend test asserting the temp password is displayed once.
  - fix: Add a frontend test (Users.test.jsx) where the create mutation resolves with {user, temp_password, temp_password_notice} and assert the UI presents the temp password to the operator exactly once (modal/toast). The test will currently fail, pinning the dropped one-time-password display. 
- **[notification-bell/test·test-gap]** CSRF protection on the two state-changing notification endpoints (POST /api/notifications/read-all and POST /api/notifications/{id}/read) is a per-route dependency (dependencies=[Depends(require_csrf)]), NOT global middleware (confirmed: CSRF is only a route dep in app/core/deps.py:113). tests/security/test_csrf.py only exercises /logout and /change-password. Deleting the require_csrf dependency from app/notifications/router.py would leave every test green while opening both endpoints to CSRF.
  - fix: Add tests in tests/integration/test_notifications.py (or test_csrf.py): test_read_all_without_csrf_rejected and test_read_one_without_csrf_rejected — POST both endpoints while logged in but WITHOUT X-CSRF-Token, assert 403 and error.code == 'csrf_failed'; add a wrong-token variant. Assert the DB state is unchanged (unread-count not decremented) so the guard is proven to actually block the mutation, not just return 403. 
- **[notification-bell/test·test-gap]** Idempotent re-read is an explicitly documented contract (service.mark_read docstring: 'marking an already-read notification is a success (not 404)... does not change read_at'), but no test pins it. A regression to return 404 on second read, or to bump read_at on re-read, would pass all current tests. The frontend calls readOne only when !n.read_at, so the server is the sole guardian of this behavior.
  - fix: Add test_read_already_read_is_idempotent: notify_user, POST /{id}/read twice, assert both return 200 and {ok:true}, and re-read the row asserting read_at equals the FIRST timestamp (advance fake_clock between calls to prove it is not overwritten). Assert unread-count stays 0, not negative. 
- **[notification-bell/test·test-gap]** The entire React NotificationBell.jsx is untested — frontend/ has no test files, no vitest config, and tests/js only covers the legacy vanilla chat UI. The highest-risk ported logic is openItem(): deep-link routing via OBJ_ROUTE[n.related_object_type], the isUser -> nav('/chat') diversion (comment says users get bounced from admin routes), and 'mark read only if !read_at'. None of this is verified anywhere; a wrong route map or dropped isUser branch ships silently.
  - fix: Stand up a component test (vitest + @testing-library/react, or a jsdom harness like tests/js) and add: openItem_admin_deeplinks_by_related_type (asserts nav called with OBJ_ROUTE mapping e.g. related_object_type 'approval' -> '/approvals'; unknown type -> no nav), openItem_user_always_routes_to_chat (isUser=true -> nav('/chat') regardless of type), openItem_marks_unread_only (readOne.mutate called for unread item, NOT called for already-read item), and openItem_closes_popover. 
- **[diagnostics+maintenance/test·test-gap]** No test asserts the diagnostics bundle's tighter RBAC. GET /api/admin/diagnostics/bundle is admin/system_admin-only (app/health/router.py:48), strictly narrower than the dashboard which also allows operator+auditor (READ_ROLES, router.py:19/40). Existing tests only hit the bundle as admin (test_dashboard_backups.py:118) and system_admin (test_secret_exposure_sweep.py:49). Nothing verifies operator or auditor are DENIED. The bundle exposes recent job error messages and down-integration status, so a regression loosening this to READ_ROLES would silently leak diagnostics to auditor/operator and no test would fail.
  - fix: Add test_diagnostics_bundle_rbac in tests/security/test_admin_rbac.py (or test_dashboard_backups.py), parametrized: login_as('operator')->GET bundle==403, login_as('auditor')->403, login_as('admin')->200, login_as('system_admin')->200. Contrast with the dashboard matrix (operator/auditor==200) to lock the asymmetry. 
- **[diagnostics+maintenance/test·test-gap]** The Maintenance toggle action flow in frontend/src/screens/Ops.jsx (the page's core lifecycle) has zero automated coverage. frontend/package.json has no test runner (no vitest/jest, no `test` script) and no *.test/*.spec files exist under frontend/src, so none of this logic is tested: the confirm() gate (mutation must NOT fire on cancel, Ops.jsx:42-45), PUT body value = !on (Ops.jsx:38/44), the danger flag only when turning ON (Ops.jsx:43), onSuccess query-invalidation + success toast (Ops.jsx:39), and onError toast of e.message (Ops.jsx:40).
  - fix: Stand up a component test runner (vitest + @testing-library/react + a mocked api()) and add Ops.maintenance.test.jsx asserting: confirm resolves false -> api() never called; confirm true -> api('/api/admin/settings/maintenance_mode',{method:'PUT',body:{value:true}}) called when currently off; success -> toast('...success') and ['settings'] invalidated; api rejects -> error toast shows the message. 
- **[diagnostics+maintenance/test·test-gap]** CSRF enforcement on the settings/maintenance mutations is untested. The settings router applies require_csrf at router level (app/settings/router.py:26) but no test sends a settings PUT without X-CSRF-Token. test_csrf.py only covers /logout and /change-password; test_admin_mutations_require_csrf covers only /api/admin/users. Removing require_csrf from the settings router (the exact endpoint the maintenance toggle calls) would pass the whole suite.
  - fix: Add to tests/security/test_csrf.py: login_as('admin'), then client.put('/api/admin/settings/maintenance_mode', json={'value': True}) with NO X-CSRF-Token -> assert 403 and error.code == 'csrf_failed'. Optionally also cover the /{key}/dry-run and /{key}/rollback POSTs. 
- **[notification-bell/design·dead-affordance]** For regular users (isUser), the footer button '전체 알림 보기' navigates to /notifications, but Layout's redirect effect (App.jsx:189 — any non-/chat path for a user is replaced with /chat) instantly bounces them to the chat screen. Worse, the /notifications full page only exists inside AdminBody (App.jsx:164-166), which is never rendered for users, so a regular user has NO full notifications view at all — the 8-item popover is their entire notification surface, and this button silently dumps them into chat.
  - fix: In NotificationBell.jsx render the footer '전체 알림 보기' only when !isUser (NotificationBell.jsx:100-102). Alternatively, add a user-reachable notifications route so the deep-link is not a dead-end. Same applies to item deep-links which for users always go to /chat (line 71). 
- **[notification-bell/design·dead-affordance]** The '모두 읽음' (mark all read) button is shown only when one of the loaded 8 items is unread — items.some(n => !n.read_at) at line 86. But the bell badge is driven by the server unread-count, which can be > 0 while the newest 8 loaded rows are all read (unread notifications older than the top 8). In that state the badge shows unread exist yet '모두 읽음' is hidden, so the user cannot clear them from the popover — the mutation already clears ALL server-side (comment line 47), only the visibility gate is wrong.
  - fix: Gate the button on the real unread count, e.g. {count > 0 ? <button ...>모두 읽음</button> : null} at NotificationBell.jsx:86, instead of items.some(...). 
- **[notification-bell/code·dead-affordance]** The bell + popover render for every role (Topbar always mounts <NotificationBell isUser={isUser}/>). The footer button '전체 알림 보기' does nav('/notifications'), but for role==='user' Layout's redirect effect (App.jsx:188-190) immediately bounces any non-/chat path back to /chat. So a regular user who clicks '전체 알림 보기' is silently dumped on the chat screen and can NEVER reach the full notifications page. Note the individual-item deep-link (openItem) was specifically routed to /chat for isUser to avoid exactly this dead-end, but the footer link was left un-guarded.
  - fix: In NotificationBell, guard the footer link the same way openItem is guarded: for isUser either hide the '전체 알림 보기' button or nav('/chat'). Better: don't offer a full-page link that the user's role can't render. (frontend/src/app/NotificationBell.jsx:101)
- **[workflows/feature·dead-affordance]** The workflow ID is never displayed on this screen, yet two other screens instruct users to obtain it here. documents create-form help (registry.js:224) says '‘업무 자동화 흐름(워크플로)’ 화면에서 대상 워크플로의 ID를 확인해 입력하세요' and schedules uses target_ref = workflow id. But columns (registry.js:89-90) omit 'id', and the detail drawer (DataScreen.jsx:95-100) renders only config.columns — so 'id' appears nowhere. The user is sent on a dead-end hunt for an ID the screen refuses to show.
  - fix: Add col('id','ID') to the workflows columns, or render the id in the detail drawer, so the cross-screen instruction resolves. registry.js:89-90. 
- **[workflows/feature·missing-feature]** payload_schema and response_schema — the webhook's input/output contract — are supported by WorkflowConfig (schemas.py:15-16) and by PATCH (WorkflowUpdateRequest), stored as payload_schema_json/response_schema_json, and returned in workflow_view, but are exposed nowhere in the UI: not in create, not in edit, not in columns/detail. Peer screens surface exactly this kind of field via type:'json' (templates.input_schema registry.js:176, schedules.payload_template registry.js:209). A workflow whose payload contract can't be defined or viewed through the console is only half-wired.
  - fix: Add { name:'payload_schema', type:'json' } and { name:'response_schema', type:'json' } to both the workflows create and edit field lists in registry.js:91-109. 
- **[policies/feature·missing-feature]** Version diff is completely dropped. Backend exposes GET /api/admin/policies/diff/view?name=&from=&to= (router.py:216, service.diff_versions) returning a unified diff between any two versions — the core value of version management. No UI affordance exists anywhere (grep confirms 'diff' appears only in format.js label maps). Since the list shows every version as its own row, an admin has no way to see what changed between v2 and v3.
  - fix: Add a diff action (e.g. a '버전 비교' control that collects name + from/to and calls /api/admin/policies/diff/view, rendering the unified diff in a modal). File: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\registry.js:149-154. 
- **[policies/feature·missing-feature]** Rollback is completely dropped. Backend exposes POST /api/admin/policies/rollback {name, version} (router.py:225, service.rollback_to_version) which republishes an older version as a NEW published version — the primary revert path per spec §17.1. No UI affordance exists (grep confirms 'rollback' appears only in format.js). If a bad policy is published, an admin cannot revert to a known-good prior version through the screen.
  - fix: Add a rollback action/header-action that collects name + target version and POSTs to /api/admin/policies/rollback. File: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\registry.js:149-154. 
- **[policies/feature·missing-feature]** No archive / retire path. VALID_TRANSITIONS (models.py:20-26) allows →archived from draft, test, review, and published, and the transition endpoint supports it, but registry.js actions (lines 149-154) expose only the forward chain (draft→test→review→published) plus new-version. There is no button to archive/retire any policy. A published policy can only ever be superseded by publishing a newer version — it can never be simply deactivated, and drafts/test/review versions can never be discarded.
  - fix: Add an '보관(archive)' action posting to /transition {status:'archived'} with when: (r)=>['draft','test','review','published'].includes(r.status) and a confirm. File: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\registry.js:149-154. 
- **[policies/feature·dead-affordance]** A policy stuck in 'test' or 'review' cannot be corrected — a workflow trap. Backend allows test→draft and review→draft (models.py:22-23) to send a version back for edits, but registry.js exposes neither. Content edit is draft-only (editWhen, registry.js:146). So once you click '테스트로', the content becomes uneditable and there is no way back to draft; the only escape is to push the flawed policy all the way to '발행'(published) then create a new version. The forward-only action set silently strips the correction path the backend provides.
  - fix: Add a '초안으로' (send back to draft) action posting /transition {status:'draft'} with when: (r)=>['test','review'].includes(r.status). File: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\registry.js:149-154. 
- **[prompts/feature·missing-feature]** Version DIFF is entirely absent from the UI. Backend exposes GET /api/admin/prompts/diff/view (router.py:216-223) producing a unified diff between two versions — a core capability for a screen whose stated job is version management. No control, column, or drawer surfaces it anywhere in the frontend (grep confirms no 'diff' UI).
  - fix: Add a '버전 비교' affordance (header action or per-row action) that collects name/from/to and renders GET /api/admin/prompts/diff/view. registry.js prompts block ~line 130-135. 
- **[prompts/feature·missing-feature]** ROLLBACK is entirely absent from the UI. Backend exposes POST /api/admin/prompts/rollback (router.py:225-238) which republishes an old version's content as a new published version — a primary version-management safety feature. No button/action exists. Tellingly, format.js:27 already localizes 'rollback → 롤백' (so audit logs anticipate it), yet the action can never be triggered from this screen.
  - fix: Add a rollback action (form collecting name + version, POST /api/admin/prompts/rollback) to registry.js prompts.actions or headerActions. 
- **[prompts/feature·missing-feature]** runner_id (binding a prompt to a specific runner) is fully supported by the backend — accepted on create (PromptCreateRequest.runner_id, router.py:48), on PATCH (ContentUpdateRequest.runner_id, router.py:61,180-181), and returned by _prompt_view (router.py:87) — but is exposed nowhere in the screen: not in create fields, not in edit fields, not as a column, not in the detail drawer (registry.js:120-129). Admins cannot associate a prompt with a runner through the UI at all.
  - fix: Add a 'runner_id' text field to prompts.create.fields and prompts.edit.fields, and surface it as a column/detail field. registry.js:120-129. 
- **[schedules/feature·missing-feature]** Manual execution (run-now) is not exposed. Backend POST /api/admin/schedules/{id}/run-now is available to operator/admin/system_admin (OPS_ROLES) and includes double-click dedup and an execution gate, but the schedules screen's only actions are enable/disable (onoff, registry.js:196). There is no way to force/test-run a schedule from the console — the single most important operational action for a scheduler is missing.
  - fix: Add a '지금 실행' action to the schedules config actions array: { label: '지금 실행', when: (r) => r.enabled, path: (r) => '/api/admin/schedules/' + r.id + '/run-now' } with a confirm. Optionally gate on enabled since run-now 409s for disabled schedules. (frontend/src/screens/registry.js:196)
- **[schedules/feature·missing-feature]** Retry of a failed run is not exposed. Backend POST /api/admin/schedules/runs/{run_id}/retry re-queues a failed run (OPS_ROLES). Because run history itself is never shown (see Critical), failed runs can be neither seen nor retried from the UI, so a scheduled job that silently fails has no recovery path in the console.
  - fix: Once a run-history surface exists (per the Critical finding), add a per-run '재시도' action gated on status === 'failed' pointing at /api/admin/schedules/runs/{run_id}/retry. (frontend/src/screens/registry.js:196)
- **[schedules/feature·design]** The read/detail drawer hides the fields that say WHAT a schedule runs and how. DataScreen renders only config.columns in the detail drawer (DataScreen.jsx:95), despite the comment on line 8 claiming '상세 모달(전체 필드)'. The schedules columns are only name/type/cron/enabled/next_run/last_run, so target_type, target_ref, timezone, description, payload_template, retry_policy, misfire_policy, concurrency_policy, timeout_seconds, start_at, end_at, owner_user_id (all returned by _view) are never visible in the read view. To see what a schedule targets you must open the edit form — and auditor role (in READ_ROLES) has no write access, so auditors can view a schedule's target/policies nowhere.
  - fix: Either add target_type/target_ref to the schedules columns, or extend DataScreen to support a separate detailFields/detail config (distinct from columns) so the read drawer can show the full _view payload. Fix the misleading '전체 필드' comment either way. (frontend/src/screens/DataScreen.jsx:95)
- **[approvals/feature·bug]** The '요청 시각' (request time) column is bound to a field that does not exist. registry uses dateCol('created_at', '요청 시각') but the API (approval_view) returns 'requested_at', never 'created_at'. fmtDateTime(undefined) returns '—', so this column ALWAYS shows an empty dash for every approval — the one time-sensitive datum an approver needs is permanently blank.
  - fix: registry.js:236 change dateCol('created_at', '요청 시각') to dateCol('requested_at', '요청 시각'). Consider also adding dateCol('expires_at', '만료') since pending approvals expire after 72h and the deadline is invisible. 
- **[approvals/feature·missing-feature]** The decision comment feature is entirely dropped. Backend /approve and /reject accept DecisionRequest.comment (max 2000 chars), store it as decision_comment, and send it to the requester as the decision notification body. But the approve/reject registry actions have no `fields`, so runAction always POSTs an empty body — a rejection can never carry a reason. And decision_comment is not a column, so previously-entered comments are never displayed either. The whole comment round-trip the backend supports is unreachable from the UI.
  - fix: registry.js:238-239 add `fields: [{ name: 'comment', label: '사유(선택)', type: 'textarea' }]` to the approve and reject actions (DataScreen already opens a form when an action has fields), and add decision_comment to the drawer detail so decided approvals show why. 
- **[approvals/feature·dead-affordance]** Approve/Reject/Cancel buttons are shown to roles the backend forbids. The nav (App.jsx NAV) has zero role gating, so operator and auditor (both in READ_ROLES) reach the approvals screen. DataScreen gates actions only by status via a.when(sel) — no role check. Result: an auditor (pure read-only) sees all three buttons and every one 403s; an operator sees 승인/거절 (not in DECIDE_ROLES) which 403, while only 취소 works. Users get error toasts for controls that can never succeed for them.
  - fix: Gate the approvals nav item and/or the approve/reject/cancel actions by the current user's role (auth.data.role is available in App.jsx). Approve/reject should only render for admin/system_admin; cancel for operator/admin/system_admin; auditor should see none. Pass role into the DataScreen config `when` or filter actions before render (frontend/src/screens/registry.js:237-241, frontend/src/app/App.jsx:48). 
- **[runners/feature·missing-feature]** Config versioning is a core runner feature (every create/update/rollback snapshots config, runner_view returns config_version, and there are dedicated endpoints GET /{id}/versions and POST /{id}/rollback with an approval gate for sensitive rollbacks). The runners screen surfaces NONE of it: DataScreen has no version-history view and registry.js runners has no action for versions or rollback. An admin can never inspect prior configs or revert a bad change from the UI.
  - fix: Version history + rollback have no home in the config-driven DataScreen (it only renders columns + flat actions). Either add a version-history/rollback affordance to DataScreen and wire it in registry.js runners (endpoints at app/runners/router.py:249 GET /versions and app/runners/router.py:270 POST /rollback), or build a dedicated runner-detail screen. At minimum add a rollback action per version. 
- **[runners/feature·missing-feature]** POST /api/admin/runners/{id}/clone (app/runners/router.py:223) is a full backend endpoint with its own schema (RunnerCloneRequest) and deliberate security gating (_guard_secret_binding_create blocks admins from cloning a system_admin-only secret-bound runner). It is completely unreachable — registry.js runners.actions has only enable/disable/health/test (lines 78-82). The clone capability, including its security path, can never be exercised or reviewed via the UI.
  - fix: Add a clone action to registry.js runners.actions using the existing action-form mechanism (a.fields), e.g. { label: '복제', path: (r) => '/api/admin/runners/'+r.id+'/clone', fields: [{ name: 'name', label: '새 러너 이름', type: 'text', required: true }] }. 
- **[runners/feature·design]** The detail drawer renders ONLY config.columns (DataScreen.jsx:95-100), and runners exposes just 6 columns (name, enabled, maintenance_state, last_health_status, consecutive_failures, base_url — registry.js:66-67). So most of what runner_view returns (app/runners/service.py:52-69) is invisible in the UI: secret_status (whether the bound secret file actually exists — a silent-failure signal), circuit_open_until (whether/when the circuit breaker reopens), last_health_at, auth_type, health_url, timeout_seconds, concurrency_limit, config_version, owner, description, tags, version, capabilities, retry_policy. An operator cannot diagnose a runner (e.g. see it's blocked by an open circuit, or its secret is missing) from this screen.
  - fix: Give runners a detail view that shows the full runner_view, not just the 6 columns — e.g. add a `detailFields` config the drawer renders (DataScreen.jsx:95), or expand runners.columns to include secret_status, circuit_open_until, last_health_at, auth_type, timeout_seconds, concurrency_limit, config_version. Prioritize secret_status and circuit_open_until (ops-critical). 
- **[runners/feature·dead-affordance]** maintenance_state is shown as a badge column (registry.js:66, '상태': 정상/성능저하/점검) but there is no control to change it. The create form and the fallback edit form (create.fields, registry.js:68-77) both omit maintenance_state, and there's no action for it — yet PATCH accepts maintenance_state as a non-sensitive change (app/runners/router.py:87-108, sensitive_change excludes it) and can_dispatch blocks dispatch when state=='maintenance' (service.py:189). Result: an operator can never put a runner into maintenance mode, nor manually clear a stuck 'degraded' / circuit-open runner, from the UI. The status column implies manageable state that has no corresponding control.
  - fix: Expose maintenance_state control: add it to the edit form (add an `edit` block or extend create.fields with a select of normal/degraded/maintenance), or add explicit '점검 시작'/'점검 해제' actions that PATCH maintenance_state. This is a plain non-approval PATCH so it's low-risk to wire. 
- **[integrations/feature·missing-feature]** The entire config-versioning + rollback subsystem is invisible. Backend fully implements GET /api/admin/integrations/{id}/versions (router.py:192-209, returns full version history with snapshots) and POST /{id}/rollback (router.py:216-273, with an approval gate for sensitive fields), but registry.js integrations config (lines 41-60) exposes NO action or headerAction for either. The table even shows a config_version column ('버전', registry.js:46) so the user can see there are versions, yet there is no way to view history or restore any version. This is a dead-end column and a dropped feature.
  - fix: Add a versions view + rollback affordance for integrations. Minimum: a per-row action opening GET /api/admin/integrations/{r.id}/versions and a rollback action POST /{r.id}/rollback with body {version}. registry.js:56-59 (actions array) is where to add them; the DataScreen action/form plumbing (DataScreen.jsx:33-41, 113-117) already supports input-form actions. Mirror the version workflow used by prompts/policies. 
- **[integrations/feature·bug]** Health check result is discarded and a green success toast is shown even when the integration is DOWN. POST /{id}/health (router.py:177-189) returns {status: up|down, latency_ms, detail, checked_url} where detail is 'timeout'/'connection_error'/'HTTP 503' etc. DataScreen.runAction (DataScreen.jsx:33-41) fires announce(res, '헬스체크 완료') — which only special-cases approval_pending — so a down/timeout/error result produces a success-styled '헬스체크 완료' toast, then closes the drawer (setSel(null)). latency_ms and the failure detail are never surfaced; the operator cannot see WHY a service is down (only the list badge silently flips to '중단' after refresh).
  - fix: Special-case health responses so the toast reflects res.status and includes res.detail/res.latency_ms (e.g. toast kind 'error' when res.status==='down'). Either extend announce() (DataScreen.jsx:29-32) to inspect res.status for health, or give the health action a resultRenderer/result modal. Do not close the drawer on a failed check. 
- **[integrations/feature·missing-feature]** The detail view never surfaces secret_status or last_health_at (nor capabilities/created_at/updated_at). integration_view (service.py:38-54) returns secret_status (present/missing, the primary auth diagnostic per spec §0.2), last_health_at (health freshness), capabilities, created_at, updated_at — but the detail modal only renders config.columns (DataScreen.jsx:95-100), i.e. the 6 columns in registry.js:45-46 (name, provider_type, enabled, last_health_status, base_url, config_version). So an operator can never tell whether the bound secret file actually exists, or when the last health check ran (a stale 'up' from days ago looks identical to a fresh one). Note DataScreen's own comment (line 8) claims '상세 모달(전체 필드)' / all fields, but it is columns-only.
  - fix: Give integrations a detail field set beyond columns (e.g. a config.detailFields the drawer renders in addition to columns, or add secret_status via badgeCol and last_health_at via dateCol to columns in registry.js:45-46). At minimum expose secret_status and last_health_at; ideally also health_url, auth_type, description, capabilities in the read-only detail (currently only reachable by opening the edit form). 
- **[templates/feature·missing-feature]** The read-only detail modal renders ONLY the 4 list columns (name, target_type, enabled, created_at). DataScreen.jsx:95-100 maps `config.columns`, not the full record, so 7 of the 12 backend `_view` fields are never visible read-only: description, target_ref, prompt_id, policy_id, input_schema, approval_policy, updated_at. For a template these hidden fields ARE its substance (what workflow/runner it drives, its input schema, its prompt/policy bindings, its approval policy). The DataScreen docstring even claims '상세 모달(전체 필드)' (detail modal = all fields), which is false. The only way to inspect this config is to open the '수정'(edit) modal — a write-framed action.
  - fix: Render the full record in the detail drawer, or add a `detailFields` config for templates that includes target_ref, prompt_id, policy_id, input_schema, approval_policy, description, updated_at (with JSON pretty-printed). File: frontend/src/screens/DataScreen.jsx:95-100 and frontend/src/screens/registry.js:160. 
- **[templates/feature·missing-feature]** target_ref — the template's core binding (which workflow/runner it targets, required, String(64)) — is surfaced NOWHERE read-only. It is not a list column (registry.js:160 shows only name/target_type/enabled/created_at) and the detail drawer only shows columns. You can see '대상 유형: 워크플로' but never WHICH workflow, so two templates targeting different workflows are indistinguishable and no one can verify a binding without opening the edit form. Contrast workflows(webhook_url), runners(base_url), integrations(base_url), all of which surface their target as a column.
  - fix: Add target_ref as a column and to the detail view; ideally resolve and show the target's name, not the raw UUID. File: frontend/src/screens/registry.js:160. 
- **[documents/feature·dead-affordance]** awaiting_approval is a dead end on this screen, and the mandated 'review then approve' flow is impossible end-to-end. preview_then_approve is the DEFAULT mode; its rows land in awaiting_approval, but registry `documents` has NO `actions` array at all — no approve/reject/view button on the row. The user must guess to navigate to a separate Approvals screen. There, the approval's request_payload is only {generation_id} (document_generate.py:169) and the Approvals screen columns don't render payload/preview, so the approver cannot see the preview they are explicitly told to review (notify body '생성된 문서 미리보기를 검토하고 발행을 승인하세요', document_generate.py:180). The review-before-approve instruction is a dead affordance everywhere.
  - fix: On the documents detail for awaiting_approval rows, show the preview and either a deep-link to the matching approval or (for authorized roles) an inline approve/reject; and make the Approvals screen render the linked generation preview. registry.js documents block (add actions/detail) + approvals payload rendering. 
- **[documents/feature·missing-feature]** failed rows give no error reason. on_failure stores error_message (up to 2000 chars) on the generation (document_generate.py:194) and generation_view returns it. The parallel `jobs` screen surfaces the equivalent via a `last_error` column (registry.js:283), but the `documents` screen has no column or detail field for error_message, so a '실패' row shows no cause (n8n error, publish read-back failure, etc.).
  - fix: Add error_message to the documents detail view (and optionally a truncated column), mirroring the jobs screen's last_error. registry.js:230. 
- **[backup/feature·missing-feature]** The backend endpoint GET /api/admin/backups/restore-instructions (router.py:68-79) is fully implemented and returns the safe step-by-step restore procedure (maintenance mode, snapshot, rollback script, health check) for system_admin, but NOTHING in the backup screen surfaces it. registry.js 'backup' has no headerAction, action, or link for it. The screen serves only the 'backup' half of its Backup/Restore domain (docs/BACKUP_RESTORE.md) — an admin who needs to restore has no affordance to discover how. This is a dead, orphaned backend capability.
  - fix: Add a headerAction in registry.js 'backup' that GETs /api/admin/backups/restore-instructions and renders the returned note+steps (e.g. in a Modal/Callout), e.g. { label: '복원 안내', path: () => '/api/admin/backups/restore-instructions', method: 'GET', ... }. Note DataScreen headerActions only POST and toast — a GET-and-display action needs a small handler or a dedicated info panel. 
- **[backup/feature·bug]** The '검증' action always reports success. The verify endpoint returns {backup, verify:{ok:false, reason:...}} and verify_existing() demotes the row to 'failed' when a backup is corrupt/missing, but DataScreen runAction (DataScreen.jsx:38-39) calls announce(res, '검증 완료') which shows a GREEN success toast regardless because res has no top-level status field. So verifying a corrupt backup tells the admin '검증 완료' (verification complete) — defeating the entire purpose of the verify feature, which is to detect corruption. The verify.reason is never shown.
  - fix: Handle the verify result: after the verify call, inspect res.verify.ok and toast success only when ok, otherwise toast an error including res.verify.reason. This requires a custom handler rather than the generic runAction success path (DataScreen.jsx:33-41). 
- **[jobs/feature·missing-feature]** GET /api/admin/jobs/stats returns queued/running/succeeded/failed/cancelled/ready/oldest_queued_at — the queue-status dashboard (spec §14.1). grep confirms nothing in frontend/src consumes it. The jobs screen shows only a raw list, giving the operator no at-a-glance counts, no backlog depth (ready), and no oldest-queued age to detect a stalled worker. A StatCard component exists (kit.jsx:55) and is used on Dashboard, but the jobs screen renders none.
  - fix: The config-driven DataScreen has no stats slot; either add a `stats` capability to DataScreen (fetch config.statsEndpoint and render a StatCard row above the table) and set jobs.statsEndpoint='/api/admin/jobs/stats', or build a small custom header strip on the jobs screen. Surface queued/running/failed/ready and oldest_queued_at. 
- **[jobs/feature·dead-affordance]** The list endpoint accepts ?status= (validated against ALL_STATUSES) and ?job_type= (router.py:64-73), but there is no filter UI. jobs config sets neither `searchable` nor any filter mechanism, so DataScreen only does a client-side text match over the already-loaded page (DataScreen.jsx:52-53). The primary job-queue use case — 'show me all failed jobs' — cannot be done: those server filters are dead, and failures beyond the first page are unreachable.
  - fix: Add a filter mechanism to DataScreen (e.g. config.filters = [{param:'status', options:[...]}, {param:'job_type', options: JOB_TYPE}]) that appends query params to the endpoint and includes them in the react-query key, then populate it in the jobs config. Reuse the STATUS/JOB_TYPE maps already defined in registry.js. 
- **[notion-mapping/feature·missing-feature]** error_message is never surfaced, and verify feedback is misleading. verify_mapping (service.py) sets actionable error_message on every failure path ('일치하는 Notion 사용자가 없습니다', '매핑 조회 실패', 'Notion 매핑 Workflow가 구성/활성화되지 않았습니다', conflict count), but there is no column/detail for it (DataScreen shows only config.columns). Worse, runAction (DataScreen.jsx L39) always toasts '검증 완료' as a green success even when verify left the row unmapped with an error — announce() only special-cases approval_pending. Admin sees success, sees no change, and never learns why.
  - fix: registry.js: add error_message to columns/detail. DataScreen announce()/runAction (L29-40): inspect the returned mapping.status and show a warn/error toast (with error_message) when verify results in unmapped/conflict instead of blanket '완료'. 
- **[notion-mapping/feature·missing-feature]** No pagination — only the first 20 users are shown and mappable. list_mappings enumerates ALL users (left join) and returns total/page/page_size, accepting a page param, but DataScreen never sends page and renders no pagination control (grep confirms the only page usage in the whole frontend is NotificationBell). DEFAULT_PAGE_SIZE=20 (pagination.py L13), so users 21+ are invisible and cannot be connected. The search box is client-side (config.searchable unset → DataScreen.jsx L52-53 filters only the 20 loaded rows), so a user on 'page 2' can't even be found by searching. The backend's server-side q search is also unused.
  - fix: DataScreen.jsx: implement pagination using the returned total/page/page_size, and/or set searchable:true on this screen and wire the endpoint's q param (endpoint already supports ?q=). Screen purpose is to map every employee, so all users must be reachable. 
- **[audit/feature·dead-affordance]** The backend exposes six purpose-built filters (action, object_type, object_id, user_id, since, until incl. date-range) but the audit config sets no 'searchable' flag, so DataScreen falls back to client-side filtering only: items.filter(r => JSON.stringify(r).includes(q)) over the ~20 loaded rows (DataScreen.jsx:52-53). No date pickers, no user/action/object selectors are wired to the server. The search box looks like it searches the audit trail but only substring-matches the current page — a misleading dead affordance. An auditor cannot answer 'all deletes', 'everything user X did', 'activity between two dates', or 'history of object Y'.
  - fix: Expose the server filters as controls (date-range for since/until, dropdowns for action/object_type, text for user_id/object_id) and pass them to the endpoint. Add a filter-fields capability to the registry config (registry.js:285-290) and have DataScreen forward them as query params, similar to the searchable path at DataScreen.jsx:23-24. 
- **[audit/feature·bug]** Many real audit action values render as raw/half-translated English in the '작업' column. actionKo (lib/format.js:35-42) splits on the FIRST dot only. Concrete breakage from actual backend actions: all 9 CLI actions cli.user.* render as e.g. 'cli · user.enable' (obj 'cli' untranslated); every '_requested' variant (integration.change_requested, integration.rollback_requested, runner.change_requested, runner.rollback_requested, schedule.enable_requested, user.role_change_requested, document.generate_requested) shows the raw verb; underscore verbs missing from VERB_KO (notion_mapping.resolve_conflict — VERB_KO has hyphen 'resolve-conflict', schedule.retry_run, schedule.run_now, runner.clone, job.retry); and dotless actions 'preview'/'publish' render raw. These are common rows, broken in the very column that names what happened.
  - fix: In lib/format.js:35-42, split on the LAST dot (or match known object prefixes) so multi-segment actions map correctly, add a 'cli' object prefix to OBJECT_KO (or strip it), and add the missing verbs (change_requested, rollback_requested, enable_requested, role_change_requested, generate_requested, resolve_conflict, retry_run, run_now, clone, retry, preview, publish) to VERB_KO. Add a regression test over the full backend action list. 
- **[notifications/feature·dead-affordance]** The '읽음' (read) column is broken for BOTH states. registry.js:295 uses badgeCol("read_at", "읽음"), passing read_at straight to <Badge>. For an UNREAD notification read_at is null, so Badge -> statusText(null) -> STATUS_TEXT["unknown"] renders '알 수 없음' (Unknown). For a READ notification read_at is an ISO string, so the badge renders the raw ISO timestamp text (e.g. '2026-07-18T16:21:23.704973'). The column never shows '읽음'/'안 읽음' — it shows 'Unknown' or a raw datetime.
  - fix: registry.js:295 — replace badgeCol("read_at",...) with a boolean/label render, e.g. { key:"read_at", label:"읽음", render:(r)=> <Badge value={!!r.read_at} kind={r.read_at?"ok":"warn"} /> } (yields 예/아니오) or a custom map to '읽음'/'안 읽음'. Never pass a nullable ISO timestamp to Badge, which maps null->'알 수 없음'. 
- **[notifications/feature·missing-feature]** The dedicated notifications screen is entirely read-only despite the backend supporting mark-read. Its stated purpose is '나에게 온 알림을 확인합니다', but the config defines no `actions` and no `headerActions`, so POST /api/notifications/{id}/read and POST /api/notifications/read-all are never wired here. Opening a row's detail drawer shows fields but its footer has no button (config.actions empty, no edit fields), so a user can look at notifications but cannot mark any read from this page. The NotificationBell popover has both '모두 읽음' and per-item read; the full page has neither.
  - fix: registry.js:291-296 — add headerActions: [{ label:"모두 읽음", path:()=>"/api/notifications/read-all", confirm:"모든 알림을 읽음 처리할까요?" }] and actions: [{ label:"읽음 처리", when:(r)=>!r.read_at, path:(r)=>"/api/notifications/"+r.id+"/read" }] so the page can exercise the mark-read endpoints the backend already provides. 
- **[notifications/feature·dead-affordance]** Regular users can never reach their full notification list, and the bell's '전체 알림 보기' is a silent dead-end for them. Notifications are per-user (notify_user fires for account_locked/job_failed/etc.), and the NotificationBell is rendered for all users (App.jsx:144). Its footer '전체 알림 보기' always nav("/notifications") with NO isUser guard (NotificationBell.jsx:101), but /notifications only exists inside AdminBody, and Layout (App.jsx:188-190) bounces any non-/chat route for role==="user" back to /chat. So a regular user clicking it is silently redirected to chat and can only ever see the 8 most recent items in the popover.
  - fix: NotificationBell.jsx:101 — guard the footer for isUser (hide the '전체 알림 보기' link, or route it somewhere reachable), OR expose a user-accessible full notifications route so regular users can view their complete notification history. 
- **[shared-infra/design·missing-feature]** The admin console has NO mobile navigation. global.css:76 sets `.c-sidebar { display: none; }` at ≤860px, and the sidebar is the ONLY way to reach the ~21 admin sections. There is no hamburger, drawer toggle, or any alternative nav — the shared Topbar (App.jsx:119-147) has only the brand (→dashboard), the admin/user segment (admin-only), the bell, and the user menu. On any phone/narrow window an admin can reach only the current page, dashboard (brand), and chat (segment). Users, integrations, workflows, prompts, policies, templates, schedules, documents, approvals, audit, backup, diagnostics, maintenance, departments, job-titles, notion-mapping, jobs, notifications all become unreachable — the console is effectively unusable on mobile.
  - fix: Add a hamburger button in `.c-topbar` shown at ≤860px (App.jsx Topbar + global.css) that opens `<Sidebar>` as a slide-in overlay drawer with a backdrop, Esc-to-close, and close-on-navigate. Same flaw exists for `.chat-side` at ≤760px (screens.css:108), which hides new-conversation/switch controls on mobile chat. 
- **[shared-infra/design·bug]** No React error boundary anywhere in the shell (main.jsx and App.jsx have none). Any render-time exception in a screen — a malformed API row, `.map` on a non-array, a throwing column `render` — unmounts the entire SPA to a blank white page with no message and no recovery except a manual reload. react-query error states only cover fetch failures, not render crashes.
  - fix: Wrap `<Layout>` (or the `<Routes>` in AdminBody, App.jsx:158) in an ErrorBoundary that renders the shared `ErrorState` (kit.jsx:85) with a reload/retry action so one bad screen doesn't take down the whole app. 
- **[shared-infra/code·missing-feature]** Server pagination is dropped entirely. audit/jobs (and notifications) endpoints paginate via PageParams (DEFAULT_PAGE_SIZE=20) and return total/page/page_size, but DataScreen never sends page params (DataScreen.jsx:23-24) and DataTable renders no pager and ignores `total`. Only the latest 20 rows are ever visible; older audit entries and jobs are unreachable and there is no "20 of N" indicator.
  - fix: Thread a page state into the queryKey/queryFn (append ?page=&page_size=) and render pagination controls from response.total/page/page_size. DataScreen.jsx:20-24 and ui/kit.jsx DataTable. 
- **[shared-infra/code·dead-affordance]** The "검색" box only filters already-loaded rows and the `searchable` branch is dead — no registry config sets searchable:true, so DataScreen.jsx:23-24 server-search path never runs and every search is a client-side JSON filter over loaded items (DataScreen.jsx:52). Combined with the 20-row cap, searching audit/jobs/notifications only matches within the latest 20 and returns a misleading "검색 결과가 없습니다" for anything older; the audit server filters (action/object_type/user/since/until) and jobs (status/job_type) are never exposed.
  - fix: Wire true server-side search/filters (send q and filter params, key them into the query) or, at minimum, relabel the input as page-scoped and add the server filter controls. DataScreen.jsx:52. 

## Medium (126)

- **[login/design·dead-affordance]** The 'forgot password' hint (login.html:84-87) tells the user '관리자에게 재발급을 요청하면...' but provides NO way to actually do it — no mailto link, no admin/IT-helpdesk email, no phone, no link. The <strong> text is styled emphasis, not a control. A user who forgot their password reaches a genuine dead end: the only stated recovery path has zero mechanism on the page. This is the one real user flow (account recovery) that the login screen does not serve end-to-end.
  - fix: In login.html:85-86 make the recovery path actionable: wrap the guidance in an <a href="mailto:helpdesk@goodmit.co.kr?subject=비밀번호 재발급 요청"> (or the real IT/admin contact), or at minimum surface a concrete contact address/extension so the instruction is completable. Pull the address from branding/settings rather than hardcoding. 
- **[login/design·ux]** Submitting gives almost no progress feedback. On submit, login.js:30 only sets submitButton.disabled=true (CSS dims it to opacity 0.65, login.css:213); the button label stays '로그인' with no spinner and no '로그인 중…' text. Argon2 verification is deliberately slow (timing-equalized in auth/router.py) so on a slow link the user sees a dimmed-but-unchanged button and may re-click or assume nothing happened. Loading state has no explicit indicator.
  - fix: In login.js submit handler set a busy state: swap button text to '로그인 중…' (restore in finally) and/or toggle an aria-busy attribute plus a small inline spinner. Keep the existing disable. Ensure the label is restored on both success (before redirect it's fine to leave) and error paths. 
- **[change-password/test·test-gap]** The client-only confirm-password check (change_password.js:47-50) has no test. This mismatch guard exists ONLY on the client — the backend never receives confirm-password, so there is no server-side backstop. If it regresses, mismatched passwords silently reach the server. Also untested: submitting while csrfToken is still null (bootstrap failed) must show an error and make NO network call.
  - fix: In change_password_ui_test.js add: set new-password != confirm-password, dispatch submit, assert errorBox shows the mismatch message AND that fetch('/change-password') was never called (inspect harness calls[]). Separately: with /api/me having failed (no csrf), submit → error shown, no POST issued. 
- **[change-password/test·test-gap]** The GET /change-password template (change_password.html:10-14) renders two entirely different subtitles depending on must_change, and interpolates display_name into the must_change message. No test asserts either branch. test_pages.py:53 only checks the static title '비밀번호 변경'. A regression flipping the {% if must_change %} condition or dropping {{ display_name }} would pass all current tests. Also relevant: HTML-escaping of display_name (XSS via a name containing markup) is unverified on this page.
  - fix: In test_pages.py add test_change_password_page_shows_first_login_prompt (make_user must_change_password=True, display_name with markup like '<b>홍</b>'): assert '첫 로그인입니다' and the escaped display name appear; and test_change_password_page_shows_policy_hint_for_normal_user: assert '3종류' policy hint appears and '첫 로그인' does NOT. 
- **[change-password/test·test-gap]** POST /change-password with a malformed / non-JSON body is untested. router.py:243 depends on _json_body (router.py:100-110) which raises ValidationAppError→422 on unparseable input. Without a test, a regression could turn a bad body into an unhandled 500. Related: bodies with missing fields (empty {} → current_password='' → 401; missing only new_password → 422) and non-string JSON values (new_password: 12345 or null, coerced via str()) are all untested edge paths.
  - fix: In test_auth_change_password.py add test_change_password_malformed_body_returns_422 (POST with content=b'not-json' + valid CSRF → 422, error envelope, not 500) and test_change_password_missing_fields (POST {} → 401 for empty current; POST with only current_password → 422). Optionally assert str-coercion of a numeric new_password behaves as intended. 
- **[dashboard/test·test-gap]** The counts 'enabled' filters are not verified. test_dashboard_counts_active_workflows only asserts active_workflows >= 1 after creating one enabled workflow — it never proves a DISABLED workflow is excluded. active_schedules (enabled filter) and runners count have no dashboard test at all (service.py:154-160).
  - fix: Add test_dashboard_counts_exclude_disabled: create one enabled and one disabled Workflow and Schedule, seed a Runner; assert counts.active_workflows and counts.active_schedules each equal exactly 1 (not 2) and counts.runners equals the seeded count. 
- **[dashboard/test·test-gap]** No frontend test exists for Dashboard.jsx, so the operator-alert threshold logic (§6.1) is completely uncovered. The boundaries cert_days_remaining <= 30 (warn), disk.used_pct >= 85 (danger), !last_backup_at (danger), failed_open (danger), queued (warn) live only in the React component (Dashboard.jsx:38-45). There is no JS test runner for the ported React screens (tests/js only has domstub/harness for the old vanilla UI). Flipping >=85 to >85 or <=30 to <30 would pass all tests.
  - fix: Introduce a component/unit test for DashboardBody (or extract the alert-building logic to a pure function e.g. buildAlerts(d) and unit-test it): assert boundary cases — used_pct 85 produces a danger alert, 84 does not; cert_days 30 -> warn, 31 -> none; missing last_backup_at -> danger alert with to='/backup'. 
- **[dashboard/test·test-gap]** success_rate_pct and avg_processing_seconds null-guards are untested. Both return None when there are no jobs / no finished jobs (service.py:126-128). The division-by-zero guard (total_24h falsy -> None) and the empty-durations guard have no test, and the frontend renders success_rate null as '—' (Dashboard.jsx:82) which is also unverified.
  - fix: Add test_dashboard_metrics_none_when_no_jobs: with an empty jobs table assert jobs_24h.success_rate_pct is None and jobs_24h.avg_processing_seconds is None (and jobs_24h.total == 0), guarding against a ZeroDivisionError regression. 
- **[dashboard/test·test-gap]** The integrations block of the dashboard is untested. build_dashboard emits {name: {enabled, last_health}} for every Integration (service.py:149-152) and the frontend merges last_health into the services grid (Dashboard.jsx:47-48). No test seeds an integration and asserts it appears under body['integrations'] with the expected last_health, so a broken mapping or renamed key would not be caught here.
  - fix: Add test_dashboard_lists_integration_health: create an integration (or set last_health_status='down' via the integrations API), then assert body['integrations'][name]['last_health'] and ['enabled'] match the seeded values. 
- **[change-password/design·dead-affordance]** There is no show/hide password toggle on any of the three fields (change_password.html:15-30 are plain <input> with no toggle button), even though the login page provides one (login.html:73, login.js:12-17). This is precisely the screen where a toggle matters most: the user is CREATING a long, complex new password and re-typing it into a confirm field — both masked — with no way to verify what they typed. Login (re-entering a known password) has the affordance; password creation (where errors are silent until submit) does not.
  - fix: Add a '표시/숨김' toggle button (reuse login's .input-wrap + .btn-ghost pattern and the toggle handler from login.js:12-17) to at least the new-password field, ideally new + confirm. 
- **[change-password/design·design]** The error box #cp-error uses only the plain .form-error style (base.css:108-113: bare red text, no background), while login gives its error box an emphasized badge because plain red text 'was easy to miss' (login.css:181-190, #login-error:not(:empty) adds badge-error-bg + padding). The lesson login learned was never applied here — and this screen depends ENTIRELY on the error box to communicate the multi-line password-policy violations (details.join), so the one thing the user must read is styled as the easy-to-miss variant.
  - fix: Give #cp-error the same not(:empty) badge treatment as #login-error (background var(--badge-error-bg), radius, padding, reserved min-height) so policy-violation errors stand out; consider rendering error.details as a list rather than a space-joined string. 
- **[change-password/design·bug]** The submit button is enabled on page load (change_password.js:55 only disables it inside the submit handler), but csrfToken stays null until the /api/me fetch resolves (lines 21-42). A user who fills the form and submits before that request returns hits the !csrfToken guard (line 51) and is told '세션 정보를 불러오지 못했습니다. 새로고침 후 다시 시도하세요.' — i.e. a transient loading state is reported as a failure and the user is told to refresh a perfectly healthy session. The 'still loading' state and the 'failed to load' state are conflated.
  - fix: Disable #cp-submit until csrfToken is populated (re-enable in the /api/me .then), and while pending show a neutral '세션 정보를 확인하는 중…' rather than the failure/refresh message. Only surface the refresh instruction on an actual non-401 error (already handled at lines 24-28/33-35). 
- **[change-password/design·ux]** No live/inline validation of any kind. The confirm-match check and the entire password policy are evaluated only on submit (change_password.js:47-48 for match; server round-trip for policy). There is no requirements checklist that ticks off as the user types (length met / 3 classes met / passwords match), which is the standard pattern for a mandatory password-creation gate. Every mistake costs a full submit + error read.
  - fix: Add a live requirements checklist under the new-password field that updates on input (length, class count, match with confirm), driven by the same policy numbers from finding #2, so the user sees progress and knows they'll pass before submitting. 
- **[change-password/code·dead-affordance]** The form has novalidate (change_password.html:15) so the required attributes on the three inputs (lines 18/22/26) never fire, and change_password.js only checks new===confirm (line 47) — it never checks for empty fields. Submitting an empty current password POSTs '' and the backend returns 401 '현재 비밀번호가 올바르지 않습니다' (router.py:256-257), i.e. an empty required field produces a misleading 'wrong password' error instead of 'fill this field'. The required attributes are decorative.
  - fix: Either drop novalidate to let native required validation run, or add empty-field checks in change_password.js before the csrf/POST step with a clear '모든 항목을 입력하세요' message. 
- **[change-password/code·contract]** change_password.html:13 hardcodes the policy as '3종류 이상... 12자 이상', but the backend reads an admin-configurable password_policy (router.py:258-263 -> policy.get('min_length',12)/get('min_classes',3)) and validate_password_policy emits messages built from those effective values. If an admin raises min_length to 16, the page still advertises '12자 이상' while the server rejects <16 with '최소 16자', so the on-screen guidance is wrong.
  - fix: Render the hint from the effective policy: pass min_length/min_classes into the template context in change_password_page (router.py:229-237) and interpolate them, instead of hardcoding 12/3. 
- **[change-password/code·bug]** Race / no loading state: change_password.js loads csrfToken asynchronously from /api/me (line 21) while the form is fully interactive from first paint. If the user (or password-manager autofill + Enter) submits before the fetch resolves, csrfToken is null and they get '세션 정보를 불러오지 못했습니다. 새로고침 후 다시 시도하세요.' (line 52). The advice to refresh just restarts the same race; the correct action is to wait a moment. There is no disabled/loading state on the submit button while CSRF is in flight.
  - fix: Disable #cp-submit until csrfToken resolves (re-enable in the /api/me .then), and change the null-csrf message to '세션 정보를 불러오는 중입니다. 잠시 후 다시 시도해 주세요.' 
- **[dashboard/design·dead-affordance]** The '새로고침' (refresh) button gives no feedback (Dashboard.jsx:21). It calls q.refetch() but never uses q.isFetching — the button doesn't disable, show a spinner, or change label, and the page shows no loading state on refetch (isLoading is only true on first load). The user cannot tell the click did anything. The codebase's own Ops.jsx:15 already does this correctly (disabled={q.isFetching}, '수집 중…'), so this is an inconsistent regression.
  - fix: Wire isFetching: <Button ... disabled={q.isFetching}>{q.isFetching ? '새로고침 중…' : '새로고침'}</Button>, matching Ops.jsx. Dashboard.jsx:21. 
- **[dashboard/design·missing-feature]** A live ops/status dashboard shows no data-freshness indicator. With staleTime 30s and no refetchInterval, the health/metrics can be minutes old and nothing tells the operator when it was last fetched, so 'worker: up' or 'failed jobs: 0' may be stale and silently trusted.
  - fix: Show an 'HH:MM 기준' timestamp near the header from q.dataUpdatedAt (formatted via fmtDateTime), and/or add refetchInterval to keep it live. Dashboard.jsx:20-21. 
- **[dashboard/design·ux]** The '최근 주요 변경' (recent critical changes) section under-serves its purpose (Dashboard.jsx:105-118). (1) It prints actionKo(action) then objKo(object_type), which duplicates the object noun — e.g. action 'user.role_change' + object_type 'user' renders '사용자 · 역할 변경  사용자'. (2) It never shows WHICH object (object_id is present in the payload at service.py:171 but unused) nor WHO made the change (actor is not even selected in build_dashboard), so entries read as 'someone changed some user'. (3) There is no link to the full audit log, making it a dead-end display.
  - fix: Drop the redundant objKo column (the action already carries the object noun), render object_id, add actor to the backend payload and show it, and add a '전체 보기' link to /audit. Dashboard.jsx:105-118; service.py:170-174. 
- **[dashboard/design·dead-affordance]** In '확인이 필요한 항목', the cert-expiry (Dashboard.jsx:41-42) and disk-usage (44-45) alerts have no 'to', so they render as non-clickable divs while the failed-job/queued/backup alerts render as clickable buttons in the same grid. An operator sees 'D-7 인증서 만료' or '디스크 90%' flagged as danger but is given no next step and no visual cue that these two behave differently from their neighbors.
  - fix: Give these alerts a destination (e.g. cert → /settings or a maintenance page, disk → /diagnostics), or if none exists, render remediation guidance instead of a look-alike clickable card. Dashboard.jsx:41-45. 
- **[dashboard/code·missing-feature]** The dashboard is a live operational health view (service up/down via 90s heartbeat staleness, disk %, memory %, queued/failed jobs, cert D-day) but the query has no refetchInterval, and the global react-query config sets refetchOnWindowFocus:false with staleTime:30s (frontend/src/main.jsx:12). So after initial load the page freezes: a worker/scheduler going 'down' (or jobs failing/queuing) never surfaces until the operator manually clicks 새로고침. Other live views poll (NotificationBell 60s, Chat), but the health dashboard — the one screen whose whole purpose is real-time awareness — does not. It cannot serve its stated §6.1 purpose end-to-end.
  - fix: Add polling to the dashboard query in Dashboard.jsx:16, e.g. useQuery({ queryKey:['dashboard'], queryFn:..., retry:false, refetchInterval: 30*1000, refetchIntervalInBackground:false }). 30s aligns with the 90s heartbeat-stale window. 
- **[dashboard/code·bug]** The 성공률 (success-rate) StatCard is hardcoded kind="ok" (green) regardless of the value (Dashboard.jsx:82). A 10% success rate renders in the same green 'good' tone as 99%, and even a null rate (no jobs → '—') is tinted green. On an ops dashboard this actively misleads the operator about the single metric the color is supposed to communicate.
  - fix: Make the tone conditional on the value, e.g. kind = success_rate_pct==null ? undefined : success_rate_pct>=95 ? 'ok' : success_rate_pct>=80 ? 'warn' : 'danger', at Dashboard.jsx:82. Also add a low-success-rate entry to the alerts block (Dashboard.jsx:38-45) so a degraded rate shows under '확인이 필요한 항목'. 
- **[chat (user)/test·test-gap]** PATCH /api/conversations only has the archive=True path tested (test_chat_api.py:40). Unarchive (archived:false restoring visibility), combined {title, archived} in one call (router.py:96-99 applies both), and an empty {} body (no-op still 200 returning current view) are all untested. A regression in the two-branch patch handler (e.g., archived overwriting title, or empty body 500) would go unnoticed.
  - fix: Extend tests/integration/test_chat_api.py: test_unarchive_restores_to_default_list (archived True then False → reappears in GET /api/conversations), test_patch_title_and_archived_together, and test_patch_empty_body_is_noop_200. 
- **[chat (user)/test·test-gap]** The retry endpoint's maintenance-mode gate is untested. router.py:165 decorates /api/messages/{id}/retry with block_if_maintenance, but tests/integration/test_maintenance_mode.py only exercises POST .../messages (503). Retry during maintenance (should 503 for users, pass for operators) has no test. Also worth pinning: retry has NO rate limiter (only post_message does, router.py:142) — an intentional-looking asymmetry with no test documenting it, so a user could hammer retry unbounded.
  - fix: Add test_maintenance_blocks_retry (user retry during maintenance → 503 maintenance_mode; operator → not blocked). Add a test asserting the intended retry rate-limit policy (either that retry IS limited, or an explicit xfail/comment test documenting it is deliberately unlimited). 
- **[chat (user)/test·test-gap]** safeNotion() (Chat.jsx:28-31) is a security-relevant allowlist deciding whether a ticket URL renders as a clickable target=_blank link or inert plain text — it only permits https notion.so/notion.com/app.notion.com and *.notion.site. As pure frontend logic with no test runner it is entirely uncovered. A regression could render attacker-controlled URLs (e.g. javascript:, http://evil, phishing hosts) from n8n/ticket data as clickable links.
  - fix: In frontend/src/screens/Chat.test.jsx add unit tests for safeNotion: accepts https notion.so / www.notion.so / x.notion.site; rejects http://, javascript:, evil.com, notion.so.attacker.com. Assert TicketCard renders <a> only for accepted URLs and plain <span> otherwise. 
- **[chat (user)/test·test-gap]** newClientMessageId() (Chat.jsx:22-25) must produce ids matching the server contract regex ^[A-Za-z0-9_-]{8,64}$ (service.py:27) — both the crypto.randomUUID path and the non-secure-context fallback ('m-'+base36+'-'+base36). No test pins the generator to the validator. If the generator drifts (adds '.', '+', wrong length) every send silently 422s with no test catching the cross-boundary break.
  - fix: Add frontend/src/screens/Chat.test.jsx unit test asserting newClientMessageId() matches /^[A-Za-z0-9_-]{8,64}$/ in both the randomUUID-present and fallback branches (stub window.crypto). Optionally add a Python-side contract constant shared/asserted so the two stay in lockstep. 
- **[chat (user)/test·test-gap]** doSend()'s optimistic-clear-then-restore-on-failure (Chat.jsx:195-210) — the core '입력·첨부 유실 방지' behavior — is untested. On send it clears text+pending immediately; on error it restores both and toasts. It also lazily creates a conversation when cid is null (line 203-204) and guards against double-send while send.isPending. None of this is exercised (no frontend runner).
  - fix: In Chat.test.jsx: test that a failing api() send restores the textarea value and attachment chips and shows an error toast; that a successful send clears them; that sending with cid=null first calls POST /api/conversations then POST .../messages; and that a second send is ignored while the first is pending. 
- **[chat (user)/test·test-gap]** The thread refetchInterval polling logic (Chat.jsx:128-137) is untested. It polls every 1500ms while any message is pending/processing OR the last message is a user message awaiting a reply — a deliberate fix for 'answer arriving between polls'. With no frontend runner, a regression that stops polling (leaving the UI stuck on '처리 중' forever) is uncaught.
  - fix: Add a Chat.test.jsx test with fake timers: seed a thread whose last item is a user message with processing_status pending, advance timers, assert api() is re-called; then seed a terminal (done, last=assistant) thread and assert polling stops. 
- **[chat (user)/design·ux]** The composer textarea is `rows={1}` with `resize:none` and no auto-grow logic (Chat.jsx:248-251; onChange only calls setText). The CSS `max-height:160px` (screens.css:98) can never take effect because nothing ever grows the element. The placeholder advertises 'Shift+Enter 줄바꿈' (Chat.jsx:249), but a multi-line message is crammed into a single-line-height box that just scrolls internally — the user cannot see what they typed. The advertised newline affordance is not visually supported.
  - fix: Add auto-grow: on change set textarea height to scrollHeight capped at max-height (e.g. reset height to 'auto' then set to min(scrollHeight,160)), or use a rows-calculation. Chat.jsx:248-251. 
- **[chat (user)/design·states]** No optimistic echo of the sent message and no in-thread send feedback. doSend clears the composer immediately (Chat.jsx:200) but the message only appears after the POST round-trip resolves and the thread refetches via invalidate (Chat.jsx:205-206, 146). For a NEW conversation it awaits createConv THEN send THEN refetch (Chat.jsx:203-205), a longer gap. During that window an empty conversation still shows the '아래에 메시지를 입력해 시작하세요' welcome (Chat.jsx:232) even though a message was just sent — the text vanished and nothing replaced it. Only the disabled send button hints activity; the 'thinking' dots appear only after the server returns the pending row.
  - fix: Render an optimistic user bubble (and a pending/sending indicator) immediately in doSend before the await, reconcile on refetch; suppress the empty-welcome once a send is in flight. Chat.jsx:195-210, 232. 
- **[chat (user)/design·missing-feature]** Conversation management is destructive-only. The backend fully supports non-destructive archiving (PATCH /api/conversations {archived}, set_conversation_archived, list_conversations(include_archived)) — service.py:67-72 / router.py:88-100 — but the UI (ConvItem, Chat.jsx:100-109, 222) exposes ONLY rename and permanent delete ('되돌릴 수 없습니다'). Users who want to declutter must permanently destroy conversations; a shipped, safer capability is completely unwired in the frontend.
  - fix: Add an '보관(archive)' action to ConvItem calling PATCH {archived:true}, plus a way to view/unarchive archived conversations (list already supports include_archived). Chat.jsx:103-106 / 219-223. 
- **[chat (user)/design·empty-state]** The welcome/empty states state capability but give zero actionable guidance. `!cid`: '무엇을 도와드릴까요? / 대화 한 줄이면 티켓 생성·조회·요약까지 처리합니다.' (Chat.jsx:229) and empty thread: '새 대화 / 아래에 메시지를 입력해 시작하세요.' (Chat.jsx:232). No seeded example prompts or one-tap starters, despite the page already rendering clickable choice chips for assistant replies (chat-choice, Chat.jsx:69-74) — the exact pattern could seed starters like '내 티켓 보여줘'. A first-time user is told what the bot can do but given nothing to try.
  - fix: Render 3-4 example starter chips in the welcome state that call doSend(text) on click, reusing the chat-choice styling. Chat.jsx:228-229, 232. 
- **[chat (user)/design·contract]** TicketCard renders external (n8n/Notion) values with no boundary normalization. `t.status` goes through Badge which falls back to the raw string when unmapped (kit.jsx:26-29,32-34), and `t.due_date` is printed verbatim (Chat.jsx:45). If the workflow returns an English enum ('In Progress', 'Todo') or an ISO timestamp ('2026-07-19T00:00:00Z'), raw English/ISO leaks into the otherwise-Korean UI. STATUS_TEXT (kit.jsx:8-17) does not cover typical Notion status names.
  - fix: Normalize/format status and due_date at render (map known statuses to Korean; format due_date as a locale date). Chat.jsx:43-45; extend STATUS_TEXT or add a ticket-status map. 
- **[users/code·missing-feature]** Session management is unexposed. Backend GET /api/admin/users/{id}/sessions (router.py:376-402) and POST .../revoke-sessions (router.py:405-414) exist, and GET /api/admin/users/{id} returns active_session_count (router.py:188-191). The frontend never calls GET /{id} at all — the drawer renders the stale list-row object (sel), so active_session_count is never fetched or shown, and there is no force-logout (revoke-sessions) control. A security-relevant capability (kill active sessions on compromise/offboarding) is missing.
  - fix: Fetch full detail via GET /api/admin/users/{id} when the drawer opens (useQuery keyed on user.id) to show active_session_count and the sessions list; add a "모든 세션 종료" danger action calling POST .../revoke-sessions with confirm. 
- **[users/code·missing-feature]** No pagination. Backend paginates and returns total/page/page_size (router.py:112-119, PageParams offset/limit), but the frontend renders only query.data.items from the first page (Users.jsx:46) and ignores total entirely. With more users than one page, the remainder are invisible and unsearchable-to (search also returns only the first page of matches), and the admin gets no indication rows are missing.
  - fix: Read total/page/page_size and render a pager (or infinite scroll) that sends page/page_size to the list endpoint; include page in the react-query key. 
- **[users/design·missing-feature]** No pagination. Backend paginates at DEFAULT_PAGE_SIZE=20 (pagination.py) and returns total/page/page_size, but the frontend reads only query.data.items (Users.jsx:46) and ignores all pagination metadata — there are no next/prev controls and no total count is displayed. In an org with >20 users, users 21+ are invisible unless you already know their exact name/email to search for.
  - fix: Render total ('사용자 N명') and add prev/next (or load-more) controls that pass page to the /api/admin/users query; include page in the react-query key. 
- **[users/design·missing-feature]** Role and active filters are dropped, leaving the toolbar bare. Backend list supports ?role= and ?active= filters (router.py:93-98) but the toolbar (Users.jsx:76-79) offers only a free-text search box. Common admin tasks like 'show all inactive accounts' or 'show all admins' are impossible without scanning the whole table.
  - fix: Add role and active (전체/활성/비활성) select filters to the toolbar Card and thread them into the /api/admin/users query string and query key. 
- **[users/design·contract]** The detail drawer renders stale list-row data instead of fetching the detail resource. UserDetail receives the row object from the table (Users.jsx:93) and never calls GET /api/admin/users/{id} (Users.jsx:111-118), so any detail-only field (active_session_count, and richer data the detail endpoint may add) is missing and the drawer can drift from server truth between refreshes.
  - fix: Fetch GET /api/admin/users/{id} with react-query when a row is selected and render from that response, falling back to the row for the initial paint. 
- **[settings/code·missing-feature]** The dry-run endpoint POST /api/admin/settings/{key}/dry-run (router.py:42-44, service.dry_run) — spec §14.4 step 2 pre-apply validation — is never called by the frontend. The user gets no validation preview; they only discover an invalid value after a live PUT fails (and for ui_branding the live PUT succeeds with corrupt data, see Critical). This is the safety net the object-editing bug most needs.
  - fix: Settings.jsx onSave() — before/instead of the live PUT, POST to /{key}/dry-run and show the {ok, restart_required} result (or the validation error) inline, requiring an explicit confirm before the real PUT. At minimum wire dry-run for object-typed settings. 
- **[settings/code·design]** The '적용'(restart) column (Settings.jsx:19) and the editor restart note (Settings.jsx:72) are dead: every one of the 8 registry specs sets restart_required=False (registry.py:88-105), so the badge always renders the same 'ok/정상' and the note is unreachable. Meanwhile effective_settings returns a genuinely useful `is_default` flag (service.py:86) telling whether a setting is still at its default or has been overridden — the UI drops it entirely, so admins cannot tell which settings have been changed from defaults.
  - fix: Settings.jsx:16-21 — replace the always-constant restart_required column with an is_default indicator (e.g. Badge '기본값' vs '변경됨'), or surface both. Keep restart_required only in the editor and only when true. 
- **[settings/design·ux]** The page gives no success feedback and no guard on destructive changes, breaking consistency with every other admin screen. Saving just silently closes the drawer (Settings.jsx:31,52) — unlike DataScreen which fires a useToast success toast on every save/action. Worse, maintenance_mode (blocks ALL normal users' new requests) and allowed_email_domains (gates user creation) are edited exactly like a retention-days row: pick 켜기/끄기, hit 저장, drawer closes, no confirmation, no warning. High-impact global switches deserve a confirm dialog and a visible success/toast.
  - fix: Wire useToast()/useConfirm() (already exported from kit.jsx) into Settings.jsx: toast '저장했습니다.' on save, and require a useConfirm() for maintenance_mode/allowed_email_domains changes. Settings.jsx:30-32, 50-54. (frontend/src/screens/Settings.jsx:50)
- **[settings/design·dead-affordance]** The '적용'(restart_required) column is dead and misleading: every SettingSpec in the registry has restart_required=False (registry.py:88-105), so this column renders a green '정상' badge on every single row forever, and the editor's restart warning at Settings.jsx:72 can never appear. A column whose value never varies is pure noise, and '적용: 정상' does not communicate 'no restart needed' to a user. It reads as meaningful status when it is constant.
  - fix: Remove the restart_required column (Settings.jsx:19) and the unreachable restart note (Settings.jsx:72), or only render the note/column conditionally if any setting actually requires restart. Better: surface the backend's is_default flag (returned by effective_settings but currently ignored) so users can see which settings differ from defaults. (frontend/src/screens/Settings.jsx:19)
- **[settings/design·a11y]** Read-only roles get a fully functional-looking editor that 403s. GET settings is allowed for operator and auditor (READ_ROLES, router.py:29), but PUT is admin/system_admin only (WRITE_ROLES). Settings.jsx imports no auth/role info and never gates the UI, so an operator/auditor can open a row, edit the value, and click 저장 — only to get a server 403. The whole edit affordance is dead for read-only roles.
  - fix: Read the current role (useAuth, as other screens do) and for non-write roles either hide the row-click/editor or render it read-only with the 저장 button removed. Settings.jsx:29-31, 56-59. (frontend/src/screens/Settings.jsx:29)
- **[settings/design·design]** Raw English boolean leaks into the Korean UI. The value column renders String(r.value) (Settings.jsx:18), so maintenance_mode shows literally 'true' / 'false' in the table instead of Korean. The kit already has statusText() which maps true→'예' / false→'아니오'; the bespoke render bypasses it. Task explicitly flags raw English/enum leaking.
  - fix: In the value column render, use statusText(r.value) for bool settings (or type-aware formatting) instead of String(r.value). Settings.jsx:18. (frontend/src/screens/Settings.jsx:18)
- **[settings/test·test-gap]** Per-endpoint RBAC is only tested for PUT (operator 403). dry-run and rollback require WRITE_ROLES (admin/system_admin) and versions requires READ_ROLES (operator/admin/system_admin/auditor). There is no test that operator/auditor get 403 on dry-run and rollback, nor that auditor gets 200 on GET /versions but user gets 403. Notably dry-run is gated to WRITE_ROLES, so a read-only auditor cannot even preview — untested either way.
  - fix: Add a parametrized role-matrix test over the settings sub-endpoints: for role in (user,operator,auditor,admin,system_admin) assert expected status for POST /{key}/dry-run and POST /{key}/rollback (403 unless admin+) and GET /{key}/versions (403 for user, 200 for auditor+). 
- **[settings/test·test-gap]** The is_default flag in the effective_settings response (service.py:80-89) is part of the GET contract but is never asserted. It should be True for keys never written and flip to False after a PUT. A regression in the stored_keys computation would silently mislabel every setting.
  - fix: Add test_is_default_flag: assert GET returns is_default is True for notification_retention_days before any change, then after PUT assert is_default is False for that key while other untouched keys remain True. 
- **[settings/test·test-gap]** Object-key update paths for allowed_email_domains and ui_branding are untested. allowed_email_domains has a validator requiring a non-empty list of domain strings (registry.py:66-70) and is claimed to affect user creation immediately; ui_branding has validate=None so it accepts arbitrary shapes. Only password_policy and session_policy object updates are covered.
  - fix: Add test_allowed_email_domains_rejects_bad_shapes (empty list, non-string entries, string without '.') -> 422, and test_allowed_email_domains_wiring: after tightening the domain list, POST /api/admin/users with an out-of-domain email -> 422. Add a test that ui_branding update round-trips and surfaces in the login/chat product name. 
- **[settings/test·test-gap]** coerce()'s int/error path in the editor is untested (Settings.jsx:47,52-53). Non-integer input throws '정수를 입력하세요.' and is shown inline, but Number('') === 0 (an integer) so an empty int field silently coerces to 0 and is sent; a server 422 error message is surfaced via setErr + role='alert' (line 73). None of these paths are verified.
  - fix: Add frontend tests: (a) entering '1.5'/'abc' in an int field on save shows the inline '정수를 입력하세요.' alert and does NOT call api; (b) a server 422 (rejected value) sets the role='alert' message to the server's error text; (c) empty int field behavior is asserted (documents the 0-coercion). 
- **[settings/test·test-gap]** Unauthenticated (401) access to settings is untested. Every test logs in first, so a regression that made GET/PUT /api/admin/settings reachable without a session would not be caught by this suite.
  - fix: Add test_settings_requires_authentication: with a fresh unauthenticated client, GET /api/admin/settings and PUT /api/admin/settings/{key} -> assert 401. 
- **[diagnostics+maintenance/design·missing-feature]** The Maintenance screen only offers an on/off toggle. The backend registry defines maintenance_message ('유지보수 공지 메시지' — the notice users see during maintenance), but this dedicated page never shows or lets you edit/preview it. An admin turning maintenance ON here has no control over, or even visibility into, what users will actually see. The message is only reachable via the generic raw key/value Settings table.
  - fix: On the Maintenance card, surface maintenance_message: show the current message and add an inline edit (PUT /api/admin/settings/maintenance_message) plus a preview of what users see, so this page fully serves maintenance-mode management end-to-end (Ops.jsx:29-64). (frontend/src/screens/Ops.jsx:29)
- **[diagnostics+maintenance/design·dead-affordance]** The sidebar renders 진단/유지보수 for every admin-console role, but the backend gates them tighter than READ: /api/admin/diagnostics/bundle requires admin/system_admin (health/router.py:48) and the maintenance toggle PUT requires WRITE_ROLES=admin/system_admin (settings/router.py:30). operator and auditor can open both pages and see the '진단 수집' and '유지보수 모드 켜기' buttons enabled, but clicking returns 403 — an error only appears after the click. Dead/deceptive affordance for read-only roles.
  - fix: Gate the action controls by role: for non-admin roles disable the buttons and show a '이 작업에는 관리자 권한이 필요합니다' note (or hide the nav items for those roles). Pass the current role from useAuth into these components (Ops.jsx:15,57). (frontend/src/screens/Ops.jsx:15)
- **[diagnostics+maintenance/design·dead-affordance]** The '복사' button is a silent failure in edge cases: onClick is `navigator.clipboard && navigator.clipboard.writeText(text).then(...)` with no else branch and no .catch(). If the Clipboard API is unavailable (insecure context / older browser) the click does nothing at all; if writeText rejects (permission denied) the '복사됨' confirmation never fires and the user gets no feedback — inconsistent with the app's toast pattern used elsewhere in this same file.
  - fix: Add a .catch that shows a toast('복사에 실패했습니다', 'error') and an else path (or a manual-copy fallback) when navigator.clipboard is missing, so success and failure both give feedback (Ops.jsx:16). (frontend/src/screens/Ops.jsx:16)
- **[users/test·bug]** list_users search is case-inconsistent and untested for display names. router.py:91 lowercases email (`func.lower(User.email).like(needle)`) but matches display_name case-sensitively (`User.display_name.like(f"%{q.strip()}%")`). The only search test (test_admin_users.py:85-88) searches an email substring, so display-name search has zero coverage and this asymmetry is a latent bug: searching 'kim' would miss a display_name 'Kim'.
  - fix: Add tests/integration/test_admin_users.py::test_list_users_search_display_name_case_insensitive: create display_name 'Kim Dev', GET ?q=kim, assert total==1. It will fail today, exposing router.py:91; fix by lowering display_name too (func.lower). 
- **[users/test·test-gap]** The list `active` query filter is untested. router.py:97-98 narrows by User.active, but test_admin_users.py::test_list_users_search_and_filters only exercises q and role. A regression removing/inverting the active filter would go unnoticed (the only 'active' filter test in the suite is for departments, test_admin_org.py:181, not users).
  - fix: Add tests/integration/test_admin_users.py::test_list_users_active_filter: create one active and one disabled user, assert GET ?active=false returns only the disabled one and ?active=true excludes it. 
- **[users/test·test-gap]** Session revocation on a direct role change is untested. service.py:214-220 revokes all sessions when a role actually changes, but only the disable path's revocation is tested (test_admin_users.py::test_disable_user_revokes_sessions). A privilege change (e.g., operator→user) that failed to invalidate live sessions would be a real privilege-persistence bug with no guard.
  - fix: Add tests/integration/test_admin_users.py::test_direct_role_change_revokes_target_sessions: target logs in (GET /api/me == 200), admin PATCH role user→operator (200, non-gated), assert target's GET /api/me == 401. 
- **[users/test·test-gap]** notion_mapping_status is only ever tested in its 'unmapped' default. test_admin_users.py::test_notion_mapping_verify_returns_status asserts the verify endpoint returns 'unmapped'; nothing seeds a UserNotionMapping and asserts the list row / detail (_notion_status_map, router.py:58-66,111-114,187) reports the real status ('mapped'/'pending'/etc). The join that decorates every list row is effectively untested for any non-empty case.
  - fix: Add tests/integration/test_admin_users.py::test_list_and_detail_report_mapped_notion_status: insert a UserNotionMapping(status='mapped') for a user, assert both the list row and GET /users/{id} return notion_mapping_status=='mapped'. 
- **[users/test·test-gap]** Create with non-default flags is untested. schemas.py:23-24 accept active and must_change_password, and router.py:151-152 pass them through, but no test creates a user with active=False (cannot log in until enabled) or must_change_password=False (login without a forced change). test_create_user_returns_temp_password_once only asserts the default must_change_password=True.
  - fix: Add test_create_user_inactive_cannot_login (create active=false → login 403/blocked, then enable → login works) and test_create_user_no_forced_change (create must_change_password=false with a chosen password → login returns must_change_password False). 
- **[users/test·test-gap]** get_user_detail's active_session_count field is untested. router.py:181-191 computes and returns active_session_count for the detail view, but tests only cover the separate /sessions endpoint (test_admin_users.py::test_sessions_listing_and_revoke). No test asserts the detail payload's count reflects live sessions, so an off-by-one or a broken join filter (revoked_at.is_(None)) would slip through.
  - fix: Add tests/integration/test_admin_users.py::test_user_detail_reports_active_session_count: log the target in once, GET /api/admin/users/{id}, assert active_session_count == 1; revoke, assert == 0. 
- **[users/test·test-gap]** Recovering an archived user via search (archived=true + q) is untested. Tests cover that archived users are hidden by default and from search (test_admin_user_archive.py:36-54) and that archived=true lists them, but never that archived=true combined with q returns a specific archived match — the exact path an admin needs to find and restore one archived account by name/email (router.py:77-92).
  - fix: Add tests/integration/test_admin_user_archive.py::test_archived_user_findable_with_archived_true_and_query: archive a user, GET ?archived=true&q=<name>, assert total==1 and the archived_at is set. 
- **[diagnostics+maintenance/code·dead-affordance]** The maintenance toggle button (Ops.jsx:57-59) is always rendered enabled, but the backend PUT /api/admin/settings/{key} requires WRITE_ROLES (admin, system_admin) while GET /api/admin/settings allows READ_ROLES (operator, admin, system_admin, auditor). An operator or auditor loads the page fine, sees a full-strength 'danger'-styled '유지보수 모드 켜기' button, clicks it, confirms the destructive dialog, and only then gets a 403 surfaced as an error toast. A read-only role is shown a working-looking destructive control that always fails. The component never calls useAuth() to check role.
  - fix: Import useAuth() in Ops.jsx and, for roles not in {admin, system_admin}, either hide the toggle or render it disabled with an explanatory note (e.g. '변경 권한이 없습니다'). Server RBAC stays authoritative; this only removes the misleading affordance. Also consider role-filtering the NAV entries in App.jsx (App.jsx:26-27). 
- **[diagnostics+maintenance/code·missing-feature]** The Maintenance page (Ops.jsx:46-63) only toggles maintenance_mode. The backend pairs it with a maintenance_message setting (registry.py:96-97) that is the exact text users see when blocked (app/settings/gate.py:31 -> maintenance_message()). The page neither lets the admin edit that notice nor even displays what message users will see while maintenance is on. The confirm copy says '사용자 쓰기가 차단됩니다' but the actual user-facing notice is unreachable from this screen; an admin must hunt for maintenance_message in the generic Settings table. The page does not fully serve its stated purpose.
  - fix: Add a maintenance_message editor/preview to the Maintenance card: read q.data.settings.maintenance_message.value, show it, and allow editing via the same PUT /api/admin/settings/maintenance_message pipeline before/while enabling the mode. 
- **[diagnostics+maintenance/code·bug]** The Diagnostics copy control (Ops.jsx:16) is 'navigator.clipboard && navigator.clipboard.writeText(text).then(...)' with no .catch and no fallback. If navigator.clipboard is undefined the click is a silent no-op with zero feedback; if writeText rejects (document not focused / permission denied) the rejection is unhandled and the '복사' label never changes, so the user cannot tell it failed. Copy is the ONLY export path for the bundle (no download, no select-all helper), so a silent failure defeats the whole point of the page (getting the bundle to support).
  - fix: Add a .catch that surfaces an error toast (useToast is already imported in this file's kit dependency set), and provide a fallback when navigator.clipboard is unavailable — e.g. select the <pre> text or offer a download-as-file affordance so the bundle is always exportable. 
- **[notification-bell/test·test-gap]** Unauthenticated access is untested for all four notification endpoints. Every route depends on get_current_user, and the frontend queries use retry:false with special 401 handling (lib/api.js throws status 401 -> 'login required'), but no test asserts GET /api/notifications, GET /unread-count, POST /read-all, POST /{id}/read return 401 when no session cookie is present. A regression making any of them public would leak one user's notifications.
  - fix: Add test_notifications_require_auth (parametrized over the 4 routes) using a fresh TestClient with no login: assert 401 for each, and that no notification data is returned in the body. 
- **[notification-bell/test·test-gap]** Pagination and newest-first ordering are unverified, yet the bell hard-codes ?page_size=8 and shows '최근 알림'. test_read_all_marks_every_unread creates 12 rows but only checks unread-count, never the list. Nothing asserts that page_size caps items while total reflects all rows, that page 2 returns the next slice, or that ordering is created_at DESC, id DESC (the tie-break the router applies). A broken ORDER BY or offset math would silently show wrong/stale notifications in the popover.
  - fix: Add test_list_respects_page_size_and_total: create 12 rows, GET /api/notifications?page_size=8, assert len(items)==8 and total==12; GET page=2 asserts remaining 4. Add test_list_orders_newest_first: create rows at increasing fake_clock times, assert items[0] is the most recent; create two rows at the SAME timestamp and assert the id-DESC tie-break ordering. 
- **[notification-bell/test·test-gap]** The unread_only query param on GET /api/notifications (router.py:37, unread_only: bool = Query(default=False)) has no test at all. Its filtering branch (adds .where(read_at.is_(None))) and its effect on the returned total are completely uncovered.
  - fix: Add test_list_unread_only_filters: create 3 read + 2 unread notifications, GET /api/notifications?unread_only=true, assert total==2 and every returned item has read_at is None; GET without the flag returns all 5. 
- **[notification-bell/test·test-gap]** The JSON response contract from _view() (router.py:19) is untested field-by-field. The frontend depends on exact shapes: read_at null when unread vs ISO string when read, related_object_type/related_object_id round-tripping (drives OBJ_ROUTE deep-link), created_at as ISO string (drives fmtDateTime), plus body/type/title/id. Existing tests only check title and type. A renamed/dropped field (common in the vanilla->React port) would break the bell without failing any test.
  - fix: Add test_notification_view_contract: create a notification with related=('approval','abc123'), assert the item dict has exactly the expected keys and that read_at is None before read and a parseable ISO string after read, related_object_type=='approval', related_object_id=='abc123', created_at parses as ISO. Guard the deep-link field the UI routes on. 
- **[diagnostics+maintenance/test·test-gap]** The audit record written on a settings change is untested. update_setting emits action='setting.update' with before/after (app/settings/router.py:59-63), and rollback emits 'setting.rollback' (router.py:91-94), but grep of tests/integration/test_audit_api.py finds no 'setting.' assertions. No test confirms toggling maintenance_mode (or any setting change) produces an audit entry, nor that before/after are masked. This is the audit trail for a production-impacting action (blocking all user writes).
  - fix: Add test_settings_change_is_audited in test_settings_api.py or test_audit_api.py: PUT maintenance_mode value=true, then GET /api/admin/audit and assert an entry exists with action='setting.update', object_type='app_setting', object_id='maintenance_mode', and before/after present (and not leaking any sensitive value). 
- **[diagnostics+maintenance/test·test-gap]** The diagnostic bundle's own payload (beyond secret masking) is untested. build_diagnostic_bundle assembles recent_job_errors from failed jobs incl. last_error, limit 20 (app/health/service.py:188-196) and integration_errors listing ONLY down integrations (service.py:197-202). test_diagnostic_bundle_masks_and_excludes_secrets only checks a plaintext secret is absent; nothing asserts these fields are populated correctly or that generated_at exists. A regression dropping recent_job_errors or including up integrations would go unnoticed.
  - fix: Add test_diagnostic_bundle_reports_errors: seed a Job with status=failed and last_error='boom' and an Integration with last_health_status='down' plus one with 'up'; GET bundle and assert bundle['recent_job_errors'][0]['error']=='boom', bundle['integration_errors']=={down_name:'down'} (up integration absent), and 'generated_at' present. 
- **[diagnostics+maintenance/test·test-gap]** The dashboard jobs_24h arithmetic (embedded verbatim in the diagnostics bundle) is untested. build_dashboard computes success_rate_pct and avg_processing_seconds from jobs in the 24h window (app/health/service.py:99-128), including the None-when-empty branches. Existing dashboard tests assert only structural presence ('jobs_24h' in body) and component/heartbeat states; no test seeds jobs to verify the success-rate or average-duration math, or the empty->None fallbacks.
  - fix: Add test_dashboard_jobs_24h_math: seed 4 jobs created within 24h (3 succeeded, 1 failed with started_at/finished_at set) via db + fake_clock; assert jobs_24h['success_rate_pct']==75.0 and avg_processing_seconds equals the mean of the finished durations; separately assert both are None when there are no jobs. 
- **[diagnostics+maintenance/test·test-gap]** recent_critical_audit filtering is untested. build_dashboard returns the last 5 audit rows whose action is in CRITICAL_ACTIONS, newest-first (app/health/service.py:25-28, 130-139). No test seeds audit rows to confirm non-critical actions are excluded, the limit of 5 is enforced, or descending order. This drives the operator's at-a-glance 'what just changed' view on the diagnostics page.
  - fix: Add test_dashboard_recent_critical_audit: seed 6 critical (e.g. user.disable, backup.restore) + several non-critical audit rows with increasing timestamps; assert dashboard['recent_critical_audit'] has exactly 5 items, all with critical actions, ordered newest-first, and that a non-critical action never appears. 
- **[notification-bell/design·missing-feature]** The notification body is dropped. The backend _view returns `body` (router.py:24) but the popover renders only type, title, and time (NotificationBell.jsx:94-96). The actual message detail carried in `body` is never shown, so notifications can be uninformative — the user sees a bare title with no context, and there is no detail/expand view anywhere.
  - fix: Render n.body under the title (line-clamped) in the item, e.g. add a <span className="noti-item-body">{n.body}</span> at NotificationBell.jsx:96 with 2-line clamp CSS. 
- **[notification-bell/design·dead-affordance]** openItem (lines 67-74) deep-links to the coarse list route (OBJ_ROUTE, e.g. /approvals) and never uses related_object_id (returned by the backend, router.py:27) to reach the specific object — the user is dropped on a list and must hunt for the item. Additionally, when related_object_type is null or unmapped (e.g. type 'system'), `route` is undefined so the click just closes the popover with no navigation and no feedback: a dead click on something styled as an actionable button.
  - fix: Use related_object_id to deep-link to the specific record where the target screen supports it (NotificationBell.jsx:72-73). For non-navigable notifications, don't render them as clickable, or give a visual cue / toast so the click isn't a silent no-op. 
- **[notification-bell/design·a11y]** Read vs unread is conveyed only by a ~7% primary-color background tint (.noti-item.is-unread, screens.css:45) which is nearly identical to the 5% hover tint (line 44). There is no unread dot, bold weight, or text marker — a color-only distinction with very low contrast that is hard to perceive and fails accessibility guidance on not relying on color alone.
  - fix: Add a non-color unread indicator: an unread dot in the item, bold title for unread, and/or a stronger left border/background on .noti-item.is-unread (screens.css:45). 
- **[notification-bell/code·missing-feature]** The full notifications page (registry key 'notifications' rendered by DataScreen) is entirely read-only: the config has no `actions` and no `headerActions`, and a row click only opens a read-only detail Drawer. Yet the backend fully supports POST /api/notifications/{id}/read and POST /api/notifications/read-all (used by the popover). So on '전체 알림 보기' you cannot mark a notification read, cannot mark-all, and cannot deep-link to the related object. It also has no pagination, so DataScreen fetches only the default page_size=20 — 'all notifications' silently truncates at 20.
  - fix: Add to the notifications registry config: a row action { label: '읽음', when: r => !r.read_at, path: r => '/api/notifications/' + r.id + '/read' } and a headerAction { label: '모두 읽음', path: () => '/api/notifications/read-all', confirm: '...' }. Consider a page_size param / pagination for the full list. (frontend/src/screens/registry.js:279)
- **[notification-bell/code·bug]** badgeCol('read_at', '읽음') passes the raw read_at value to Badge, which routes through statusText (kit.jsx:26-34). read_at is either null or an ISO timestamp string. Null renders as '알 수 없음' (unknown) for UNREAD rows, and a read row renders the raw ISO timestamp (e.g. '2026-07-18T16:21:23.704973') as the badge text because that string is not in STATUS_TEXT. The read-status column therefore never shows a clean 읽음/안읽음 — it shows 'unknown' for unread and a garbage timestamp for read.
  - fix: Replace with a boolean-derived column, e.g. { key:'read_at', label:'읽음', render: r => r.read_at ? '읽음' : '안읽음' } (or a mapped badge with an explicit kind), rather than badging the timestamp itself. (frontend/src/screens/registry.js:283)
- **[notification-bell/code·design]** The full page uses col('type','유형'), which renders the raw English enum (e.g. 'approval_requested', 'job_failed', 'schedule_failed') directly to end users. The popover translates these via TYPE_KO in NotificationBell.jsx:11-15, so the two surfaces are inconsistent and the full page leaks internal enum strings.
  - fix: Extract TYPE_KO to a shared module and use mapCol('type','유형', TYPE_KO) in the notifications registry config (same pattern already used for jobs via JOB_TYPE). (frontend/src/screens/registry.js:283)
- **[workflows/feature·bug]** The '테스트' action reports success even when the reachability test fails. provider_n8n.test (provider_n8n.py:46-64) returns HTTP 200 with {status:'reachable'|'unreachable', detail:'HTTP 404'|'timeout'|'connection_error'} — it only raises on truly unexpected errors. runAction → announce (DataScreen.jsx:29-41) shows a green '테스트 완료' toast whenever no exception is thrown and status !== 'approval_pending', so an UNREACHABLE workflow yields a success toast, and the useful 'detail' string is discarded entirely. The only real signal is the last_test_status badge after refresh.
  - fix: In the test action (or announce), inspect res.status: when it is not 'reachable', toast an error/warning that includes res.detail (e.g. '테스트 실패: timeout'). Requires the test action to carry a custom result handler or announce() to special-case reachability results. DataScreen.jsx:29-41 / registry.js:112. 
- **[workflows/feature·empty-state]** The row-click detail drawer shows only the 6 table columns (name, operation_mode, approval_required, enabled, last_test_status, webhook_url) because DataScreen.jsx:95-100 renders config.columns exclusively. Fields the user sets at create — owner, purpose, http_method — and useful metadata returned by workflow_view (config_version, last_test_at, created_at, updated_at) are not viewable at all; you must open '수정' just to re-read purpose/owner, and last_test_at/timestamps/config_version can never be seen. So the screen shows WHETHER the last test passed but not WHEN, and hides the owner/purpose you entered.
  - fix: Render a full field list in the detail drawer (a config.detailFields or the union of create+column fields) rather than only config.columns, so owner/purpose/http_method/config_version/last_test_at/timestamps are visible. DataScreen.jsx:95-100. 
- **[policies/feature·missing-feature]** The detail view omits published_at and created_by, both returned by _policy_view (router.py:102-103). For a version-management screen, 'when did this version go live' (published_at) and 'who authored it' (created_by) are core accountability facts. Because the detail drawer only renders columns and the columns list is name/version/status/created_at (registry.js:141), neither is ever shown.
  - fix: Add published_at (dateCol) and created_by columns to registry.js:141, or surface them via the detail-fields mechanism proposed for the content viewer. File: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\registry.js:141. 
- **[policies/feature·ux]** The list renders every version of every policy as a separate flat row (backend returns up to 500 rows ordered by name, version desc — router.py:123) with no grouping, no status filter, and no 'current published version' indicator. As versions accumulate (draft/test/review/published/archived all coexist), the table fills with archived clutter and it becomes hard to identify the one live policy for a given name.
  - fix: Add a status filter (or default to hiding archived) and/or group rows by name with the latest/published version highlighted. Consider using the backend's ?name= filter (router.py:117) for a per-name version history view. File: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\registry.js:137-155. 
- **[prompts/feature·contract]** The status column shows RAW ENGLISH enum for test/review/archived. Badge localization (kit.jsx STATUS_TEXT, lines 8-17) maps only draft→'초안' and published→'발행됨'; it has no entry for 'test', 'review', or 'archived', so statusText falls through to the raw value. Since two of the four exposed actions transition INTO 'test' and 'review' (registry.js:131-132), the most common working states routinely display untranslated 'test'/'review' badges (also uncolored: STATUS_KIND lacks these keys too).
  - fix: Add test/review/archived (and draft for color) to STATUS_TEXT and STATUS_KIND in kit.jsx:8-25. 
- **[prompts/feature·dead-affordance]** Backend VALID_TRANSITIONS (models.py:20-26) allows test→draft, review→draft (go back to re-edit) and →archived from draft/test/review/published, but the UI exposes none of these. Consequences: (1) a prompt in test/review with a typo is a workflow dead-end — it can't be edited (edit is draft-only), can't be sent back to draft (no button), and can't be archived (no button), only forced forward; (2) the 'archived' terminal state is completely unreachable from the UI, so prompts can never be retired.
  - fix: Add '초안으로'(→draft, when status is test/review) and '보관'(→archived, from draft/test/review/published) actions to registry.js prompts.actions, each POSTing /transition with the target status. 
- **[prompts/feature·missing-feature]** User-entered and audit-relevant fields are collected/returned but never displayed. 'purpose' (용도) is a create field (registry.js:122) yet is never echoed back anywhere (not a column, drawer shows only columns). 'published_at' and 'created_by' are returned by _prompt_view (router.py:88,86) but are invisible in the UI. Same root cause as the content finding: the drawer renders only config.columns.
  - fix: Add purpose/published_at (and optionally created_by) as columns or detail fields; published_at should use dateCol. registry.js:119. 
- **[schedules/feature·missing-feature]** Dry-run preview is not exposed. Backend POST /api/admin/schedules/{id}/dry-run returns payload_preview, target, and the next 3 fire times (UTC) with no execution — the natural way to validate a cron expression before enabling. No action wires it, so admins must enable-and-wait to find out if a cron expression fires when they expect.
  - fix: Add a '미리보기(dry-run)' action to the schedules actions array pointing at /api/admin/schedules/{id}/dry-run and render next_fire_times_utc + payload_preview (formatted to KST) in the resulting drawer/toast. (frontend/src/screens/registry.js:196)
- **[schedules/feature·ux]** target_ref is a free-text 'text' field ({name:'target_ref', label:'대상 ID'}, registry.js:194/208) with no guidance about valid values per target_type. Backend requires target_ref to be an existing Workflow ID when target_type=workflow, or exactly 'noop' when target_type=system (SYSTEM_TARGETS). There is no picker and the help text is empty, so users will paste wrong values and hit 422 ('target_ref에 해당하는 Workflow가 없습니다' / 'system target은 [noop] 중 하나여야 합니다').
  - fix: Add help text explaining target_ref rules per target_type (workflow → a Workflow ID from the 워크플로 screen; system → 'noop'), or make target_ref a select populated from the workflows list when target_type=workflow, mirroring how the domain actually constrains it. (frontend/src/screens/registry.js:194)
- **[schedules/feature·missing-feature]** The friendly 'preset' scheduling option is not exposed anywhere. Backend ScheduleRequest.preset accepts daily/weekly/monthly and maps to a cron_expression via cron.PRESETS, letting non-technical admins schedule without writing cron syntax. Neither the create nor edit form offers a preset field, so every recurring schedule forces the user to hand-write raw cron (e.g. '0 9 * * 1').
  - fix: Add a 'preset' select (daily/weekly/monthly + '직접 입력') to the create/edit forms; when set, it supersedes cron_expression per backend _validate_and_normalize. (frontend/src/screens/registry.js:186)
- **[approvals/feature·dead-affordance]** Self-approval is a dead affordance that directly contradicts the screen's own help text. The help says '본인 요청은 본인이 승인할 수 없어요' (you cannot approve your own request), but the 승인 action's `when` checks only status. An admin viewing their own pending request sees the 승인 button, clicks it, and the backend 403s (ForbiddenError '자기 승인을 허용하지 않습니다') unless the self_approval_allowed flag is on. The button should not appear for one's own request.
  - fix: Add a current-user check to the approve action's `when` (e.g. when: (r) => !DONE.includes(r.status) && r.requested_by !== currentUserId), which requires threading the current user id into the registry config. 
- **[approvals/feature·empty-state]** No status filter is surfaced, and the empty-state text lies about what the list shows. Backend list supports ?status= (pending/approved/rejected/expired/cancelled) but the screen never sends it, so it renders ALL statuses mixed together, newest-first. The emptyTitle says '대기 중인 승인 요청이 없습니다' (no PENDING approval requests) — but the list is NOT filtered to pending: if only decided rows exist, the list is non-empty and that message never shows; and there is no way to view just the pending queue an approver actually needs to act on.
  - fix: Add a status filter (tabs or a select) that sets ?status=pending by default and forwards it to the endpoint, or make the screen default to pending. Reconcile emptyTitle/emptyHelp with whatever the list is actually filtered to (frontend/src/screens/registry.js:232-236; backend supports status at app/approvals/router.py:51). 
- **[approvals/feature·missing-feature]** No pagination. The list endpoint returns total/page/page_size (DEFAULT_PAGE_SIZE=20) but DataScreen ignores them and renders only items[]. Approvals are an append-only ledger — approved/rejected/expired/cancelled rows all persist — so after 20 total rows, older approvals become unreachable. The '검색' box only filters the 20 rows already loaded (client-side JSON.stringify match), so historical approvals cannot be found at all.
  - fix: Add pagination controls (next/prev using total & page_size from the response) to DataScreen, and/or wire server-side search. Acute for approvals given it accumulates history (frontend/src/screens/DataScreen.jsx:51; envelope at app/core/pagination.py). 
- **[runners/feature·bug]** The 헬스/테스트 actions give misleading positive feedback. runAction always shows a green success toast (a.label + ' 완료') on any non-throwing response (DataScreen.jsx:33-41; announce only special-cases approval_pending, lines 29-32). But POST /{id}/health returns 200 with {status:'down'} for an unreachable runner, and POST /{id}/test returns 200 with {ok:false, status_code:500} for a 5xx target (app/runners/router.py:178-220). So a failed health check or a failing test both surface as green '헬스 완료'/'테스트 완료'. The rich result the backend returns (latency_ms, status_code, detail like 'timeout'/'connection_error', checked_url) is discarded entirely.
  - fix: In runAction, inspect the response and toast accordingly (e.g. res.status/res.ok/res.status_code) — show an error/warn toast and the HTTP status/detail when status=='down' or ok===false, instead of a blanket success. Consider surfacing latency/status_code in the message. 
- **[integrations/feature·missing-feature]** The capabilities field has no UI whatsoever. capabilities is a first-class config field (schemas.py:24, model capabilities_json models.py:27) seeded by discovery.py with real values ({webhooks:true}, {chat:true}, {tickets:true}, {interpretation:true}). It is absent from the integrations create form (registry.js:47-55) and there is no edit block, and it is not displayed anywhere. It can only ever be set via the discovery seed or a raw API call — never viewed or changed through the admin screen.
  - fix: Add a { name: 'capabilities', label: '기능(JSON)', type: 'json' } field to the integrations create fields (registry.js:47-55); the FormModal already parses/validates JSON (kit.jsx:226). Also display it in the detail view (see previous finding). 
- **[integrations/feature·empty-state]** The integrations config is the only connectivity screen with no empty-state guidance. registry.js integrations block (lines 41-60) omits emptyTitle and emptyHelp, unlike every sibling (runners registry.js:64, workflows registry.js:87, prompts/policies/templates/schedules etc.). On a fresh DB before discovery seeding runs, the list is empty and EmptyState (DataScreen.jsx:81-83) falls back to the generic '표시할 항목이 없습니다' with no help text — no explanation of what an integration is or that '+ 외부 연동 추가' registers services like n8n/Claude runners.
  - fix: Add emptyTitle (e.g. '등록된 외부 연동이 없습니다') and emptyHelp (e.g. "'+ 외부 연동 추가'로 이 시스템이 불러다 쓰는 외부 서비스(n8n, Claude 러너 등)를 등록하고 상태를 점검하세요.") to registry.js:41-43, matching the runners/workflows pattern. 
- **[templates/feature·missing-feature]** The create form (registry.js:161-166) offers only name, description, target_type, target_ref, but the backend POST TemplateRequest also accepts input_schema, prompt_id, policy_id, approval_policy (router.py:30-38). So a template cannot be fully configured at creation — you must create it 'bare' and then immediately open '수정'(PUT) to attach a prompt/policy/input schema/approval policy (the edit form at registry.js:169-178 has all 8). Nothing tells the user this second step is required.
  - fix: Add prompt_id, policy_id, input_schema, approval_policy to `create.fields` (mirroring `edit.fields`), or add help text stating configuration continues via edit after creation. File: frontend/src/screens/registry.js:161-166. 
- **[templates/feature·dead-affordance]** The templates screen shows create ('+ 템플릿 추가'), '수정', and '활성화/비활성화' buttons to every admin-console role, but the backend restricts these to WRITE_ROLES = admin/system_admin (router.py:27), while READ_ROLES also include operator and auditor (router.py:26). The frontend does no role gating for admin sub-screens — App.jsx (AdminBody, lines 150-174 / Layout 180-190) only redirects role==='user'; operator/auditor get the full console. Every create/edit/enable/disable submit for those roles returns 403, so all write buttons are dead affordances for the screen's own read audience.
  - fix: Gate create/edit/actions by role (hide or disable the buttons for non-WRITE roles), threading the current user's role from auth into DataScreen. File: frontend/src/app/App.jsx:150-174 and frontend/src/screens/DataScreen.jsx:55-65,91-93. 
- **[templates/feature·ux]** target_ref, prompt_id, and policy_id are free-text ID inputs with no picker, even though workflows/runners/prompts/policies are all admin-managed lists in the same console. The backend only validates existence after submit (_validate_references → ValidationAppError, router.py:83-93), so the user must leave the screen, find, and paste a raw UUID. Worse, the create target_ref field (registry.js:165) and the edit prompt_id/policy_id fields (registry.js:174-175) have NO help text at all — unlike the documents screen (registry.js:224) which explicitly tells the user where to find the workflow ID.
  - fix: Replace the ID text fields with selects populated from /api/admin/workflows|runners|prompts|policies; at minimum add help text pointing to where each ID is found, matching the documents screen. File: frontend/src/screens/registry.js:165,173-175. 
- **[documents/feature·dead-affordance]** The 'published_ref' column is labeled '발행 링크' (publish link) but is rendered as non-clickable plain text. It uses plain col('published_ref','발행 링크') (registry.js:230); DataTable (kit.jsx:117) and the detail drawer (DataScreen.jsx:98) render it as String(value), not an <a>. Worse, clicking the row (or the URL text) opens the detail drawer, not the Notion document. A column literally called a 'link' is not a link, so users must hand-copy the Notion URL.
  - fix: Give published_ref a render that outputs an anchor: render:(r)=> r.published_ref ? <a href={r.published_ref} target="_blank" rel="noopener">문서 열기</a> : '—' (stopPropagation so it doesn't trigger the row-detail click). registry.js:230. 
- **[documents/feature·design]** The 'preview_ready' status renders as raw English. STATUS_TEXT (kit.jsx:8-17) localizes every other document status (pending→'대기', quality_failed→'품질 미달', awaiting_approval→'승인 대기', published→'발행됨', failed→'실패') but has no entry for preview_ready — so the terminal state of the '미리보기만' mode shows the untranslated enum 'preview_ready' in the status badge.
  - fix: Add preview_ready to STATUS_TEXT (e.g. '미리보기 준비됨') and STATUS_KIND (e.g. 'info') in kit.jsx:8-25. 
- **[documents/feature·missing-feature]** workflow_id and config are never shown back to the user. workflow_id is a required create input and config is user-submitted, and both are returned by generation_view, but neither appears in the columns nor the 5-field detail drawer. When browsing generation history a user cannot tell which workflow or what settings produced a given row, making the '기록' (history) largely non-diagnostic.
  - fix: Add workflow_id and config (pretty-printed JSON) to the documents detail rendering. registry.js:230 / detail view. 
- **[documents/feature·ux]** The '+ 문서 생성' form makes the user hand-type an opaque workflow ID. The workflow_id field is free text whose help says to go to the workflows screen, look up the ID, and type it (registry.js:224); period and config are also free-form (raw JSON). There is no workflow picker. The backend rejects unknown IDs with a 422 ('대상 Workflow가 없습니다', service.py:142), so the error-prone manual step surfaces only after submit.
  - fix: Replace the workflow_id text field with a select populated from GET /api/admin/workflows (enabled write workflows). registry.js:222-228. 
- **[documents/feature·missing-feature]** Backend list capabilities are unused and history beyond the first page is unreachable. GET /api/admin/documents supports server-side status filtering (?status=, router.py:44-68) and pagination (PageParams), but the screen exposes neither: DataScreen fetches page 1 only (no page param, DataScreen.jsx:20-26) and offers just a generic client-side search box that filters only the already-loaded page (DataScreen.jsx:52-53). Once there are more than page_size (default 20) generations, older records — which the empty-state promises will 'remain here' — can never be seen or searched. (Shared DataScreen limitation, but it directly defeats this screen's stated purpose as a durable history.)
  - fix: Add a status filter control wired to ?status= and pagination (pass page params + a pager), or at minimum surface server search. DataScreen.jsx query + registry.js documents config. 
- **[departments/feature·missing-feature]** Delete dead-ends with no recovery path. The backend blocks deleting an in-use department (app/org/service.py:106-121, 409 '이 부서를 쓰는 사용자가 N명 있어...'), but the screen gives no way to reach those users. The 'user_count' (소속 인원) column shows only a number with no drill-down, the Users screen (frontend/src/screens/Users.jsx) can only search by email/name (backend app/users/router.py:69 has role/active filters but NO department_id filter), and it hides archived users by default (archived=false, router.py:77) while usage_count deliberately counts archived users too (service.py:65-72). So an admin told 'N users use this department' cannot list those N (especially archived ones) to reassign them, making the department permanently un-deletable through the UI.
  - fix: Add a department_id (and title_id) filter param to GET /api/admin/users (app/users/router.py:69) and a '이 부서 사용자 보기' link from the department detail drawer that navigates to Users filtered by that dept; surface the archived toggle in Users.jsx so archived FK-holders can be found; and clarify in the departments help/tooltip that 소속 인원 includes archived accounts. 
- **[departments/feature·dead-affordance]** The domain's recommended safe action (deactivate) is buried while the discouraged one (delete) is headlined. The backend treats setting active=false as the graceful alternative to deletion and its 409 error even tells the user '비활성으로 두면...' (service.py:117-121). Yet the only first-class row action in registry.js:249 is the danger '삭제' button, which for any in-use department almost always fails; deactivation is reachable only via 행 클릭 → 수정 → uncheck '사용' → 저장 (registry.js:248). Every peer screen (integrations/runners/workflows/templates/schedules) exposes one-click enable/disable via the onoff() helper (registry.js:33-36). After a blocked delete, the toast advises deactivating but offers no control to do it.
  - fix: Add first-class 활성화/비활성화 row actions to the departments (and job-titles) config in registry.js:249, mirroring onoff() but PATCHing {active:false}/{active:true} conditioned on r.active (org uses PATCH, not /enable /disable endpoints), so the recommended action is one click; keep 삭제 as the secondary/least-prominent action. 
- **[departments/feature·empty-state]** No empty-state guidance on a foundational, zero-seeded screen. The departments config (registry.js:243-250) defines no emptyTitle/emptyHelp, so a fresh install (migration 0015 seeds nothing — it only migrates pre-existing free-text values) shows the generic DataScreen fallback '표시할 항목이 없습니다' with no help text (DataScreen.jsx:81-83). Almost every peer screen provides specific empty copy. This matters because departments feed the user-creation form's '부서' dropdown (Users.jsx:26,53), which useNameOptions renders as only '— 없음 —' when the list is empty, so admins get no hint that they must seed departments first.
  - fix: Add emptyTitle/emptyHelp to the departments (and job-titles) config in registry.js:243, e.g. emptyTitle:'등록된 부서가 없습니다', emptyHelp:"'+ 부서 추가'로 부서를 만들면 사용자 폼의 '부서' 목록에 바로 나타납니다." Optionally seed a few example departments in migration 0015 for fresh installs. 
- **[job-titles/feature·empty-state]** The "job-titles" config defines no emptyTitle/emptyHelp. The job_titles table ships EMPTY on a fresh install (migration 0015 only creates the table and migrates pre-existing user strings; there is no seed data), so the very first thing an admin sees is DataScreen's generic fallback "표시할 항목이 없습니다" with NO help text (config.emptyHelp is undefined) explaining what job titles are for or that they feed the user form's '직책' dropdown. Every other list screen that starts empty (runners, workflows, prompts, policies, templates, schedules, documents, jobs, audit, backup, notifications) provides domain-specific emptyTitle+emptyHelp; departments and job-titles are the only two exceptions.
  - fix: Add to the config (registry.js:251): emptyTitle: "등록된 직책이 없습니다", emptyHelp: "'+ 직책 추가'로 직책을 만들면 사용자 폼의 '직책' 목록에서 바로 고를 수 있습니다." The empty-state create button already works (config.create present), so only the guidance text is missing. (frontend/src/screens/registry.js:251)
- **[backup/feature·missing-feature]** error_message is captured by the backend for every failed backup (service.py:63,66,81 — e.g. 'restore verify 실패: ...', exception text) and returned in backup_view, but it is not a column and the detail drawer only renders config.columns (DataScreen.jsx:95), so error_message is invisible everywhere. A user sees a red '실패' badge with zero explanation of why. Same for verified_at (can't tell WHEN last verified), checksum (integrity hash), created_by (who ran it), and backup_type — all returned by the API, none ever shown. The DataScreen comment claims the detail modal shows '전체 필드' but it does not.
  - fix: Add error_message (and verified_at, checksum, created_by) to registry.js 'backup' columns, or extend the DataScreen detail drawer to render all fields of the row rather than only config.columns. At minimum surface error_message for failed rows. 
- **[backup/feature·design]** The '크기' (size) column uses col('size_bytes', '크기') (registry.js:304) which renders the raw byte integer via the default String() renderer — e.g. a 20 MB backup shows as '20971520' instead of '20 MB'. There is no human-readable byte formatter in the codebase (grep for formatBytes/fmtBytes returns nothing). Raw byte counts are unreadable for the stated purpose of at-a-glance backup review.
  - fix: Add a byte-formatting render helper (KB/MB/GB) in format.js and use it for the size_bytes column, e.g. { key:'size_bytes', label:'크기', render:(r)=>fmtBytes(r.size_bytes) }. 
- **[jobs/feature·missing-feature]** DataScreen fetches config.endpoint with no ?page/?page_size (DataScreen.jsx:24), so it shows only page 1 (DEFAULT_PAGE_SIZE=20, pagination.py) and renders no pager even though the response carries total/page/page_size. The jobs table gains a row for every chat message, document generation, schedule run, and sync, so it grows quickly; older jobs (including older failures) become permanently invisible. This compounds the missing stats and missing filters — the operator can only ever see the newest 20 rows.
  - fix: Add pagination controls to DataScreen driven by query.data.total/page/page_size, appending ?page= to the endpoint (and to the query key). Applies to all list screens but is most damaging on high-volume jobs/audit. 
- **[jobs/feature·missing-feature]** _job_view returns duration_ms, started_at, finished_at, available_at, max_attempts, idempotency_key, user_id, conversation_id, message_id (router.py:40-57), but the detail drawer only iterates config.columns (DataScreen.jsx:95) — the 5 list columns. So on row click the operator never sees how long the job ran (duration_ms), when it started/finished, when a backed-off retry will fire (available_at), or the correlation IDs the backend deliberately exposes for tracing. attempt_count shows as a bare number ('시도: 2') with no max_attempts, so it's impossible to tell whether a job has retries left or is exhausted.
  - fix: Give DataScreen a config.detailColumns (fall back to columns) and set jobs.detailColumns to include dateCol('started_at'), dateCol('finished_at'), col('duration_ms','처리시간(ms)'), dateCol('available_at','다음 시도'), a combined attempt render ('2 / 3' from attempt_count+max_attempts), and the correlation IDs. Render duration_ms in seconds for readability. 
- **[jobs/feature·empty-state]** A job queue is a live, changing resource (queued→running→succeeded/failed), but the screen has no auto-refresh (no refetchInterval) and no manual refresh button. DataScreen.refresh() is only invoked after an action, and jobs has no actions — so once loaded, a 'running' job stays 'running' in the UI until the operator navigates away and back or reloads the page. There is no way to see live progress or confirm a worker is draining the queue.
  - fix: Add refetchInterval (e.g. 5-10s) to the useQuery in DataScreen for queue-type screens (config.pollMs), and/or add a manual '새로고침' button in the PageHeader actions that calls query.refetch(). Set jobs.pollMs so the queue view stays live. 
- **[notion-mapping/feature·missing-feature]** Status filter not exposed. list_mappings supports ?status=unmapped|verified|conflict (router.py L84,110-117) — the natural way for an admin to jump straight to conflicts or unmapped users across the org — but there is no filter control in the registry config or DataScreen. Combined with the 20-row cap, an admin cannot triage conflicts that sit on later pages.
  - fix: Add a status filter (select: 미연결/확인됨/충돌) that feeds the endpoint's status query param; requires DataScreen to support filter controls that re-query. 
- **[notion-mapping/feature·bug]** '자동 동기화' reports completion for a job that is only queued. POST /sync returns 202 {job_id, status:'queued'} for a background job the router docstring says takes 9-13s+ per batch. runHeaderAction (DataScreen.jsx L47-48) toasts '자동 동기화 완료' (success) and immediately refresh()es, but the job has not run so nothing changes. There is no polling, no job link, and no 'requested/processing' state. The router docstring assumes the screen reloads when the job finishes, but no such mechanism exists — the admin is told sync finished when it hasn't started.
  - fix: Toast '동기화 요청됨(백그라운드 처리 중)' for 202 responses; poll the job (GET /api/admin/jobs) or link to 작업 큐, and refresh the list when the notion_mapping_sync job completes. 
- **[notion-mapping/feature·missing-feature]** notion_user_id_masked and last_verified_at are returned by the backend but never shown. mapping_view provides a masked Notion id and last_verified_at; neither is a column nor visible in the detail drawer (detail renders only config.columns). For a manual mapping created without notion_email, the row shows status only — the admin cannot confirm which Notion account is linked or how stale the verification is.
  - fix: registry.js: add the masked Notion id to the detail/columns (server already masks it) and a dateCol for last_verified_at. 
- **[audit/feature·ux]** The '행위자' (actor) column shows the raw 36-char user UUID (registry.js:289, col('user_id')). The backend does no user join, so an auditor sees an opaque UUID instead of a name/email — undermining the '누가'(who) half of the screen's purpose. CLI/system actions have user_id=null and render '—' with no indication it was a system/CLI-originated action.
  - fix: Resolve user_id to display name/email — either enrich the backend audit response with an actor display field (app/audit/router.py:66-82) or have the frontend look it up — and render '시스템/CLI' for null actors instead of a bare '—'. 
- **[notifications/feature·missing-feature]** The notification `body` (the actual message text) and `related_object_type`/`related_object_id` are returned by the backend _view() (router.py:19-29) but are never surfaced. The config columns (registry.js:295) omit all three, and the detail drawer iterates only config.columns (DataScreen.jsx:95-100), so even opening a notification shows no body text and no related object. The single most informative field is invisible on the dedicated page (and the bell omits it too, so body is shown nowhere).
  - fix: Add body (and related object) to the notifications columns in registry.js:295, e.g. col("body","내용") plus a related-object column, so the detail drawer renders the message body and linkage. 
- **[notifications/feature·ux]** No row-level deep-link or mark-on-open; the detail drawer is a dead-end that is strictly less capable than the bell popover. NotificationBell.openItem (NotificationBell.jsx:67-74) marks an unread item read and deep-links to the related screen via OBJ_ROUTE. The DataScreen just opens a generic key/value drawer with no navigation to the related object and no mark-read. Clicking an 'approval_requested' notification on the full page cannot take you to the approval.
  - fix: Add a row action or drawer action that navigates to the related object (mirror NotificationBell's OBJ_ROUTE map) and marks the item read on open. 
- **[notifications/feature·design]** The '유형' column shows raw English enum values. registry.js:295 uses col("type","유형") which renders r.type verbatim ('approval_requested', 'job_failed', 'document_ready', ...). The NotificationBell has a TYPE_KO map (NotificationBell.jsx:11-15) that Koreanizes these, so the same data reads as Korean in the popover but as raw snake_case enums on the full page. Client-side search (config.searchable unset) also matches only these raw enum strings, so searching '승인' won't find approval notifications.
  - fix: Add a NOTI_TYPE map (reuse NotificationBell's TYPE_KO) in registry.js and switch to mapCol("type","유형", NOTI_TYPE) so types render in Korean consistently with the bell. 
- **[notifications/feature·missing-feature]** No pagination: only the 20 most recent notifications are ever reachable. The endpoint defaults to page_size=20 (pagination.py:13) and DataScreen renders no pagination controls at all — it never reads total/page/page_size from the response. The page title '알림' and the bell's '전체 알림 보기' (View ALL) promise the full history, but rows past the first 20 cannot be viewed.
  - fix: Add pagination controls to DataScreen (consume total/page/page_size) or a 'load more' affordance so the notifications history beyond page 1 is reachable. 
- **[shared-infra/design·a11y]** The shared Modal (kit.jsx:153-171) sets `role="dialog"` and `aria-modal="true"` but does no focus management: focus is never moved into the dialog on open, Tab is not trapped (keyboard focus escapes to the background page behind the 38% overlay), and focus is not restored to the trigger on close. This affects every create/edit/confirm/detail/FormModal flow, since all of them use Modal.
  - fix: On open, move focus to the first focusable element (or the ✕ button); trap Tab/Shift+Tab within `.k-modal`; store and restore focus to the triggering element on close. kit.jsx Modal component. 
- **[shared-infra/design·ux]** Mid-session session expiry leaves the shell broken. AdminBody (App.jsx:157) only checks `auth.isLoading`; it never handles `auth.isError`. When `/api/me` returns 401 (expired session), `role` is undefined so the full admin chrome (sidebar+topbar) still renders and every screen independently 401s into its own ErrorState. There is no single shell-level 'session expired → go to login' handling.
  - fix: In App.jsx handle `auth.isError` (401) by redirecting to `/login` (or rendering one ErrorState with the '로그인 화면으로' button from kit.jsx:94) instead of rendering AdminBody with dead content. 
- **[shared-infra/design·ux]** Flash of wrong chrome during auth load. Topbar renders the admin '사용자/관리자' segment whenever `!isUser` (App.jsx:138), but during the `/api/me` load `role` is undefined so `isUser` is false — the admin-only segment briefly flashes even for regular users (who are then redirected to /chat). The UserMenu also shows the placeholder '관리자 / ?' (App.jsx:105-106) until identity resolves.
  - fix: Gate the segment control and identity-dependent topbar chrome on `auth.isLoading` / resolved role so regular users never see the admin segment, and show a neutral placeholder rather than defaulting to '관리자'. 
- **[shared-infra/code·bug]** Row and header actions have no in-flight guard, allowing duplicate non-idempotent POSTs. runAction/runHeaderAction (DataScreen.jsx:33-50) await the request then refresh, but the footer/header Buttons (DataScreen.jsx:61,92-93) are never disabled while awaiting. Double-clicking "+백업 실행", "+문서 생성", "새 버전", or "승인" fires the request twice (duplicate backups/documents/approvals). FormModal has a busy state; these action buttons do not.
  - fix: Track an in-flight action key in state and disable the corresponding Button (and show a busy label) until the await resolves. DataScreen.jsx:33-50,61,92-93. 
- **[shared-infra/code·bug]** Optional text/number/textarea/json fields cannot be cleared on edit. FormModal.submit only writes a key when `val !== "" && val != null` (kit.jsx:229), so an emptied field is omitted and a PATCH treats it as unchanged. Only `select` explicitly sends null (kit.jsx:227). Concrete: set a workflow `owner` or `purpose`, then blank it and save — the old value persists.
  - fix: For fields present in `initial` that the user cleared, send explicit null (diff against initial) rather than omitting them. kit.jsx:229. 
- **[shared-infra/code·bug]** Client-side search matches serialized JSON keys and hidden fields. DataScreen.jsx:52-53 does `JSON.stringify(r).toLowerCase().includes(q)`, so it matches object keys (id, name, status, created_at, enabled) and non-displayed fields. Common substrings like "id", "created", "name", "true", or "http" match every/most rows, making results meaningless; it also re-stringifies every row on each keystroke.
  - fix: Filter only the visible column values (run each column's render/text extractor and match against that), not the whole serialized object. DataScreen.jsx:52. 
- **[shared-infra/code·dead-affordance]** An unintended edit button appears on integrations/runners via a fallback. `editFields = config.edit ? ... : config.create.fields` (DataScreen.jsx:19) means screens the registry deliberately left without an `edit` form still render "수정" (DataScreen.jsx:91) and PATCH the full create field set (integrations: provider_type/base_url/auth_type/secret_ref). These PATCH routes exist and flag base_url/secret_ref/auth_type/health_url changes as sensitive, so this unreviewed, undocumented form can silently route into the approval flow or change secret targets — behavior the author built careful reduced edit forms to avoid elsewhere.
  - fix: Only render the edit button and edit FormDrawer when `config.edit` is explicitly defined; drop the create.fields fallback. DataScreen.jsx:19,91,108. 
- **[shared-infra/code·ux]** No global 401 handling; an expired session shows a broken shell instead of redirecting. api.js throws status 401 (api.js:24-28) and AuthProvider merely errors (auth.jsx), but App.jsx Layout still renders Topbar+Sidebar+AdminBody, so every panel independently renders an ErrorState with a manual "로그인 화면으로" link (kit.jsx:93-94). There is no app-level bounce to /login on session expiry — the console looks broken (every panel erroring).
  - fix: Detect 401 centrally (react-query QueryCache onError, or the /api/me auth error in Layout) and redirect to /login. app/App.jsx Layout; lib/api.js. 
- **[shared-infra/code·a11y]** No client-side role gating; lower RBAC roles see controls they cannot use. App.jsx only redirects the `user` role to chat (App.jsx:188-190); all other roles render every nav group and DataScreen shows all create/action/headerAction buttons unconditionally. A limited admin sees "+추가", "삭제", "발행", "+백업 실행", and the maintenance toggle, and only discovers they are forbidden via a 403 error toast — dead affordances that mislead about capability.
  - fix: Return the caller's role/capabilities from /api/me and gate NAV items plus create/action buttons on them. app/App.jsx NAV/Sidebar; screens/DataScreen.jsx action rendering. 

## Low (94)

- **[login/design·ux]** The '서버 상태' indicator (login.js:19-25, login.html:88) is near-useless chrome. The login page is served by the same app that answers /healthz, so if the server were down the page itself would not load — the 'cannot connect' branch is effectively unreachable and the field shows a permanent '서버 연결 정상'. It adds a persistent green status line to a login screen (unusual pattern) and, via aria-live="polite", announces '서버 연결 정상' to screen readers on every load as noise.
  - fix: Either drop the always-on status entirely, or only surface the message when it is actionable (e.g. show text ONLY on the failure/catch branch and leave it empty on ok). Do not render '서버 연결 정상' by default. 
- **[login/design·ux]** No client-side validation or focus assist. The email field has no autofocus (login.html:61) and login.js never calls .focus(), so the user must click before typing. The form is novalidate (login.html:54), so type="email"/required are disabled and empty or malformed submissions round-trip to the server (returns 422 '이메일과 비밀번호를 입력하세요.') instead of failing fast in the UI.
  - fix: Add autofocus to the email input (or emailInput.focus() on load). Add a lightweight pre-submit check in login.js (empty email/password, basic email shape) that fills #login-error and returns before the fetch, so obvious mistakes get instant feedback without a network round-trip. 
- **[login/design·design]** English error text can leak into the otherwise all-Korean UI. login.js:45-46 renders data.error.message verbatim; for a 500 (errors.py:142 'Internal server error') or a FastAPI RequestValidationError (errors.py:129 'Invalid request data') the message is English. These are edge cases on this endpoint but would display raw English inside the Korean login screen.
  - fix: In login.js, map non-Korean/unknown error codes to the Korean fallback: only show data.error.message for known user-facing codes (invalid_credentials, account_locked/disabled/archived, rate_limited, validation_error from _extract_credentials), otherwise fall back to '로그인에 실패했습니다.' — or localize the 500/validation defaults server-side. 
- **[login/design·design]** On short viewports the form panel can clip. login.css:15/108-112 use min-height:100vh with the panel centered via justify-content:center and no overflow-y, and html,body are pinned to height:100% (login.css:11). On a short screen (or mobile with the on-screen keyboard raised) the logo/title/form/hint/footer stack can exceed the viewport, and centered flex overflow is clipped at the top with no scroll to reach it.
  - fix: Allow the panel to scroll when content exceeds the viewport: change justify-content to a top-safe layout on short heights (e.g. justify-content:flex-start with vertical padding, or add overflow-y:auto and use min-height instead of forcing 100vh centering) so no field is ever cut off. 
- **[change-password/test·test-gap]** Unauthenticated GET /change-password → 303 redirect to /login (via get_page_auth → PageAuthRequired, deps.py:92-99) is untested. Tests exist for GET /admin and GET / redirecting unauthenticated users to /login (test_admin_console.py:38, test_auth_sessions.py:22) but none for the change-password page, leaving this page's auth-lifecycle path unpinned.
  - fix: In test_pages.py add test_change_password_page_redirects_unauthenticated: client.get('/change-password', follow_redirects=False) with no session → assert status_code 303 and headers['location'] == '/login'. 
- **[change-password/test·test-gap]** Two bootstrap branches of change_password.js are untested beyond the 5xx case: /api/me returning 200 but with no csrf_token in the body → errorBox shows '세션 정보를 읽지 못했습니다' and csrfToken stays null; and the fetch('/api/me') promise rejecting (network down) → .catch sets the connection-error message. Both leave the user unable to submit and must surface a clear message rather than a blank screen.
  - fix: In change_password_ui_test.js add: /api/me → 200 with {} (no csrf_token) asserts errorBox is set and a subsequent submit makes no POST; and a rejecting /api/me fetch asserts the '서버에 연결할 수 없어' message is shown and no redirect occurs. 
- **[dashboard/test·test-gap]** cert_days_remaining is only tested for the None (no cert path) case (test_dashboard_aggregates line 24). The openssl parse path in _cert_days_remaining — running openssl, parsing 'notAfter=...' into a D-day int, and the error fallbacks (non-zero returncode, ValueError/IndexError -> None) — has no coverage (service.py:79-95), including the <=30 alert threshold it feeds.
  - fix: Add a unit test in tests/unit that monkeypatches subprocess.run (or points settings.tls_cert_path at a generated self-signed cert) and asserts _cert_days_remaining returns the expected day count, plus a case where openssl output is malformed returns None. 
- **[dashboard/test·test-gap]** Frontend loading / error / all-clear / navigation states of the dashboard are untested (no React test harness). Untested behaviors: the loading Skeleton, ErrorState with onRetry, the '지금 조치가 필요한 문제가 없습니다' all-clear branch when alerts is empty (Dashboard.jsx:62-64), StatCard onClick navigating to /jobs and /backup (lines 39-43, 57-58), the refresh button calling q.refetch(), and null-safe access (d.jobs_24h||{}, d.counts||{}).
  - fix: Once a React test runner (e.g. Vitest + Testing Library, which is absent) is added, cover: renders Skeleton while isLoading; renders ErrorState and retry calls refetch on isError; renders all-clear message when no alerts; clicking a failed-job StatCard calls navigate('/jobs') and the backup StatCard navigate('/backup'). 
- **[change-password/design·ux]** Success gives no confirmation. On success change_password.js:70 does a bare window.location.href = '/' with no toast/message. The backend performs a security-significant side effect — revoke_all_for_user + session rotation (auth/router.py:272-283), i.e. every other device is logged out — but the user is never told the change succeeded or that other sessions were signed out.
  - fix: Before redirecting, show a brief success state (e.g. '비밀번호가 변경되었습니다. 다른 기기의 로그인은 모두 해제되었습니다.') or pass a flash/query param so the destination surfaces confirmation. 
- **[change-password/design·dead-affordance]** For the voluntary path this page is a dead-end. Users reach it as a full-page navigation from the React user menu (frontend/src/app/App.jsx:111, <a href="/change-password">), leaving the SPA. If they change their mind there is no cancel/back link anywhere on the card (change_password.html:4-32) — the only action is submit. They must manually retype a URL to get back into the app.
  - fix: When must_change is False, render a secondary '취소' / '돌아가기' link to '/'. Keep it hidden for must_change=True since that is an intentional gate. 
- **[change-password/design·ux]** The first field (current-password, change_password.html:18) has no autofocus. On a single-purpose form that is always the user's next action, they must click into the field first.
  - fix: Add autofocus to the current-password input. 
- **[change-password/design·a11y]** The error container mixes contradictory ARIA: role="alert" (implicitly assertive) with aria-live="polite" (change_password.html:28). Screen readers get conflicting urgency signals for the same region.
  - fix: Pick one — for a validation error region, aria-live="assertive" with role="alert", or drop role="alert" and keep aria-live="polite". (Same issue exists on login.html:77 if standardizing.) 
- **[change-password/code·ux]** No in-progress feedback during submission. On submit the handler only sets submitButton.disabled=true (change_password.js:55) with the label unchanged; the backend does an Argon2 verify + Argon2 hash (router.py:256,269), which is deliberately slow (~0.5s+). The user sees a frozen, label-unchanged button with no spinner, '처리 중' text, or aria-busy, so a slow response reads as a hang.
  - fix: While awaiting, set aria-busy and swap the button label to '변경 중...' (restore both in the finally block at change_password.js:81-83). 
- **[change-password/code·ux]** No success feedback and a silent security side effect. On success change_password.js:70 immediately does window.location.href='/' with no '비밀번호가 변경되었습니다' confirmation. More notably, a successful change revokes ALL the user's other sessions (router.py:273, spec §11.3) — the user is never told other devices were logged out, which is a surprising security-relevant outcome to hide.
  - fix: Before redirecting, show a brief success state (or pass a flash/toast to '/') that confirms the change and notes that other devices have been signed out. 
- **[change-password/code·a11y]** The error region uses both role="alert" and aria-live="polite" (change_password.html:28). role=alert already implies an assertive live region; pairing it with polite is contradictory and some screen readers will downgrade the announcement, delaying the error for AT users. Focus is also never moved to the error on failure.
  - fix: Drop the redundant aria-live="polite" (keep role="alert"), or use aria-live="assertive"; optionally move focus to the error box on submit failure. 
- **[change-password/code·ux]** No show/hide password toggle on any of the three password fields (change_password.html:18/22/26). On a high-stakes, no-second-chance form where a typo in 'new' vs 'confirm' only surfaces as a generic mismatch error, a reveal control materially reduces failed attempts.
  - fix: Add a per-field or form-level show/hide toggle (button type=button toggling input type between password/text) wired in change_password.js. 
- **[dashboard/design·design]** The '백업 없음' alert puts the CTA word '확인' in the StatCard's big-number slot (Dashboard.jsx:43), so the card shows a giant '확인' over a small '백업 없음' label. StatCard's value slot is styled for metrics/numbers; placing an action word there produces confusing visual hierarchy versus the numeric alerts beside it.
  - fix: Use a non-numeric alert treatment for text-valued alerts (icon + message + action button), or set value to '없음' and rely on the danger tone; reserve the number slot for counts. Dashboard.jsx:43. 
- **[dashboard/design·ux]** Failed-job and queued-job alerts both navigate to /jobs with no filter applied (Dashboard.jsx:39-40), dropping the operator onto the full unfiltered jobs list where they must re-locate the failed/queued rows the alert just counted.
  - fix: Pass a status filter in the destination (e.g. /jobs?status=failed / ?status=queued) and have the Jobs screen honor it. Dashboard.jsx:39-40. 
- **[dashboard/design·design]** Metric units are applied inconsistently in the '지표 (최근 24시간)' grid: some are appended to the value ('%' for 성공률/메모리) while others are baked into the label ('평균 처리(초)', '디스크 여유(GB)', '인증서 만료(D-day)'), and plain counts have no unit. This reads as an unpolished template rather than a coherent system, and '인증서 만료(D-day)' shows a bare number whose meaning (days remaining) isn't obvious.
  - fix: Standardize on one convention — value carries the number, a small unit suffix element renders the unit consistently ('초', 'GB', '일'). Dashboard.jsx:81-88. 
- **[dashboard/design·bug]** Integrations are merged into the '서비스 상태' grid using only last_health (Dashboard.jsx:48), discarding each integration's 'enabled' flag that the backend provides (service.py:150). A disabled integration is shown as a live service badge with no indication it's turned off, misrepresenting the actual configuration.
  - fix: Reflect enabled=false as a distinct state (e.g. show '비활성화' badge instead of last_health, or dim the card) rather than rendering last_health unconditionally. Dashboard.jsx:47-48. 
- **[dashboard/design·design]** .dash-backup uses display:flex with justify-content:space-between and no flex-wrap (screens.css:10). On very narrow viewports the backup line and the action button share one row with no wrap fallback, risking cramped/overlapping layout when the '지금 백업 설정' button and a long '마지막 백업: …' line compete for width.
  - fix: Add flex-wrap:wrap (and a gap that survives wrapping) to .dash-backup so the button drops below the line on narrow screens. screens.css:10. 
- **[dashboard/code·ux]** 새로고침 (refresh) calls q.refetch() (Dashboard.jsx:21) but gives no in-flight feedback: on a refetch q.isLoading stays false (only q.isFetching is true), so the button neither disables nor shows progress and the Skeleton branch (q.isLoading) never fires. The operator clicks and sees nothing happen until (and unless) the data changes — a silent action with no success/loading state.
  - fix: Reflect q.isFetching on the button, e.g. <Button disabled={q.isFetching} ...>{q.isFetching ? '새로고침 중…' : '새로고침'}</Button> at Dashboard.jsx:21, and/or show a subtle refreshing indicator on the body while isFetching. 
- **[dashboard/code·missing-feature]** build_dashboard returns counts.runners (app/health/service.py:154) but the dashboard renders only active_workflows and active_schedules (Dashboard.jsx:84-85). The runner count — a first-class registry entity — is computed on the backend on every request yet dropped from the UI, so the operator gets no at-a-glance runner inventory.
  - fix: Add a StatCard for runners in the 지표 grid, e.g. <StatCard value={(d.counts||{}).runners} label="러너" /> near Dashboard.jsx:84 (or drop the backend field if intentionally unused). 
- **[dashboard/code·design]** Under the '확인이 필요한 항목' (items needing action) heading, the '실패 작업', '대기 작업', and '백업 없음' cards are clickable and navigate (to /jobs, /backup), but the '인증서 만료' and '디스크 사용(%)' alert cards have no `to` (Dashboard.jsx:41-45) so they render as non-clickable divs — dead-ends. An operator told an item needs action has nowhere to go for exactly the two hardware/security alerts.
  - fix: Give the cert and disk alerts a destination (e.g. a maintenance/system screen, or /diagnostics) via a `to` field at Dashboard.jsx:42/45, or add inline guidance text so the alert is actionable rather than a static number. 
- **[login/code·bug]** The form carries `novalidate` (login.html:54), which disables the native `required` (email line 61, password line 71) and `type="email"` (line 61) validations — yet login.js provides no client-side replacement. Empty or malformed-email submissions are sent straight to POST /login, so a user with an empty field or a typo like 'foo' gets a server round-trip and a generic error instead of instant field feedback. The `required`/`type=email` attributes are effectively dead affordances.
  - fix: Either drop `novalidate` (login.html:54) to let native validation fire, or add a client-side guard in login.js before the fetch (login.js:27-39): if `!emailInput.value.trim() || !passwordInput.value`, set errorBox and focus the empty field, and optionally check basic email shape. Keep the generic server error for wrong-but-valid credentials. 
- **[login/code·ux]** During submit, login.js only sets `submitButton.disabled = true` (login.js:30); the button keeps the label '로그인' and merely dims via `:disabled { opacity: 0.65 }` (login.css:213). There is no spinner or '로그인 중…' text, so on a slow network the user gets almost no feedback that the request is in flight and may re-trigger via Enter.
  - fix: In login.js swap the button label/content to a busy state (e.g. store original, set textContent to '로그인 중…' plus optional spinner) on submit and restore it in the `finally` block (login.js:49-51). Optionally add `aria-busy="true"` on the form while pending. 
- **[login/code·a11y]** No initial focus is placed on the email field. The email input (login.html:61) has no `autofocus`, and login.js sets no focus on load, so keyboard and screen-reader users must tab/click to the first field before typing.
  - fix: Add `autofocus` to the email input (login.html:61), or call `emailInput.focus()` at the end of the login.js IIFE. 
- **[login/code·a11y]** #login-error declares both `role="alert"` and `aria-live="polite"` (login.html:77). `role="alert"` already implies `aria-live="assertive"`; the explicit `polite` conflicts and can downgrade a login-failure announcement to polite, delaying or suppressing the read-out on some screen readers.
  - fix: Remove the redundant `aria-live="polite"` from #login-error (login.html:77) and rely on `role="alert"` (assertive) for the error, which is the appropriate urgency for a failed login. 
- **[login/code·contract]** POST /login returns `csrf_token` and a `user` object (router.py:198-209), but login.js reads only `data.ok` and `data.must_change_password` (login.js:41-42) and discards the rest. After the redirect, both the React app (auth.jsx:13-17) and change_password.js (change_password.js:21-37) re-fetch the same CSRF token and user via GET /api/me — an extra round-trip for data the login response already delivered. Not a defect (it works), but redundant server work and a wasted response payload.
  - fix: Optional: since the token/user are intentionally re-fetched from /api/me, drop `csrf_token`/`user` from the /login response body (router.py:198-209) to keep the contract honest, or have login.js seed them (e.g. into sessionStorage / a bootstrap value) so /api/me can be skipped on first paint. Leaving as-is is acceptable; flag only for contract tidiness. 
- **[chat (user)/test·test-gap]** Client-side attachment gating in pickFiles() (Chat.jsx:159-168) is untested: non-image rejected with toast, >3MB rejected with toast, hard cap of 3 pending attachments (setPending guard `p.length>=3`), and FileReader base64 stripping in fileToAttachment (Chat.jsx:8-15). The backend validate_attachments is well-covered (test_chat_attachments.py) but the browser-side pre-filtering and the 3-item cap are not.
  - fix: Add Chat.test.jsx tests using stubbed File/FileReader: dropping a non-image toasts and adds nothing; a 4MB image toasts and adds nothing; adding a 4th image is ignored (max 3 chips); fileToAttachment returns base64 without the data: prefix. 
- **[chat (user)/test·test-gap]** list_messages rowid ordering (service.py:116-132) — the fix that orders by SQLite rowid because created_at ties are common on coarse OS clocks — has no direct test. Existing tests only ever create 1 message per conversation, so a same-timestamp multi-message ordering regression (messages shown out of order) would pass unnoticed.
  - fix: Add tests/integration/test_chat_api.py::test_messages_returned_in_insertion_order: post several messages within the same fake_clock tick and assert GET .../messages returns them in send order (and that the `after` cursor slices correctly across them). 
- **[chat (user)/test·test-gap]** The duplicate-submit response contract is only half-asserted. On a duplicate client_message_id, post_user_message returns (existing, job) where job may be None, and router.py:159 returns job_id accordingly. test_duplicate_client_message_id_not_duplicated (test_chat_api.py:81) checks no dup message and Job count==1 but never asserts the second response's status/body (e.g. job_id value) the client (Chat.jsx send.mutationFn) consumes.
  - fix: Extend the duplicate test to assert the second POST still returns 202 and its message.id equals the first, documenting the idempotent client-facing contract. 
- **[chat (user)/design·a11y]** `aria-live="polite"` is placed on the entire scrollable message container (Chat.jsx:227), which contains the full conversation history. On initial load screen readers may announce the whole thread, and because the thread refetches every 1.5s while awaiting a reply (Chat.jsx:128-137), reconciliation can trigger repeated/noisy announcements of large regions rather than just the new assistant response.
  - fix: Scope the live region to only the latest assistant message (wrap the newest reply in an aria-live span) and leave the scroll container non-live. Chat.jsx:227, 233. 
- **[chat (user)/design·design]** No timestamps or metadata anywhere in the chat UI. Messages show no sent-time and the conversation list shows title only (Chat.jsx:102), even though message_view/conversation_view return created_at/updated_at (service.py:325-336). Users cannot tell when a message was sent or which conversation was active most recently; the sidebar list is bare compared to standard chat apps (no last-activity date, no preview).
  - fix: Add a subtle message timestamp (on hover or per group) and a last-activity date/preview line in ConvItem, formatting created_at/updated_at in Asia/Seoul. Chat.jsx:54-83 (Message), 100-109 (ConvItem). 
- **[users/code·missing-feature]** Backend supports role and active query filters (router.py:74-98) but the UI offers only free-text search (Users.jsx:77-78). No way to filter by role or active/inactive, which the columns already display — limits usefulness on larger tenants.
  - fix: Add role and active dropdown filters that append role=/active= to the list request and are part of the queryKey. 
- **[users/code·ux]** The search input fires a new request on every keystroke: queryKey is ["users", q] with value bound directly to q (Users.jsx:29,77-78) and no debounce. Each character typed hits /api/admin/users, wasting requests and causing flicker on slower links.
  - fix: Debounce q (e.g. useDebounce ~300ms) before using it in the queryKey/request, or search on submit. 
- **[users/design·ux]** The search box fires a network request on every keystroke — queryKey ['users', q] with q bound directly to onChange (Users.jsx:29-32, 77-78) and no debounce — producing a request per character and flicker/rate pressure while typing.
  - fix: Debounce q (e.g. a useDebounce hook, ~250-300ms) before it feeds the query key. 
- **[users/design·ux]** The detail drawer shows a 'Notion 연결' status badge (Users.jsx:149) that is purely informational — there is no action or link, even though a dedicated 'Notion 사용자 연결' screen (registry.js:247-265) can verify/map/unmap it. Users must know to navigate elsewhere to act on the status they see here.
  - fix: Turn the Notion status row into a link to the Notion mapping screen (or add a '연결 검증' action calling /api/admin/users/{id}/notion-mapping/verify, which the backend supports at router.py:417). 
- **[settings/code·ux]** The '값'(value) column renders String(r.value) (Settings.jsx:18), so object settings display '[object Object]' and give no visibility into the actual configured value anywhere in the read-only table — the admin cannot see current password_policy/session_policy/ui_branding values at a glance.
  - fix: Settings.jsx:18 — render objects via JSON.stringify(r.value) (truncated) or a compact key:value summary; keep String() only for scalar types. 
- **[settings/code·ux]** Saving gives no explicit success feedback. onSaved() (Settings.jsx:31,52) just invalidates and closes the drawer; the app has a ToastProvider/useToast used elsewhere (kit.jsx:272-291) but Settings ignores it. On success the drawer silently vanishes with no confirmation.
  - fix: Settings.jsx — call useToast() and push a '설정을 저장했습니다' toast in onSaved (and consider surfacing result.restart_required from the PUT response, which the code currently discards). 
- **[settings/code·empty-state]** The EmptyState branch (Settings.jsx:28, 'rows.length === 0') is unreachable dead code: effective_settings always returns all REGISTRY keys (service.py:80-89), so rows always has 8 entries. Not harmful, but it is untested/never-rendered UI masquerading as a handled state.
  - fix: Either remove the empty branch, or make it meaningful (e.g. show it only when a search/filter yields no matches — but no filter exists on this page today). 
- **[settings/code·bug]** int coerce() edge: for an int setting an empty input yields Number("") === 0, which passes Number.isInteger (Settings.jsx:47) and sends value 0; the backend _positive_int validator requires >=1 (registry.py:29) and rejects with 422. The error is displayed so it is not silent, but the client-side check gives a misleading pass for empty/blank input.
  - fix: Settings.jsx:47 — treat empty/whitespace val as an error ('정수를 입력하세요') before Number(), and consider range validation matching the registry bounds. 
- **[settings/design·design]** Validation errors are styled as muted helper text. Settings.jsx:73 renders the save error with className="k-empty-help" (the grey empty-state helper style) — the same low-contrast muted grey used for empty-state hints — rather than the app's dedicated error style 'k-form-err' used by FormModal (kit.jsx:242). A validation failure (e.g. '정수를 입력하세요.') should read as an error, not as passive help text. Similarly the setting description at Settings.jsx:62 reuses the 'pending-note' class (meant for the restart warning) instead of a field-help style.
  - fix: Use className="k-form-err" for the error paragraph (Settings.jsx:73) and 'k-field-help' for the description (Settings.jsx:62) to match the established design system. (frontend/src/screens/Settings.jsx:73)
- **[settings/design·missing-feature]** Two backend affordances are dropped. (1) The dry-run / validate-before-apply endpoint POST /{key}/dry-run (router.py:42, spec §14.4 step 2) is never called — there is no '검증' preview before committing a change. (2) The effective_settings response includes an is_default flag (service.py:86) indicating whether a value is still the registry default vs. an admin override; the UI ignores it, so users cannot tell which settings have actually been changed.
  - fix: Optionally call /{key}/dry-run to preview validation before PUT, and add a '기본값'/'변경됨' badge column driven by is_default so overrides are visible. Settings.jsx:16-21 (columns), 50-54 (save). (frontend/src/screens/Settings.jsx:16)
- **[settings/test·test-gap]** The /versions endpoint response shape and empty case are only touched indirectly (len==2 in the rollback happy path). No test asserts the item shape {version, snapshot, created_at} (router.py:69-75) or that a never-changed key returns {items: []}. Also rollback is only proven for a scalar int key — restoring a full object value (e.g. session_policy dict) is not verified.
  - fix: Add test_versions_empty_for_unchanged_key (GET returns items==[]), test_versions_item_shape (assert keys version/snapshot/created_at present), and test_rollback_restores_object_value (change session_policy twice, rollback, assert the full prior dict is restored). 
- **[diagnostics+maintenance/design·empty-state]** The Diagnostics empty-state promises the bundle contains '서비스·버전·설정 요약', but build_diagnostic_bundle (health/service.py:183) returns no application/build version field anywhere — only components, settings, job counts, disk/memory, cert days, backups, and errors. The copy advertises version info the data never includes.
  - fix: Either add a version field to the bundle (app/build version) or drop '버전' from the empty-state note so the copy matches what is actually shown (Ops.jsx:22). (frontend/src/screens/Ops.jsx:22)
- **[diagnostics+maintenance/design·ux]** The diag query uses enabled:false, so react-query caches the last bundle. On navigating away and back, the previously collected bundle re-renders with no visible freshness indicator — generated_at is buried inside the raw JSON. A user can mistake a stale snapshot (minutes/hours old) for the current system state, which is dangerous on a diagnostics screen.
  - fix: Show a human-readable '수집 시각: YYYY-MM-DD HH:mm' above/near the output derived from data.generated_at, and consider a subtle 'cached' hint until re-collected (Ops.jsx:8,10). (frontend/src/screens/Ops.jsx:8)
- **[diagnostics+maintenance/design·missing-feature]** A diagnostics bundle is typically exported and sent to support, but the only affordance is '복사' (copy the entire large JSON to clipboard), which is awkward for a big blob. There is no 'download / 파일로 저장' option.
  - fix: Add a '파일로 저장' button that downloads the bundle as clovirone-diagnostics-<timestamp>.json via a Blob + object URL, alongside the copy button (Ops.jsx:16). (frontend/src/screens/Ops.jsx:16)
- **[diagnostics+maintenance/design·design]** .ops-maint is `display:flex; justify-content:space-between` with no flex-wrap and no mobile breakpoint. On narrow screens the description paragraph and the toggle button compete for width; the button label ('유지보수 모드 켜기') can't shrink, so the text block gets cramped. Other screens in screens.css have @media(max-width:760px) stacking rules; this row has none.
  - fix: Add a @media (max-width:600px) rule that sets .ops-maint { flex-direction: column; align-items: stretch } (and full-width button) so it stacks cleanly on mobile (screens.css:109). (frontend/src/styles/screens.css:109)
- **[users/test·test-gap]** UserDetail's state-driven lifecycle controls are untested. Users.jsx:133-138 chooses enable vs disable based on user.active and archive vs unarchive based on archived_at, and marks disable/archive as danger-confirm. With no frontend test tooling, nothing verifies the correct endpoint/confirm-danger flag is used per user state, so a swapped condition (e.g., calling /disable on an inactive user) would ship silently.
  - fix: Add Users.test.jsx cases: render UserDetail with {active:true} → primary action calls /disable with danger confirm; with {active:false} → /enable non-danger; with archived_at set → /unarchive; else → /archive with danger confirm. 
- **[users/test·test-gap]** Backend-supported user actions dropped from the page have no UI/e2e coverage. router.py exposes reset-password (339), unlock (366), sessions list (376), revoke-sessions (405) and notion-mapping/verify (417), but UserDetail (Users.jsx:130-140) only wires enable/disable/archive/unarchive/edit. No test asserts these actions are reachable from the users page, so their absence from the UI is invisible to the suite.
  - fix: Once a frontend/e2e harness exists, add coverage asserting the user detail exposes reset-password, unlock, revoke-sessions, session listing, and notion verify (or explicitly document/remove them). Until then, add an assertion-level TODO test that fails to flag the missing affordances. 
- **[diagnostics+maintenance/code·ux]** The diagnostics bundle (dashboard summary, effective settings, recent_job_errors, integration_errors from build_diagnostic_bundle) is rendered as a single raw JSON dump in a <pre> (Ops.jsx:21). There is no structured/human-readable view, no highlighting of the error sections that actually matter (recent_job_errors, integration_errors), and no guidance on what to do with it. It is functional for copy-to-support but crude as an on-screen diagnostics view.
  - fix: Render at least the error-bearing sections (recent_job_errors, integration_errors) as readable rows, or add a short caption explaining the blob is meant to be copied to support, while keeping the raw JSON available for copy. 
- **[diagnostics+maintenance/code·dead-affordance]** The '진단' nav item and the '진단 수집' button are shown to operator and auditor roles, but GET /api/admin/diagnostics/bundle requires only admin/system_admin (health/router.py:47-48). Those roles can open the page and click 진단 수집, which returns 403 rendered as the generic ErrorState '불러오지 못했습니다'. The affordance is visible and clickable for roles that can never use it.
  - fix: Filter the diagnostics nav entry / hide the collect button for roles outside {admin, system_admin}, consistent with the endpoint's RBAC. 
- **[notification-bell/test·test-gap]** read-all edge case with zero unread is untested — POST /read-all when nothing is unread should return {ok:true, read_count:0}. mark_all_read returns int(result.rowcount or 0); a driver returning rowcount -1 or None would surface here and is never asserted.
  - fix: Add test_read_all_with_no_unread_returns_zero: log in a user with no notifications (or all already read), POST /read-all, assert 200 and read_count == 0, unread-count still 0. 
- **[notification-bell/test·test-gap]** Frontend popover UI states and controls are untested: '모두 읽음' button visibility (rendered only when items.some(n => !n.read_at)), badge '99+' cap for count>99 and hidden badge when count==0, list query enabled:open lazy-loading (no fetch until popover opens), and the error-state '다시 시도' refetch / empty-state / loading-skeleton branches. These ported branches can regress unnoticed with no coverage.
  - fix: Add component tests: mark_all_hidden_when_no_unread vs shown_when_some_unread; badge shows '99+' at count>99 and no badge at 0; list query not fired until open toggled true; error state renders retry button that calls list.refetch(); empty state renders '새 알림이 없습니다.' 
- **[notification-bell/test·test-gap]** POST /{id}/read with a syntactically valid but nonexistent id (not merely another user's) is only indirectly covered by the IDOR test. The distinct 'unknown id -> 404 with error envelope' path (raise NotFoundError) that the frontend readOne.onError toast depends on is not asserted directly, and title truncation to 200 chars (service.notify_user title[:200]) is also unverified.
  - fix: Add test_read_nonexistent_notification_returns_404 asserting 404 and a standard error envelope for a random UUID; add test_notify_user_truncates_title asserting a >200-char title is stored/returned truncated to 200. 
- **[diagnostics+maintenance/test·test-gap]** The Diagnostics component's lazy-fetch and copy behaviors (frontend/src/screens/Ops.jsx) are untested. The query is enabled:false (Ops.jsx:8) so the bundle must load only on the '진단 수집' button click via q.refetch() (Ops.jsx:15); the copy button appears only when text is present and calls navigator.clipboard.writeText(JSON) then flips the label to '복사됨' for 1500ms (Ops.jsx:16). No frontend test exists (see the missing test runner). A regression making the fetch eager, or the copy silently failing when navigator.clipboard is undefined, would not be caught.
  - fix: In the new component test suite, add Ops.diagnostics.test.jsx: initial render shows the pending note and api() is NOT called (enabled:false); clicking '진단 수집' triggers the bundle fetch and renders the JSON <pre>; clicking copy calls a mocked clipboard.writeText with the exact JSON string and toggles the label to '복사됨'. 
- **[diagnostics+maintenance/test·test-gap]** Environment-dependent dashboard fields have only their empty/None branch covered. cert_days_remaining is asserted None (no cert path in dev, test_dashboard_backups.py:24) but the openssl-parse success path (app/health/service.py:79-95) is never exercised; _memory_usage's /proc/meminfo parse (service.py:64-76) is never covered (dev is Windows). These feed the diagnostics bundle on the Linux production host, so parsing bugs (e.g. the strptime format at service.py:92) would surface only in prod.
  - fix: Add unit tests in tests/unit for the parsers directly: feed a fixed openssl 'notAfter=...' string / a fake cert path and monkeypatch subprocess.run to assert _cert_days_remaining returns a plausible int; feed a sample /proc/meminfo blob (via a temp file / monkeypatched Path) to _memory_usage and assert used_pct is computed. This avoids needing a real Linux host. 
- **[notification-bell/design·design]** The bell uses a raw emoji 🔔 (NotificationBell.jsx:80) while the rest of the topbar uses a crafted inline SVG brand mark (App.jsx:128-133). The emoji renders inconsistently across OS/browsers (Windows vs macOS glyphs, color vs monochrome) and reads as a lower-polish placeholder next to the custom SVG. CSP already forbids icon fonts, so an inline SVG bell is the consistent choice.
  - fix: Replace the 🔔 emoji at NotificationBell.jsx:80 with an inline SVG bell icon matching the topbar's SVG style. 
- **[notification-bell/design·ux]** Read actions give no success feedback or pending state. readOne/readAll (lines 42-52) only toast on error; on success they rely on silent query invalidation. The '모두 읽음' and item buttons are never disabled while the mutation is in flight, so a double-click can fire duplicate POSTs, and marking-all's only cue is the badge/button quietly disappearing.
  - fix: Disable the buttons while readAll.isPending / readOne.isPending (NotificationBell.jsx:86,93) and optionally show a brief success toast for '모두 읽음'. 
- **[notification-bell/design·design]** Notification types not present in TYPE_KO fall back to the raw enum n.type (NotificationBell.jsx:94), so any type the backend emits outside the hardcoded map (e.g. a newly added type) leaks an English underscore_enum string into the Korean UI. (The full-page /notifications DataScreen this bell links to is worse — it renders col('type') with no Korean mapping at all, registry.js:283 — so the two surfaces are also inconsistent.)
  - fix: Provide a safe Korean fallback (e.g. '알림') instead of raw n.type at NotificationBell.jsx:94, and align the /notifications DataScreen column to use the same TYPE_KO mapping. 
- **[notification-bell/design·a11y]** The unread count badge (line 80) has no aria-live region, and the count is refreshed silently every 60s (refetchInterval, line 33). Screen-reader users get no announcement when new notifications arrive; only the button's aria-label is updated, which is not announced unless the button is focused.
  - fix: Wrap the count in an aria-live="polite" region (or add role="status") so new-notification changes are announced (NotificationBell.jsx:80). 
- **[notification-bell/code·bug]** The '모두 읽음' button visibility is gated on items.some(n => !n.read_at), where items is only the 8 newest notifications loaded by the popover. If the 8 newest are all read but older notifications are unread, the count badge still shows count>0 (derived from the true server unread total) while the '모두 읽음' button is hidden — so the user sees unread notifications they cannot clear from the popover (and the full page offers no such action either). The button and the badge disagree.
  - fix: Gate the '모두 읽음' button on the true unread total (count > 0) instead of the loaded items, since read-all is a server-wide operation anyway. (frontend/src/app/NotificationBell.jsx:86)
- **[notification-bell/code·dead-affordance]** openItem: for admins, a notification whose related_object_type is null (e.g. type 'system' or an 'account_locked' with no related object) or maps to no OBJ_ROUTE entry marks-read, closes the popover, and navigates nowhere — yet each item is a <button>, implying it does something, so the click gives no visible feedback. Additionally, for items that DO route, it navigates to the LIST screen (e.g. /approvals) and ignores related_object_id, so you never land on the specific object the notification is about.
  - fix: Render non-navigable notifications as non-interactive (or surface a toast/inline body), and use related_object_id to build a detail deep-link where the target screen supports it. (frontend/src/app/NotificationBell.jsx:67)
- **[notification-bell/code·a11y]** The popover has role='dialog' but no focus management: focus is not moved into the popover when it opens, not restored to the bell button when it closes, there is no focus trap, and no aria-modal. Keyboard users must tab through the entire page to reach the notification items. (Escape-to-close and outside-click-to-close are correctly implemented.)
  - fix: On open, move focus to the first actionable element in the popover; on close, restore focus to the trigger button. Add aria-modal and a minimal focus trap, or use a headless dialog primitive. (frontend/src/app/NotificationBell.jsx:82)
- **[workflows/feature·missing-feature]** The 'tags' field (WorkflowConfig.tags: list[str], schemas.py:21; returned by workflow_view) is dropped from the UI entirely — absent from create, edit, columns, and detail. Not fatal, but it is a real backend field with no way to set or see it.
  - fix: If tags are intended for use, add a tags input to create/edit; otherwise document it as intentionally omitted. registry.js:91-109. 
- **[policies/feature·missing-feature]** The '새 버전' (new-version) action is only offered when status==='published' (registry.js:153), but the backend new-version endpoint (router.py:206) works from any version row. This means you cannot branch a new draft from a test/review/archived version even though the backend permits it — a minor loss of flexibility (though moot for archived while archive itself is unexposed).
  - fix: Relax the `when` guard on the new-version action if branching from non-published versions is intended, or document the restriction. File: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\registry.js:153. 
- **[prompts/feature·design]** The list shows every version of every prompt as a flat row (list_all returns up to 500 rows ordered by name, version desc — router.py:122-124), so the same prompt name repeats once per version with no grouping or latest-only view. The backend's per-name filter (GET /api/admin/prompts?name=..., router.py:117-121) is not leveraged because the screen isn't searchable (client-side JSON substring filter only), so there's no clean 'show all versions of X' drill-down — a poor fit for a version-management screen.
  - fix: Consider a latest-per-name default view with a per-row 'versions' drill-down using the ?name= filter, or at least visually group rows by name. registry.js:115-119. 
- **[schedules/feature·missing-feature]** The create form omits advanced fields that the edit form includes: payload_template, retry_policy, misfire_policy, concurrency_policy, timeout_seconds, start_at, end_at (compare create fields registry.js:186-195 vs edit fields 200-216). All are accepted by POST at creation, so a user who wants a payload/retry policy/start-end window must create the schedule first and then immediately edit it — an avoidable two-step.
  - fix: Add the advanced fields to the create form (or note the asymmetry intentionally). The backend ScheduleRequest already defaults them, so surfacing them on create is low-risk. (frontend/src/screens/registry.js:186)
- **[approvals/feature·design]** Raw/untranslated values leak through. request_type 'document.publish' renders as '문서 · publish' because 'publish' is missing from VERB_KO (actionKo falls back to the raw token). And if requested_by/object_id were surfaced (see Critical finding) they are raw UUIDs with no display name/email — approval_view returns only IDs, so even a correct drawer would show unreadable identifiers.
  - fix: Add 'publish' to VERB_KO in frontend/src/lib/format.js. For readable actors/objects, have the backend approval_view join requested_by/approver_id to user display_name/email (and object_id to a human label) rather than returning bare UUIDs. 
- **[runners/feature·missing-feature]** The create form and the fallback edit form omit several fields RunnerConfig/RunnerUpdateRequest accept: owner, tags, retry_policy, capabilities, version (registry.js:68-77 vs app/runners/schemas.py:14-77). owner in particular is offered on the sibling workflows screen but not runners. These cannot be set at create time nor edited via the UI.
  - fix: Add owner (text) and optionally version/tags to the runners create/edit fields; expose retry_policy/capabilities as JSON fields if they are meant to be operator-managed (mirror the schedules/templates JSON-field pattern in registry.js:169-178). 
- **[runners/feature·design]** config_version is shown as a column for the sibling integrations screen (registry.js:46) but not for runners (registry.js:66-67), even though runner_view returns it (service.py:63). Given versioning/rollback exist for runners, the current config version is more relevant here than for integrations, yet it's invisible in both the list and the detail drawer.
  - fix: Add col('config_version', '버전') to runners.columns for parity with integrations and so operators can see which config version is live (also needed context once rollback is wired). 
- **[integrations/feature·dead-affordance]** Write/action buttons render for read-only roles. Backend gates enable/disable/edit to admin+ (WRITE_ROLES, router.py:36) and health to operator+ (router.py:179), while READ_ROLES includes operator and auditor (router.py:35). DataScreen renders all actions unconditionally (DataScreen.jsx:92-93) with no role check, so an auditor sees 활성화/비활성화/수정/헬스체크 and an operator sees 활성화/비활성화/수정 — all of which 403 on click. Systemic to config-driven screens but manifests on this screen.
  - fix: Gate action/edit rendering by the current user's role (thread the session role into DataScreen and add an optional roles predicate per action in registry, or hide edit/enable/disable for non-write roles). At minimum, the drawer footer (DataScreen.jsx:91-93) should not show admin-only actions to auditors. 
- **[integrations/feature·missing-feature]** The create form cannot set enabled state. registry.js integrations create fields (lines 47-55) omit enabled, so every new integration is created enabled=True (schemas.py:25 default). By contrast workflows exposes an 'enabled' checkbox at create (registry.js:99). Minor because a disable action exists post-create, but you cannot register an integration in a disabled/staged state in one step.
  - fix: Add { name: 'enabled', label: '활성', type: 'checkbox', value: true, checkLabel: '활성' } to the integrations create fields (registry.js:47-55), matching workflows. 
- **[templates/feature·ux]** description is declared type 'text' in both create (registry.js:163) and edit (registry.js:171), rendering a single-line input, but the backend allows up to 2000 chars (router.py:32). A multi-line description is awkward to enter/read in a one-line field; peer screens use textarea for comparable prose.
  - fix: Change description field type to 'textarea' in both create and edit. File: frontend/src/screens/registry.js:163,171. 
- **[departments/feature·missing-feature]** Backend list filter '?active=' is never surfaced. GET /api/admin/departments accepts active: Optional[bool] to show only assignable (or only retired) items (app/org/router.py:43-53), but the screen provides no filter control — it fetches everything and relies on the '사용' badge + client-side text search. There is no way to quickly view only active or only deactivated departments.
  - fix: Add an active/inactive filter toggle to the departments toolbar that appends '?active=true|false' to config.endpoint (DataScreen would need a filter hook), or document that client-side filtering is intentional. 
- **[departments/feature·design]** Deactivated departments are not visually distinguished. badgeCol('active', '사용') renders <Badge value={boolean}/>; for a boolean the value maps to a neutral gray '예'/'아니오' (kit.jsx:26-34 — STATUS_KIND has no true/false keys), so a retired (active=false) department shows a muted '아니오' that reads the same weight as an active one. Admins scanning the list can't easily spot which departments are switched off.
  - fix: Give inactive departments a warn/danger tone — e.g., replace badgeCol('active',...) with a custom render passing kind='warn' when !r.active, or map true→'사용 중'(ok) / false→'미사용'(warn) in registry.js:246. 
- **[job-titles/feature·ux]** The help card text "직책 이름을 한 곳에서 관리합니다." omits where these titles are actually used, unlike the sibling departments screen whose help adds "사용자 폼의 '부서'는 여기 목록에서 고릅니다." (registry.js:245). The admin is not told that job titles populate the user form's 직책 selector, so the screen's purpose is under-explained relative to its identical-mechanics sibling.
  - fix: Append the same connective clause used by departments: help: "직책 이름을 한 곳에서 관리합니다. 사용자 폼의 '직책'은 여기 목록에서 고릅니다." (frontend/src/screens/registry.js:253)
- **[job-titles/feature·ux]** The edit form's 'active' checkbox uses checkLabel "사용" with no explanation of the effect, whereas the sibling departments screen uses "사용(끄면 새로 고를 수 없음)" (registry.js:248). Toggling active off is the primary alternative to deletion (backend delete_item blocks with a 409 that suggests deactivating instead), yet a job-titles admin gets no hint that unchecking prevents NEW assignments while keeping existing holders. Copy-paste omission vs departments.
  - fix: Set the edit active field checkLabel to "사용(끄면 새로 고를 수 없음)" to match departments and convey the semantics. (frontend/src/screens/registry.js:256)
- **[job-titles/feature·missing-feature]** The backend list endpoint accepts an `active` query param ("true면 새로 고를 수 있는 항목만") to filter to active or inactive items, but the registry config never sends it — the screen always fetches all rows and offers only a generic client-side text search. There is no way to isolate deactivated titles on a large list. The active state is visible via the badge column, but the supported server-side filter capability is not surfaced.
  - fix: Optional low-priority: add a filter control (e.g. an active/inactive/all toggle) that appends ?active=true|false to config.endpoint, or accept as a known minor gap. The active badge column mitigates severity. (app/org/router.py:43)
- **[job-titles/feature·design]** The user_count column reuses the departments label "소속 인원" (affiliated headcount). This reads naturally for a department but is awkward for a job title — people hold a title rather than 'belong to' it. Minor wording carry-over from the shared/copied config.
  - fix: Use a title-appropriate label such as "이 직책 인원" or "보유 인원" for the job-titles user_count column. (frontend/src/screens/registry.js:254)
- **[backup/feature·contract]** The '검증' action (registry.js:305) has no `when` guard, so it renders for rows in every status including 'running' (file still being written) and already-'failed'. Verifying a running backup reads a partially-written file and can produce a false verification failure that demotes a still-in-progress backup; verifying a failed backup whose file was pruned just re-fails.
  - fix: Gate the verify action, e.g. when:(r)=> r.status === 'succeeded' || r.status === 'verified', so it is only offered for backups whose file exists and is complete. 
- **[jobs/feature·ux]** last_error is TEXT truncated to 2000 chars server-side (repository.py:132) and is rendered with a plain col('last_error','마지막 오류') both in the table cell and in the detail drawer. A long, multi-line, stack-trace-like error will blow out the table row and is shown unformatted in the drawer — yet this is the single column meant to explain why a job failed.
  - fix: In registry.js give last_error a render that truncates in the table (e.g. first line / first ~80 chars with title tooltip) and, in the detail view, render it in a <pre> / monospace block so the full error is readable without breaking layout. 
- **[notion-mapping/feature·ux]** Slow synchronous '검증' has no in-flight feedback and allows duplicate submissions. Per-user verify calls n8n synchronously (~9-13s per the router docstring). runAction (DataScreen.jsx L33-41) tracks no busy/disabled state on action buttons (only FormModal tracks busy), so the admin waits ~10s with no indicator and can re-click, firing duplicate lookups.
  - fix: Disable the action button / show a spinner while the verify request is in flight in runAction. 
- **[notion-mapping/feature·empty-state]** No empty-state guidance and no prerequisite hint. Unlike sibling screens (runners/workflows/prompts all set emptyTitle+emptyHelp), notion-mapping sets neither, so an empty result shows only the bare default with no help. The help text ('자동 매칭되며 수동 지정도 가능합니다') also implies auto-matching just works, but verify/sync require a workflow named 'notion-user-mapping' to be registered and enabled (service.py MAPPING_WORKFLOW_NAME) — there is no guidance about this prerequisite.
  - fix: registry.js: add emptyTitle/emptyHelp and extend help text to state that auto-matching requires the 'notion-user-mapping' workflow to be registered and enabled, then run 자동 동기화. 
- **[audit/feature·ux]** The backend returns total (app/audit/router.py:83) but the screen never surfaces it — no 'N total entries' or 'showing 20 of N'. Combined with the missing pagination (finding 1), the user has no signal that they are viewing only a small recent slice of a much larger, growing log.
  - fix: Display the total count and current range in the toolbar/header of DataScreen when query.data.total is present (DataScreen.jsx:51/72-75). 
- **[notifications/feature·missing-feature]** The backend supports ?unread_only=true (router.py:37,40-41) and returns an `unread` count in the list response, but the screen exposes neither. There is no 'unread only' filter/toggle and no unread indicator on the page, so users cannot narrow the list to just unread notifications.
  - fix: Add an '안 읽음만' filter toggle that requests ?unread_only=true, and optionally surface the response `unread` count in the header. 
- **[shared-infra/design·a11y]** The UserMenu (App.jsx:108-114) and NotificationBell (NotificationBell.jsx:82-104) dropdowns declare `role="menu"`/`role="menuitem"` but implement none of the expected keyboard menu semantics: focus is not moved into the menu on open, and there is no Up/Down arrow navigation or roving tabindex between items. (Esc and outside-click close are correctly implemented.)
  - fix: Either move focus to the first item on open and add arrow-key navigation, or drop the `menu`/`menuitem` roles and treat them as a simple list of buttons/links to match actual behavior. 
- **[shared-infra/design·dead-affordance]** Stale metadata and a misleading comment. App.jsx:15-17 states that not-yet-migrated screens are shown as a 'Pending' state that links to the legacy console, and NAV items carry `ready`/`legacy` fields (App.jsx:20-49). No `Pending` component exists, and Sidebar (App.jsx:52-68) ignores `ready`/`legacy` entirely — every item is a plain NavLink and every route is actually wired via REGISTRY. The comment describes behavior that does not exist and the fields are dead.
  - fix: Remove the `ready`/`legacy` fields and the stale comment (or implement what the comment claims). Not user-facing, but exactly the kind of ported-over drift to clean up. 
- **[shared-infra/design·empty-state]** DataScreen always renders the search toolbar Card (DataScreen.jsx:72-75) regardless of state — the search box appears above the loading skeleton, above the ErrorState, and above the genuine first-run empty state (a table that has never had rows). Searching an empty or errored dataset is meaningless and adds visual noise to exactly the states that should feel calm/guiding.
  - fix: Only render the search toolbar when there is data (or at least suppress it on error and on the no-data-yet empty state) in DataScreen.jsx. 
- **[shared-infra/design·ux]** The app ignores the OS dark-mode preference. Dark is applied only via an explicit `[data-theme="dark"]` attribute set by the user-menu toggle (App.jsx:70-76, tokens.css:161); there is no `@media (prefers-color-scheme: dark)` fallback. A user whose OS/browser is in dark mode still gets the light theme on first load until they manually toggle. (This is a documented decision, but it is a first-impression UX gap.)
  - fix: When no theme is stored, seed the initial theme from `window.matchMedia('(prefers-color-scheme: dark)')` in getStoredTheme/applyTheme (App.jsx), keeping the manual toggle as an override. 
- **[shared-infra/design·a11y]** The toast container is `aria-live="assertive"` (kit.jsx:285), so every routine success toast (e.g. '저장했습니다', '추가했습니다') interrupts screen-reader output immediately. Assertive should be reserved for errors/critical alerts; routine confirmations should be announced politely.
  - fix: Use `aria-live="polite"` for the toast region (kit.jsx:285), or set live-region politeness per-toast so only error toasts are assertive. 
- **[shared-infra/code·dead-affordance]** NAV `ready`/`legacy` fields and the Pending/legacy-bridge comment are dead. App.jsx:15-17 describes a Pending state that links to the legacy console for un-ported screens, and every NAV item carries `ready`/`legacy` (App.jsx:18-50), but Sidebar (App.jsx:52-67) renders a plain NavLink for each and all routes resolve to real screens. Unused config plus a stale, misleading comment for future maintainers.
  - fix: Remove the `ready`/`legacy` fields and the Pending/legacy comment, or implement the described bridge. app/App.jsx:15-50. 
- **[shared-infra/code·a11y]** Toasts auto-dismiss after 3.5s with no manual close and no persistence for errors. ToastProvider removes every toast at 3500ms (kit.jsx:280) with no dismiss button, so error details (e.g. a 422 validation message surfaced via toast(e.message,"error")) vanish before the user can read or act on them, and the assertive live region re-announces on each push.
  - fix: Keep error-kind toasts until dismissed (or extend their timeout) and add a close button. ui/kit.jsx:275-290. 
