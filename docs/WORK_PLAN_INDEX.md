# ClovirONE 전체 작업 마스터 목록 (단일 색인, 2026-08-03 갱신)

> 흩어진 계획을 한 곳에 모은 **권위 있는 전체 목록**이다. 세부는 각 문서를 가리킨다. 새 세션은 여기부터.
> 상태 표기: [계획] 아직 / [대기] 사용자 승인·조건 대기 / [부분] 일부 완료 / [완료] 검증됨.
> 최우선 기준: 기존 정상 동작 무손상, 단계적, 회귀로 증명.
>
> **"완료"는 코드를 고친 게 아니라 사용자가 보는 층에서 결과를 확인한 것이다.** 이 문서의
> [완료]에는 무엇을 어떻게 확인했는지가 함께 적혀 있다. 증거 없는 [완료]는 쓰지 않는다.
> 실행 계획 전문: `~/.claude/plans/flickering-percolating-clover.md`

## 0. 2026-08-03 세션 — UI 전면 재설계 + 저장된 계획 실행

**운영 반영은 전 작업 완료 후 한 번에**(사용자 결정). 지금까지 모두 브랜치 `ui/mui-migration`.

- [완료] **안전장치**: 미커밋 100개 경로 전량 커밋(계획 4종 + 운영 배포된 마이그레이션 0020/0021이
  git에 없던 상태였다). 태그 `pre-mui-baseline`.
- [완료] **API 골든 계약 동결**: 9개 엔드포인트 × 3상태 = 30개. 본문을 실제로 해석하는 Notion
  페이크를 먼저 만들었다 — 기존 페이크는 URL 접두사만 봐서 mine/unassigned/team이 같은 행을
  돌려줬고, 그대로 뜬 골든은 가짜 기준선이 됐을 것이다.
- [완료] **러너 티켓 생성 계약 고정**(17 테스트). 실제 전송 본문을 관찰해 고정했고, 위조된
  requester가 무시되는 것도 검증.
- [완료] **MUI 7 전환 기반**: CSP는 `style-src`만 완화(`script-src 'self'` 유지, 회귀 테스트로 고정),
  디자인 자산 98개를 `app/static/brand/`로, 파비콘 5종 + webmanifest 배선.
- [완료] **4K 지원**: 브레이크포인트 `xxl(2200)`/`uhd(3000)` 신설, `styles/root.css`의 루트
  폰트사이즈 레버 하나로 글자·여백 동시 스케일. px 글자 크기 96곳을 rem으로.
  **측정: 3840에서 콘텐츠 폭 53.12% → 79.17%, 12px 미만 글자 6실패 → 0.**
- [완료] **공용 키트·셸·DataScreen**: `ui/kit.jsx` 내부만 MUI로 바꿔 화면 파일 수정 0건으로
  21개 화면 재스킨. 셸 재작성(명령 팔레트, 마스코트 도우미 입구). DataScreen 하나로 관리자 16화면.
- [완료] **채팅**: 순수 함수/훅 추출 후 뷰 재작성, 4K에서 3분할, 마스코트를 실제 대화 단계에 결선.
- [완료] **Phase 2c 문서 재분류**(아래 §7 참조).
- [완료] **러너 조용한 실패 3건 제거**(아래 §7 참조).
- [진행] 로그인 Jinja 이식, 티켓 캐시(0022/0023), 나머지 화면 웨이브(대시보드·Ops·리포트 /
  사용자·설정·티켓·스프린트 / 게시판·문서·놀이).
- [계획] CSS 철거(A9), Phase 3~8.

**검증 도구**: `scripts/ui_qa/` — 로그인된 세션으로 41개 라우트 × 라이트/다크 × 8해상도
(3840×2160 포함) 캡처 + 가로 overflow·콘솔 오류·깨진 이미지·중복 id·12px 미만 글자·
콘텐츠 폭 비율·세로 텍스트 붕괴 검사. `scripts/check_bundle_size.sh`로 초기 번들 gzip 예산 강제.

## 1. 근시일 계획 (상세: `NEXT_SESSION_PLAN.md`)
- [진행] A. 티켓 성능 근본 개선 (문서식 로컬 캐시 미러 + 워커 백그라운드 동기화, ticket_cache)
- [진행] B. 스프린트 회의 재설계 (티켓 배분 제거, 담당자별 티켓 리스트 클릭 시 상세)
- [진행] C. 홈 2단 레이아웃 (오른쪽 채팅, 왼쪽 카드+티켓+게시판)
- [계획] D. 채팅방 완성 (전체채팅 안읽음, 이모지 피커, 이미지 붙여넣기, 방 파하기, 초대 알림)
- [계획] E. 티켓 본문 수정 + 댓글
- [계획] F. 추가(메시지 삭제, 1:1 읽음, 온라인 표시, 그룹 관리, @멘션, 링크 인식, 알림 딥링크 일반화, 스프린트 진척 시각화, 반응형/접근성)

> 착수 시 반영해야 할 사실(코드 확인):
> - `_query_tasks`에 잘림 신호가 없다 — 티켓이 2000행을 넘는 순간 prune이 나머지를 전부 지운다.
> - 캐시 스펙에 `tid`·`url`·`project_ids`가 빠져 있다(화면이 `"GIT-"+tid`를 렌더한다).
> - 티켓 목록 1회 = Notion 왕복 3회, 스프린트 ~7회. 폼 드롭다운도 캐시해야 한다.
> - 채팅 이미지를 게시판 업로드 네임스페이스로 재사용하면 1:1 이미지가 전원에게 노출된다.
> - DM 방 파하기는 `dm_key` unique 때문에 재대화 시 500이 난다.

## 2. 대규모 구조/제품화 (상세: `PRODUCTIZATION_ARCHITECTURE.md`)
- [계획] 조직/부서 계층 + 관리자 티어(포탈/조직/부서) + row-level scoping. §0 확정=a안.
- [계획] Postgres 전환 (1000명/제품화 신호 시).
- [계획] Notion 티켓 백엔드 한계 대응(장기: 네이티브 저장 + Notion 선택 동기화).
- [계획] 운영 준비(조직/부서별 사용량·쿼터, 프로비저닝, 조직별 백업/내보내기, 관측성, 브랜딩).

> 착수 시 반영: presence는 이미 전체 채팅방에서 쓰기가 없다 — 1000명에서 진짜 병목은
> `_append_message`(INSERT + event_seq UPDATE, SAVEPOINT 12회 재시도)와 폴링 직렬화다.
> `WEB_WORKERS` 다중화는 공유 rate-limit 저장소 없이는 로그인 무차별 대입 제한을 N배로 약화시킨다.

## 3. 제품 도약 3축 (상세: `IDEAS_BACKLOG.md` 최우선 3축)
- [계획] 통합 검색 + 명령 팔레트(Ctrl+K). **팔레트 UI는 이미 있다**(현재는 메뉴 검색만 — 라벨도
  '메뉴 검색'으로 정직하게 뒀다). 백엔드 인덱스가 붙으면 같은 자리에 통합 검색이 들어간다.
  SQLite FTS5 확인 결과: `unicode61`은 한국어에 못 쓴다(`회의`가 `회의록`에 안 걸림).
  **`trigram` 토크나이저 + 3자 미만은 LIKE 폴백**. 채팅은 방별 ACL 때문에 v1 범위에서 제외.
- [계획] AI 도우미 심화: 오늘 브리핑, 문서 Q&A(로컬 캐시 RAG), 주간 다이제스트, 스탠드업 초안,
  미할당 티켓 트리아지 추천. **결정적 집계를 먼저 만들고 LLM 없이 테스트한 뒤 문장만 맡긴다.**
- [계획] 홈을 '오늘' 커맨드 센터로.

## 4. AI 플로팅 챗봇 (상세: `IDEAS_BACKLOG.md` 부록 A)
- [부분] 전역 마스코트 입구는 붙었다(우하단 버튼 + 좁은 화면 상단바 버튼 → `/chat`).
  도우미 탭에서 AI를 빼고 스프린트만 남기고 아이콘도 교체했다.
- [계획] 문맥 인지(보는 화면 기준 액션 제안), 드로어 내장 대화.
- 열린 결정 중 위치는 **우하단으로 확정**(관례 + 오른손 엄지). 나머지는 미결.

## 5. UI 전면 재설계 + 디자인 자산 (상세: `IDEAS_BACKLOG.md` 부록 C)
- [진행] 위 §0 참조. 기준 디자인은 `C:\ChatGPT\clovirassist-ui-final-project.bak\clovirassist-ui-final`.
- [완료] 마스코트 클로비: ChatGPT가 제작한 자산 채택(포즈 10종 + 프레임/레이어 보존).
  런타임은 완성 포즈만 교체한다(프레임/레이어 합성 금지 — 정적 검사로 고정).
- [완료] 디자인 자산 98개 배치. 단 `frontend/src/assets/`가 아니라 **`app/static/brand/`**다 —
  로그인(Jinja)과 SPA가 같은 파일을 써야 하고, `stage-static-update.sh`로 재시작 없이 교체된다.
- 참고: 로고는 인라인 SVG로 바꿨다(테마별 파일 고르기 금지 — 같은 함정을 두 번 밟았던 규칙).
  원본 자산의 viewBox에 빈 공간이 36% 있어 함께 잘라냈다.

## 6. 영역별 백로그 (상세: `IDEAS_BACKLOG.md` 영역 1~9 + 부록 B)
### 티켓/업무
- [계획] 칸반 보드 뷰, 리스트 인라인 빠른 변경, 티켓 의존성/차단, 마감 타임라인, @멘션.
### 문서
- [계획] 문서 전문 검색(FTS), 자주 보는 문서 추천, 문서→티켓 연결, 팔로우 변경 알림.
### 팀 공간
- [계획] 채팅 스레드/답글, 고정 공지, 게시판↔채팅↔티켓 상호 링크, 게임 리더보드/전적, 정기 이벤트.
### 알림/프레즌스
- [계획] 딥링크 일반화, 하루 1회 요약 알림, 온라인/최근 접속 표시, (선택) 사내 SMTP/LINE/Slack.
### 관리자/운영
- [계획] 사용자용 시스템 상태 배너, 러너/워크플로 헬스 대시보드.
- [계획] 관리자 대시보드 사용량/에러 추세, 감사 로그 이상 탐지/내보내기/저장 필터.
- [계획] **입·퇴사 워크플로(퇴사 시 담당 티켓 재배정)** — 이 그룹에서 가장 먼저. 지금 실제 운영 공백이고
  ticket_cache + RBAC 스코프가 둘 다 필요한 유일한 항목이다.
- [계획] 사용자 대량 작업+CSV, RBAC 매트릭스, 읽기전용 임퍼소네이션(감사 강제).
- [계획] 승인 위임/에스컬레이션/템플릿, 스케줄러 캘린더 뷰, 프롬프트/정책 diff·사용량 통계.
- [계획] 백업 예약+복구 리허설+무결성 검증, 전체 공지, AI 쿼터, 기능 플래그 UI, 조직도 뷰.
### 사용자 셀프서비스
- [계획] 내 다른 세션 해제, 알림 환경설정, 방해 금지, 내 업무량(WD), 완료 통계, 저장된 뷰,
  첫 로그인 투어, 내 활동 피드.
### 성능/모바일/접근성/신뢰
- [부분] 반응형·접근성은 재설계와 함께 진행 중(4K 포함 8해상도 자동 검사 도입).
- [계획] 캐시 전략 전반화, 백업/복구 리허설, 유지보수 모드 UX.

## 7. 기존 로드맵 잔여
- [완료] **Phase 2c 문서 분류 구조 개편** (2026-08-03).
  Notion 원본 104건의 유형/카테고리를 신규 택소노미로 재분류(성공 104, 실패 0).
  관계 대상 DB에 신규 분류 12종을 만들고, 참조 0건인 옛 분류 25종은 보관 처리했다.
  문서 DB에 없던 `기술 태그` 다중 선택 속성을 만들어 16건에 값을 채웠다.
  **검증: Notion 재조회 분포와 포탈 API 분포가 종류·분야·태그 모두 일치.**
  스크립트 `scripts/phase2c_reclassify.py`(--rollback 지원), `scripts/phase2c_taxonomy_align.py`.
  기록: `var/phase2c/{backup,audit,created,pruned,tags}-*.{json,jsonl}`.
  > 이 작업에서 배운 것: Notion 원본을 고쳤더니 화면은 오히려 나빠졌다.
  > `classify.py`의 매핑표가 옛 이름만 알고 있어 새 이름이 전부 '기타'로 떨어졌다.
  > 화면 층에서 확인하지 않았다면 '적용 완료'로 넘어갈 뻔했다. 지금은 유효한 택소노미 값이면
  > 그대로 통과하도록 고쳐 두었다 — 앞으로 분류가 늘어도 여기를 다시 고칠 일이 없다.
- [부분] Phase 3 러너 티켓 라우팅. 계약은 테스트로 고정했고 **조용한 실패 3건을 제거**했다:
  워크플로가 GET이면 본문이 통째로 버려지는데도 성공으로 끝나던 것, 빈 2xx를 '처리되었습니다'로
  답하던 것, allowlist 밖 주소를 3회까지 무의미하게 재시도하던 것.
  **여전히 미검증**: 실제 채팅 → 진짜 Notion 티켓 생성. 테스트로는 증명할 수 없다(체크리스트는
  `tests/integration/test_chat_ticket_routing_contract.py` 하단).
  **미해결 2건**: n8n이 우리 `idempotency_key`를 읽지 않아 중복 방지가 n8n 메모리 상태에만
  의존한다. 사용자 재시도와 워커 재시도가 본문상 구분되지 않는다. 둘 다 n8n 워크플로 변경 필요.
- [계획] Phase 7 하드닝: 동시성, 보안, 전체 회귀, 성능/장애 격리, 단계 배포+롤백 총점검.
- 참고: 게시판/문서/문서생성/놀이 7종/게임 AI(다크런치)는 이미 [완료] 배포.

## 8. 사람이 해야 하는 확인 (에이전트가 증명할 수 없는 것)
1. 실제 채팅으로 티켓 생성 e2e — 초안 → "등록해줘" → Notion 페이지를 열어 제목/담당자/마감/
   우선순위 대조. 재전달 시 티켓이 **하나만** 생기는지.
2. 실제 4K 패널에서 **100%와 150% 배율 양쪽**.
3. 다크모드가 재로그인 후에도 유지되는지(공용 PC에서 계정별로 갈리는지).
4. 관리자 화면군별 쓰기 액션 1개씩.
5. 알림 딥링크 → 목적 화면 + 나브 하이라이트.
6. 채팅 이미지 전송, 실패 후 재시도.
7. 비밀번호 강제 변경 흐름.

## 착수 순서(권장)
UI 재설계 완주 → 티켓 캐시 컷오버 → 근시일 D/E/F → 제품화 0024~0026 + RBAC → 3대 축 →
관리자/사용자 백로그(퇴사 워크플로 우선) → 하드닝 → 통합 배포.
각 항목은 착수 시 스펙 수렴 후 구현. YAGNI, 한꺼번에 다 만들지 않는다.
