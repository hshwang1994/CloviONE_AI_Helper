# PROGRESS STATUS — 지금 실제로 얼마나 됐는가

> 이 문서는 보고서가 아니라 **최신 Snapshot**이다. 의미 있는 작업 단위(사이클 배치, 검증
> 루프)가 끝날 때 갱신한다 — 작은 수정 하나마다 갱신하지 않는다.
>
> **작성 규칙**: 기억이나 문서의 "완료" 표시를 그대로 믿지 않고, 그때마다 실제 Source·
> Git diff·테스트 결과·배포 상태·QA_COVERAGE·Chrome 검증 결과를 대조해서 쓴다. 숫자는
> 근거 기반 근사치이며 체크박스 개수 단순 집계가 아니다. 불확실한 작업은 완료율에 넣지 않는다.
>
> 진입점은 여전히 [WORK_STATE.md](WORK_STATE.md)다 — "지금 뭘 하고 있는가"는 거기,
> "전체로 보면 어디쯤인가"는 이 문서.

**마지막 갱신**: 2026-08-10 (Cycle 4 배치 5 배포 직후, 대조 근거는 아래 §7)

---

## 1. 한눈에 보는 숫자 (근거 기반 근사치)

| 지표 | 근사치 | 근거 |
|---|---|---|
| BACKLOG 전체 항목 수 | **약 522건** | `docs/BACKLOG.md`의 `\| XX-NN \|` 형식 행 정규식 카운트 (2026-08-10 재확인) |
| 손댄 항목(구현완료+실환경검증완료+부분) | **약 39건 (약 7%)** | 구현완료 26 + 실환경검증완료 12 + 부분 구현완료 1 |
| 미착수("발견"만 있음) | **약 285건 (약 55%)** | 같은 카운트 |
| 기타 상태(작업예정/작업중/러너 서브시스템 등) | **약 198건 (약 38%)** | 아래 §5 참고 — 상당수가 `RN-*`(러너 별도 코드베이스)·`QA-*`(하네스 자체) |
| QA_COVERAGE 7축 중 실제로 진행된 축 | **S(캡처) 70/73, S(판독) 9/70, 나머지(F/A/D/C/R/L/V) 거의 0~부분** | `docs/QA_COVERAGE.md` §0 전체 요약 (2026-08-08 갱신, 이후 재실행 안 됨) |
| 실환경 검증 완료율(이번 세션이 다룬 항목 기준) | **약 55%** (Cycle 4에서 다룬 24건 중 약 13건이 실서버에서 직접 확인됨) | 아래 §3 |
| 실환경 검증 완료율(BACKLOG 전체 기준) | **약 2~3%** (522건 중 약 12~13건) | 위 두 줄의 합성 — **이 숫자가 진짜 기준선이다** |

**해석**: "Cycle 4를 4배치 넘게 돌았다"는 사실이 제품 전체의 완성도를 대변하지 않는다.
Cycle 4는 **이전 Opus 6라운드 조사가 CORE/UA/UB로 분류해 둔 보안·안정성 결함 묶음**을 파고든
것이고, BACKLOG의 나머지 약 93%(대부분 `FN-*`·`RN-*`·`DS-*`·`AI-*`·`IA-*`)와 QA_COVERAGE의
73라우트 × 8축 체계적 전수검증은 **거의 손대지 않았다.** 이 문서는 그 격차를 숨기지 않는다.

---

## 2. 완료 단계 정의 (이 문서 전체에서 이 구분을 지킨다)

```
미착수 → 진행 중 → 구현완료 → 테스트완료 → 배포완료 → 실환경검증완료
```

- **구현완료** = 코드 수정 + 회귀 테스트 작성 + revert-to-verify(고치기 전으로 되돌려 테스트가
  실패하는 것 확인) 통과. **아직 실서버에 없을 수 있다.**
- **배포완료** = `UPGRADE_OK` + 서비스 3종 `active` + `/healthz`·`/readyz` 200.
- **실환경검증완료** = 배포된 실제 서버에서 그 버그의 구체적 증상이 재현 안 되는 것을(또는
  고쳐진 동작을) **직접 관찰**했다(curl/Chrome/DB 조회 등). 코드를 고쳤다는 사실만으로는
  이 라벨을 못 붙인다.

BACKLOG.md의 상태 문자열이 이 구분과 정확히 대응한다: `발견` = 미착수, `구현완료` = 구현+
테스트완료(배포 여부는 커밋/배포 이력으로 별도 확인), `실환경검증완료` = 전체 완료.

---

## 3. 현재 작업 중인 영역

**Cycle 4, 배치 5** — CORE-12 잔여 4개 소항목(모두 방금 배포·부분 실환경검증):
- ratelimit 버킷 무한 증가 → LRU 상한(10k) — **구현완료**, 실환경검증 불가(순수 프로세스
  내부 상태, HTTP로 관측 불가 — 정당한 사유, §6 참고)
- request-id 유니코드 `isalnum()` 반사 → ASCII 전용 검사 — **실환경검증완료**
  (`curl -H "X-Request-ID: 한글한글테스트" .../healthz` → 200 + 헤더가 생성된 uuid로 교체됨,
  2026-08-10 확인)
- `audit._SENSITIVE_KEY`가 `secret_ref` 이름까지 가리던 것 → 정확히 그 필드명만 예외 —
  **구현완료**, 실환경검증 안 함(정당한 사유, §6)
- `SecretMissingError`가 ref 이름을 응답에 흘리던 것 → 기본 메시지 + 서버 로그로 이동 —
  **구현완료**. **부수 발견**: 이 수정이 Notion 관련 호출부 4곳(`notion_source.py`,
  `notion_docs.py` ×2, `notion_write.py`, `probe_notion.py`)에서 "토큰 미설정"을
  문자열 매칭(`ref_name in str(exc)`)으로 판별하던 코드를 깨뜨렸다 — 전체 테스트 스위트
  1차 실행에서 10건 실패로 잡혔고, 타입 기반(`except SecretMissingError`) 판별로 교체 +
  누락돼 있던 회귀 테스트 2건 신규 추가로 고쳤다. **실서버에 이 회귀가 배포된 적은 없다**
  (같은 세션 안에서 잡고 고쳐서 커밋했다).

다음 결정 대기: 배치 6에서 무엇을 다룰지 — 남은 `UA-04`~`UA-10`(Med 등급, 아직 스케줄 안 됨),
아니면 두 Critical(`AI-30` 러너 컨텍스트 누출, `FAIL-01` 빈 상태/에러 상태 혼용 5+화면)의
전용 사이클, 아니면 아래 §6이 요구하는 QA_COVERAGE 전수검증 착수. **미결정.**

---

## 4. 완료 / 부분완료 / 미착수 영역 (거시적 지도)

### 완료된 주요 작업 (실환경검증완료 또는 그에 준함)
- UA-01, UA-02(RBAC 스코프 누출, 배포 전부터 실서버에서 재현 확인된 항목 — 코드 수정 후
  재검증은 서버 토폴로지 한계로 결론 없음, 정직하게 기록됨)
- CORE-11(공지 링크 검증/저장 형태 불일치) — 실서버에서 `link_url=""` → 201 + null 저장 실측
- CORE-12 중 request-id 유니코드 건(위 §3)
- 배포 파이프라인 자체(build-bundle → scp → 매니페스트 검증 → upgrade 스크립트 → 서비스·
  healthz/readyz 검증)는 5회 연속 성공 — 이 메커니즘은 신뢰할 수 있다

### 구현완료이나 실환경검증 미완(코드 근거는 있으나 실서버에서 직접 못 봄)
CORE-01~10 대부분, UB-01~05, SEC-01 — 각 항목의 BACKLOG 행에 **왜** 실서버 검증이 어려운지
개별 사유가 적혀 있다(파이썬 객체 참조라 HTTP로 관측 불가, DB 잠금 주입은 운영 위험, 등).
이 사유들 자체가 §6의 재검증 대상이다 — 아직 전부를 다시 따지지 못했다.

### 사실상 미착수(발견만 되고 손 안 댐) — 약 285건, 대표 범주
- **`DS-*` 디자인 시스템**(토큰·Variant·Typography·반응형) — Master Plan(§3, 사이클 1)이
  **다른 모든 사이클보다 먼저** 하라고 명시했는데, Cycle 4는 이를 건너뛰고 곧장 CORE/UA/UB로
  들어갔다. **이것이 원래 계획과의 가장 큰 이탈이다** (§5-1 참고).
  이미 확정된 회귀 하나: `DS-32`(4K `tiny_text` 검사 전 화면 실패 — 2026-08-04엔 통과했었다).
- **`AI-*` AI 도우미 아키텍처**(스트리밍·중단·직렬화·기억) — 사용자가 직접 한계를 지적한
  영역인데 Cycle 4는 손대지 않았다.
- **`IA-*` 관리자 IA 재편**, **`RN-*` 러너(다른 코드베이스, `runner/claude-work-assistant`)
  전용 결함 약 14건**(RN-01~RN-14, 완료 승인 오검출·락 유실·재시도 유실 등 — 상당수 High) —
  이 세션은 `app/`(웹 백엔드)만 다뤘고 러너 코드베이스는 한 줄도 안 건드렸다.
- **`FN-*` 기능·데이터·권한 E2E**(사이클 4 대상) 중 아직 안 다룬 항목 다수 — 예: `FN-01`
  메일 모듈 UI 0개(High, 서버는 원인을 알지만 화면이 없음).

### 검증 대기(구현 여부와 무관하게 QA_COVERAGE 7축이 비어 있음)
73라우트 전체. `docs/QA_COVERAGE.md` §0에 따르면 F(기능 실행)·D(DB 흐름)·C(Console/Network)는
**전 라우트 0**, R(RBAC)·A(API)는 부분(`~`), S(판독)는 9/70만 눈으로 봄. **이것이 지금 이
제품에서 가장 큰 미검증 표면적이다.**

---

## 5. 원래 계획과의 역대조 (Master Plan vs 실제 진행)

### 5-1. 순서 이탈
`docs/WORK_PLAN_INDEX.md` §3은 **"디자인 시스템(1) → AI 도우미(2) → 관리자 IA(3) →
기능/권한 E2E(4) → 전체 재순회(5+)"** 순서를 "근본 원인이 아래로 흐르는 순서"라고 명시하며
"뒤집으면 두 번 일한다"고 경고한다. Cycle 4는 이 순서 없이 **Opus 6라운드가 남긴 CORE/UA/UB
카테고리**(성격상 §3의 4번 "기능·데이터·권한 E2E"에 가장 가깝다)로 곧장 들어갔다.

**재판단**: 이 이탈을 지금 되돌릴지는 미결정이다. CORE/UA/UB가 보안·데이터무결성 성격이라
"먼저 고쳐야 사고를 막는다"는 정당성은 있지만, 디자인 시스템(1)을 건너뛴 채 화면을 계속
고치면 Master Plan이 경고한 "뒤집으면 두 번 일한다"가 실제로 발생할 위험이 있다(예: 지금
고친 컴포넌트가 나중 디자인 시스템 재설계에서 다시 바뀜). **다음 결정 필요 — 그때까지
"보류"가 아니라 "결정 안 됨"으로 기록한다.**

### 5-2. Decisions 재검토 (규칙 때문에 누락된 것이 있는가)
`docs/DECISIONS.md`를 훑어 "하지 않는다"류 결정을 확인했다. 주요 1건:

- **D-21**(`RN-01`~`RN-03`을 실서버에서 재현하지 않는다): **여전히 유효** — 실서버에 연결된
  Notion 워크스페이스가 현대모비스·SK하이닉스 등 **실제 고객사** 데이터라서, 서버가 테스트용
  이어도 그 쓰기 부작용은 진짜다. 이건 낡은 판단이 아니라 지금도 유효한 안전 경계다.
  **단, 이 결정은 "서버 재현 방법"만 배제한다 — `RN-01`~`RN-14` 자체(로컬 스텁 재현 +
  코드 수정)는 여전히 미착수 상태이고 이 문서 §4가 그렇게 기록한다.** "규칙 때문에 안 했다"가
  "그러니까 안 해도 된다"로 슬며시 바뀐 곳은 없었다.

다른 "제외" 성격 결정(D-08 하드코딩 이식 금지, D-23 스크린샷만으로 겹침 판정 금지 등)은
설계 원칙이지 작업 배제가 아니어서 재검토 대상이 아니다. **D-21 외에 추가로 재검토가 필요한
결정이 있는지는 아직 전수로 못 봤다** — `docs/DECISIONS.md` 전체 477줄 중 발췌만 확인함.

---

## 6. 이번 세션에서 "정당한 미검증"으로 남긴 것들 (핑계가 아니라 근거)

아래는 §1의 "실환경검증완료" 분모에서 뺀 항목들과 그 이유다. 매 배치 문서(WORK_STATE.md)에
개별적으로도 적혀 있다 — 여기 모은 이유는 패턴을 보기 위해서다.

| 유형 | 예시 | 왜 실서버에서 못/안 하는가 |
|---|---|---|
| 순수 프로세스 내부 상태 | ratelimit 버킷 LRU, SettingsCache 참조 공유 | HTTP로 관측 불가 — 파이썬 객체 자체를 봐야 함 |
| 운영 위험이 더 큰 재현 | DB 잠금·손상 주입, 파일 타임스탬프 조작 | 공유 프로덕션 서버에서 고의로 장애를 만드는 것 |
| 데이터 규모 재현 불가 | ratelimit 1만 버킷 도달 | 실서버에 1만 개 서로 다른 클라이언트를 만드는 건 낭비/비현실적 |
| 정리 경로가 없는 리소스 | SecretMissingError용 테스트 Integration | Integration Registry에 **삭제 API가 없다** — 만들면 영구히 남는다(비활성화만 가능) → **이번엔 만들지 않기로 결정** |
| 도구 자체의 한계 | 제어문자를 담은 curl 요청 | 셸 도구가 제어문자 포함 명령을 승인 UI에서 숨겨질 위험으로 차단 |
| 실제 고객 데이터 보호 (D-21) | RN-01~03 서버 재현 | 연결된 Notion 워크스페이스가 실제 고객사 데이터 |

**이 표에 없는 "구현완료" 항목**(예: 상당수 CORE 항목)은 **아직 이유를 안 적었거나 단순히
시도 안 한 것**이다 — "이 표에 있어야 정당하다"가 아니라 "이 표에 없으면 아직 판단 안
됐다"로 읽는다.

---

## 7. 최근 테스트 / 배포 상태 (근거 시각)

- **백엔드 pytest**: 2026-08-10 07:0x 기준 전체 green(1회 실패 → 원인 수정 → 재실행 green,
  이력은 `docs/BUILD_LOG.md` Cycle 4 배치 5 항목 참고). 정확한 전체 개수는 CLAUDE.md §1이
  명시한 대로 여기 박지 않는다 — `pytest --collect-only`로 그때그때 센다.
- **정적 검사**: `STATIC_CHECKS_OK` (2026-08-10, 배치 5 배포 직전)
- **배포**: 커밋 `6e300b3` → `UPGRADE_OK` (2026-08-10 07:06 KST) → 서비스 3종 `active` →
  `/healthz`·`/readyz` 200
- **QA_COVERAGE**: 마지막 갱신 2026-08-08(사이클 0) — **Cycle 4 배치 1~5가 배포된 뒤로 한 번도
  재실행되지 않았다.** 그 사이 배포된 변경이 기존 캡처(`c1-admin`)와 화면상 회귀를 만들었는지
  확인 안 됨.
- **프런트(React)**: 배치 1~5는 전부 백엔드(`app/`)만 건드렸다 — `frontend/` 변경 없음,
  `npm test` 재실행 불필요(BUNDLE_FRESH_OK가 이를 확인).

---

## 8. Blocker

없음. 배치 5는 배포·기본 검증까지 끝났고 다음 배치로 자동 진행 가능한 상태.
**결정이 필요한 것**(Blocker는 아니지만 다음 방향을 가른다): §5-1의 순서 이탈을 이대로 둘지,
§3의 "다음 배치 대상"을 무엇으로 할지.

---

## 9. 다음 자동 진행 작업

1. Cycle 4 배치 5 문서 마감(WORK_STATE.md·BACKLOG.md·BUILD_LOG.md 갱신 + 커밋) — **진행 중**
2. 이어서 QA_COVERAGE 전수검증 착수 여부를 포함해 다음 배치 범위를 정하고 계속한다 —
   사용자 확인을 기다리지 않는다(위 지시 §8·§9에 따름).
