# INVENTORY 09 — n8n · 외부 Runner

**정본**: 서버 systemd · n8n DB · `runner/**` Source
**측정**: 2026-08-20 (Plan Mode)
**소유**: **S11** — 제거. **S0~S10 은 아무것도 정지시키지 않는다.**

## 실행 중인 것

| 서비스 | 포트 | 실행 주체 | 비고 |
|---|---|---|---|
| `n8n` | 127.0.0.1:5678 / 5679 | `n8n` | |
| `claude-work-assistant` | 127.0.0.1:8789 | `n8n` | `assistant.py` (**375 KB · 6,395줄**) |
| `claude-request-interpreter` | 127.0.0.1:8787 | `n8n` | |
| `claude-ticket-runner` | 127.0.0.1:8788 | `n8n` | |

> 참고로 제품 서비스는 `clovirone-web-assistant`(127.0.0.1:8080, `--workers 1`) ·
> `clovirone-web-worker` · `clovirone-web-worker-conversational` · `clovirone-privhelper`(root) ·
> `nginx`(10.100.64.71:80/443) 다.

## 워크플로

**23개 중 활성 2개.** 실행 이력 총 **23회**.

| 활성 워크플로 | 실제 하는 일 | 처리 |
|---|---|---|
| `clovirone-work-assistant` | Notion **전량** 조회 → `127.0.0.1:8789` 호출 → Notion 쓰기/댓글/첨부 | **제거.** 코퍼스는 PG 에서 **권한 필터를 거쳐** 오고, 쓰기는 PG 도메인이 한다 |
| `clovirone-notion-user-mapping` | Notion 사용자 매핑 | **Notion 과 함께 소멸** |

## 이것이 지금 보안 구멍인 이유

n8n 이 `returnAll: true` 로 Notion 작업 DB **전체**를 가져와 runner 에 넘기고, runner 는
`tickets[:800]` 을 그대로 모델 프롬프트에 싣는다 (`runner/claude-work-assistant/assistant.py:3156`).
**요청자는 대명사 해석(`내 티켓`)에만 쓰이고 필터로는 쓰이지 않는다.**

같은 저장소의 `app/assistant/facts.py:169-176` 은 정반대로 `viewer=user` 를 넘기며
「사용자가 화면에서 볼 수 없는 일을 답변으로 알려 주면 그것도 유출이다」라고 적어 두었다.

**즉 이것은 설계 선택이 아니라 한 경로만 이 모델을 우회하고 있는 것이다.**
S10 이 `effective_visibility_clause` 를 AI 경로에 적용하면서 닫힌다.

## 제거 대상 (S11)

| 대상 |
|---|
| `n8n.service` · `claude-work-assistant.service` · `claude-request-interpreter.service` · `claude-ticket-runner.service` |
| 포트 **5678 · 5679 · 8787 · 8788 · 8789** |
| `/var/lib/n8n` |
| `backup-cron.sh` 의 n8n 절 |
| **`scripts/validate-clovirone-web-assistant.sh:23` 의 n8n 활성 단언** — 그대로 두면 n8n 제거 시 검증이 실패한다 |
| `app/workflows/` n8n Provider |
| `tests/conftest.py` 의 n8n Integration seed |
| `docs/n8n/` |

## 순서 — 끄고 나서 옮기지 않는다

```
워크플로 Export 보관 → 로직 이관 → 자체 경로 검증 → 병행 운영 → n8n 정지 → 제거
```

`assistant.py` 의 **의도 분류 · 규칙 기반 티켓 질의 · JSON Schema 강제**는 **제품 안으로 흡수**한다
— 거기가 권한을 강제할 수 있는 유일한 자리다. Claude CLI 호출만 `claude_cli` Adapter 로 남는다.

`runner/claude-work-assistant/test_assistant.py` **293건** 중 이관된 로직을 덮는 것은 앱 테스트로
**재작성**하고, 나머지는 서비스와 함께 퇴역한다 (R9).

## 완성도 — S1 이 마저 할 것

없다. **S11 의 입력으로 그대로 쓴다.**
다만 S9 가 Model Gateway 를 만들 때 `assistant.py` 6,395줄 중 **무엇이 이관 대상인지**를
293개 테스트로 판별한다 — 그 판별 자체가 R9 의 완화책이다.
