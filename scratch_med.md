- [dead-affordance|login] The 'forgot password' hint (login.html:84-87) tells the user '관리자에게 재발급을 요청하면...' but provides NO way to actually do it — no mailto link, no admin/IT-helpdesk email, no phone, no link. The <strong> text is styled emphasis, not a control. A user who forgot their password reaches a genuine dead end: the only stated recovery path has zero mechanism on the page. This is the one real user flow (account recovery) that the login screen does not serve end-to-end.
    FIX: In login.html:85-86 make the recovery path actionable: wrap the guidance in an <a href="mailto:helpdesk@goodmit.co.kr?subject=비밀번호 재발급 요청"> (or the real IT/admin contact), or at minimum surface a concrete contact address/extension so the instruction is completable. Pull the address from branding/settings rather than hardcoding.
- [ux|login] Submitting gives almost no progress feedback. On submit, login.js:30 only sets submitButton.disabled=true (CSS dims it to opacity 0.65, login.css:213); the button label stays '로그인' with no spinner and no '로그인 중…' text. Argon2 verification is deliberately slow (timing-equalized in auth/router.py) so on a slow link the user sees a dimmed-but-unchanged button and may re-click or assume nothing happened. Loading state has no explicit indicator.
    FIX: In login.js submit handler set a busy state: swap button text to '로그인 중…' (restore in finally) and/or toggle an aria-busy attribute plus a small inline spinner. Keep the existing disable. Ensure the label is restored on both success (before redirect it's fine to leave) and error paths.
- [dead-affordance|change-password] There is no show/hide password toggle on any of the three fields (change_password.html:15-30 are plain <input> with no toggle button), even though the login page provides one (login.html:73, login.js:12-17). This is precisely the screen where a toggle matters most: the user is CREATING a long, complex new password and re-typing it into a confirm field — both masked — with no way to verify what they typed. Login (re-entering a known password) has the affordance; password creation (where errors are silent until submit) does not.
    FIX: Add a '표시/숨김' toggle button (reuse login's .input-wrap + .btn-ghost pattern and the toggle handler from login.js:12-17) to at least the new-password field, ideally new + confirm.
- [design|change-password] The error box #cp-error uses only the plain .form-error style (base.css:108-113: bare red text, no background), while login gives its error box an emphasized badge because plain red text 'was easy to miss' (login.css:181-190, #login-error:not(:empty) adds badge-error-bg + padding). The lesson login learned was never applied here — and this screen depends ENTIRELY on the error box to communicate the multi-line password-policy violations (details.join), so the one thing the user must read is styled as the easy-to-miss variant.
    FIX: Give #cp-error the same not(:empty) badge treatment as #login-error (background var(--badge-error-bg), radius, padding, reserved min-height) so policy-violation errors stand out; consider rendering error.details as a list rather than a space-joined string.
- [bug|change-password] The submit button is enabled on page load (change_password.js:55 only disables it inside the submit handler), but csrfToken stays null until the /api/me fetch resolves (lines 21-42). A user who fills the form and submits before that request returns hits the !csrfToken guard (line 51) and is told '세션 정보를 불러오지 못했습니다. 새로고침 후 다시 시도하세요.' — i.e. a transient loading state is reported as a failure and the user is told to refresh a perfectly healthy session. The 'still loading' state and the 'failed to load' state are conflated.
    FIX: Disable #cp-submit until csrfToken is populated (re-enable in the /api/me .then), and while pending show a neutral '세션 정보를 확인하는 중…' rather than the failure/refresh message. Only surface the refresh instruction on an actual non-401 error (already handled at lines 24-28/33-35).
- [ux|change-password] No live/inline validation of any kind. The confirm-match check and the entire password policy are evaluated only on submit (change_password.js:47-48 for match; server round-trip for policy). There is no requirements checklist that ticks off as the user types (length met / 3 classes met / passwords match), which is the standard pattern for a mandatory password-creation gate. Every mistake costs a full submit + error read.
    FIX: Add a live requirements checklist under the new-password field that updates on input (length, class count, match with confirm), driven by the same policy numbers from finding #2, so the user sees progress and knows they'll pass before submitting.
- [dead-affordance|change-password] The form has novalidate (change_password.html:15) so the required attributes on the three inputs (lines 18/22/26) never fire, and change_password.js only checks new===confirm (line 47) — it never checks for empty fields. Submitting an empty current password POSTs '' and the backend returns 401 '현재 비밀번호가 올바르지 않습니다' (router.py:256-257), i.e. an empty required field produces a misleading 'wrong password' error instead of 'fill this field'. The required attributes are decorative.
    FIX: Either drop novalidate to let native required validation run, or add empty-field checks in change_password.js before the csrf/POST step with a clear '모든 항목을 입력하세요' message.
- [contract|change-password] change_password.html:13 hardcodes the policy as '3종류 이상... 12자 이상', but the backend reads an admin-configurable password_policy (router.py:258-263 -> policy.get('min_length',12)/get('min_classes',3)) and validate_password_policy emits messages built from those effective values. If an admin raises min_length to 16, the page still advertises '12자 이상' while the server rejects <16 with '최소 16자', so the on-screen guidance is wrong.
    FIX: Render the hint from the effective policy: pass min_length/min_classes into the template context in change_password_page (router.py:229-237) and interpolate them, instead of hardcoding 12/3.
- [bug|change-password] Race / no loading state: change_password.js loads csrfToken asynchronously from /api/me (line 21) while the form is fully interactive from first paint. If the user (or password-manager autofill + Enter) submits before the fetch resolves, csrfToken is null and they get '세션 정보를 불러오지 못했습니다. 새로고침 후 다시 시도하세요.' (line 52). The advice to refresh just restarts the same race; the correct action is to wait a moment. There is no disabled/loading state on the submit button while CSRF is in flight.
    FIX: Disable #cp-submit until csrfToken resolves (re-enable in the /api/me .then), and change the null-csrf message to '세션 정보를 불러오는 중입니다. 잠시 후 다시 시도해 주세요.'
- [dead-affordance|dashboard] The '새로고침' (refresh) button gives no feedback (Dashboard.jsx:21). It calls q.refetch() but never uses q.isFetching — the button doesn't disable, show a spinner, or change label, and the page shows no loading state on refetch (isLoading is only true on first load). The user cannot tell the click did anything. The codebase's own Ops.jsx:15 already does this correctly (disabled={q.isFetching}, '수집 중…'), so this is an inconsistent regression.
    FIX: Wire isFetching: <Button ... disabled={q.isFetching}>{q.isFetching ? '새로고침 중…' : '새로고침'}</Button>, matching Ops.jsx. Dashboard.jsx:21.
- [missing-feature|dashboard] A live ops/status dashboard shows no data-freshness indicator. With staleTime 30s and no refetchInterval, the health/metrics can be minutes old and nothing tells the operator when it was last fetched, so 'worker: up' or 'failed jobs: 0' may be stale and silently trusted.
    FIX: Show an 'HH:MM 기준' timestamp near the header from q.dataUpdatedAt (formatted via fmtDateTime), and/or add refetchInterval to keep it live. Dashboard.jsx:20-21.
- [ux|dashboard] The '최근 주요 변경' (recent critical changes) section under-serves its purpose (Dashboard.jsx:105-118). (1) It prints actionKo(action) then objKo(object_type), which duplicates the object noun — e.g. action 'user.role_change' + object_type 'user' renders '사용자 · 역할 변경  사용자'. (2) It never shows WHICH object (object_id is present in the payload at service.py:171 but unused) nor WHO made the change (actor is not even selected in build_dashboard), so entries read as 'someone changed some user'. (3) There is no link to the full audit log, making it a dead-end display.
    FIX: Drop the redundant objKo column (the action already carries the object noun), render object_id, add actor to the backend payload and show it, and add a '전체 보기' link to /audit. Dashboard.jsx:105-118; service.py:170-174.
- [dead-affordance|dashboard] In '확인이 필요한 항목', the cert-expiry (Dashboard.jsx:41-42) and disk-usage (44-45) alerts have no 'to', so they render as non-clickable divs while the failed-job/queued/backup alerts render as clickable buttons in the same grid. An operator sees 'D-7 인증서 만료' or '디스크 90%' flagged as danger but is given no next step and no visual cue that these two behave differently from their neighbors.
    FIX: Give these alerts a destination (e.g. cert → /settings or a maintenance page, disk → /diagnostics), or if none exists, render remediation guidance instead of a look-alike clickable card. Dashboard.jsx:41-45.
- [missing-feature|dashboard] The dashboard is a live operational health view (service up/down via 90s heartbeat staleness, disk %, memory %, queued/failed jobs, cert D-day) but the query has no refetchInterval, and the global react-query config sets refetchOnWindowFocus:false with staleTime:30s (frontend/src/main.jsx:12). So after initial load the page freezes: a worker/scheduler going 'down' (or jobs failing/queuing) never surfaces until the operator manually clicks 새로고침. Other live views poll (NotificationBell 60s, Chat), but the health dashboard — the one screen whose whole purpose is real-time awareness — does not. It cannot serve its stated §6.1 purpose end-to-end.
    FIX: Add polling to the dashboard query in Dashboard.jsx:16, e.g. useQuery({ queryKey:['dashboard'], queryFn:..., retry:false, refetchInterval: 30*1000, refetchIntervalInBackground:false }). 30s aligns with the 90s heartbeat-stale window.
- [bug|dashboard] The 성공률 (success-rate) StatCard is hardcoded kind="ok" (green) regardless of the value (Dashboard.jsx:82). A 10% success rate renders in the same green 'good' tone as 99%, and even a null rate (no jobs → '—') is tinted green. On an ops dashboard this actively misleads the operator about the single metric the color is supposed to communicate.
    FIX: Make the tone conditional on the value, e.g. kind = success_rate_pct==null ? undefined : success_rate_pct>=95 ? 'ok' : success_rate_pct>=80 ? 'warn' : 'danger', at Dashboard.jsx:82. Also add a low-success-rate entry to the alerts block (Dashboard.jsx:38-45) so a degraded rate shows under '확인이 필요한 항목'.
- [ux|chat (user)] The composer textarea is `rows={1}` with `resize:none` and no auto-grow logic (Chat.jsx:248-251; onChange only calls setText). The CSS `max-height:160px` (screens.css:98) can never take effect because nothing ever grows the element. The placeholder advertises 'Shift+Enter 줄바꿈' (Chat.jsx:249), but a multi-line message is crammed into a single-line-height box that just scrolls internally — the user cannot see what they typed. The advertised newline affordance is not visually supported.
    FIX: Add auto-grow: on change set textarea height to scrollHeight capped at max-height (e.g. reset height to 'auto' then set to min(scrollHeight,160)), or use a rows-calculation. Chat.jsx:248-251.
- [states|chat (user)] No optimistic echo of the sent message and no in-thread send feedback. doSend clears the composer immediately (Chat.jsx:200) but the message only appears after the POST round-trip resolves and the thread refetches via invalidate (Chat.jsx:205-206, 146). For a NEW conversation it awaits createConv THEN send THEN refetch (Chat.jsx:203-205), a longer gap. During that window an empty conversation still shows the '아래에 메시지를 입력해 시작하세요' welcome (Chat.jsx:232) even though a message was just sent — the text vanished and nothing replaced it. Only the disabled send button hints activity; the 'thinking' dots appear only after the server returns the pending row.
    FIX: Render an optimistic user bubble (and a pending/sending indicator) immediately in doSend before the await, reconcile on refetch; suppress the empty-welcome once a send is in flight. Chat.jsx:195-210, 232.
- [missing-feature|chat (user)] Conversation management is destructive-only. The backend fully supports non-destructive archiving (PATCH /api/conversations {archived}, set_conversation_archived, list_conversations(include_archived)) — service.py:67-72 / router.py:88-100 — but the UI (ConvItem, Chat.jsx:100-109, 222) exposes ONLY rename and permanent delete ('되돌릴 수 없습니다'). Users who want to declutter must permanently destroy conversations; a shipped, safer capability is completely unwired in the frontend.
    FIX: Add an '보관(archive)' action to ConvItem calling PATCH {archived:true}, plus a way to view/unarchive archived conversations (list already supports include_archived). Chat.jsx:103-106 / 219-223.
- [empty-state|chat (user)] The welcome/empty states state capability but give zero actionable guidance. `!cid`: '무엇을 도와드릴까요? / 대화 한 줄이면 티켓 생성·조회·요약까지 처리합니다.' (Chat.jsx:229) and empty thread: '새 대화 / 아래에 메시지를 입력해 시작하세요.' (Chat.jsx:232). No seeded example prompts or one-tap starters, despite the page already rendering clickable choice chips for assistant replies (chat-choice, Chat.jsx:69-74) — the exact pattern could seed starters like '내 티켓 보여줘'. A first-time user is told what the bot can do but given nothing to try.
    FIX: Render 3-4 example starter chips in the welcome state that call doSend(text) on click, reusing the chat-choice styling. Chat.jsx:228-229, 232.
- [contract|chat (user)] TicketCard renders external (n8n/Notion) values with no boundary normalization. `t.status` goes through Badge which falls back to the raw string when unmapped (kit.jsx:26-29,32-34), and `t.due_date` is printed verbatim (Chat.jsx:45). If the workflow returns an English enum ('In Progress', 'Todo') or an ISO timestamp ('2026-07-19T00:00:00Z'), raw English/ISO leaks into the otherwise-Korean UI. STATUS_TEXT (kit.jsx:8-17) does not cover typical Notion status names.
    FIX: Normalize/format status and due_date at render (map known statuses to Korean; format due_date as a locale date). Chat.jsx:43-45; extend STATUS_TEXT or add a ticket-status map.
- [missing-feature|users] Session management is unexposed. Backend GET /api/admin/users/{id}/sessions (router.py:376-402) and POST .../revoke-sessions (router.py:405-414) exist, and GET /api/admin/users/{id} returns active_session_count (router.py:188-191). The frontend never calls GET /{id} at all — the drawer renders the stale list-row object (sel), so active_session_count is never fetched or shown, and there is no force-logout (revoke-sessions) control. A security-relevant capability (kill active sessions on compromise/offboarding) is missing.
    FIX: Fetch full detail via GET /api/admin/users/{id} when the drawer opens (useQuery keyed on user.id) to show active_session_count and the sessions list; add a "모든 세션 종료" danger action calling POST .../revoke-sessions with confirm.
- [missing-feature|users] No pagination. Backend paginates and returns total/page/page_size (router.py:112-119, PageParams offset/limit), but the frontend renders only query.data.items from the first page (Users.jsx:46) and ignores total entirely. With more users than one page, the remainder are invisible and unsearchable-to (search also returns only the first page of matches), and the admin gets no indication rows are missing.
    FIX: Read total/page/page_size and render a pager (or infinite scroll) that sends page/page_size to the list endpoint; include page in the react-query key.
- [missing-feature|users] No pagination. Backend paginates at DEFAULT_PAGE_SIZE=20 (pagination.py) and returns total/page/page_size, but the frontend reads only query.data.items (Users.jsx:46) and ignores all pagination metadata — there are no next/prev controls and no total count is displayed. In an org with >20 users, users 21+ are invisible unless you already know their exact name/email to search for.
    FIX: Render total ('사용자 N명') and add prev/next (or load-more) controls that pass page to the /api/admin/users query; include page in the react-query key.
- [missing-feature|users] Role and active filters are dropped, leaving the toolbar bare. Backend list supports ?role= and ?active= filters (router.py:93-98) but the toolbar (Users.jsx:76-79) offers only a free-text search box. Common admin tasks like 'show all inactive accounts' or 'show all admins' are impossible without scanning the whole table.
    FIX: Add role and active (전체/활성/비활성) select filters to the toolbar Card and thread them into the /api/admin/users query string and query key.
- [contract|users] The detail drawer renders stale list-row data instead of fetching the detail resource. UserDetail receives the row object from the table (Users.jsx:93) and never calls GET /api/admin/users/{id} (Users.jsx:111-118), so any detail-only field (active_session_count, and richer data the detail endpoint may add) is missing and the drawer can drift from server truth between refreshes.
    FIX: Fetch GET /api/admin/users/{id} with react-query when a row is selected and render from that response, falling back to the row for the initial paint.
- [missing-feature|settings] The dry-run endpoint POST /api/admin/settings/{key}/dry-run (router.py:42-44, service.dry_run) — spec §14.4 step 2 pre-apply validation — is never called by the frontend. The user gets no validation preview; they only discover an invalid value after a live PUT fails (and for ui_branding the live PUT succeeds with corrupt data, see Critical). This is the safety net the object-editing bug most needs.
    FIX: Settings.jsx onSave() — before/instead of the live PUT, POST to /{key}/dry-run and show the {ok, restart_required} result (or the validation error) inline, requiring an explicit confirm before the real PUT. At minimum wire dry-run for object-typed settings.
- [design|settings] The '적용'(restart) column (Settings.jsx:19) and the editor restart note (Settings.jsx:72) are dead: every one of the 8 registry specs sets restart_required=False (registry.py:88-105), so the badge always renders the same 'ok/정상' and the note is unreachable. Meanwhile effective_settings returns a genuinely useful `is_default` flag (service.py:86) telling whether a setting is still at its default or has been overridden — the UI drops it entirely, so admins cannot tell which settings have been changed from defaults.
    FIX: Settings.jsx:16-21 — replace the always-constant restart_required column with an is_default indicator (e.g. Badge '기본값' vs '변경됨'), or surface both. Keep restart_required only in the editor and only when true.
- [ux|settings] The page gives no success feedback and no guard on destructive changes, breaking consistency with every other admin screen. Saving just silently closes the drawer (Settings.jsx:31,52) — unlike DataScreen which fires a useToast success toast on every save/action. Worse, maintenance_mode (blocks ALL normal users' new requests) and allowed_email_domains (gates user creation) are edited exactly like a retention-days row: pick 켜기/끄기, hit 저장, drawer closes, no confirmation, no warning. High-impact global switches deserve a confirm dialog and a visible success/toast.
    FIX: Wire useToast()/useConfirm() (already exported from kit.jsx) into Settings.jsx: toast '저장했습니다.' on save, and require a useConfirm() for maintenance_mode/allowed_email_domains changes. Settings.jsx:30-32, 50-54.
- [dead-affordance|settings] The '적용'(restart_required) column is dead and misleading: every SettingSpec in the registry has restart_required=False (registry.py:88-105), so this column renders a green '정상' badge on every single row forever, and the editor's restart warning at Settings.jsx:72 can never appear. A column whose value never varies is pure noise, and '적용: 정상' does not communicate 'no restart needed' to a user. It reads as meaningful status when it is constant.
    FIX: Remove the restart_required column (Settings.jsx:19) and the unreachable restart note (Settings.jsx:72), or only render the note/column conditionally if any setting actually requires restart. Better: surface the backend's is_default flag (returned by effective_settings but currently ignored) so users can see which settings differ from defaults.
- [a11y|settings] Read-only roles get a fully functional-looking editor that 403s. GET settings is allowed for operator and auditor (READ_ROLES, router.py:29), but PUT is admin/system_admin only (WRITE_ROLES). Settings.jsx imports no auth/role info and never gates the UI, so an operator/auditor can open a row, edit the value, and click 저장 — only to get a server 403. The whole edit affordance is dead for read-only roles.
    FIX: Read the current role (useAuth, as other screens do) and for non-write roles either hide the row-click/editor or render it read-only with the 저장 button removed. Settings.jsx:29-31, 56-59.
- [design|settings] Raw English boolean leaks into the Korean UI. The value column renders String(r.value) (Settings.jsx:18), so maintenance_mode shows literally 'true' / 'false' in the table instead of Korean. The kit already has statusText() which maps true→'예' / false→'아니오'; the bespoke render bypasses it. Task explicitly flags raw English/enum leaking.
    FIX: In the value column render, use statusText(r.value) for bool settings (or type-aware formatting) instead of String(r.value). Settings.jsx:18.
- [missing-feature|diagnostics+maintenance] The Maintenance screen only offers an on/off toggle. The backend registry defines maintenance_message ('유지보수 공지 메시지' — the notice users see during maintenance), but this dedicated page never shows or lets you edit/preview it. An admin turning maintenance ON here has no control over, or even visibility into, what users will actually see. The message is only reachable via the generic raw key/value Settings table.
    FIX: On the Maintenance card, surface maintenance_message: show the current message and add an inline edit (PUT /api/admin/settings/maintenance_message) plus a preview of what users see, so this page fully serves maintenance-mode management end-to-end (Ops.jsx:29-64).
- [dead-affordance|diagnostics+maintenance] The sidebar renders 진단/유지보수 for every admin-console role, but the backend gates them tighter than READ: /api/admin/diagnostics/bundle requires admin/system_admin (health/router.py:48) and the maintenance toggle PUT requires WRITE_ROLES=admin/system_admin (settings/router.py:30). operator and auditor can open both pages and see the '진단 수집' and '유지보수 모드 켜기' buttons enabled, but clicking returns 403 — an error only appears after the click. Dead/deceptive affordance for read-only roles.
    FIX: Gate the action controls by role: for non-admin roles disable the buttons and show a '이 작업에는 관리자 권한이 필요합니다' note (or hide the nav items for those roles). Pass the current role from useAuth into these components (Ops.jsx:15,57).
- [dead-affordance|diagnostics+maintenance] The '복사' button is a silent failure in edge cases: onClick is `navigator.clipboard && navigator.clipboard.writeText(text).then(...)` with no else branch and no .catch(). If the Clipboard API is unavailable (insecure context / older browser) the click does nothing at all; if writeText rejects (permission denied) the '복사됨' confirmation never fires and the user gets no feedback — inconsistent with the app's toast pattern used elsewhere in this same file.
    FIX: Add a .catch that shows a toast('복사에 실패했습니다', 'error') and an else path (or a manual-copy fallback) when navigator.clipboard is missing, so success and failure both give feedback (Ops.jsx:16).
- [bug|users] list_users search is case-inconsistent and untested for display names. router.py:91 lowercases email (`func.lower(User.email).like(needle)`) but matches display_name case-sensitively (`User.display_name.like(f"%{q.strip()}%")`). The only search test (test_admin_users.py:85-88) searches an email substring, so display-name search has zero coverage and this asymmetry is a latent bug: searching 'kim' would miss a display_name 'Kim'.
    FIX: Add tests/integration/test_admin_users.py::test_list_users_search_display_name_case_insensitive: create display_name 'Kim Dev', GET ?q=kim, assert total==1. It will fail today, exposing router.py:91; fix by lowering display_name too (func.lower).
- [dead-affordance|diagnostics+maintenance] The maintenance toggle button (Ops.jsx:57-59) is always rendered enabled, but the backend PUT /api/admin/settings/{key} requires WRITE_ROLES (admin, system_admin) while GET /api/admin/settings allows READ_ROLES (operator, admin, system_admin, auditor). An operator or auditor loads the page fine, sees a full-strength 'danger'-styled '유지보수 모드 켜기' button, clicks it, confirms the destructive dialog, and only then gets a 403 surfaced as an error toast. A read-only role is shown a working-looking destructive control that always fails. The component never calls useAuth() to check role.
    FIX: Import useAuth() in Ops.jsx and, for roles not in {admin, system_admin}, either hide the toggle or render it disabled with an explanatory note (e.g. '변경 권한이 없습니다'). Server RBAC stays authoritative; this only removes the misleading affordance. Also consider role-filtering the NAV entries in App.jsx (App.jsx:26-27).
- [missing-feature|diagnostics+maintenance] The Maintenance page (Ops.jsx:46-63) only toggles maintenance_mode. The backend pairs it with a maintenance_message setting (registry.py:96-97) that is the exact text users see when blocked (app/settings/gate.py:31 -> maintenance_message()). The page neither lets the admin edit that notice nor even displays what message users will see while maintenance is on. The confirm copy says '사용자 쓰기가 차단됩니다' but the actual user-facing notice is unreachable from this screen; an admin must hunt for maintenance_message in the generic Settings table. The page does not fully serve its stated purpose.
    FIX: Add a maintenance_message editor/preview to the Maintenance card: read q.data.settings.maintenance_message.value, show it, and allow editing via the same PUT /api/admin/settings/maintenance_message pipeline before/while enabling the mode.
- [bug|diagnostics+maintenance] The Diagnostics copy control (Ops.jsx:16) is 'navigator.clipboard && navigator.clipboard.writeText(text).then(...)' with no .catch and no fallback. If navigator.clipboard is undefined the click is a silent no-op with zero feedback; if writeText rejects (document not focused / permission denied) the rejection is unhandled and the '복사' label never changes, so the user cannot tell it failed. Copy is the ONLY export path for the bundle (no download, no select-all helper), so a silent failure defeats the whole point of the page (getting the bundle to support).
    FIX: Add a .catch that surfaces an error toast (useToast is already imported in this file's kit dependency set), and provide a fallback when navigator.clipboard is unavailable — e.g. select the <pre> text or offer a download-as-file affordance so the bundle is always exportable.
- [missing-feature|notification-bell] The notification body is dropped. The backend _view returns `body` (router.py:24) but the popover renders only type, title, and time (NotificationBell.jsx:94-96). The actual message detail carried in `body` is never shown, so notifications can be uninformative — the user sees a bare title with no context, and there is no detail/expand view anywhere.
    FIX: Render n.body under the title (line-clamped) in the item, e.g. add a <span className="noti-item-body">{n.body}</span> at NotificationBell.jsx:96 with 2-line clamp CSS.
- [dead-affordance|notification-bell] openItem (lines 67-74) deep-links to the coarse list route (OBJ_ROUTE, e.g. /approvals) and never uses related_object_id (returned by the backend, router.py:27) to reach the specific object — the user is dropped on a list and must hunt for the item. Additionally, when related_object_type is null or unmapped (e.g. type 'system'), `route` is undefined so the click just closes the popover with no navigation and no feedback: a dead click on something styled as an actionable button.
    FIX: Use related_object_id to deep-link to the specific record where the target screen supports it (NotificationBell.jsx:72-73). For non-navigable notifications, don't render them as clickable, or give a visual cue / toast so the click isn't a silent no-op.
- [a11y|notification-bell] Read vs unread is conveyed only by a ~7% primary-color background tint (.noti-item.is-unread, screens.css:45) which is nearly identical to the 5% hover tint (line 44). There is no unread dot, bold weight, or text marker — a color-only distinction with very low contrast that is hard to perceive and fails accessibility guidance on not relying on color alone.
    FIX: Add a non-color unread indicator: an unread dot in the item, bold title for unread, and/or a stronger left border/background on .noti-item.is-unread (screens.css:45).
- [missing-feature|notification-bell] The full notifications page (registry key 'notifications' rendered by DataScreen) is entirely read-only: the config has no `actions` and no `headerActions`, and a row click only opens a read-only detail Drawer. Yet the backend fully supports POST /api/notifications/{id}/read and POST /api/notifications/read-all (used by the popover). So on '전체 알림 보기' you cannot mark a notification read, cannot mark-all, and cannot deep-link to the related object. It also has no pagination, so DataScreen fetches only the default page_size=20 — 'all notifications' silently truncates at 20.
    FIX: Add to the notifications registry config: a row action { label: '읽음', when: r => !r.read_at, path: r => '/api/notifications/' + r.id + '/read' } and a headerAction { label: '모두 읽음', path: () => '/api/notifications/read-all', confirm: '...' }. Consider a page_size param / pagination for the full list.
- [bug|notification-bell] badgeCol('read_at', '읽음') passes the raw read_at value to Badge, which routes through statusText (kit.jsx:26-34). read_at is either null or an ISO timestamp string. Null renders as '알 수 없음' (unknown) for UNREAD rows, and a read row renders the raw ISO timestamp (e.g. '2026-07-18T16:21:23.704973') as the badge text because that string is not in STATUS_TEXT. The read-status column therefore never shows a clean 읽음/안읽음 — it shows 'unknown' for unread and a garbage timestamp for read.
    FIX: Replace with a boolean-derived column, e.g. { key:'read_at', label:'읽음', render: r => r.read_at ? '읽음' : '안읽음' } (or a mapped badge with an explicit kind), rather than badging the timestamp itself.
- [design|notification-bell] The full page uses col('type','유형'), which renders the raw English enum (e.g. 'approval_requested', 'job_failed', 'schedule_failed') directly to end users. The popover translates these via TYPE_KO in NotificationBell.jsx:11-15, so the two surfaces are inconsistent and the full page leaks internal enum strings.
    FIX: Extract TYPE_KO to a shared module and use mapCol('type','유형', TYPE_KO) in the notifications registry config (same pattern already used for jobs via JOB_TYPE).
- [bug|workflows] The '테스트' action reports success even when the reachability test fails. provider_n8n.test (provider_n8n.py:46-64) returns HTTP 200 with {status:'reachable'|'unreachable', detail:'HTTP 404'|'timeout'|'connection_error'} — it only raises on truly unexpected errors. runAction → announce (DataScreen.jsx:29-41) shows a green '테스트 완료' toast whenever no exception is thrown and status !== 'approval_pending', so an UNREACHABLE workflow yields a success toast, and the useful 'detail' string is discarded entirely. The only real signal is the last_test_status badge after refresh.
    FIX: In the test action (or announce), inspect res.status: when it is not 'reachable', toast an error/warning that includes res.detail (e.g. '테스트 실패: timeout'). Requires the test action to carry a custom result handler or announce() to special-case reachability results. DataScreen.jsx:29-41 / registry.js:112.
- [empty-state|workflows] The row-click detail drawer shows only the 6 table columns (name, operation_mode, approval_required, enabled, last_test_status, webhook_url) because DataScreen.jsx:95-100 renders config.columns exclusively. Fields the user sets at create — owner, purpose, http_method — and useful metadata returned by workflow_view (config_version, last_test_at, created_at, updated_at) are not viewable at all; you must open '수정' just to re-read purpose/owner, and last_test_at/timestamps/config_version can never be seen. So the screen shows WHETHER the last test passed but not WHEN, and hides the owner/purpose you entered.
    FIX: Render a full field list in the detail drawer (a config.detailFields or the union of create+column fields) rather than only config.columns, so owner/purpose/http_method/config_version/last_test_at/timestamps are visible. DataScreen.jsx:95-100.
- [missing-feature|policies] The detail view omits published_at and created_by, both returned by _policy_view (router.py:102-103). For a version-management screen, 'when did this version go live' (published_at) and 'who authored it' (created_by) are core accountability facts. Because the detail drawer only renders columns and the columns list is name/version/status/created_at (registry.js:141), neither is ever shown.
    FIX: Add published_at (dateCol) and created_by columns to registry.js:141, or surface them via the detail-fields mechanism proposed for the content viewer. File: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\registry.js:141.
- [ux|policies] The list renders every version of every policy as a separate flat row (backend returns up to 500 rows ordered by name, version desc — router.py:123) with no grouping, no status filter, and no 'current published version' indicator. As versions accumulate (draft/test/review/published/archived all coexist), the table fills with archived clutter and it becomes hard to identify the one live policy for a given name.
    FIX: Add a status filter (or default to hiding archived) and/or group rows by name with the latest/published version highlighted. Consider using the backend's ?name= filter (router.py:117) for a per-name version history view. File: C:\Users\hshwa\clovirone-web-assistant\frontend\src\screens\registry.js:137-155.
- [contract|prompts] The status column shows RAW ENGLISH enum for test/review/archived. Badge localization (kit.jsx STATUS_TEXT, lines 8-17) maps only draft→'초안' and published→'발행됨'; it has no entry for 'test', 'review', or 'archived', so statusText falls through to the raw value. Since two of the four exposed actions transition INTO 'test' and 'review' (registry.js:131-132), the most common working states routinely display untranslated 'test'/'review' badges (also uncolored: STATUS_KIND lacks these keys too).
    FIX: Add test/review/archived (and draft for color) to STATUS_TEXT and STATUS_KIND in kit.jsx:8-25.
- [dead-affordance|prompts] Backend VALID_TRANSITIONS (models.py:20-26) allows test→draft, review→draft (go back to re-edit) and →archived from draft/test/review/published, but the UI exposes none of these. Consequences: (1) a prompt in test/review with a typo is a workflow dead-end — it can't be edited (edit is draft-only), can't be sent back to draft (no button), and can't be archived (no button), only forced forward; (2) the 'archived' terminal state is completely unreachable from the UI, so prompts can never be retired.
    FIX: Add '초안으로'(→draft, when status is test/review) and '보관'(→archived, from draft/test/review/published) actions to registry.js prompts.actions, each POSTing /transition with the target status.
- [missing-feature|prompts] User-entered and audit-relevant fields are collected/returned but never displayed. 'purpose' (용도) is a create field (registry.js:122) yet is never echoed back anywhere (not a column, drawer shows only columns). 'published_at' and 'created_by' are returned by _prompt_view (router.py:88,86) but are invisible in the UI. Same root cause as the content finding: the drawer renders only config.columns.
    FIX: Add purpose/published_at (and optionally created_by) as columns or detail fields; published_at should use dateCol. registry.js:119.
- [missing-feature|schedules] Dry-run preview is not exposed. Backend POST /api/admin/schedules/{id}/dry-run returns payload_preview, target, and the next 3 fire times (UTC) with no execution — the natural way to validate a cron expression before enabling. No action wires it, so admins must enable-and-wait to find out if a cron expression fires when they expect.
    FIX: Add a '미리보기(dry-run)' action to the schedules actions array pointing at /api/admin/schedules/{id}/dry-run and render next_fire_times_utc + payload_preview (formatted to KST) in the resulting drawer/toast.
- [ux|schedules] target_ref is a free-text 'text' field ({name:'target_ref', label:'대상 ID'}, registry.js:194/208) with no guidance about valid values per target_type. Backend requires target_ref to be an existing Workflow ID when target_type=workflow, or exactly 'noop' when target_type=system (SYSTEM_TARGETS). There is no picker and the help text is empty, so users will paste wrong values and hit 422 ('target_ref에 해당하는 Workflow가 없습니다' / 'system target은 [noop] 중 하나여야 합니다').
    FIX: Add help text explaining target_ref rules per target_type (workflow → a Workflow ID from the 워크플로 screen; system → 'noop'), or make target_ref a select populated from the workflows list when target_type=workflow, mirroring how the domain actually constrains it.
- [missing-feature|schedules] The friendly 'preset' scheduling option is not exposed anywhere. Backend ScheduleRequest.preset accepts daily/weekly/monthly and maps to a cron_expression via cron.PRESETS, letting non-technical admins schedule without writing cron syntax. Neither the create nor edit form offers a preset field, so every recurring schedule forces the user to hand-write raw cron (e.g. '0 9 * * 1').
    FIX: Add a 'preset' select (daily/weekly/monthly + '직접 입력') to the create/edit forms; when set, it supersedes cron_expression per backend _validate_and_normalize.
- [dead-affordance|approvals] Self-approval is a dead affordance that directly contradicts the screen's own help text. The help says '본인 요청은 본인이 승인할 수 없어요' (you cannot approve your own request), but the 승인 action's `when` checks only status. An admin viewing their own pending request sees the 승인 button, clicks it, and the backend 403s (ForbiddenError '자기 승인을 허용하지 않습니다') unless the self_approval_allowed flag is on. The button should not appear for one's own request.
    FIX: Add a current-user check to the approve action's `when` (e.g. when: (r) => !DONE.includes(r.status) && r.requested_by !== currentUserId), which requires threading the current user id into the registry config.
- [empty-state|approvals] No status filter is surfaced, and the empty-state text lies about what the list shows. Backend list supports ?status= (pending/approved/rejected/expired/cancelled) but the screen never sends it, so it renders ALL statuses mixed together, newest-first. The emptyTitle says '대기 중인 승인 요청이 없습니다' (no PENDING approval requests) — but the list is NOT filtered to pending: if only decided rows exist, the list is non-empty and that message never shows; and there is no way to view just the pending queue an approver actually needs to act on.
    FIX: Add a status filter (tabs or a select) that sets ?status=pending by default and forwards it to the endpoint, or make the screen default to pending. Reconcile emptyTitle/emptyHelp with whatever the list is actually filtered to (frontend/src/screens/registry.js:232-236; backend supports status at app/approvals/router.py:51).
- [missing-feature|approvals] No pagination. The list endpoint returns total/page/page_size (DEFAULT_PAGE_SIZE=20) but DataScreen ignores them and renders only items[]. Approvals are an append-only ledger — approved/rejected/expired/cancelled rows all persist — so after 20 total rows, older approvals become unreachable. The '검색' box only filters the 20 rows already loaded (client-side JSON.stringify match), so historical approvals cannot be found at all.
    FIX: Add pagination controls (next/prev using total & page_size from the response) to DataScreen, and/or wire server-side search. Acute for approvals given it accumulates history (frontend/src/screens/DataScreen.jsx:51; envelope at app/core/pagination.py).
- [bug|runners] The 헬스/테스트 actions give misleading positive feedback. runAction always shows a green success toast (a.label + ' 완료') on any non-throwing response (DataScreen.jsx:33-41; announce only special-cases approval_pending, lines 29-32). But POST /{id}/health returns 200 with {status:'down'} for an unreachable runner, and POST /{id}/test returns 200 with {ok:false, status_code:500} for a 5xx target (app/runners/router.py:178-220). So a failed health check or a failing test both surface as green '헬스 완료'/'테스트 완료'. The rich result the backend returns (latency_ms, status_code, detail like 'timeout'/'connection_error', checked_url) is discarded entirely.
    FIX: In runAction, inspect the response and toast accordingly (e.g. res.status/res.ok/res.status_code) — show an error/warn toast and the HTTP status/detail when status=='down' or ok===false, instead of a blanket success. Consider surfacing latency/status_code in the message.
- [missing-feature|integrations] The capabilities field has no UI whatsoever. capabilities is a first-class config field (schemas.py:24, model capabilities_json models.py:27) seeded by discovery.py with real values ({webhooks:true}, {chat:true}, {tickets:true}, {interpretation:true}). It is absent from the integrations create form (registry.js:47-55) and there is no edit block, and it is not displayed anywhere. It can only ever be set via the discovery seed or a raw API call — never viewed or changed through the admin screen.
    FIX: Add a { name: 'capabilities', label: '기능(JSON)', type: 'json' } field to the integrations create fields (registry.js:47-55); the FormModal already parses/validates JSON (kit.jsx:226). Also display it in the detail view (see previous finding).
- [empty-state|integrations] The integrations config is the only connectivity screen with no empty-state guidance. registry.js integrations block (lines 41-60) omits emptyTitle and emptyHelp, unlike every sibling (runners registry.js:64, workflows registry.js:87, prompts/policies/templates/schedules etc.). On a fresh DB before discovery seeding runs, the list is empty and EmptyState (DataScreen.jsx:81-83) falls back to the generic '표시할 항목이 없습니다' with no help text — no explanation of what an integration is or that '+ 외부 연동 추가' registers services like n8n/Claude runners.
    FIX: Add emptyTitle (e.g. '등록된 외부 연동이 없습니다') and emptyHelp (e.g. "'+ 외부 연동 추가'로 이 시스템이 불러다 쓰는 외부 서비스(n8n, Claude 러너 등)를 등록하고 상태를 점검하세요.") to registry.js:41-43, matching the runners/workflows pattern.
- [missing-feature|templates] The create form (registry.js:161-166) offers only name, description, target_type, target_ref, but the backend POST TemplateRequest also accepts input_schema, prompt_id, policy_id, approval_policy (router.py:30-38). So a template cannot be fully configured at creation — you must create it 'bare' and then immediately open '수정'(PUT) to attach a prompt/policy/input schema/approval policy (the edit form at registry.js:169-178 has all 8). Nothing tells the user this second step is required.
    FIX: Add prompt_id, policy_id, input_schema, approval_policy to `create.fields` (mirroring `edit.fields`), or add help text stating configuration continues via edit after creation. File: frontend/src/screens/registry.js:161-166.
- [dead-affordance|templates] The templates screen shows create ('+ 템플릿 추가'), '수정', and '활성화/비활성화' buttons to every admin-console role, but the backend restricts these to WRITE_ROLES = admin/system_admin (router.py:27), while READ_ROLES also include operator and auditor (router.py:26). The frontend does no role gating for admin sub-screens — App.jsx (AdminBody, lines 150-174 / Layout 180-190) only redirects role==='user'; operator/auditor get the full console. Every create/edit/enable/disable submit for those roles returns 403, so all write buttons are dead affordances for the screen's own read audience.
    FIX: Gate create/edit/actions by role (hide or disable the buttons for non-WRITE roles), threading the current user's role from auth into DataScreen. File: frontend/src/app/App.jsx:150-174 and frontend/src/screens/DataScreen.jsx:55-65,91-93.
- [ux|templates] target_ref, prompt_id, and policy_id are free-text ID inputs with no picker, even though workflows/runners/prompts/policies are all admin-managed lists in the same console. The backend only validates existence after submit (_validate_references → ValidationAppError, router.py:83-93), so the user must leave the screen, find, and paste a raw UUID. Worse, the create target_ref field (registry.js:165) and the edit prompt_id/policy_id fields (registry.js:174-175) have NO help text at all — unlike the documents screen (registry.js:224) which explicitly tells the user where to find the workflow ID.
    FIX: Replace the ID text fields with selects populated from /api/admin/workflows|runners|prompts|policies; at minimum add help text pointing to where each ID is found, matching the documents screen. File: frontend/src/screens/registry.js:165,173-175.
- [dead-affordance|documents] The 'published_ref' column is labeled '발행 링크' (publish link) but is rendered as non-clickable plain text. It uses plain col('published_ref','발행 링크') (registry.js:230); DataTable (kit.jsx:117) and the detail drawer (DataScreen.jsx:98) render it as String(value), not an <a>. Worse, clicking the row (or the URL text) opens the detail drawer, not the Notion document. A column literally called a 'link' is not a link, so users must hand-copy the Notion URL.
    FIX: Give published_ref a render that outputs an anchor: render:(r)=> r.published_ref ? <a href={r.published_ref} target="_blank" rel="noopener">문서 열기</a> : '—' (stopPropagation so it doesn't trigger the row-detail click). registry.js:230.
- [design|documents] The 'preview_ready' status renders as raw English. STATUS_TEXT (kit.jsx:8-17) localizes every other document status (pending→'대기', quality_failed→'품질 미달', awaiting_approval→'승인 대기', published→'발행됨', failed→'실패') but has no entry for preview_ready — so the terminal state of the '미리보기만' mode shows the untranslated enum 'preview_ready' in the status badge.
    FIX: Add preview_ready to STATUS_TEXT (e.g. '미리보기 준비됨') and STATUS_KIND (e.g. 'info') in kit.jsx:8-25.
- [missing-feature|documents] workflow_id and config are never shown back to the user. workflow_id is a required create input and config is user-submitted, and both are returned by generation_view, but neither appears in the columns nor the 5-field detail drawer. When browsing generation history a user cannot tell which workflow or what settings produced a given row, making the '기록' (history) largely non-diagnostic.
    FIX: Add workflow_id and config (pretty-printed JSON) to the documents detail rendering. registry.js:230 / detail view.
- [ux|documents] The '+ 문서 생성' form makes the user hand-type an opaque workflow ID. The workflow_id field is free text whose help says to go to the workflows screen, look up the ID, and type it (registry.js:224); period and config are also free-form (raw JSON). There is no workflow picker. The backend rejects unknown IDs with a 422 ('대상 Workflow가 없습니다', service.py:142), so the error-prone manual step surfaces only after submit.
    FIX: Replace the workflow_id text field with a select populated from GET /api/admin/workflows (enabled write workflows). registry.js:222-228.
- [missing-feature|documents] Backend list capabilities are unused and history beyond the first page is unreachable. GET /api/admin/documents supports server-side status filtering (?status=, router.py:44-68) and pagination (PageParams), but the screen exposes neither: DataScreen fetches page 1 only (no page param, DataScreen.jsx:20-26) and offers just a generic client-side search box that filters only the already-loaded page (DataScreen.jsx:52-53). Once there are more than page_size (default 20) generations, older records — which the empty-state promises will 'remain here' — can never be seen or searched. (Shared DataScreen limitation, but it directly defeats this screen's stated purpose as a durable history.)
    FIX: Add a status filter control wired to ?status= and pagination (pass page params + a pager), or at minimum surface server search. DataScreen.jsx query + registry.js documents config.
- [missing-feature|departments] Delete dead-ends with no recovery path. The backend blocks deleting an in-use department (app/org/service.py:106-121, 409 '이 부서를 쓰는 사용자가 N명 있어...'), but the screen gives no way to reach those users. The 'user_count' (소속 인원) column shows only a number with no drill-down, the Users screen (frontend/src/screens/Users.jsx) can only search by email/name (backend app/users/router.py:69 has role/active filters but NO department_id filter), and it hides archived users by default (archived=false, router.py:77) while usage_count deliberately counts archived users too (service.py:65-72). So an admin told 'N users use this department' cannot list those N (especially archived ones) to reassign them, making the department permanently un-deletable through the UI.
    FIX: Add a department_id (and title_id) filter param to GET /api/admin/users (app/users/router.py:69) and a '이 부서 사용자 보기' link from the department detail drawer that navigates to Users filtered by that dept; surface the archived toggle in Users.jsx so archived FK-holders can be found; and clarify in the departments help/tooltip that 소속 인원 includes archived accounts.
- [dead-affordance|departments] The domain's recommended safe action (deactivate) is buried while the discouraged one (delete) is headlined. The backend treats setting active=false as the graceful alternative to deletion and its 409 error even tells the user '비활성으로 두면...' (service.py:117-121). Yet the only first-class row action in registry.js:249 is the danger '삭제' button, which for any in-use department almost always fails; deactivation is reachable only via 행 클릭 → 수정 → uncheck '사용' → 저장 (registry.js:248). Every peer screen (integrations/runners/workflows/templates/schedules) exposes one-click enable/disable via the onoff() helper (registry.js:33-36). After a blocked delete, the toast advises deactivating but offers no control to do it.
    FIX: Add first-class 활성화/비활성화 row actions to the departments (and job-titles) config in registry.js:249, mirroring onoff() but PATCHing {active:false}/{active:true} conditioned on r.active (org uses PATCH, not /enable /disable endpoints), so the recommended action is one click; keep 삭제 as the secondary/least-prominent action.
- [empty-state|departments] No empty-state guidance on a foundational, zero-seeded screen. The departments config (registry.js:243-250) defines no emptyTitle/emptyHelp, so a fresh install (migration 0015 seeds nothing — it only migrates pre-existing free-text values) shows the generic DataScreen fallback '표시할 항목이 없습니다' with no help text (DataScreen.jsx:81-83). Almost every peer screen provides specific empty copy. This matters because departments feed the user-creation form's '부서' dropdown (Users.jsx:26,53), which useNameOptions renders as only '— 없음 —' when the list is empty, so admins get no hint that they must seed departments first.
    FIX: Add emptyTitle/emptyHelp to the departments (and job-titles) config in registry.js:243, e.g. emptyTitle:'등록된 부서가 없습니다', emptyHelp:"'+ 부서 추가'로 부서를 만들면 사용자 폼의 '부서' 목록에 바로 나타납니다." Optionally seed a few example departments in migration 0015 for fresh installs.
- [empty-state|job-titles] The "job-titles" config defines no emptyTitle/emptyHelp. The job_titles table ships EMPTY on a fresh install (migration 0015 only creates the table and migrates pre-existing user strings; there is no seed data), so the very first thing an admin sees is DataScreen's generic fallback "표시할 항목이 없습니다" with NO help text (config.emptyHelp is undefined) explaining what job titles are for or that they feed the user form's '직책' dropdown. Every other list screen that starts empty (runners, workflows, prompts, policies, templates, schedules, documents, jobs, audit, backup, notifications) provides domain-specific emptyTitle+emptyHelp; departments and job-titles are the only two exceptions.
    FIX: Add to the config (registry.js:251): emptyTitle: "등록된 직책이 없습니다", emptyHelp: "'+ 직책 추가'로 직책을 만들면 사용자 폼의 '직책' 목록에서 바로 고를 수 있습니다." The empty-state create button already works (config.create present), so only the guidance text is missing.
- [missing-feature|backup] error_message is captured by the backend for every failed backup (service.py:63,66,81 — e.g. 'restore verify 실패: ...', exception text) and returned in backup_view, but it is not a column and the detail drawer only renders config.columns (DataScreen.jsx:95), so error_message is invisible everywhere. A user sees a red '실패' badge with zero explanation of why. Same for verified_at (can't tell WHEN last verified), checksum (integrity hash), created_by (who ran it), and backup_type — all returned by the API, none ever shown. The DataScreen comment claims the detail modal shows '전체 필드' but it does not.
    FIX: Add error_message (and verified_at, checksum, created_by) to registry.js 'backup' columns, or extend the DataScreen detail drawer to render all fields of the row rather than only config.columns. At minimum surface error_message for failed rows.
- [design|backup] The '크기' (size) column uses col('size_bytes', '크기') (registry.js:304) which renders the raw byte integer via the default String() renderer — e.g. a 20 MB backup shows as '20971520' instead of '20 MB'. There is no human-readable byte formatter in the codebase (grep for formatBytes/fmtBytes returns nothing). Raw byte counts are unreadable for the stated purpose of at-a-glance backup review.
    FIX: Add a byte-formatting render helper (KB/MB/GB) in format.js and use it for the size_bytes column, e.g. { key:'size_bytes', label:'크기', render:(r)=>fmtBytes(r.size_bytes) }.
- [missing-feature|jobs] DataScreen fetches config.endpoint with no ?page/?page_size (DataScreen.jsx:24), so it shows only page 1 (DEFAULT_PAGE_SIZE=20, pagination.py) and renders no pager even though the response carries total/page/page_size. The jobs table gains a row for every chat message, document generation, schedule run, and sync, so it grows quickly; older jobs (including older failures) become permanently invisible. This compounds the missing stats and missing filters — the operator can only ever see the newest 20 rows.
    FIX: Add pagination controls to DataScreen driven by query.data.total/page/page_size, appending ?page= to the endpoint (and to the query key). Applies to all list screens but is most damaging on high-volume jobs/audit.
- [missing-feature|jobs] _job_view returns duration_ms, started_at, finished_at, available_at, max_attempts, idempotency_key, user_id, conversation_id, message_id (router.py:40-57), but the detail drawer only iterates config.columns (DataScreen.jsx:95) — the 5 list columns. So on row click the operator never sees how long the job ran (duration_ms), when it started/finished, when a backed-off retry will fire (available_at), or the correlation IDs the backend deliberately exposes for tracing. attempt_count shows as a bare number ('시도: 2') with no max_attempts, so it's impossible to tell whether a job has retries left or is exhausted.
    FIX: Give DataScreen a config.detailColumns (fall back to columns) and set jobs.detailColumns to include dateCol('started_at'), dateCol('finished_at'), col('duration_ms','처리시간(ms)'), dateCol('available_at','다음 시도'), a combined attempt render ('2 / 3' from attempt_count+max_attempts), and the correlation IDs. Render duration_ms in seconds for readability.
- [empty-state|jobs] A job queue is a live, changing resource (queued→running→succeeded/failed), but the screen has no auto-refresh (no refetchInterval) and no manual refresh button. DataScreen.refresh() is only invoked after an action, and jobs has no actions — so once loaded, a 'running' job stays 'running' in the UI until the operator navigates away and back or reloads the page. There is no way to see live progress or confirm a worker is draining the queue.
    FIX: Add refetchInterval (e.g. 5-10s) to the useQuery in DataScreen for queue-type screens (config.pollMs), and/or add a manual '새로고침' button in the PageHeader actions that calls query.refetch(). Set jobs.pollMs so the queue view stays live.
- [missing-feature|notion-mapping] Status filter not exposed. list_mappings supports ?status=unmapped|verified|conflict (router.py L84,110-117) — the natural way for an admin to jump straight to conflicts or unmapped users across the org — but there is no filter control in the registry config or DataScreen. Combined with the 20-row cap, an admin cannot triage conflicts that sit on later pages.
    FIX: Add a status filter (select: 미연결/확인됨/충돌) that feeds the endpoint's status query param; requires DataScreen to support filter controls that re-query.
- [bug|notion-mapping] '자동 동기화' reports completion for a job that is only queued. POST /sync returns 202 {job_id, status:'queued'} for a background job the router docstring says takes 9-13s+ per batch. runHeaderAction (DataScreen.jsx L47-48) toasts '자동 동기화 완료' (success) and immediately refresh()es, but the job has not run so nothing changes. There is no polling, no job link, and no 'requested/processing' state. The router docstring assumes the screen reloads when the job finishes, but no such mechanism exists — the admin is told sync finished when it hasn't started.
    FIX: Toast '동기화 요청됨(백그라운드 처리 중)' for 202 responses; poll the job (GET /api/admin/jobs) or link to 작업 큐, and refresh the list when the notion_mapping_sync job completes.
- [missing-feature|notion-mapping] notion_user_id_masked and last_verified_at are returned by the backend but never shown. mapping_view provides a masked Notion id and last_verified_at; neither is a column nor visible in the detail drawer (detail renders only config.columns). For a manual mapping created without notion_email, the row shows status only — the admin cannot confirm which Notion account is linked or how stale the verification is.
    FIX: registry.js: add the masked Notion id to the detail/columns (server already masks it) and a dateCol for last_verified_at.
- [ux|audit] The '행위자' (actor) column shows the raw 36-char user UUID (registry.js:289, col('user_id')). The backend does no user join, so an auditor sees an opaque UUID instead of a name/email — undermining the '누가'(who) half of the screen's purpose. CLI/system actions have user_id=null and render '—' with no indication it was a system/CLI-originated action.
    FIX: Resolve user_id to display name/email — either enrich the backend audit response with an actor display field (app/audit/router.py:66-82) or have the frontend look it up — and render '시스템/CLI' for null actors instead of a bare '—'.
- [missing-feature|notifications] The notification `body` (the actual message text) and `related_object_type`/`related_object_id` are returned by the backend _view() (router.py:19-29) but are never surfaced. The config columns (registry.js:295) omit all three, and the detail drawer iterates only config.columns (DataScreen.jsx:95-100), so even opening a notification shows no body text and no related object. The single most informative field is invisible on the dedicated page (and the bell omits it too, so body is shown nowhere).
    FIX: Add body (and related object) to the notifications columns in registry.js:295, e.g. col("body","내용") plus a related-object column, so the detail drawer renders the message body and linkage.
- [ux|notifications] No row-level deep-link or mark-on-open; the detail drawer is a dead-end that is strictly less capable than the bell popover. NotificationBell.openItem (NotificationBell.jsx:67-74) marks an unread item read and deep-links to the related screen via OBJ_ROUTE. The DataScreen just opens a generic key/value drawer with no navigation to the related object and no mark-read. Clicking an 'approval_requested' notification on the full page cannot take you to the approval.
    FIX: Add a row action or drawer action that navigates to the related object (mirror NotificationBell's OBJ_ROUTE map) and marks the item read on open.
- [design|notifications] The '유형' column shows raw English enum values. registry.js:295 uses col("type","유형") which renders r.type verbatim ('approval_requested', 'job_failed', 'document_ready', ...). The NotificationBell has a TYPE_KO map (NotificationBell.jsx:11-15) that Koreanizes these, so the same data reads as Korean in the popover but as raw snake_case enums on the full page. Client-side search (config.searchable unset) also matches only these raw enum strings, so searching '승인' won't find approval notifications.
    FIX: Add a NOTI_TYPE map (reuse NotificationBell's TYPE_KO) in registry.js and switch to mapCol("type","유형", NOTI_TYPE) so types render in Korean consistently with the bell.
- [missing-feature|notifications] No pagination: only the 20 most recent notifications are ever reachable. The endpoint defaults to page_size=20 (pagination.py:13) and DataScreen renders no pagination controls at all — it never reads total/page/page_size from the response. The page title '알림' and the bell's '전체 알림 보기' (View ALL) promise the full history, but rows past the first 20 cannot be viewed.
    FIX: Add pagination controls to DataScreen (consume total/page/page_size) or a 'load more' affordance so the notifications history beyond page 1 is reachable.
- [a11y|shared-infra] The shared Modal (kit.jsx:153-171) sets `role="dialog"` and `aria-modal="true"` but does no focus management: focus is never moved into the dialog on open, Tab is not trapped (keyboard focus escapes to the background page behind the 38% overlay), and focus is not restored to the trigger on close. This affects every create/edit/confirm/detail/FormModal flow, since all of them use Modal.
    FIX: On open, move focus to the first focusable element (or the ✕ button); trap Tab/Shift+Tab within `.k-modal`; store and restore focus to the triggering element on close. kit.jsx Modal component.
- [ux|shared-infra] Mid-session session expiry leaves the shell broken. AdminBody (App.jsx:157) only checks `auth.isLoading`; it never handles `auth.isError`. When `/api/me` returns 401 (expired session), `role` is undefined so the full admin chrome (sidebar+topbar) still renders and every screen independently 401s into its own ErrorState. There is no single shell-level 'session expired → go to login' handling.
    FIX: In App.jsx handle `auth.isError` (401) by redirecting to `/login` (or rendering one ErrorState with the '로그인 화면으로' button from kit.jsx:94) instead of rendering AdminBody with dead content.
- [ux|shared-infra] Flash of wrong chrome during auth load. Topbar renders the admin '사용자/관리자' segment whenever `!isUser` (App.jsx:138), but during the `/api/me` load `role` is undefined so `isUser` is false — the admin-only segment briefly flashes even for regular users (who are then redirected to /chat). The UserMenu also shows the placeholder '관리자 / ?' (App.jsx:105-106) until identity resolves.
    FIX: Gate the segment control and identity-dependent topbar chrome on `auth.isLoading` / resolved role so regular users never see the admin segment, and show a neutral placeholder rather than defaulting to '관리자'.
- [bug|shared-infra] Row and header actions have no in-flight guard, allowing duplicate non-idempotent POSTs. runAction/runHeaderAction (DataScreen.jsx:33-50) await the request then refresh, but the footer/header Buttons (DataScreen.jsx:61,92-93) are never disabled while awaiting. Double-clicking "+백업 실행", "+문서 생성", "새 버전", or "승인" fires the request twice (duplicate backups/documents/approvals). FormModal has a busy state; these action buttons do not.
    FIX: Track an in-flight action key in state and disable the corresponding Button (and show a busy label) until the await resolves. DataScreen.jsx:33-50,61,92-93.
- [bug|shared-infra] Optional text/number/textarea/json fields cannot be cleared on edit. FormModal.submit only writes a key when `val !== "" && val != null` (kit.jsx:229), so an emptied field is omitted and a PATCH treats it as unchanged. Only `select` explicitly sends null (kit.jsx:227). Concrete: set a workflow `owner` or `purpose`, then blank it and save — the old value persists.
    FIX: For fields present in `initial` that the user cleared, send explicit null (diff against initial) rather than omitting them. kit.jsx:229.
- [bug|shared-infra] Client-side search matches serialized JSON keys and hidden fields. DataScreen.jsx:52-53 does `JSON.stringify(r).toLowerCase().includes(q)`, so it matches object keys (id, name, status, created_at, enabled) and non-displayed fields. Common substrings like "id", "created", "name", "true", or "http" match every/most rows, making results meaningless; it also re-stringifies every row on each keystroke.
    FIX: Filter only the visible column values (run each column's render/text extractor and match against that), not the whole serialized object. DataScreen.jsx:52.
- [dead-affordance|shared-infra] An unintended edit button appears on integrations/runners via a fallback. `editFields = config.edit ? ... : config.create.fields` (DataScreen.jsx:19) means screens the registry deliberately left without an `edit` form still render "수정" (DataScreen.jsx:91) and PATCH the full create field set (integrations: provider_type/base_url/auth_type/secret_ref). These PATCH routes exist and flag base_url/secret_ref/auth_type/health_url changes as sensitive, so this unreviewed, undocumented form can silently route into the approval flow or change secret targets — behavior the author built careful reduced edit forms to avoid elsewhere.
    FIX: Only render the edit button and edit FormDrawer when `config.edit` is explicitly defined; drop the create.fields fallback. DataScreen.jsx:19,91,108.
- [ux|shared-infra] No global 401 handling; an expired session shows a broken shell instead of redirecting. api.js throws status 401 (api.js:24-28) and AuthProvider merely errors (auth.jsx), but App.jsx Layout still renders Topbar+Sidebar+AdminBody, so every panel independently renders an ErrorState with a manual "로그인 화면으로" link (kit.jsx:93-94). There is no app-level bounce to /login on session expiry — the console looks broken (every panel erroring).
    FIX: Detect 401 centrally (react-query QueryCache onError, or the /api/me auth error in Layout) and redirect to /login. app/App.jsx Layout; lib/api.js.
- [a11y|shared-infra] No client-side role gating; lower RBAC roles see controls they cannot use. App.jsx only redirects the `user` role to chat (App.jsx:188-190); all other roles render every nav group and DataScreen shows all create/action/headerAction buttons unconditionally. A limited admin sees "+추가", "삭제", "발행", "+백업 실행", and the maintenance toggle, and only discovers they are forbidden via a 403 error toast — dead affordances that mislead about capability.
    FIX: Return the caller's role/capabilities from /api/me and gate NAV items plus create/action buttons on them. app/App.jsx NAV/Sidebar; screens/DataScreen.jsx action rendering.
